<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>2019年寒假到来啦</title>
    <url>/2019%E5%B9%B4%E5%AF%92%E5%81%87%E5%88%B0%E6%9D%A5%E5%95%A6/</url>
    <content><![CDATA[<h3 id="2018年的12月"><a href="#2018年的12月" class="headerlink" title="2018年的12月"></a>2018年的12月</h3><p>2018年的最后一月 在学校那边呆了整整两周 因为电脑也早在之前送回了家 所以这两周过得实在是无趣</p>
<h4 id="2018年12月31日跨年夜"><a href="#2018年12月31日跨年夜" class="headerlink" title="2018年12月31日跨年夜"></a>2018年12月31日跨年夜</h4><p>和往常一样 没有什么特别的 因为回不了深圳 只能在学校那边和宿友玩玩 PUBG 自个儿玩玩任天堂明星大乱斗 倒是挺羡慕某个人跨年夜玩的挺开心的呢</p>
<h3 id="2019年1月1日至1月11日"><a href="#2019年1月1日至1月11日" class="headerlink" title="2019年1月1日至1月11日"></a>2019年1月1日至1月11日</h3><p>这十来天 一直在准备期末考试 虽然也没什么好准备的 一路都是在玩大乱斗度过 不过经过这次期末考试倒是让我知道 写博文的好处. 那就是随时想复习的时候一打开就能马上回忆起来 要坚持写下去</p>
<a id="more"></a>

<h4 id="2019年1月11日晚"><a href="#2019年1月11日晚" class="headerlink" title="2019年1月11日晚"></a>2019年1月11日晚</h4><p>考完了最后一科 <code>软件工程</code> 先式把被子快递回家 后和宿友他们一起去外面吃了个火锅 点了两份牛鞭花 我不敢吃 😹 其实我觉得应该和海参差不了多少 吃完以后感觉有点涨 我的胃啊总消化不良 和往常一样去禽兽湖走了两圈回宿舍 突然感觉到一阵空虚 很想写一篇博文 但是没带电脑 所以放弃了(找个机会要想想怎么用手机发布博文) 终于结束了考试 可以回家继续做自己的事情了 洗了个澡 开着空调(为了吹干衣服 另外广东 1月份 25度你能信?) 打着电话上床睡觉了</p>
<h4 id="2019年1月12日"><a href="#2019年1月12日" class="headerlink" title="2019年1月12日"></a>2019年1月12日</h4><p>凌晨5点 被宿友吵醒 因为他要收拾东西去赶早上七点四十五的车回湛江 然后自己再睡到了八点多 和另外两个宿友 一起洗漱 然后一起吃了个早餐 坐了车 途中塞车一个多小时 终于回到了深圳 等到家的时候 已是下午一点多了 下午三点多被顺丰的人毒到了 寄行李选顺丰不就为了送上门 结果扔速递柜了 我倒是很好奇被子是怎么塞进速递柜的 带着疑惑果断投诉了 然后在下午五点多送到了 投诉还是很有效果的</p>
<h3 id="寒假到来"><a href="#寒假到来" class="headerlink" title="寒假到来"></a>寒假到来</h3><p>时间总是这么快 四个月一下就过了 算了一下今年寒假有四十多天的假期 希望能好好利用 这不 一回来就把上次回学校之前还没写的那半篇博文给补上了 有点累 但是也玩的差不多了(考试周对我而言就是脱产 + 娱乐周) 希望能在年26前完成掉大部分事情吧!</p>
]]></content>
      <categories>
        <category>胡言乱语</category>
      </categories>
      <tags>
        <tag>2019</tag>
        <tag>跨年</tag>
        <tag>假期</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 内存管理 之 G4.9 扩展内存分配器</title>
    <url>/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-G4.9-%E6%89%A9%E5%B1%95%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%99%A8/</url>
    <content><![CDATA[<p>本文主要讲讲 GNU-C++ 4.9 下的扩展内存分配器</p>
<p>首先 GNU-C++ 4.9 有 7 个扩展的内存分配器</p>
<ul>
<li>new_allocator</li>
<li>malloc_allocator</li>
<li>pool_allocator</li>
<li>__mt_alloc</li>
<li>array_allocator</li>
<li>debug_allocator</li>
<li>bitmap_allocator</li>
</ul>
<p>主要看看 <code>pool_allocator</code> <code>array_allocator</code> <code>bitmap_allocator</code></p>
<p>以下源码都可在 <code>.../ext/*.h</code> 下找到 我将其进行了适当的 删减和修改</p>
<h2 id="gnu-cxx-new-allocator"><a href="#gnu-cxx-new-allocator" class="headerlink" title="__gnu_cxx::new_allocator"></a>__gnu_cxx::new_allocator</h2><p>直接用 <code>::operator new</code> 和 <code>::operator delete</code> 实现出来的 好处是 可以被重载 没啥特色</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">new_allocator</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">pointer <span class="title">allocate</span><span class="params">(size_type __n, <span class="keyword">const</span> <span class="keyword">void</span>* = <span class="number">0</span>)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">static_cast</span>&lt;_Tp*&gt;(::<span class="keyword">operator</span> <span class="keyword">new</span>(__n * <span class="keyword">sizeof</span>(_Tp)));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(pointer __p, size_type)</span> </span>&#123;</span><br><span class="line">        ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(__p)</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h2 id="gnu-cxx-malloc-allocator"><a href="#gnu-cxx-malloc-allocator" class="headerlink" title="__gnu_cxx::malloc_allocator"></a>__gnu_cxx::malloc_allocator</h2><p>直接用 <code>std::malloc</code> 和 <code>std::free</code> 实现的 好处是 少调用 一次 <code>operator new</code> 和 <code>operator delete</code> 函数 节省一次入栈出栈开销 也没啥特色</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">malloc_allocator</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">pointer <span class="title">allocate</span><span class="params">(size_type __n, <span class="keyword">const</span> <span class="keyword">void</span>* = <span class="number">0</span>)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (__n &gt; <span class="keyword">this</span>-&gt;max_size()) &#123;</span><br><span class="line">            <span class="built_in">std</span>::__throw_bad_alloc();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        pointer __ret = <span class="keyword">static_cast</span>&lt;_Tp*&gt;(<span class="built_in">std</span>::<span class="built_in">malloc</span>(__n * <span class="keyword">sizeof</span>(_Tp)));</span><br><span class="line">        <span class="keyword">if</span> (!__ret) &#123;</span><br><span class="line">            <span class="built_in">std</span>::__throw_bad_alloc();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> __ret;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// __p is not permitted to be a null pointer.</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(pointer __p, size_type)</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">free</span>(<span class="keyword">static_cast</span>&lt;<span class="keyword">void</span>*&gt;(__p));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="gnu-cxx-pool-allocator"><a href="#gnu-cxx-pool-allocator" class="headerlink" title="__gnu_cxx::pool_allocator"></a>__gnu_cxx::pool_allocator</h2><p>G2.9 容器默认配置器 在 我的 <a href="https://www.yuerer.com/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-STL%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/" target="_blank" rel="noopener">C++ 内存管理 之 STL内存分配实现原理</a> 这篇文章中 的 <code>GNU-C++ 2.9 std::alloc 原理</code> 已经分析过了</p>
<h2 id="gnu-cxx-mt-alloc"><a href="#gnu-cxx-mt-alloc" class="headerlink" title="gnu_cxx::mt_alloc"></a><strong>gnu_cxx::</strong>mt_alloc</h2><p>适用于多线程的内存分配</p>
<h2 id="gnu-cxx-array-allocator"><a href="#gnu-cxx-array-allocator" class="headerlink" title="__gnu_cxx::array_allocator"></a>__gnu_cxx::array_allocator</h2><p>用于分配固定大小的内存块 使用 标准库 中的 <code>std::array</code> 实现 无需再去调用 <code>::operator new</code> 和 <code>::operator delete</code> 在进入 main 之前就可以用了 因为是用的 静态数组</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// tr1 是因为 在 C++ 11之前 array不属于标准库的内容 属于一个小版本</span></span><br><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp, <span class="keyword">typename</span> _Array = <span class="built_in">std</span>::tr1::<span class="built_in">array</span>&lt;_Tp, <span class="number">1</span>&gt; &gt;</span><br><span class="line">class array_allocator : <span class="keyword">public</span> array_allocator_base&lt;_Tp&gt; &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> _Array  array_type;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    <span class="comment">// 数组指针</span></span><br><span class="line">    array_type*     _M_array;</span><br><span class="line">    <span class="comment">// 数组已使用量</span></span><br><span class="line">    size_type       _M_used;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="comment">// 简单的说就是 从外面将数组指针传进来 然后保存到 data member</span></span><br><span class="line">    array_allocator(array_type* __array = <span class="number">0</span>) <span class="keyword">throw</span>() </span><br><span class="line">    : _M_array(__array), _M_used(size_type()) &#123; &#125;</span><br><span class="line"></span><br><span class="line">    pointer allocate(size_type __n, <span class="keyword">const</span> <span class="keyword">void</span>* = <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (_M_array == <span class="number">0</span> || _M_used + __n &gt; _M_array-&gt;<span class="built_in">size</span>()) &#123;</span><br><span class="line">            <span class="built_in">std</span>::__throw_bad_alloc();</span><br><span class="line">        &#125;</span><br><span class="line">        pointer __ret = _M_array-&gt;<span class="built_in">begin</span>() + _M_used;</span><br><span class="line">        _M_used += __n;</span><br><span class="line">        <span class="keyword">return</span> __ret;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>要注意的是 array 因为是静态的 所以不需要 释放 因此 你调用 <code>deallocate</code> 是没有任何操作的</p>
<p>不过 如果说 <code>deallocate</code> 能够回收掉已经分出去的数组中的某块的话 那可能利用率更高一些</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 静态数组</span></span><br><span class="line"><span class="keyword">int</span> my[<span class="number">65536</span>];</span><br><span class="line">array_allocator&lt;int, array&lt;int, 65536&gt;&gt; myAlloc(&amp;my);</span><br><span class="line"><span class="comment">// 相当于分配了 数组中的 3块出去</span></span><br><span class="line"><span class="keyword">int</span> *p = myAlloc.allocate(<span class="number">3</span>);</span><br><span class="line"><span class="comment">// 不会有任何操作</span></span><br><span class="line">myAlloc.deallocate(p);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 堆中的数组</span></span><br><span class="line"><span class="keyword">typedef</span> ARRAY <span class="built_in">std</span>::<span class="built_in">array</span>&lt;<span class="keyword">int</span>, 65536&gt;;</span><br><span class="line">ARRAY *pa = <span class="keyword">new</span> ARRAY;</span><br><span class="line">array_allocator&lt;int, ARRAY&gt; myAlloc(pa);</span><br></pre></td></tr></table></figure>

<h2 id="gnu-cxx-debug-allocator"><a href="#gnu-cxx-debug-allocator" class="headerlink" title="__gnu_cxx::debug_allocator"></a>__gnu_cxx::debug_allocator</h2><p>不做分配归还动作 里面传入一个真正的 <code>allocator</code> 来分配归还内存 正如其名 不做事情 只是用来 debug 用的 每次申请 多申请一块 来记录 分配的内存 然后归还的时候 <code>assert</code> 来查看 分配的 <code>size</code> 是否正确</p>
<p>有点类似 <code>malloc</code> 中的 cookie</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Alloc&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">debug_allocator</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">typename</span> _Traits::size_type      size_type;</span><br><span class="line">    <span class="keyword">typedef</span> <span class="keyword">typename</span> _Traits::value_type     value_type;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    size_type 		_M_extra;  </span><br><span class="line">    _Alloc			_M_allocator;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算 用来记录我分配的内存块的大小的区块 占用了几个元素单位</span></span><br><span class="line">    size_type _S_extra() &#123;</span><br><span class="line">        <span class="keyword">const</span> <span class="keyword">size_t</span> __obj_size = <span class="keyword">sizeof</span>(value_type);</span><br><span class="line">        <span class="keyword">return</span> (<span class="keyword">sizeof</span>(size_type) + __obj_size - <span class="number">1</span>) / __obj_size; </span><br><span class="line">    &#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    debug_allocator(<span class="keyword">const</span> _Alloc&amp; __a)</span><br><span class="line">    : _M_allocator(__a), _M_extra(_S_extra()) &#123; &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">pointer <span class="title">allocate</span><span class="params">(size_type __n)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 多分配 extra 个 单位 用于记录 当前分配的区块大小</span></span><br><span class="line">        pointer __res = _M_allocator.allocate(__n + _M_extra);      </span><br><span class="line">        size_type* __ps = <span class="keyword">reinterpret_cast</span>&lt;size_type*&gt;(__res);</span><br><span class="line">        *__ps = __n;</span><br><span class="line">        <span class="keyword">return</span> __res + _M_extra;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(pointer __p, size_type __n)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 检查 回收回来的大小和 区块记录的大小是否一致</span></span><br><span class="line">        <span class="keyword">using</span> <span class="built_in">std</span>::__throw_runtime_error;</span><br><span class="line">        <span class="keyword">if</span> (__p) &#123;</span><br><span class="line">            pointer __real_p = __p - _M_extra;</span><br><span class="line">            <span class="keyword">if</span> (*<span class="keyword">reinterpret_cast</span>&lt;size_type*&gt;(__real_p) != __n) &#123;</span><br><span class="line">                __throw_runtime_error(<span class="string">"debug_allocator::deallocate wrong size"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            _M_allocator.deallocate(__real_p, __n + _M_extra);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            __throw_runtime_error(<span class="string">"debug_allocator::deallocate null pointer"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="gnu-cxx-bitmap-allocator"><a href="#gnu-cxx-bitmap-allocator" class="headerlink" title="__gnu_cxx::bitmap_allocator"></a>__gnu_cxx::bitmap_allocator</h2><p>使用 <code>bitmap</code> 来查找 被使用和未被使用的内存块</p>
<p>内部实现了一个 <code>mini vector</code> 和 普通的 <code>vector</code> 一样 会 <code>两倍</code> 成长</p>
<p>一整块称之为 Super Block 每一个 Block 相当于一个元素单位</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  Super Blocks Size     记录已使用的块数    位图数组 记录某块是否被使用        64个Blocks 被 mini vector 所管理</span><br><span class="line">[|  Super Blocks Size  |  Use Counts  |  bitmap[1]  |  bitmap[0]  |  [1][][][][][][]...[][][][][][][][][]  |]</span><br><span class="line">                                                            1110 </span><br><span class="line">                                               &lt;-bitmap 记录的方向     Super Blocks 使用的方向-&gt;</span><br></pre></td></tr></table></figure>
<p>假设 此时在32位系统下 一个内存块为 8 字节 则 Super Blocks Size 为 = 4 + (4 * 2) + 8 * (64 * 8) = 524字节</p>
<p>如果 全回收了 就 按照 Super Block 的大小 顺序放到 <code>free_list</code> 中 其实也是一个 vector 并且下次分配规模减半 (因为vector 每次分配都是两倍递增) 当 <code>free_list</code> 超过64个 Super Block 时 如果最后进来的 比最后面的还要大 就将其还给 OS (总而言之就是把最大的还给 OS)</p>
<p>如果 前面一个 Super Block 本来没区块 现在回收到了区块 此时又请求了区块的话 会从后面一个 Super Block 去分配 但是如果 后面一个 Super Block 不存在的话 则从 前面一个 Super Block 取</p>
<p><code>free_list</code> 存在的意义就是 先把 Super Block 存起来 万一以后有用 就不用重新创建了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> _Tp&gt;</span><br><span class="line"><span class="comment">// free list 其实就是个 vector</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">bitmap_allocator</span> :</span> <span class="keyword">private</span> free_list &#123;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function">pointer <span class="title">allocate</span><span class="params">(size_type __n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (__n &gt; <span class="keyword">this</span>-&gt;max_size()) &#123;</span><br><span class="line">	        <span class="built_in">std</span>::__throw_bad_alloc();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 只负责一个元素单位 超过的话 就不归它管了 实际上正常使用容器 都是一个个元素申请内存的</span></span><br><span class="line">        <span class="comment">// 除非你直接通过构造函数 给容器塞东西 就是一次申请 N个元素单位内存</span></span><br><span class="line">        <span class="keyword">if</span> (__builtin_expect(__n == <span class="number">1</span>, <span class="literal">true</span>)) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>-&gt;_M_allocate_single_object();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123; </span><br><span class="line">            <span class="keyword">const</span> size_type __b = __n * <span class="keyword">sizeof</span>(value_type);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">reinterpret_cast</span>&lt;pointer&gt;(::<span class="keyword">operator</span> <span class="keyword">new</span>(__b));</span><br><span class="line">	    &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(pointer __p, size_type __n)</span> <span class="title">throw</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (__builtin_expect(__p != <span class="number">0</span>, <span class="literal">true</span>)) &#123;</span><br><span class="line">            <span class="keyword">if</span> (__builtin_expect(__n == <span class="number">1</span>, <span class="literal">true</span>)) &#123;</span><br><span class="line">                <span class="keyword">this</span>-&gt;_M_deallocate_single_object(__p);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                ::<span class="keyword">operator</span> <span class="keyword">delete</span>(__p);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>




]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>内存管理</tag>
        <tag>GNU</tag>
        <tag>STL</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 内存管理 之 Loki Allocator</title>
    <url>/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-Loki-Allocator/</url>
    <content><![CDATA[<p>Loki Allocator 有三个类 这玩意对比起 pool allocator 的优点就在于 它有把内存还给系统</p>
<p>但是它作为一个 内存分配器 却在里面使用了 vector 作为容器… 按道理来说 应该在其内部实现一个简易版的 vector 才不会那么奇怪 但也没所谓 只是 先用了一次标准库的分配器 后续使用容器的时候 就可以用 Loki 分配器了.</p>
<ul>
<li>Chunk</li>
<li>FixedAllocator</li>
<li>SmallObjAllocator</li>
</ul>
<h2 id="Chunk-解剖"><a href="#Chunk-解剖" class="headerlink" title="Chunk 解剖"></a>Chunk 解剖</h2><p>Chunk 是整个分配器的最底层 里面主要是三个 成员变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 指向内存块</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">char</span> * pData_;</span><br><span class="line"><span class="comment">// 目前可用区块的第一块的索引</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">char</span> firstAvailableBlock_;</span><br><span class="line"><span class="comment">// 有多少块可用</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">char</span> blocksAvailable_;</span><br></pre></td></tr></table></figure>

<p>Chunk 的几个关键函数 已进行 剪裁 和 修改</p>
<h3 id="Chunk-分配"><a href="#Chunk-分配" class="headerlink" title="Chunk 分配"></a>Chunk 分配</h3><p>分配流程很简单 就是 申请了内存以后 将每个小区块的第一个字节设置为索引 排好号</p>
<p>取的流程:</p>
<ol>
<li>从 当前可用区块的第一块索引中取 得当前可用区块</li>
<li>当前可用区块会被返回</li>
<li>被返回的可用区块中的索引 被设置到 当前可用区块索引去(这样下一次就会使用到它)</li>
<li>可用区块数目 - 1</li>
</ol>
<a id="more"></a>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">firstAvailableBlock_ &#x3D; 4</span><br><span class="line">blocksAvailable_     &#x3D; 63</span><br><span class="line">-------------------------</span><br><span class="line">            4</span><br><span class="line">-------------------------</span><br><span class="line">            0</span><br><span class="line">-------------------------</span><br><span class="line">            3</span><br><span class="line">-------------------------</span><br><span class="line">            1</span><br><span class="line">-------------------------</span><br><span class="line">            2            &lt;- 即将被分出去</span><br><span class="line">-------------------------</span><br><span class="line">            6</span><br><span class="line">-------------------------</span><br><span class="line">            7</span><br><span class="line">-------------------------</span><br><span class="line">           ...</span><br><span class="line">-------------------------</span><br><span class="line">           64</span><br><span class="line">-------------------------</span><br><span class="line">首先 因为当前可用区块索引 为 4 所以会找到 索引号为 2的区块</span><br><span class="line">其次 找到了以后 这个区块的索引号 会被设置到 当前可用区块索引去 </span><br><span class="line">这样下次就会用索引为 2 的区块</span><br><span class="line">然后 区块可用数 - 1</span><br><span class="line">-------------------------</span><br><span class="line">firstAvailableBlock_ &#x3D; 2</span><br><span class="line">blocksAvailable_     &#x3D; 62</span><br><span class="line">-------------------------</span><br><span class="line">            4</span><br><span class="line">-------------------------</span><br><span class="line">            0</span><br><span class="line">-------------------------</span><br><span class="line">            3            &lt;-即将被分出去</span><br><span class="line">-------------------------</span><br><span class="line">            1</span><br><span class="line">-------------------------</span><br><span class="line">        已经被分配了</span><br><span class="line">-------------------------</span><br><span class="line">            6</span><br><span class="line">-------------------------</span><br><span class="line">            7</span><br><span class="line">-------------------------</span><br><span class="line">           ...</span><br><span class="line">-------------------------</span><br><span class="line">           64</span><br><span class="line">-------------------------</span><br></pre></td></tr></table></figure>

<h3 id="Chunk-回收"><a href="#Chunk-回收" class="headerlink" title="Chunk 回收"></a>Chunk 回收</h3><p>存的流程:</p>
<ol>
<li>首先 我们要明确 回收回来的区块 下次分配优先分配</li>
<li>计算 回收回来的区块 应该放到哪里 (idx 为索引)</li>
<li>那么 当前可用区块的索引号就应该设置为 idx 这样才能保证下次一定先用它</li>
<li>那之前的索引号 就会被设置到 回收回来的区块的索引中去(分配完回收的区块后 就会分配之前本应该被分配的区块 后来因为回收了新的区块 而没被分配出去的区块 好绕口…)</li>
<li>当前可用区块 + 1</li>
</ol>
<h3 id="Chunk-函数实现"><a href="#Chunk-函数实现" class="headerlink" title="Chunk 函数实现"></a>Chunk 函数实现</h3><p>这块设计我个人觉得有一点不好的地方 就是 <code>Chunk::Init</code> 的时候 会传入 <code>blockSize</code> 但是 <code>Chunk::Allocate</code> 也会传入 <code>blockSize</code> 如果 这两块的 <code>blockSize</code> 不一致 则会出错</p>
<p>个人想法是 在 Init 的时候 将 <code>blockSize</code> 存入 data member 中去  在 Allocator 则不再传入</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 简单的说 就是开辟 blockSize * blocks 那么大内存</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">Chunk::Init</span><span class="params">( ::<span class="built_in">std</span>::<span class="keyword">size_t</span> blockSize, <span class="keyword">unsigned</span> <span class="keyword">char</span> blocks )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> allocSize = blockSize * blocks;</span><br><span class="line">    pData_ = <span class="keyword">static_cast</span>&lt; <span class="keyword">unsigned</span> <span class="keyword">char</span> * &gt;( ::<span class="keyword">operator</span> <span class="keyword">new</span> ( allocSize ) );</span><br><span class="line">    Reset( blockSize, blocks );</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 填补 data member 含义上面已经给出了</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Chunk::Reset</span><span class="params">(::<span class="built_in">std</span>::<span class="keyword">size_t</span> blockSize, <span class="keyword">unsigned</span> <span class="keyword">char</span> blocks)</span> </span>&#123;</span><br><span class="line">    firstAvailableBlock_ = <span class="number">0</span>;</span><br><span class="line">    blocksAvailable_ = blocks;</span><br><span class="line">    <span class="comment">// 这里很骚啊 把每个区块的前一个字节 来记录索引...</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> ( <span class="keyword">unsigned</span> <span class="keyword">char</span> * p = pData_; i != blocks; p += blockSize ) &#123;</span><br><span class="line">        <span class="comment">// 此处注意 是从 1开始排噢 因为是 ++i</span></span><br><span class="line">        *p = ++i;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 释放内存块</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Chunk::Release</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span> <span class="params">( pData_ )</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span>* <span class="title">Chunk::Allocate</span><span class="params">(::<span class="built_in">std</span>::<span class="keyword">size_t</span> blockSize)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ( <span class="number">0</span> == blocksAvailable_ ) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 1. 取得 目前可用的第一块</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> * pResult = pData_ + (firstAvailableBlock_ * blockSize);</span><br><span class="line">    firstAvailableBlock_ = *pResult;</span><br><span class="line">    <span class="comment">// 2. 当前可用区块 - 1</span></span><br><span class="line">    --blocksAvailable_;</span><br><span class="line">    <span class="comment">// 3. 将目前可用第一块返回回去</span></span><br><span class="line">    <span class="keyword">return</span> pResult;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Chunk::Deallocate</span><span class="params">(<span class="keyword">void</span>* p, ::<span class="built_in">std</span>::<span class="keyword">size_t</span> blockSize)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span>* toRelease = <span class="keyword">static_cast</span>&lt;<span class="keyword">unsigned</span> <span class="keyword">char</span>*&gt;(p);</span><br><span class="line">    <span class="comment">// 找这个指针在 区块中的索引</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">char</span> index = <span class="keyword">static_cast</span>&lt; <span class="keyword">unsigned</span> <span class="keyword">char</span> &gt;(</span><br><span class="line">        ( toRelease - pData_ ) / blockSize);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 回收回来以后 下次优先用它</span></span><br><span class="line">    <span class="comment">// 1. 回收的区块里的索引 设置为 当前可用区块的索引</span></span><br><span class="line">    *toRelease = firstAvailableBlock_;</span><br><span class="line">    <span class="comment">// 2. 当前的第一块可用索引修改为 回收回来的区块应该放置的位置</span></span><br><span class="line">    <span class="comment">// 不理解的可以多看几次 很绕口!</span></span><br><span class="line">    firstAvailableBlock_ = index;</span><br><span class="line">    <span class="comment">// 3. 可用数 + 1</span></span><br><span class="line">    ++blocksAvailable_;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="FixedAllocator-解剖"><a href="#FixedAllocator-解剖" class="headerlink" title="FixedAllocator 解剖"></a>FixedAllocator 解剖</h2><p>FixedAllocator 里面存着 vector<Chunk></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vector&lt;Chunk&gt;</span></span><br><span class="line"><span class="keyword">typedef</span> ::<span class="built_in">std</span>::<span class="built_in">vector</span>&lt; Chunk &gt; Chunks;</span><br><span class="line">Chunks chunks_;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指向 vector&lt;Chunk&gt; 其中某两个 Chunk</span></span><br><span class="line"><span class="comment">// allocChunk_ 指向 上一次 分配出去过区块的 Chunk</span></span><br><span class="line">Chunk * allocChunk_;</span><br><span class="line"><span class="comment">// deallocChunk_ 指向 上一次 回收过区块的 Chunk</span></span><br><span class="line"><span class="comment">// 个人觉得是局部性原理 就上次满足过了 这次很大概率也有可能满足</span></span><br><span class="line">Chunk * deallocChunk_;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 指向 唯一一个空Chunk 如果没有 则 为 nullptr</span></span><br><span class="line">Chunk * emptyChunk_;</span><br></pre></td></tr></table></figure>

<h3 id="FixedAllocator-分配"><a href="#FixedAllocator-分配" class="headerlink" title="FixedAllocator 分配"></a>FixedAllocator 分配</h3><p>分配流程 简单的说就是 用 Chunk 去分配 那么 FixedAllocator 最主要的任务就是去找 能够分配内存的 Chunk</p>
<ol>
<li>先看看 allocChunk_(指向上次分配过区块的Chunk) 有没有效 有效则直接分配</li>
<li>无效 则去找 emptyChunk_(指向一个空Chunk) 有效直接分配</li>
<li>无效 则依次遍历 vector 找到 其中一个 能分配区块的 Chunk</li>
<li>找不到 则直接 创建一个新的 Chunk 并将其 push_back 到 vector 中</li>
</ol>
<h3 id="FixedAllocator-回收"><a href="#FixedAllocator-回收" class="headerlink" title="FixedAllocator 回收"></a>FixedAllocator 回收</h3><p>回收流程 就是先去找 要回收的区块在哪个 Chunk 中 这里先从 上一次回收过区块的 Chunk 中查看 再从 上一次分配过区块的 Chunk 中查看 如果都没有 则用一种 特殊的搜索方法 就是从 上一次回收过区块的 Chunk 作为临界点 每次循环 一次向上找 一次向下找 直到找到以后 再去调用释放函数 如果发生了全回收 则 看看是否已经有一块全回收了(因为要2块全回收才释放掉其中一块 避免突然又要用到那块Chunk)</p>
<h3 id="FixedAllocator-函数实现"><a href="#FixedAllocator-函数实现" class="headerlink" title="FixedAllocator 函数实现"></a>FixedAllocator 函数实现</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">FixedAllocator::Allocate</span><span class="params">( <span class="keyword">void</span> )</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 上次分配出去过区块的chunk 如果为 空 或者是 该chunk可用区块为 0</span></span><br><span class="line">    <span class="comment">// 则 将 空 chunk 赋值给它</span></span><br><span class="line">    <span class="comment">// 如果没有空 chunk  </span></span><br><span class="line">    <span class="keyword">if</span> ( ( <span class="literal">nullptr</span> == allocChunk_ ) || <span class="number">0</span> == allocChunk_-&gt;blocksAvailable_ ) &#123;</span><br><span class="line">        <span class="keyword">if</span> ( <span class="literal">nullptr</span> != emptyChunk_ ) &#123;</span><br><span class="line">            allocChunk_ = emptyChunk_;</span><br><span class="line">            emptyChunk_ = <span class="literal">nullptr</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 都没有 则从头找其 遍历一遍 vector</span></span><br><span class="line">            Chunks::iterator i = chunks_.<span class="built_in">begin</span>();</span><br><span class="line">            <span class="keyword">for</span> ( ; ; ++i ) &#123;</span><br><span class="line">                <span class="comment">// 到达尾部 还是没找着 就创建一个 临时对象 init 这个chunk</span></span><br><span class="line">                <span class="comment">// 然后将其 push 到 容器中</span></span><br><span class="line">                <span class="keyword">if</span> ( chunks_.<span class="built_in">end</span>() == i ) &#123;</span><br><span class="line">                    chunks_.push_back(Chunk());</span><br><span class="line">                    Chunk &amp;newChunk = chunks_.back();</span><br><span class="line">                    newChunk.Init(blockSize_, numBlocks_);</span><br><span class="line">                    <span class="comment">// 记录好 下次就从这里面去取区块</span></span><br><span class="line">                    allocChunk_ = &amp;newChunk;</span><br><span class="line">                    <span class="comment">// vector push_back的时候有可能发生成长</span></span><br><span class="line">                    <span class="comment">// vector 成长是两倍申请内存然后将其拷贝过去</span></span><br><span class="line">                    <span class="comment">// 如果不重新取迭代器地址的话 有可能该迭代器失效</span></span><br><span class="line">                    deallocChunk_ = &amp;chunks_.front();</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 如果 找到一个chunk 的可用区块大于 0 则 用它来分配</span></span><br><span class="line">                <span class="keyword">if</span> ( i-&gt;blocksAvailable_ &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// i是迭代器 *i 等于 取迭代器的值就是 Chunk &amp;*i 等于 取迭代器的值的地址 pData_</span></span><br><span class="line">                    allocChunk_ = &amp;*i;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="comment">// 如果 上次分配区块的chunk是emptyChunk 则将 emptyChunk 置为空</span></span><br><span class="line">    <span class="comment">// 因为 emptyChunk 已经被用了</span></span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ( allocChunk_ == emptyChunk_) &#123;</span><br><span class="line">        emptyChunk_ = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 上次分配过区块的chunk 如果还能分配 则直接分配返回回去</span></span><br><span class="line">    <span class="keyword">void</span> * place = allocChunk_-&gt;Allocate( blockSize_ );</span><br><span class="line">    <span class="keyword">return</span> place;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">FixedAllocator::Deallocate</span><span class="params">( <span class="keyword">void</span> * p )</span> </span>&#123;</span><br><span class="line">    Chunk * foundChunk = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> chunkLength = numBlocks_ * blockSize_;</span><br><span class="line">    <span class="comment">// p 先看看是否在之前回收过区块的 Chunk 里面</span></span><br><span class="line">    <span class="comment">// 再看看是否在 之前分配过区块的 Chunk 里面</span></span><br><span class="line">    <span class="keyword">if</span> ( p &gt;= deallocChunk_-&gt;pData_ &amp;&amp; p &lt; deallocChunk_-&gt;pData_ + chunkLength ) &#123;</span><br><span class="line">        foundChunk = deallocChunk_;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> ( p &gt;= allocChunk_-&gt;pData_ &amp;&amp; p &lt; allocChunk_-&gt;pData_ + chunkLength ) &#123;</span><br><span class="line">        foundChunk = allocChunk_;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="comment">// 实在找不到 再去搜索 这个搜索方式很特别 是夹着搜索</span></span><br><span class="line">        foundChunk = VicinityFind( p );</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> ( <span class="literal">nullptr</span> == foundChunk ) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    deallocChunk_ = foundChunk;</span><br><span class="line">    <span class="comment">// 此时再真正的去 deallocate</span></span><br><span class="line">    DoDeallocate(p);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function">Chunk * <span class="title">FixedAllocator::VicinityFind</span><span class="params">( <span class="keyword">void</span> * p )</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ( chunks_.empty() ) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> chunkLength = numBlocks_ * blockSize_;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// lo指向 上一次回收过区块的 Chunk</span></span><br><span class="line">    <span class="comment">// hi指向 上一次回收过区块的 Chunk 的下一个</span></span><br><span class="line">    Chunk * lo = deallocChunk_;</span><br><span class="line">    Chunk * hi = deallocChunk_ + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">const</span> Chunk * loBound = &amp;chunks_.front();</span><br><span class="line">    <span class="keyword">const</span> Chunk * hiBound = &amp;chunks_.back() + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ( hi == hiBound ) &#123;</span><br><span class="line">        hi = <span class="literal">nullptr</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="comment">// lo 往上走 hi 往下走 直到找到 或者 触碰到边界</span></span><br><span class="line">        <span class="keyword">if</span> (lo) &#123;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ( p &gt;= lo-&gt;pData_ &amp;&amp; p &lt; lo-&gt;pData_ + chunkLength ) &#123;</span><br><span class="line">                <span class="keyword">return</span> lo;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> ( lo == loBound ) &#123;</span><br><span class="line">                lo = <span class="literal">nullptr</span>;</span><br><span class="line">                <span class="keyword">if</span> ( <span class="literal">nullptr</span> == hi ) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; </span><br><span class="line">                --lo;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (hi) &#123;</span><br><span class="line">            <span class="keyword">if</span> ( p &gt;= hi-&gt;pData_ &amp;&amp; p &lt; hi-&gt;pData_ + chunkLength ) &#123;</span><br><span class="line">                <span class="keyword">return</span> hi;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> ( ++hi == hiBound ) &#123;</span><br><span class="line">                hi = <span class="literal">nullptr</span>;</span><br><span class="line">                <span class="keyword">if</span> ( <span class="literal">nullptr</span> == lo ) &#123;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 总得来说就是 当有2个全回收的Chunk才释放掉一个</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">FixedAllocator::DoDeallocate</span><span class="params">(<span class="keyword">void</span>* p)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 让 Chunk 回收掉这个区块</span></span><br><span class="line">    deallocChunk_-&gt;Deallocate(p, blockSize_);</span><br><span class="line">    <span class="comment">// 如果这个Chunk 已经全回收了</span></span><br><span class="line">    <span class="keyword">if</span> ( deallocChunk_-&gt;blocksAvailable_ == numBlocks_ ) &#123;</span><br><span class="line">        <span class="comment">// emptyChunk 已经指向了 一个空 Chunk 的话</span></span><br><span class="line">        <span class="comment">// lastChunk 指向 vector 最后一个</span></span><br><span class="line">        <span class="comment">// 为了效率 如果 lastChunk 就是 回收的Chunk 那就直接将 emptyChunk 赋值给 当前回收的Chunk</span></span><br><span class="line">        <span class="comment">// 这样才能直接 pop_back</span></span><br><span class="line">        <span class="comment">// 同理 如果最后一个 Chunk 不是 emptyChunk 那就直接交换 这样才能 通过简单地 pop_back 将其移除</span></span><br><span class="line">        <span class="keyword">if</span> ( <span class="literal">nullptr</span> != emptyChunk_ ) &#123;</span><br><span class="line">            Chunk * lastChunk = &amp;chunks_.back();</span><br><span class="line">            <span class="keyword">if</span> ( lastChunk == deallocChunk_ ) &#123;</span><br><span class="line">                deallocChunk_ = emptyChunk_;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> ( lastChunk != emptyChunk_ ) &#123;</span><br><span class="line">                ::<span class="built_in">std</span>::swap( *emptyChunk_, *lastChunk );</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 最后释放掉内存</span></span><br><span class="line">            lastChunk-&gt;Release();</span><br><span class="line">            chunks_.pop_back();</span><br><span class="line">            <span class="comment">// 修正 allocChunk 指针 防止 被释放掉的是 allocChunk</span></span><br><span class="line">            <span class="keyword">if</span> ( ( allocChunk_ == lastChunk ) || allocChunk_-&gt;blocksAvailable_ == <span class="number">0</span> ) &#123;</span><br><span class="line">                allocChunk_ = deallocChunk_;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        emptyChunk_ = deallocChunk_;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="SmallObjAllocator-解剖"><a href="#SmallObjAllocator-解剖" class="headerlink" title="SmallObjAllocator 解剖"></a>SmallObjAllocator 解剖</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// vector&lt;FixedAllocator&gt; 其实是个数组 你可以理解成 vector</span></span><br><span class="line">::Loki::Private::FixedAllocator * pool_;</span><br><span class="line"><span class="comment">// 支持分配的最大内存大小</span></span><br><span class="line"><span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> maxSmallObjectSize_;</span><br><span class="line"><span class="comment">// 用于对齐操作</span></span><br><span class="line"><span class="keyword">const</span> <span class="built_in">std</span>::<span class="keyword">size_t</span> objectAlignSize_;</span><br></pre></td></tr></table></figure>

<h3 id="SmallObjAllocator-分配实现"><a href="#SmallObjAllocator-分配实现" class="headerlink" title="SmallObjAllocator 分配实现"></a>SmallObjAllocator 分配实现</h3><p>分配内存 简单的说 就是从 vector<FixedAllocator> 找合适的 FixedAllocator 然后再用底层的 FixedAllocator 去找 Chunk 分配内存</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> * <span class="title">SmallObjAllocator::Allocate</span><span class="params">( ::<span class="built_in">std</span>::<span class="keyword">size_t</span> numBytes, <span class="keyword">bool</span> doThrow )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ( numBytes &gt; GetMaxObjectSize() ) &#123;</span><br><span class="line">        <span class="comment">// 这里就是调用 ::operator new</span></span><br><span class="line">        <span class="keyword">return</span> DefaultAllocator( numBytes, doThrow );</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ( <span class="number">0</span> == numBytes ) &#123;</span><br><span class="line">        numBytes = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 获取即将要分配的内存大小 属于哪个 pool 的下标</span></span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> index = GetOffset( numBytes, GetAlignment() ) - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> allocCount = GetOffset( GetMaxObjectSize(), GetAlignment() );</span><br><span class="line"></span><br><span class="line">    FixedAllocator &amp; allocator = pool_[ index ];</span><br><span class="line">    <span class="comment">// 使用底层的 FixedAllocator 去分配</span></span><br><span class="line">    <span class="keyword">void</span> * place = allocator.Allocate();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 内存不足 则尝试去释放内存 释放其他 FixedAllocator 下的 空 Chunk 归还给 OS 然后再分配</span></span><br><span class="line">    <span class="keyword">if</span> ( ( <span class="literal">nullptr</span> == place ) &amp;&amp; TrimExcessMemory() ) &#123;</span><br><span class="line">        place = allocator.Allocate();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ( ( <span class="literal">nullptr</span> == place ) &amp;&amp; doThrow ) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="built_in">std</span>::bad_alloc();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> place;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里有一个有意思的点 <code>SmallObjAllocator::TrimExcessMemory()</code> 这个函数 会遍历所有的 FixedAllocator 然后 调用 <code>FixedAllocator::TrimChunkList()</code> 这里面有一段代码</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">FixedAllocator::TrimChunkList</span><span class="params">( <span class="keyword">void</span> )</span> </span>&#123;</span><br><span class="line"><span class="comment">// ... 省略</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// Use the "make-a-temp-and-swap" trick to remove excess capacity.</span></span><br><span class="line">        <span class="comment">// 这里的意思是说 我将 chunks_ 这个 vector 复制了一份 到 temp</span></span><br><span class="line">        <span class="comment">// 此时 temp的容量 == chunks_的大小 但不一定等于 容量 因为 vector 会扩大一部分作为后备内存</span></span><br><span class="line">        <span class="comment">// 然后进行一次交换 这样 chunks_ 假设 本来 capacity 是 140 但是 size 只有 100 进行了交换</span></span><br><span class="line">        <span class="comment">// chunks_ 现在的 capacity == size == 100 也就还了 40个 size 给了 OS</span></span><br><span class="line">        <span class="function">Chunks <span class="title">temp</span><span class="params">( chunks_ )</span></span>;</span><br><span class="line">        temp.swap( chunks_ );</span><br><span class="line">    &#125;</span><br><span class="line"><span class="comment">// ... 省略</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="SmallObjAllocator-回收实现"><a href="#SmallObjAllocator-回收实现" class="headerlink" title="SmallObjAllocator 回收实现"></a>SmallObjAllocator 回收实现</h3><p>回收内存 找回收的内存对应的 FixedAllocator 然后调用下一层的 FixedAllocator 去进行真的是回收操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">SmallObjAllocator::Deallocate</span><span class="params">( <span class="keyword">void</span> * p )</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ( <span class="literal">nullptr</span> == p ) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    FixedAllocator * pAllocator = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">const</span> ::<span class="built_in">std</span>::<span class="keyword">size_t</span> allocCount = GetOffset( GetMaxObjectSize(), GetAlignment() );</span><br><span class="line">    Chunk * chunk = <span class="literal">nullptr</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> ( ::<span class="built_in">std</span>::<span class="keyword">size_t</span> ii = <span class="number">0</span>; ii &lt; allocCount; ++ii ) &#123;</span><br><span class="line">        chunk = pool_[ ii ].HasBlock( p );</span><br><span class="line">        <span class="keyword">if</span> ( <span class="literal">nullptr</span> != chunk ) &#123;</span><br><span class="line">            pAllocator = &amp;pool_[ ii ];</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> ( <span class="literal">nullptr</span> == pAllocator ) &#123;</span><br><span class="line">        <span class="comment">// operator delete</span></span><br><span class="line">        DefaultDeallocator( p );</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">bool</span> found = pAllocator-&gt;Deallocate( p, chunk );</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>内存管理</tag>
        <tag>Loki</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 内存管理 之 STL内存分配实现原理</title>
    <url>/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-STL%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/</url>
    <content><![CDATA[<p>本文主要讲的是 各个C++版本 中的STL标准库中的默认内存分配器 的 各种实现</p>
<h2 id="VC6-标准分配器实现"><a href="#VC6-标准分配器实现" class="headerlink" title="VC6 标准分配器实现"></a>VC6 标准分配器实现</h2><p>VC6 它什么内存管理都没做 直接调用的 <code>operator new</code> 而 <code>operator new</code> 内部实际上 调用的就是 <code>malloc</code></p>
<p>此外 VC6 下 是以元素为单位 比如 int 的话 他就会分配 512 ints 如果是 double 则会分配 512 doubles</p>
<p>而 <code>GNU-C++ 2.9</code> 以字节为单位 <code>GNU-C++ 4.9</code> 以元素为单位 在 G2.9 中的容器默认分配器 被移到了 <code>pool_allocator</code> 中</p>
<p>注意 G2.9 的标准分配器 和 容器默认分配器 不一样噢!</p>
<h2 id="GNU-C-2-9-标准分配器实现"><a href="#GNU-C-2-9-标准分配器实现" class="headerlink" title="GNU-C++ 2.9 标准分配器实现"></a>GNU-C++ 2.9 标准分配器实现</h2><p><code>std::allocator</code> 标准分配器 也是什么都没做</p>
<p>但是 <code>G2.9</code> 容器使用的分配器 不是上面的那个 <code>std::allocator</code> 而是 <code>std::alloc</code></p>
<a id="more"></a>

<h2 id="GNU-C-4-9-标准分配器实现"><a href="#GNU-C-4-9-标准分配器实现" class="headerlink" title="GNU-C++ 4.9 标准分配器实现"></a>GNU-C++ 4.9 标准分配器实现</h2><p>也是什么都没做… 没有任何的特殊设计</p>
<p><code>G4.9</code> 里有很多扩展的分配器 其中 <code>G2.9</code> 中的 <code>std::alloc</code> 变成了 <code>__pool_alloc</code></p>
<p>因此 用 <code>vector&lt;string, __gnu_cxx::__pool_alloc&lt;string&gt;&gt; vec;</code> 能够节约不少 内存分配的内碎片 因为之前说过 <code>malloc()</code> 分配出来的内存 是有记录 当前区块大小的开销</p>
<h1 id="GNU-C-2-9-std-alloc-原理"><a href="#GNU-C-2-9-std-alloc-原理" class="headerlink" title="GNU-C++ 2.9 std::alloc 原理"></a>GNU-C++ 2.9 std::alloc 原理</h1><p>首先维护 16条自由链表 <code>free_list[16]</code> 掌控着16种不同小内存的分配 如果 超出了这16种小内存即 超过128字节的时候 则 交给 <code>malloc()</code> 去分配</p>
<p>16种小内存 分别为 8的倍数 如果小于8的倍数 会将其调整为 8 的倍数的边界</p>
<p>当 外界使用 <code>std::alloc</code> 分配内存时 每次 <code>std::alloc</code> 会获取 <code>20 * 2</code> 个 size 那么大的大内存块 其中 一份 <code>20</code> 进行切割为一个单向链表 剩下一份 <code>20</code> 会保留下来 以便下次使用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">比如 我向 std::alloc 分配 32 字节的内存 </span><br><span class="line">此时 std::alloc 会去获取 20 * 2 个 32个字节 + RoundUp(目前已申请的量 &gt;&gt; 4)追加量 那么大的内存块 </span><br><span class="line">其中 20 个 32个字节 会切割成单向链表 然后返回头一块回去</span><br><span class="line">剩下的 20个 会保留起来 不做切割 假如未来我向 std::alloc 要 64个字节的内存的时候 就会从 保留的20个32个字节那么大的内存块里面切割 </span><br><span class="line">相当于切成了 10份 也是一样 返回头一块</span><br></pre></td></tr></table></figure>

<p>换句话说 就是 如果没有预备内存的话 就会去申请 每次申请 <code>20 * 2 * size + [RoundUp(目前已申请的量 &gt;&gt; 4)] 追加量</code> 那么大的内存块 下次申请 如果有预备内存就直接用保留下来的那一份 如果没有了 则再次按照一开始的设定去申请一整块大内存</p>
<p><code>RoundUp()</code> 函数内部是将其调整为 <code>8</code> 的字节</p>
<p>此外 就算预备内存足够大 也不会切超出 1~20块内存 永远在 20 以内</p>
<h2 id="当系统内存不够用"><a href="#当系统内存不够用" class="headerlink" title="当系统内存不够用"></a>当系统内存不够用</h2><p>系统内存不够用了 就从所需内存的右侧链表找 如果找的到 就直接将其一块小区块 注入 内存池中 再将其分配出去</p>
<p>如果所有的右侧都没有了 那就回到一级适配器 给 new handler 处理 看看还有没有一些补救措施 释放掉一些内存给我们用</p>
<p>如果还是不行 那就 <code>凉</code> 了</p>
<h2 id="第一级配置器"><a href="#第一级配置器" class="headerlink" title="第一级配置器"></a>第一级配置器</h2><p>当第二级配置器失败了 就交给 第一级配置器 模拟 <code>new handler</code> 不断循环 想办法挖出更多的内存</p>
<p>但是 C++ 本身就有这种机制 <code>GUN-C++ 4.9</code> 不再有这段了</p>
<h2 id="第二级配置器"><a href="#第二级配置器" class="headerlink" title="第二级配置器"></a>第二级配置器</h2><p>第二级配置器就是我们上面讲的那个过程的配置器</p>
<h2 id="总结-G2-9"><a href="#总结-G2-9" class="headerlink" title="总结 G2.9"></a>总结 G2.9</h2><p>这套内存分配器 通过维护一个内存池和16个自由链表 来减少 malloc 的调用 从而减少 malloc 时所浪费掉的内存(用于记录内存块大小)</p>
<p>可能有的人会觉得 就占用这一点内存没什么 但是 如果大批量的 malloc 小内存的话 那记录内存块大小的消耗就非常可观了.</p>
<p>但是也有缺陷 这一套方案 并没有 free 掉自己所占有的内存 而是将其存回自己的自由链表中 一方面是 很难 free 因为链表到最后全都乱掉了 只有地址相邻 大小相等 才能合并起来 最后再还回去.</p>
<p>因此 这可能也是为什么 <code>G4.9</code> 的时候 容器的标准内存分配器 不用这一套的原因 而是 采取什么都不做的形式 来分配内存.</p>
<p>此外 我们知道 当内存池 或者 自由链表的区块不够了 就会去 malloc 但是 每次 malloc 都是取 <code>20 * 2 * size + 递增量</code> 那为什么不每次 除以 2 想尽办法把 系统内存全部扣光呢? 可能是为了 把那些内存让给其他进程吧..</p>
<p>代码实现可见 <a href="https://github.com/Yu2erer/Memory_Pool" target="_blank" rel="noopener">Memory_Pool</a></p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>内存管理</tag>
        <tag>STL</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 内存管理 之 内存池实现</title>
    <url>/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-%E5%86%85%E5%AD%98%E6%B1%A0%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h2 id="为什么要实现内存池"><a href="#为什么要实现内存池" class="headerlink" title="为什么要实现内存池?"></a>为什么要实现内存池?</h2><p>一方面 是为了减少 调用 <code>malloc()</code> 的次数(尽管 <code>malloc()</code> 不慢) 但是调多了 会产生(外碎片)</p>
<p>另一方面 是因为 每次用 <code>malloc()</code> 分配到的内存 是要交税的 就是上一节中讲的 <code>cookie</code> 里面记录着 这一块内存的大小信息(内碎片) 特别是在频繁申请小内存的时候尤为明显</p>
<h2 id="最终-pool-allocator-版本"><a href="#最终-pool-allocator-版本" class="headerlink" title="最终 pool_allocator 版本"></a>最终 pool_allocator 版本</h2><p>具体代码可见 <a href="https://github.com/Yu2erer/Memory_Pool" target="_blank" rel="noopener">Memory_Pool</a></p>
<p>原理可见 <a href="https://yuerer.com/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-STL%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86/">C++ 内存管理 之 STL内存分配实现原理</a></p>
<a id="more"></a>

<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="comment">// Created by Yuerer on 2019/11/7.</span></span><br><span class="line"><span class="comment">//</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> YY_ALLOCATOR_H</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> YY_ALLOCATOR_H</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> YY &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> ALIGN = <span class="number">8</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> MAX_BYTES = <span class="number">128</span>;</span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> FREE_LIST_NUMS = MAX_BYTES / ALIGN; <span class="comment">// 16</span></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Alloc</span> &#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> *<span class="title">allocate</span><span class="params">(<span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">            obj **my_free_list;</span><br><span class="line">            obj *result;</span><br><span class="line">            <span class="keyword">if</span> (n &gt; (<span class="keyword">size_t</span>)MAX_BYTES) &#123;</span><br><span class="line">                <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(n);</span><br><span class="line">            &#125;</span><br><span class="line">            my_free_list = free_list + FREE_LIST_INDEX(n);</span><br><span class="line">            result = *my_free_list;</span><br><span class="line">            <span class="keyword">if</span> (result == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                <span class="keyword">void</span> *r = refill(ROUND_UP(n));</span><br><span class="line">                <span class="keyword">return</span> r;</span><br><span class="line">            &#125;</span><br><span class="line">            *my_free_list = result-&gt;next;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(<span class="keyword">void</span> *p, <span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">            obj *q = (obj *)p;</span><br><span class="line">            obj **my_free_list;</span><br><span class="line">            <span class="keyword">if</span> (n &gt; (<span class="keyword">size_t</span>)MAX_BYTES) &#123;</span><br><span class="line">                <span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">delete</span>(p);</span><br><span class="line">            &#125;</span><br><span class="line">            my_free_list = free_list + FREE_LIST_INDEX(n);</span><br><span class="line">            q-&gt;next = *my_free_list;</span><br><span class="line">            *my_free_list = q;</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="comment">// 单向链表</span></span><br><span class="line">        <span class="keyword">union</span> obj &#123;</span><br><span class="line">            <span class="keyword">union</span> obj *next;</span><br><span class="line">        &#125;;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="comment">// 内存池</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">char</span> *start_free;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">char</span> *end_free;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">size_t</span> heap_size;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="keyword">static</span> obj *free_list[FREE_LIST_NUMS];</span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> *<span class="title">refill</span><span class="params">(<span class="keyword">size_t</span> n)</span></span>;</span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">char</span> *<span class="title">chunk_alloc</span><span class="params">(<span class="keyword">size_t</span> n, <span class="keyword">int</span> &amp;blocks)</span></span>;</span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        <span class="comment">// 从 1 起步</span></span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">size_t</span> <span class="title">FREE_LIST_INDEX</span><span class="params">(<span class="keyword">size_t</span> bytes)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> ((bytes + ALIGN - <span class="number">1</span>) / (ALIGN - <span class="number">1</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="function"><span class="keyword">static</span> <span class="keyword">size_t</span> <span class="title">ROUND_UP</span><span class="params">(<span class="keyword">size_t</span> bytes)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> ((bytes + ALIGN - <span class="number">1</span>) &amp; ~(ALIGN - <span class="number">1</span>));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">    Alloc::obj *Alloc::free_list[FREE_LIST_NUMS] = &#123;<span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>, <span class="number">0</span>&#125;;</span><br><span class="line">    <span class="keyword">char</span> *Alloc::start_free = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">char</span> *Alloc::end_free = <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">size_t</span> Alloc::heap_size = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> *<span class="title">Alloc::refill</span><span class="params">(<span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> blocks = <span class="number">20</span>;</span><br><span class="line">        <span class="keyword">char</span> *chunk = chunk_alloc(n, blocks);</span><br><span class="line"></span><br><span class="line">        obj **my_free_list;</span><br><span class="line">        obj *result;</span><br><span class="line">        obj *next_obj, *current_obj;</span><br><span class="line">        <span class="keyword">if</span> (blocks == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> chunk;</span><br><span class="line">        &#125;</span><br><span class="line">        my_free_list = free_list + FREE_LIST_INDEX(n);</span><br><span class="line">        result = (obj*)chunk;</span><br><span class="line">        *my_free_list = next_obj = (obj*)(chunk + n);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; ; ++ i) &#123;</span><br><span class="line">            current_obj = next_obj;</span><br><span class="line">            next_obj = (obj*)((<span class="keyword">char</span>*)next_obj + n);</span><br><span class="line">            <span class="keyword">if</span> (blocks - <span class="number">1</span> == i) &#123;</span><br><span class="line">                current_obj-&gt;next = <span class="literal">nullptr</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                current_obj-&gt;next = next_obj;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">char</span> *<span class="title">Alloc::chunk_alloc</span><span class="params">(<span class="keyword">size_t</span> n, <span class="keyword">int</span> &amp;blocks)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">size_t</span> bytes_left = end_free - start_free;</span><br><span class="line">        <span class="keyword">size_t</span> total_bytes = n * blocks;</span><br><span class="line">        <span class="keyword">char</span> *result;</span><br><span class="line">        <span class="keyword">if</span> (bytes_left &gt;= total_bytes) &#123;</span><br><span class="line">            result = start_free;</span><br><span class="line">            start_free += total_bytes;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (bytes_left &gt;= n) &#123;</span><br><span class="line">            blocks = bytes_left / n;</span><br><span class="line">            total_bytes = n * blocks;</span><br><span class="line">            result = start_free;</span><br><span class="line">            start_free += total_bytes;</span><br><span class="line">            <span class="keyword">return</span> result;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">size_t</span> bytes_to_get = <span class="number">2</span> * total_bytes + ROUND_UP(heap_size &gt;&gt; <span class="number">4</span>);</span><br><span class="line">            <span class="keyword">if</span> (bytes_left &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                obj **my_free_list = free_list + FREE_LIST_INDEX(bytes_left);</span><br><span class="line">                ((obj*)start_free)-&gt;next = *my_free_list;</span><br><span class="line">                *my_free_list = (obj*)start_free;</span><br><span class="line">            &#125;</span><br><span class="line">            start_free = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(bytes_to_get);</span><br><span class="line">            <span class="keyword">if</span> (start_free == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                obj **my_free_list;</span><br><span class="line">                obj *p;</span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> i = n + ALIGN; i &lt;= MAX_BYTES; i += ALIGN) &#123;</span><br><span class="line">                    my_free_list = free_list + FREE_LIST_INDEX(i);</span><br><span class="line">                    p = *my_free_list;</span><br><span class="line">                    <span class="keyword">if</span> (p != <span class="literal">nullptr</span>) &#123;</span><br><span class="line">                        *my_free_list = p-&gt;next;</span><br><span class="line">                        start_free = (<span class="keyword">char</span> *) p;</span><br><span class="line">                        end_free = (<span class="keyword">char</span> *) p + i;</span><br><span class="line">                        <span class="keyword">return</span> chunk_alloc(n, blocks);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                end_free = <span class="literal">nullptr</span>;</span><br><span class="line">                start_free = (<span class="keyword">char</span>*)::<span class="keyword">operator</span> <span class="keyword">new</span>(bytes_to_get);</span><br><span class="line">            &#125;</span><br><span class="line">            heap_size += bytes_to_get;</span><br><span class="line">            end_free = start_free + bytes_to_get;</span><br><span class="line">            <span class="keyword">return</span> chunk_alloc(n, blocks);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span> <span class="comment">//YY_ALLOCATOR_H</span></span></span><br></pre></td></tr></table></figure>

<h2 id="内存管理迭代之一"><a href="#内存管理迭代之一" class="headerlink" title="内存管理迭代之一"></a>内存管理迭代之一</h2><p>最简陋的版本 简单的来说就是 一次获取 一大块内存 然后串成链表 每次要就返回其中一块</p>
<p>多用了一个指针 成员变量也就4个字节 一个指针多占用了 8 个字节</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Screen</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	Screen(<span class="keyword">int</span> x) : i(x) &#123; &#125;;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> *<span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">		Screen *p;</span><br><span class="line">		<span class="keyword">if</span> (!freeStore) &#123;</span><br><span class="line">			<span class="keyword">size_t</span> chunk = screenChunk * <span class="built_in">size</span>;</span><br><span class="line">            <span class="comment">// reinterpret_cast 将两个毫不相关的类型进行转换</span></span><br><span class="line">			freeStore = p = <span class="keyword">reinterpret_cast</span>&lt;Screen*&gt; (<span class="keyword">new</span> <span class="keyword">char</span>[chunk]);</span><br><span class="line">			<span class="keyword">while</span> (p != &amp;freeStore[screenChunk - <span class="number">1</span>]) &#123;</span><br><span class="line">				p-&gt;next = p + <span class="number">1</span>;</span><br><span class="line">				p ++;</span><br><span class="line">			&#125;</span><br><span class="line">			p-&gt;next = <span class="literal">nullptr</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		p = freeStore;</span><br><span class="line">		freeStore = freeStore-&gt;next;</span><br><span class="line">		<span class="keyword">return</span> p;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span>* p, <span class="keyword">size_t</span>)</span> </span>&#123;</span><br><span class="line">		(<span class="keyword">static_cast</span>&lt;Screen*&gt; (p))-&gt;next = freeStore;</span><br><span class="line">		freeStore = <span class="keyword">static_cast</span>&lt;Screen*&gt; (p);</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="comment">// 多用了一个指针 成员变量也就4个字节 一个指针多占用了 8 个字节</span></span><br><span class="line">	Screen *next;</span><br><span class="line">	<span class="keyword">static</span> Screen *freeStore;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> screenChunk;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">int</span> i;</span><br><span class="line">&#125;;</span><br><span class="line">Screen *Screen::freeStore = <span class="literal">nullptr</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> Screen::screenChunk = <span class="number">24</span>;</span><br></pre></td></tr></table></figure>

<h2 id="内存管理迭代之二"><a href="#内存管理迭代之二" class="headerlink" title="内存管理迭代之二"></a>内存管理迭代之二</h2><p>上一个版本 最大的缺陷就是说 多占用了个指针 这对于只有 一个 int 的类来说 有点浪费</p>
<p>使用 <code>union</code> 将类 的 成员变量和链表指针包起来 节约内存</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Airplane</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="comment">// 9个字节 对齐后 16个字节</span></span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">AirplaneRep</span> &#123;</span></span><br><span class="line">		<span class="keyword">unsigned</span> <span class="keyword">long</span> miles;</span><br><span class="line">		<span class="keyword">char</span> type;	</span><br><span class="line">	&#125;;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">union</span> &#123; <span class="comment">// 采用 union 将数据成员和链表指针占用空间节约起来</span></span><br><span class="line">		Airplane *next;  <span class="comment">// 链表</span></span><br><span class="line">		AirplaneRep rep; <span class="comment">// 相当于使用中的 obj</span></span><br><span class="line">	&#125;;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> *<span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">		<span class="comment">// 如果发生了继承就会进入这个分支</span></span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">size</span> != <span class="keyword">sizeof</span>(Airplane)) &#123;</span><br><span class="line">			<span class="keyword">return</span> ::<span class="keyword">operator</span> <span class="keyword">new</span>(<span class="built_in">size</span>);</span><br><span class="line">		&#125;</span><br><span class="line">		Airplane *p = head;</span><br><span class="line">		<span class="keyword">if</span> (p) &#123;</span><br><span class="line">			head = p-&gt;next;</span><br><span class="line">		&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">			Airplane *newBlock = <span class="keyword">static_cast</span>&lt;Airplane*&gt; (::<span class="keyword">operator</span> <span class="keyword">new</span>(BLOCK_SIZE * <span class="keyword">sizeof</span>(Airplane)));</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; BLOCK_SIZE - <span class="number">1</span>; i ++) &#123;</span><br><span class="line">				newBlock[i].next = &amp;newBlock[i + <span class="number">1</span>];</span><br><span class="line">			&#125;</span><br><span class="line">			newBlock[BLOCK_SIZE - <span class="number">1</span>].next = <span class="literal">nullptr</span>;</span><br><span class="line">			p = newBlock;</span><br><span class="line">			head = &amp;newBlock[<span class="number">1</span>];</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> p;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *p, <span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">if</span>(p == <span class="literal">nullptr</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (<span class="built_in">size</span> != <span class="keyword">sizeof</span>(Airplane)) &#123;</span><br><span class="line">			::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(p)</span></span>;</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		Airplane *rp = <span class="keyword">static_cast</span>&lt;Airplane*&gt; (p);</span><br><span class="line">		rp-&gt;next = head;</span><br><span class="line">		head = rp;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">int</span> BLOCK_SIZE;</span><br><span class="line">	<span class="keyword">static</span> Airplane *head;</span><br><span class="line">&#125;;</span><br><span class="line">Airplane *Airplane::head = <span class="literal">nullptr</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="keyword">int</span> Airplane::BLOCK_SIZE = <span class="number">512</span>;</span><br></pre></td></tr></table></figure>

<h2 id="内存管理迭代之三"><a href="#内存管理迭代之三" class="headerlink" title="内存管理迭代之三"></a>内存管理迭代之三</h2><p>静态版本</p>
<p>前面两个 必须要为不同的 <code>classes</code> 重写一个几乎相同的 <code>member operator new 和 member operator delete</code></p>
<p>因此这个版本 将其抽取出来 放到一个 <code>class</code> 中</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">allocator</span> &#123;</span></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">obj</span> &#123;</span></span><br><span class="line">		<span class="class"><span class="keyword">struct</span> <span class="title">obj</span> *<span class="title">next</span>;</span>	</span><br><span class="line">	&#125;;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	<span class="function"><span class="keyword">void</span> *<span class="title">allocate</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">		obj *p;</span><br><span class="line">		<span class="keyword">if</span> (!freeStore) &#123;</span><br><span class="line">			<span class="keyword">size_t</span> chunk = CHUNK * <span class="built_in">size</span>;</span><br><span class="line">			freeStore = p = (obj*)<span class="built_in">malloc</span>(chunk);</span><br><span class="line">			<span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; CHUNK - <span class="number">1</span>; i ++) &#123;</span><br><span class="line">				p-&gt;next = (obj*)((<span class="keyword">char</span>*)p + <span class="built_in">size</span>);</span><br><span class="line">				p = p-&gt;next;</span><br><span class="line">			&#125;</span><br><span class="line">			p-&gt;next = <span class="literal">nullptr</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		p = freeStore;</span><br><span class="line">		freeStore = freeStore-&gt;next;</span><br><span class="line">		<span class="keyword">return</span> p;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="function"><span class="keyword">void</span> <span class="title">deallocate</span><span class="params">(<span class="keyword">void</span> *p, <span class="keyword">size_t</span>)</span> </span>&#123;</span><br><span class="line">		((obj*)p)-&gt;next = freeStore;</span><br><span class="line">		freeStore = (obj*)p;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">	obj *freeStore = <span class="literal">nullptr</span>;</span><br><span class="line">	<span class="comment">// 标准库中为 20</span></span><br><span class="line">	<span class="keyword">const</span> <span class="keyword">int</span> CHUNK = <span class="number">5</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>内存管理</tag>
        <tag>内存池</tag>
      </tags>
  </entry>
  <entry>
    <title>C++ 内存管理 之 原生版本</title>
    <url>/C++%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E4%B9%8B-%E5%8E%9F%E7%94%9F%E7%89%88%E6%9C%AC/</url>
    <content><![CDATA[<h2 id="为什么要叫原生版本"><a href="#为什么要叫原生版本" class="headerlink" title="为什么要叫原生版本?"></a>为什么要叫原生版本?</h2><p>因为我觉得这一块是 C++ 自带的表达式 表达式里面 去调用 C语言的 CRT 库中的 <code>malloc</code> 和 <code>free</code> 但这篇 只讲自带的表达式 而不去深究 CRT 中的内存分配函数 所以只叫原生版本</p>
<h2 id="常见的内存分配"><a href="#常见的内存分配" class="headerlink" title="常见的内存分配"></a>常见的内存分配</h2><table>
<thead>
<tr>
<th>分配</th>
<th>释放</th>
<th>类属</th>
<th>可否重载</th>
</tr>
</thead>
<tbody><tr>
<td>malloc()</td>
<td>free()</td>
<td>C 函数</td>
<td>否</td>
</tr>
<tr>
<td>new</td>
<td>delete</td>
<td>C++ 表达式</td>
<td>否</td>
</tr>
<tr>
<td>::operator new()</td>
<td>::operator delete()</td>
<td>C++ 函数</td>
<td>可</td>
</tr>
<tr>
<td>allocator<T>::allocate()</td>
<td>allocator<T>::deallocate()</td>
<td>C++ 标准库</td>
<td>自由设计搭配的容器</td>
</tr>
</tbody></table>
<h3 id="使用示例"><a href="#使用示例" class="headerlink" title="使用示例"></a>使用示例</h3><p>其中 <code>::operator new() 和 ::operator delete()</code> 调用了 <code>malloc() 和 free()</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">    <span class="keyword">void</span> *p1 = <span class="built_in">malloc</span>(<span class="number">512</span>);</span><br><span class="line">    <span class="built_in">free</span>(p1);</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">complex</span>&lt;<span class="keyword">int</span>&gt; *p2 = <span class="keyword">new</span> <span class="built_in">complex</span>&lt;<span class="keyword">int</span>&gt;;</span><br><span class="line">    <span class="keyword">delete</span> p2;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">void</span> *p3 = ::<span class="keyword">operator</span> <span class="keyword">new</span>(<span class="number">512</span>);</span><br><span class="line">    ::<span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(p3)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> _MSC_VER</span></span><br><span class="line">    <span class="comment">// 属于 non-static 要先实例化object再调用</span></span><br><span class="line">    <span class="keyword">int</span> *p4 = allocator&lt;<span class="keyword">int</span>&gt;().allocate(<span class="number">3</span>, (<span class="keyword">int</span>*)<span class="number">0</span>);</span><br><span class="line">    allocator&lt;<span class="keyword">int</span>&gt;().deallocate(p4, <span class="number">3</span>);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> __GUNC__</span></span><br><span class="line">    <span class="comment">// 早期 GNU-C++ 2.9 版本</span></span><br><span class="line">    <span class="comment">// void *p4 = alloc::allocate(512);</span></span><br><span class="line">    <span class="comment">// alloc::deallocate(p4, 512);</span></span><br><span class="line">    <span class="comment">// alloc 换名字了</span></span><br><span class="line">    <span class="keyword">void</span> *p5 = __gnu_cxx::__pool_alloc&lt;<span class="keyword">int</span>&gt;().allocate(<span class="number">9</span>);</span><br><span class="line">    __gnu_cxx::__pool_alloc&lt;<span class="keyword">int</span>&gt;().deallocate((<span class="keyword">int</span>*)p5, <span class="number">9</span>);</span><br><span class="line">    <span class="comment">// 4.9 版本之后</span></span><br><span class="line">    <span class="keyword">void</span> *p4 = allocator&lt;<span class="keyword">int</span>&gt;().allocate(<span class="number">7</span>);</span><br><span class="line">    allocator&lt;<span class="keyword">int</span>&gt;().deallocate((<span class="keyword">int</span>*)p4, <span class="number">7</span>);</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h2 id="new-表达式"><a href="#new-表达式" class="headerlink" title="new 表达式"></a>new 表达式</h2><p><code>Object *p = new Object(1);</code></p>
<p>编译器 在内部将其转化为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Object *p;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// operator new 可以被重载 如果没重载 调用全局版本</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">void</span> *mem = <span class="keyword">operator</span> <span class="keyword">new</span>(<span class="keyword">sizeof</span>(Object)); <span class="comment">// 1. 分配内存</span></span><br><span class="line">    p = <span class="keyword">static_cast</span>&lt;Object*&gt;(mem); <span class="comment">// 2. 转换类型</span></span><br><span class="line">    <span class="comment">// 实际上只有编译器才能这样调用 构造函数</span></span><br><span class="line">    <span class="comment">// 然鹅在 VC 6.0 下可以这样调用</span></span><br><span class="line">    <span class="comment">// p-&gt;Object::Object(1);</span></span><br><span class="line">    <span class="comment">// 可以通过 placement new 直接调用 构造函数</span></span><br><span class="line">    <span class="keyword">new</span> (p) Object(<span class="number">1</span>); <span class="comment">// 3. 调用构造函数</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">catch</span> () &#123;</span><br><span class="line">    <span class="comment">// 分配内存失败 则不执行 构造函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="operator-new"><a href="#operator-new" class="headerlink" title="operator new"></a>operator new</h3><p>前面说过 里面调用的是 <code>malloc()</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 来自于 vc98/crt/src/newop2.cpp</span></span><br><span class="line"><span class="comment">// const std::nothrow_t &amp; 不抛异常</span></span><br><span class="line"><span class="comment">// C++ 11 用 noexcept 代表不会抛出异常 如果抛出 则会调用 std::terminate() 终止程序 防止异常传播</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="keyword">nothrow_t</span> &amp;)</span> _<span class="title">THROW0</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">void</span> *p;</span><br><span class="line">    <span class="comment">// 分配内存成功 就直接返回 如果失败 进入循环</span></span><br><span class="line">    <span class="comment">// 会调用 _callnewh =&gt; newhandler 是一个由你设定的函数 去找找有什么办法可以释放掉一些内存 使得有机会成功分配内存</span></span><br><span class="line">    <span class="keyword">while</span> ((p = <span class="built_in">malloc</span>(<span class="built_in">size</span>)) == <span class="number">0</span>) &#123;</span><br><span class="line">        _TRY_BEGIN</span><br><span class="line">            <span class="keyword">if</span> (_callnewh(<span class="built_in">size</span>) == <span class="number">0</span>) <span class="keyword">break</span>;</span><br><span class="line">        _CATCH(<span class="built_in">std</span>::bad_alloc) <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        _CATCH_END</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (p);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="delete-表达式"><a href="#delete-表达式" class="headerlink" title="delete 表达式"></a>delete 表达式</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Object *p = <span class="keyword">new</span> Object(<span class="number">1</span>);</span><br><span class="line"><span class="keyword">delete</span> p;</span><br></pre></td></tr></table></figure>

<p>编译器 将 <code>delete</code> 转化为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">p-&gt;~Object(); <span class="comment">// 先调用析构函数</span></span><br><span class="line"><span class="function"><span class="keyword">operator</span> <span class="title">delete</span><span class="params">(p)</span></span>; <span class="comment">// 再释放内存</span></span><br></pre></td></tr></table></figure>

<h3 id="operator-delete"><a href="#operator-delete" class="headerlink" title="operator delete"></a>operator delete</h3><p>会调用 <code>free()</code> 来释放内存</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 来自于 vc98/crt/src/delop.cpp</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> __cdecl <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span> *p)</span> _<span class="title">THROW0</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">free</span>(p);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="array-new-array-delete"><a href="#array-new-array-delete" class="headerlink" title="array new, array delete"></a>array new, array delete</h2><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 调用三次构造函数 按顺序构造</span></span><br><span class="line">Object *p = <span class="keyword">new</span> Object[<span class="number">3</span>];</span><br><span class="line"><span class="comment">// 调用三次析构函数 逆序析构</span></span><br><span class="line"><span class="keyword">delete</span>[] p;</span><br></pre></td></tr></table></figure>

<p>如果 没配套使用的话 不一定会发生内存泄漏</p>
<ul>
<li>对 class without ptr member 可能没影响</li>
<li>对 class with ptr member 通常有影响</li>
</ul>
<p>先看第一种情况 </p>
<table>
<thead>
<tr>
<th>cookie</th>
</tr>
</thead>
<tbody><tr>
<td>Object 1</td>
</tr>
<tr>
<td>Object 2</td>
</tr>
<tr>
<td>Object 3</td>
</tr>
</tbody></table>
<p><code>cookie</code> 是调用 <code>malloc()</code> 的时候 一起带来的 记录了这块内存的大小 相当于是额外的开销<br>此时 这个 class 没有 ptr member 那么 delete 的时候 只会调用 一次析构函数 但是没关系 这里面没有指针成员 因此 析构完了以后 <code>free()</code> 会看 <code>cookie</code> 知道了整块内存的大小 直接将其 释放 没有发生内存泄漏</p>
<p>第二种情况</p>
<p>假设这个 class 有 ptr member 那么 <code>delete</code> 的时候 只调用了 一次析构函数 那另外两个 class 里面 开辟的内存 因为没有调用到析构函数 没能被成功回收 因此发生了内存泄漏</p>
<p>因此发生内存泄漏不是 数组本身 而是 class 中 有可能开了内存</p>
<h2 id="placement-new"><a href="#placement-new" class="headerlink" title="placement new"></a>placement new</h2><p>将 object 构建在 已经分配好的内存中</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;new&gt;</span></span></span><br><span class="line"><span class="keyword">char</span> *buf = <span class="keyword">new</span> <span class="keyword">char</span>[<span class="keyword">sizeof</span>(Object) * <span class="number">3</span>];</span><br><span class="line">Object *p = <span class="keyword">new</span> (buf) Object(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">delete</span> []buf;</span><br></pre></td></tr></table></figure>

<p>会被编译器转换为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Object *p;</span><br><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">// 多了第二参数</span></span><br><span class="line">    <span class="keyword">void</span> *mem = <span class="keyword">operator</span> <span class="keyword">new</span>(<span class="keyword">sizeof</span>(Object), buf);</span><br><span class="line">    p = <span class="keyword">static_cast</span>&lt;Object*&gt;(mem);</span><br><span class="line">    p-&gt;Object::Object(<span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">catch</span> () &#123;</span><br><span class="line">    <span class="comment">// 分配内存失败 则不执行 构造函数</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>此时 调用的 operator new 为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> *<span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>, <span class="keyword">void</span> *loc)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> loc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相当于 不分配内存 直接把已经有的内存指针 返回回去 然后在其上面调用构造函数</p>
<h2 id="重载-operator-new-operator-delete"><a href="#重载-operator-new-operator-delete" class="headerlink" title="重载 operator new, operator delete"></a>重载 operator new, operator delete</h2><p>重载 <code>operator new / operator delete</code> 可分为 全局重载 和 局部重载 其中全局重载影响太广了 一般是局部重载</p>
<p>要注意的点是 这两个函数 一定要是 <code>static</code> 不然你这个类都没有实例化 怎么能调到这两个函数 但是这两个函数就是拿来实例化的 产生了驳论 不过 C++ 编译器会默认给这两个函数 加上 <code>static</code> 所以你写不写 没啥所谓</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Foo</span> &#123;</span></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> *opeartor <span class="title">new</span><span class="params">(<span class="keyword">size_t</span>)</span></span>;</span><br><span class="line">    <span class="comment">// 第二参数 可有可无</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span>*, <span class="keyword">size_t</span>)</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="重载-placement-new-placement-delete"><a href="#重载-placement-new-placement-delete" class="headerlink" title="重载 placement new, placement delete"></a>重载 placement new, placement delete</h2><p>和 重载 <code>operator new</code> 类似</p>
<p>其实这真的能说是重载 <code>placement new</code> 吗? <code>placement new</code> 是在指定位置下进行构造函数</p>
<p>而这个 则很像 <code>operator new</code> 的重载了 唯一不同是参数个数不同和使用方式不同了</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 第一个参数 必须是 size_t 这是为了 自动传入大小 不然没法 new</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> *<span class="keyword">operator</span> <span class="title">new</span><span class="params">(<span class="keyword">size_t</span> <span class="built_in">size</span>, <span class="keyword">long</span> extra, <span class="keyword">char</span> init)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">malloc</span>(<span class="built_in">size</span> + extra);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="keyword">operator</span> <span class="title">delete</span><span class="params">(<span class="keyword">void</span>* p, <span class="keyword">long</span>, <span class="keyword">char</span>)</span> </span>&#123;</span><br><span class="line">    <span class="built_in">free</span>(p);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// placement delete 重载 </span></span><br><span class="line"><span class="comment">// 就算和 placement new 没有一一对应也没关系</span></span><br><span class="line"><span class="comment">// 因为重载的 placement delete</span></span><br><span class="line"><span class="comment">// 只在 placement new 后 ctor 后失败(抛出异常) 时 才会被调用</span></span><br><span class="line"><span class="comment">// 如果不对应起来 就可以看做是 你放弃处理 ctor 发出的异常</span></span><br><span class="line"><span class="comment">// 注意 全局局部重载的效果都是一样的 delete 的时候不会去调用 placement delete</span></span><br></pre></td></tr></table></figure>

<p>使用方式 <code>Foo *pf = new(300, &#39;c) Foo;</code></p>
<h3 id="basic-string-使用-new-extra-扩充申请内存量"><a href="#basic-string-使用-new-extra-扩充申请内存量" class="headerlink" title="basic_string 使用 new(extra) 扩充申请内存量"></a>basic_string 使用 new(extra) 扩充申请内存量</h3><p>basic_string 重载了 <code>placement new</code> 来多申请内存 因为 string 内部有引用计数 多申请一块内存来存放这些信息</p>
<p><code>inline static void *operator new(size_t, size_t)</code></p>
<h2 id="new-handler"><a href="#new-handler" class="headerlink" title="new handler"></a>new handler</h2><p>当 <code>operator new</code> 没能力分配你要的内存时 要么 抛出 <code>std::bad_alloc</code> 异常 要么则是直接返回 0 这也是为什么要检查分配出来的内存 是否可用</p>
<p>强行要求 编译器 不抛出异常 <code>new (nothrow) obj;</code></p>
<p>new handler 的设定方式</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">typedef</span> <span class="title">void</span> <span class="params">(*new_handler)</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 为什么要传回来一个new handler 回来</span></span><br><span class="line"><span class="comment">// 假如你之前 已经有一个 A handler 此时你传进去一个 B handler</span></span><br><span class="line"><span class="comment">// 通过返回回来 你就能把 A handler 保存起来</span></span><br><span class="line"><span class="function">new_handler <span class="title">set_new_handler</span><span class="params">(new_handler p)</span> <span class="title">throw</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<p>new handler 只有两种选择</p>
<ol>
<li>想办法释放内存</li>
<li>调用 abort() 或 exit()</li>
</ol>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
      <tags>
        <tag>C++</tag>
        <tag>内存管理</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux系统编程(一) 之时间编程</title>
    <url>/Linux%E7%B3%BB%E7%BB%9F%E7%BC%96%E7%A8%8B(%E4%B8%80)%E4%B9%8B%E6%97%B6%E9%97%B4%E7%BC%96%E7%A8%8B/</url>
    <content><![CDATA[<p>这是 Linux 系统编程系列 中的第一篇, 在这个系列里面, 我想以这么个顺序来讲述每一篇的内容, 先是快速简单地了解一下 常见的使用方法, 即API如何使用, 接着再深入其中, 了解内核为API提供的帮助, 大致是 C标准库 → 系统调用 这么个学习路线.</p>
<h2 id="时间的定义"><a href="#时间的定义" class="headerlink" title="时间的定义"></a>时间的定义</h2><p>首先要记住 计算机的起始时间 为 (格林威治时间)1970年1月1日0点, 被称作 GMT时间 是时区时间, 不过它是根据地球公转和自转来计算的, 不是特别准确. 在计算机世界中, 往往被称作 UTC时间, 它是以原子钟来计算时间, 简单来说就是 UTC时间 比 GMT时间要准确, 同时两个时间又恰好没有时差, 但是UTC时间又不是时区时间, 因此容易被人弄混.</p>
<a id="more"></a>

<h2 id="获取时间"><a href="#获取时间" class="headerlink" title="获取时间"></a>获取时间</h2><h3 id="time-秒"><a href="#time-秒" class="headerlink" title="time() 秒"></a>time() 秒</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="comment">// 结果可以通过返回值得到 也可以通过传入指针得到</span></span><br><span class="line"><span class="function"><span class="keyword">time_t</span> <span class="title">time</span><span class="params">(<span class="keyword">time_t</span> *calptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="gettimeofday-微秒"><a href="#gettimeofday-微秒" class="headerlink" title="gettimeofday() 微秒"></a>gettimeofday() 微秒</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">gettimeofday</span><span class="params">(struct timeval *<span class="keyword">restrict</span> tp, <span class="keyword">void</span> *<span class="keyword">restrict</span> tzp <span class="comment">/*一般为NULL表示时区*/</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>很多人说其性能不高 因为本质是系统调用 会频繁陷入内核</li>
<li>不过 在 x86_64下 采用了 vsyscall 并没有 陷入内核中</li>
<li>比 clock_gettime 性能高</li>
<li>兼容性不高(部分系统下 被弃用)</li>
</ul>
<h3 id="RDTSC-读取-TSC-Time-Stamp-Counter"><a href="#RDTSC-读取-TSC-Time-Stamp-Counter" class="headerlink" title="RDTSC 读取 TSC (Time Stamp Counter)"></a>RDTSC 读取 TSC (Time Stamp Counter)</h3><ul>
<li>非特权指令 可以避免 用户态和内核态的开销</li>
<li>只能在 Intel 平台下使用</li>
<li>奔腾处理器引入了乱序执行, 可能会导致想测的指令在 RDTSC之外 执行, 如果有RDTSCP 最好用这个</li>
<li>当然如果没有的话, 那就只能用 cpuid指令 来保序</li>
<li>CPU频率可能会被改变, 比如说开启了节能模式, 如果是比较新的Intel处理器 则可以采用 grep tsc /proc/cpuinfo，有constant_tsc 和 nonstop_tsc 也就足够了</li>
<li>各个CPU的核心中得TSC寄存器不一定同步, 同步的话开销太大, 用 RDTSC 指令没啥意义了</li>
</ul>
<h3 id="POSIX-Timer-纳秒"><a href="#POSIX-Timer-纳秒" class="headerlink" title="POSIX Timer 纳秒"></a>POSIX Timer 纳秒</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">clock_gettime</span><span class="params">(<span class="keyword">clockid_t</span> clock_id, struct timespec *tsp)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">clock_settime</span><span class="params">(<span class="keyword">clockid_t</span> clock_id, <span class="keyword">const</span> struct timespec *tsp)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">clock_getres</span><span class="params">(<span class="keyword">clockid_t</span> clock_id, struct timespec *tsp)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>当在 Intel x86_64 平台下获取 CLOCK_PROCESS_CPUTIME_ID 和 CLOCK_THREAD_CPUTIME_ID 本质上就是 用的 RDTSC 的 TSC(time stamp counter寄存器) 每个时间中断 + 1</li>
<li>总而言之, 大部分情况下 clock_gettime() 就够了, 如果要测纳秒上的结果, 再去用 RDTSC/RDTSCP指令.</li>
</ul>
<p>可以看到 这几个函数 有一个 clock_id 的参数 这都属于 POSIX CLOCKS 中的内容.</p>
<h3 id="CLOCK-ID"><a href="#CLOCK-ID" class="headerlink" title="CLOCK_ID"></a>CLOCK_ID</h3><ul>
<li>CLOCK_REALTIME<ul>
<li>系统真实时间 俗称墙上时间 可能会被用户修改, 受 NTP 影响</li>
</ul>
</li>
<li>CLOCK_MONOTONIC<ul>
<li>与系统真实时间无关的时间, 单调时间, 受 NTP 影响</li>
</ul>
</li>
<li>CLOCK_PROCESS_CPUTIME_ID<ul>
<li>进程所耗费时间</li>
</ul>
</li>
<li>CLOCK_THREAD_CPUTIME_ID<ul>
<li>线程为单位耗费时间</li>
</ul>
</li>
<li>CLOCK_MONOTONIC_RAW<ul>
<li>与 CLOCK_MONOTONIC 相似 不过不会被 NTP影响到其频率</li>
</ul>
</li>
<li>CLOCK_REALTIME_COARSE<ul>
<li>CLOCK_REALTIME 低精度版本 即以Tick为单位, 受 NTP 影响</li>
</ul>
</li>
<li>CLOCK_MONOTONIC_COARSE<ul>
<li>CLOCK_MONOTONIC 低精度版本 即以Tick为单位, 受 NTP 影响</li>
</ul>
</li>
<li>CLOCK_BOOTTIME<ul>
<li>与 CLOCK_MONOTONIC 类似 但是会包含suspended时间</li>
</ul>
</li>
<li>CLOCK_REALTIME_ALARM<ul>
<li>基于 RTC (Real Time Clock), 系统暂停也能运作</li>
</ul>
</li>
<li>CLOCK_BOOTTIME_ALARM<ul>
<li>基于 RTC (Real Time Clock), 系统暂停也能运作</li>
</ul>
</li>
<li>CLOCK_TAI<ul>
<li>原子钟时间 没有闰秒, 以上都受闰秒影响, 唯独这个不受.</li>
</ul>
</li>
</ul>
<p>原子秒 是各界大佬发现铯133基态的两个超精细能级之间跃迁所对应的辐射（电磁波）的9 192 631 770个周期持续的时间 非常稳定, 因此就拿它来定义 秒的概念. 物极必反, 原子秒非常精确,但与地球公转和自转存在时间差, 因此需要加入闰秒进行适当调整.</p>
<h2 id="展示时间"><a href="#展示时间" class="headerlink" title="展示时间"></a>展示时间</h2><h3 id="gmtime-和-localtime"><a href="#gmtime-和-localtime" class="headerlink" title="gmtime() 和 localtime()"></a>gmtime() 和 localtime()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="comment">// 转为 GMT时间</span></span><br><span class="line"><span class="function">struct tm *<span class="title">gmtime</span><span class="params">(<span class="keyword">const</span> <span class="keyword">time_t</span> *calptr)</span></span>;</span><br><span class="line"><span class="comment">// 转为 当前时区时间</span></span><br><span class="line"><span class="function">struct tm *<span class="title">localtime</span><span class="params">(<span class="keyword">const</span> <span class="keyword">time_t</span> *calptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="mktime"><a href="#mktime" class="headerlink" title="mktime()"></a>mktime()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt; </span></span></span><br><span class="line"><span class="comment">// 将 tm时间转化为 time_t</span></span><br><span class="line"><span class="function"><span class="keyword">time_t</span> <span class="title">mktime</span><span class="params">(struct tm *tmptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="strftime"><a href="#strftime" class="headerlink" title="strftime()"></a>strftime()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 格式化 tm 结构体来打印时间</span></span><br><span class="line"><span class="function"><span class="keyword">size_t</span> <span class="title">strftime</span><span class="params">(<span class="keyword">char</span> *<span class="keyword">restrict</span> buf, <span class="keyword">size_t</span> maxsize, </span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">const</span> <span class="keyword">char</span> *<span class="keyword">restrict</span> format, </span></span></span><br><span class="line"><span class="function"><span class="params"><span class="keyword">const</span> struct tm *<span class="keyword">restrict</span> tmptr)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="strptime"><a href="#strptime" class="headerlink" title="strptime()"></a>strptime()</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;time.h&gt;</span></span></span><br><span class="line"><span class="comment">// 字符串 转化为 tm结构体 与上面 恰好相反</span></span><br><span class="line"><span class="function"><span class="keyword">char</span> *<span class="title">strptime</span><span class="params">(<span class="keyword">const</span> <span class="keyword">char</span> *<span class="keyword">restrict</span> buf, <span class="keyword">const</span> <span class="keyword">char</span> *<span class="keyword">restrict</span> format, </span></span></span><br><span class="line"><span class="function"><span class="params">struct tm *<span class="keyword">restrict</span> tmptr)</span></span>;</span><br></pre></td></tr></table></figure>

<p>附上 Unix 环境高级编程 中的一张图.</p>
<p><img src="/images/unixTimePhoto.png" alt="unixTimePhoto.png"></p>
<h2 id="计时器"><a href="#计时器" class="headerlink" title="计时器"></a>计时器</h2><h3 id="alarm"><a href="#alarm" class="headerlink" title="alarm()"></a>alarm()</h3><p>sleep(), usleep() 都是用 SIGALRM实现</p>
<p>以秒为精度, 每个进程只能设置一个, 可以猜想到 PCB中只有一个 alarm(事实也是如此)</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 到期则发出 SIGALRM(默认动作终止进程)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;unistd.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">alarm</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> seconds)</span></span>;</span><br><span class="line"><span class="comment">// 挂起直到接收到一个信号</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">pause</span><span class="params">(<span class="keyword">void</span>)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="setitimer"><a href="#setitimer" class="headerlink" title="setitimer()"></a>setitimer()</h3><p>也是用信号实现 不适合 多线程编程</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">setitimer</span><span class="params">(<span class="keyword">int</span> which, <span class="keyword">const</span> struct itimerval *value, struct itimerval *ovalue)</span></span>;</span><br></pre></td></tr></table></figure>

<p>which 有 3种</p>
<ul>
<li>ITIMER_REAL 减少实际时间<ul>
<li>发出 SIGALRM 信号</li>
</ul>
</li>
<li>ITIMER_VIRTUAL 减少进程所执行的时间(不包含进程调度时间)<ul>
<li>发出 SIGVTALRM 信号</li>
</ul>
</li>
<li>ITIMER_PROF 减少进程所执行时间和进程调度所用时间<ul>
<li>发出 SIGPROF 信号</li>
</ul>
</li>
</ul>
<h3 id="timerfd-create"><a href="#timerfd-create" class="headerlink" title="timerfd_create()"></a>timerfd_create()</h3><p>将时间当做文件描述符, 在超时的时候, 文件变为可读, 适于多线程编程, 能很好融入 poll, epoll</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">timerfd_create</span><span class="params">(<span class="keyword">int</span> clockid, <span class="keyword">int</span> flags)</span></span>;</span><br></pre></td></tr></table></figure>

<h2 id="硬件的本领"><a href="#硬件的本领" class="headerlink" title="硬件的本领"></a>硬件的本领</h2><p>可以总结一下, 硬件有很多种, 有关时间的硬件无非要么提供时间中断功能, 要么提供了存储时间供用户读取的功能, 要么两者皆有.</p>
<ul>
<li>RTC(Real Time Clock) 实时时钟<ul>
<li>顾名思义 记录当前时间, 同时断电的时候 主板有个电池, 维持正确的时间, 精度到秒</li>
<li>既可以存储 LCT 也可以存储 UTC 凭借系统的配置文件进行配置</li>
</ul>
</li>
<li>TSC(Time Stamp Counter)<ul>
<li>就是上面讲到的 RDTSC 汇编指令所度的硬件, 只存在于 Intel 处理器, 每个Tick 都会 加 1, 即每次时钟中断都会 + 1, 纳秒级别时间精度.</li>
</ul>
</li>
</ul>
<p>还有更多的硬件, 就不一一解释了, 只要记住 硬件主要是 提供中断 和 存储时间的能力, 哪怕上层函数提供的时间精度再精细, 本质上也是因为硬件的计时单位精度提高了.</p>
<h2 id="Linux-中的时间"><a href="#Linux-中的时间" class="headerlink" title="Linux 中的时间"></a>Linux 中的时间</h2><p>这一小节, 将讲解 Linux 0.12 中是如何实现时间系统的.</p>
<h3 id="系统时间"><a href="#系统时间" class="headerlink" title="系统时间"></a>系统时间</h3><p>顾名思义, 就是维护系统时间的, 前面提到过 RTC 有一块电池, 始终保持系统时间, Linux 在 time_init 函数那, 会读取 RTC 硬件上的信息, 并将其初始化到 startup_time, 此后直到重启系统, 都不会再去读取 RTC了.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 内核中还会有这个宏, 来维护当前时间 每个jiffies 为一个Tick</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CURRENT_TIME (startup_time+(jiffies+jiffies_offset)/HZ)	<span class="comment">/* 当前时间(秒数) */</span></span></span><br></pre></td></tr></table></figure>

<p>每一次 发起时钟中断, 内核会调用到 do_timer() 这一函数 </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">简单来说 每次时钟中断的时候 都会去 判断当前特权级 即 CPL</span></span><br><span class="line"><span class="comment">如果在用户态 则 相应的 进程PCB utime ++, 否则 stime ++</span></span><br><span class="line"><span class="comment">这个版本是早期 Linux 0.12的 因此 这里面的 timer 即 计时器实现的略为粗浅</span></span><br><span class="line"><span class="comment">是将 计时器做成一个数组, 每次去遍历 哪个 timer 的时间到了</span></span><br><span class="line"><span class="comment"> 再去执行相应的操作(当讲到计时器的时候再展开)</span></span><br><span class="line"><span class="comment">接着减少 当前占用CPU的进程的时间片</span></span><br><span class="line"><span class="comment">接着再去执行调度函数</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">do_timer</span><span class="params">(<span class="keyword">long</span> cpl)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">static</span> <span class="keyword">int</span> blanked = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (blankcount || !blankinterval) &#123;</span><br><span class="line">		<span class="keyword">if</span> (blanked) &#123;</span><br><span class="line">			unblank_screen();</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (blankcount) &#123;</span><br><span class="line">			blankcount--;</span><br><span class="line">		&#125;</span><br><span class="line">		blanked = <span class="number">0</span>;</span><br><span class="line">	&#125; <span class="keyword">else</span> <span class="keyword">if</span> (!blanked) &#123;</span><br><span class="line">		blank_screen();</span><br><span class="line">		blanked = <span class="number">1</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (hd_timeout) &#123;</span><br><span class="line">		<span class="keyword">if</span> (!--hd_timeout) &#123;</span><br><span class="line">			hd_times_out();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (beepcount) &#123;</span><br><span class="line">		<span class="keyword">if</span> (!--beepcount) &#123;</span><br><span class="line">			sysbeepstop();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (cpl) &#123;</span><br><span class="line">		current-&gt;utime++;</span><br><span class="line">	&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">		current-&gt;stime++;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (next_timer) &#123;</span><br><span class="line">		next_timer-&gt;jiffies--;</span><br><span class="line">		<span class="keyword">while</span> (next_timer &amp;&amp; next_timer-&gt;jiffies &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">			<span class="keyword">void</span> (*fn)(<span class="keyword">void</span>);</span><br><span class="line">			</span><br><span class="line">			fn = next_timer-&gt;fn;</span><br><span class="line">			next_timer-&gt;fn = <span class="literal">NULL</span>;</span><br><span class="line">			next_timer = next_timer-&gt;next;</span><br><span class="line">			(fn)();</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> (current_DOR &amp; <span class="number">0xf0</span>) &#123;</span><br><span class="line">		do_floppy_timer();</span><br><span class="line">	&#125;</span><br><span class="line">	<span class="keyword">if</span> ((--current-&gt;counter)&gt;<span class="number">0</span>) &#123;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	current-&gt;counter=<span class="number">0</span>;</span><br><span class="line">	<span class="keyword">if</span> (!cpl) &#123;</span><br><span class="line">		<span class="keyword">return</span>;</span><br><span class="line">	&#125;</span><br><span class="line">	schedule();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>看完上面, 细心地观众可能会在想 jiffies 没有++啊? 那怎么维护 CURRENT_TIME 这个宏呢?</p>
<p>其实是在 系统调用的时候, 已经加了.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">timer_interrupt:</span><br><span class="line">	# ***</span><br><span class="line">	incl jiffies # 就在这</span><br><span class="line">	***</span><br><span class="line">	call do_timer		# &#39;do_timer(long CPL)&#39; does everything from</span><br><span class="line">	# ***</span><br><span class="line">	jmp ret_from_sys_call</span><br></pre></td></tr></table></figure>

<p>以上都是最基础的 Linux 0.12 版本的实现, 至于其他 clock_gettime 上面也提到过, 在大多数情况直接用的是 RDTSC 这一汇编指令.</p>
<h2 id="计时器实现"><a href="#计时器实现" class="headerlink" title="计时器实现"></a>计时器实现</h2><h3 id="alarm-实现"><a href="#alarm-实现" class="headerlink" title="alarm() 实现"></a>alarm() 实现</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 一个系统调用 设置PCB的 alarm数值</span></span><br><span class="line"><span class="comment">// sleep() 调用的就是这个系统调用</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sys_alarm</span><span class="params">(<span class="keyword">long</span> seconds)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">int</span> old = current-&gt;alarm;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">if</span> (old) &#123;</span><br><span class="line">		old = (old - jiffies) / HZ;</span><br><span class="line">	&#125;</span><br><span class="line">	current-&gt;alarm = (seconds &gt; <span class="number">0</span>) ? (jiffies + HZ * seconds) : <span class="number">0</span>; </span><br><span class="line">	<span class="comment">// 以 当前jiffies为基数</span></span><br><span class="line">	<span class="comment">// 在 schedule() 调度的时候 会去判断 如果 大于 此时的 jiffies 则发送 SIGALARM 信号</span></span><br><span class="line">	<span class="keyword">return</span> (old);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里先暂时略过 信号的工作原理, 等有机会再讲, 现在只要知道它发送了 SIGALARM 信号就行了.</p>
<h3 id="动态-Timer"><a href="#动态-Timer" class="headerlink" title="动态 Timer"></a>动态 Timer</h3><p>其实就是内核里面用到的Timer, 也是用户态计时器的基础.</p>
<p>这可以理解为最早期的时间轮算法, 不过在后期 Linux 使用了 Hierarchy 时间轮, 它本身是按照 时/分/秒 分为三个层级, 每个层级又根据 24/60/60 来分桶, 每次时钟中断的时候, 就将到期的移除, 且进行触发通知, Hierarchy 时间轮的 添加删除触发都是O(1), 但是维护起来非常费时, 比如当 分 走回0的时候, 就需要对 时 做移动, 通常我们的 Timer 达不到 60个那么多, 所以比较耗时.</p>
<p>也因此, Linux 采用的是 将 jiffies 分为 5个部分, Linux中称作 Timer Vector, 用来进行索引, 也有上面处理的缺点, 会带来不确定的延时.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 此为最早期的 时间轮, 并非是 Hierarchy 时间轮</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TIME_REQUESTS 64</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">struct</span> <span class="title">timer_list</span> &#123;</span></span><br><span class="line">	<span class="keyword">long</span> jiffies;</span><br><span class="line">	<span class="keyword">void</span> (*fn)();</span><br><span class="line">	<span class="class"><span class="keyword">struct</span> <span class="title">timer_list</span> * <span class="title">next</span>;</span></span><br><span class="line">&#125; timer_list[TIME_REQUESTS], * next_timer = <span class="literal">NULL</span>;</span><br></pre></td></tr></table></figure>

<p>可以看出 最多只能支持到 64 个 Timer, 每次发生时钟中断的时候 就遍历一下 timer_list.</p>
<h3 id="间隔-Timer-setitimer"><a href="#间隔-Timer-setitimer" class="headerlink" title="间隔 Timer setitimer()"></a>间隔 Timer setitimer()</h3><ul>
<li>ITIMER_REAL 实际时间<ul>
<li>因为是实际时间 可以直接用 内核中的 Timer实现</li>
</ul>
</li>
<li>ITIMER_VIRTUAL 进程真实执行时间<ul>
<li>由于参照的是进程时间, 要判断处于内核态还是用户态, 再修改相应数值 比如 utime, stime</li>
</ul>
</li>
<li>ITIMER_PROF 进程执行时间+进程调度时间<ul>
<li>和上面一样 也要考虑到当前内核态还是用户态</li>
</ul>
</li>
</ul>
<h3 id="POSIX-Timer"><a href="#POSIX-Timer" class="headerlink" title="POSIX Timer"></a>POSIX Timer</h3><h3 id="低精度版本-时间轮"><a href="#低精度版本-时间轮" class="headerlink" title="低精度版本 时间轮"></a>低精度版本 时间轮</h3><p>这其实就是上面 讲的 与 CLOCK_ID 相关的一类的接口, 其中 CLOCK_REALTIME 与 CLOCK_MONOTONIC 实现起来较为简单, 直接建立在 内核动态 Timer之上, 以 Tick 为单位.</p>
<p>然后 CLOCK_PROCESS_CPUTIME_ID 和 CLOCK_THREAD_CPUTIME_ID 这两个是和进程线程相关, 理所当然的是要在 PCB 下进行维护, Linux 做法是在PCB 下面加两个数组, 其中每个数组又做成了链表的形式, 有点像桶, 来维护 PROCESS 和 THREAD 的CPU Timer.</p>
<p>现在有了高精度版本的计时器, 基本上就只有内核和一些旧的驱动程序还在用了.</p>
<h3 id="高精度版本-Hrtimer"><a href="#高精度版本-Hrtimer" class="headerlink" title="高精度版本 Hrtimer"></a>高精度版本 Hrtimer</h3><p>要想提高精度, 自然要先将硬件上的HZ提高, 同时又不能太高, 高过1000的话, 时钟中断太频繁了, 性能会下降.</p>
<p>由于之前那套时间轮算法, 调整的过程 必然会有开销(因为你要重新排列位置), 因此引入了一个新的计时器 Hrtimer.</p>
<p>Hrtimer 的实现还是很简单的</p>
<ul>
<li>用红黑树以到期时间为key进行排序<ul>
<li>猜测使用红黑树的原因是 内核里面本身就有, 而且测试后特别好</li>
</ul>
</li>
<li>不用 jiffies 为计时单位, jiffies = 10ms 精度不够, 以 纳秒为计时单位</li>
</ul>
<p>在 多处理器的情况下, 每个CPU 都要维护 两个数据结构, 一个为 Monotonic 另一个是 Real time.</p>
<p>在高精度计时器中, 是直接有硬件计时器进行中断触发, 而不再是使用 Tick, 不过始终有误差, 因为始终还是通过中断机制去修改 Hrtimer 的红黑树嘛, 只不过 Hrtimer 的中断处理函数只醉心于 计时, 那原先的时钟中断所做的进程调度之类的事情, 则是由 Hrtimer 开一个计时器, 然后设置好间隔的 Tick, 最后设置 计时回调函数, 当时间到了, 就到计时回调函数那处理 进程调度.</p>
<p>换一句话说就是 Hrtimer 开了一个计时器 模拟了 时钟中断.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>经过上面的洗礼,相信读者已经对 Linux 下的时间编程原理有了一个新的认识, 用起来就按需要的精度来选择, 原理 无非就是 将时间分为5个部分 做成一个时间轮 或者 用红黑树将到期时间进行排序, 本质上都离不开中断机制.</p>
]]></content>
      <categories>
        <category>Linux系统编程</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>系统编程</tag>
        <tag>时间</tag>
        <tag>时间编程</tag>
      </tags>
  </entry>
  <entry>
    <title>Linux网络编程 I/O多路复用</title>
    <url>/Linux%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8BIO%E5%A4%9A%E8%B7%AF%E5%A4%8D%E7%94%A8/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>本节主要是讲 网络编程中, 常用的I/O 多路复用. 全文大概分为以下几点.</p>
<ol>
<li>为什么需要I/O多路复用?</li>
<li>I/O多路复用的使用场景?</li>
<li>为什么都是与非阻塞I/O进行搭配, 而不是与阻塞I/O进行搭配呢?</li>
<li>select 的 优缺点 及 内核实现</li>
<li>poll 的 优缺点 及 内核实现</li>
<li>epoll 的 优缺点 及 内核实现</li>
</ol>
<a id="more"></a>

<h2 id="为什么需要-I-O-多路复用"><a href="#为什么需要-I-O-多路复用" class="headerlink" title="为什么需要 I/O 多路复用?"></a>为什么需要 I/O 多路复用?</h2><p>首先 I/O 模型 最主要分为以下几种</p>
<ul>
<li>阻塞I/O</li>
<li>非阻塞I/O</li>
<li>I/O 复用</li>
<li>信号驱动I/O</li>
<li>异步I/O</li>
</ul>
<p>在这里只谈 阻塞与非阻塞 I/O.</p>
<h3 id="阻塞I-O"><a href="#阻塞I-O" class="headerlink" title="阻塞I/O"></a>阻塞I/O</h3><p>比如说 Socket send 一段数据给对端机器 如果TCP发送缓冲区不够大, 则会产生阻塞, 产生阻塞之后, 调度器会将CPU资源让给其他进程, 这样对于一个服务器进程来说实在是难以接受, 因此有没有什么办法, 让内核通知我们 缓冲区什么时候足够大了, 再通知我们, 我们这时候再去写入数据到TCP缓冲区呢?</p>
<p>这就引出了 I/O 多路复用, 它就是用来做这类事情, 只要你将描述符给到它们, 当可读或可写为你所关心的事件的时候, 它就会来通知你, 这时候去读就不会阻塞(注意不是一定, 原因会在下面)</p>
<h3 id="非阻塞I-O"><a href="#非阻塞I-O" class="headerlink" title="非阻塞I/O"></a>非阻塞I/O</h3><p>那非阻塞I/O send 的时候不就不会阻塞了吗? 为什么 非阻塞I/O 也要用 I/O多路复用呢? 其实原因很简单, 非阻塞I/O 你想你调用 send 的函数, 发送出去, 它是不阻塞的, 但是有可能TCP发送缓冲区不够大, 它虽然立即返回结果, 但有可能并没有发送成功, 这个时候你就要想, 我应该在什么时候再试试呢? 不知晓I/O多路复用的人 很可能写出以下 伪代码</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (Socket::send(buf) != succ) &#123;&#125;</span><br></pre></td></tr></table></figure>

<p>换句话说就是 不停地 while 循环 检测 TCP发送缓冲区是否足以成功发送了, 但是这种做法会引来一个新的问题 那就是, 服务端没办法做其他任何的事情, 它就一直在这傻傻的不停地问.</p>
<p>这时候 I/O多路复用再次出场了, 它通知你这时候可读或可写, 你再去读, 这样就不会一直处于 busy loop 中.</p>
<h2 id="I-O多路复用的使用场景"><a href="#I-O多路复用的使用场景" class="headerlink" title="I/O多路复用的使用场景?"></a>I/O多路复用的使用场景?</h2><p>其实从上面举的例子来看, 已经讲清楚了I/O多路复用的使用场景, 当你想要高效的知道一个文件描述符是否可读/可写的时候, 就可以采用I/O多路复用模型.</p>
<h2 id="为什么都是与非阻塞I-O进行搭配-而不是与阻塞I-O进行搭配呢"><a href="#为什么都是与非阻塞I-O进行搭配-而不是与阻塞I-O进行搭配呢" class="headerlink" title="为什么都是与非阻塞I/O进行搭配, 而不是与阻塞I/O进行搭配呢?"></a>为什么都是与非阻塞I/O进行搭配, 而不是与阻塞I/O进行搭配呢?</h2><p>在第一个问题中, 我分别描述了 I/O多路复用与非阻塞I/O和阻塞I/O的搭配使用, 但是有些基础的同学, 可能会想, 为什么我在网络上看到的都是说 I/O多路复用与非阻塞I/O进行搭配, 几乎没有说到和阻塞I/O进行搭配的?</p>
<p>首先假设 此时 阻塞I/O与I/O多路复用搭配使用, 内核通知到我们说 这个阻塞 I/O 可以读了, 我们就去读, 那这时候就有一个问题, 数据有多大, 我们要读多少次呢? 如果我们只读一次, 那效率又太低了, 如果我们读多次, 你怎么保证下一次读的时候, 一定不会阻塞呢? 要知道I/O多路复用只保证你当前读了一次不阻塞, 不代表读多次不阻塞, 如果阻塞了, I/O多路复用的机制就完全停住了, 因为程序一直阻塞在读中, 这就回退到了 阻塞I/O的版本.</p>
<p>还有一个问题就是说, 内核通知你去读, 但是有可能被其他线程读走了, 然后你并不知道, 再去读 阻塞了, 这就是惊群现象.</p>
<p>而与非阻塞I/O搭配, 没这么多烦恼, 反正我就一直读 读到返回 EWOULDBLOCK为止就是了, 反正不阻塞, 爱读多少读多少, 就算被别人拿走了, 我也不会阻塞.</p>
<p>可以看出 与非阻塞I/O进行搭配确实可以减轻我们不少的烦恼啊.</p>
<p>再举两个例子</p>
<p>accept() 阻塞版 与 I/O多路复用搭配, 内核通知我们可以 去建立一个连接, 但是如果服务器这时候很迟钝, 一直等到客户那边发送RST后才去连接, 这时候TCP会将客户的连接从队列中删除, 很明显 之后调用accept() 会发生阻塞.</p>
<p>connect() 阻塞版, 在调用之后 TCP完成三次握手前, 突然被中断了, 会直接返回 EINTR, 从中断服务例程回来之后, 是应该重新调用 connect() 吗? 显然是不行的 因为握手的过程还在继续, 只不过中途被中断了而已, 重新调用 connect() 如果对方已经接受连接, 则这一次connect会被拒绝, 返回 EADDRINUSE 错误, 只能通过 I/O多路复用, 等连接建立成功时, 再返回套接字可写条件.</p>
<p>题外话, 如果建立连接的过程中被中断的话, 有以下几种做法</p>
<ol>
<li>你自己重启被中断的系统调用</li>
<li>对信号设置 SA_RESTART 属性, 让被中断的系统调用自行恢复</li>
<li>忽略信号</li>
</ol>
<h2 id="select-的-优缺点-及-内核实现"><a href="#select-的-优缺点-及-内核实现" class="headerlink" title="select 的 优缺点 及 内核实现"></a>select 的 优缺点 及 内核实现</h2><p>最多只支持到 1024个描述符, 不够用</p>
<h3 id="select-使用方法"><a href="#select-使用方法" class="headerlink" title="select 使用方法"></a>select 使用方法</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">select</span><span class="params">(<span class="keyword">int</span> maxfd, fd_set *readset, fd_set *writeset, fd_set *exceptset, <span class="keyword">const</span> struct timeval *timeout)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>maxfd: 描述符数量</li>
<li>readset: 读描述符集合</li>
<li>writeset: 写描述符集合</li>
<li>exceptset: 异常描述符集合</li>
<li>timeout: 如果为NULL 表示一直等, 如果为0则不等立即返回, 如果非0则一直等到超时</li>
</ul>
<p>每次调用 select 都要重新覆盖 那三个集合, 因为监听的事件会改变着三个集合中的位, 此外, 每次返回的时候 如果select 不为0, 说明有事件发生, 如果想知道是哪个描述符的事件, 就要通过遍历 三个集合的内容, 来找到那个描述符.</p>
<h3 id="select-优缺点"><a href="#select-优缺点" class="headerlink" title="select 优缺点"></a>select 优缺点</h3><p>通过以上可知, 优点 实现简单.</p>
<p>缺点:</p>
<ol>
<li>只支持 1024 个文件描述符</li>
<li>每次都要给内核重新传递三个集合, 用户态拷贝到内核态 开销大</li>
<li>每次都要遍历三个集合, 才能知道是哪个文件描述符发送了事件, 而且因为是位图, 遍历还是用的线性遍历</li>
</ol>
<h3 id="select-内核实现"><a href="#select-内核实现" class="headerlink" title="select 内核实现"></a>select 内核实现</h3><p>就是一个简单的 bitmap</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FD_SETSIZE 1024 <span class="comment">// 可以看出 select 最多支持 1024个描述符</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> NFDBITS (8 * sizeof(unsigned long))</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __FDSET_LONGS (FD_SETSIZE/NFDBITS)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 本质上就是一个 bitmap</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> fds_bits[__FDSET_LONGS];</span><br><span class="line">&#125; fd_set;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 按位设置</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FD_SET(fd,fdsetp)	__FD_SET(fd,fdsetp)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FD_CLR(fd,fdsetp)	__FD_CLR(fd,fdsetp)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FD_ISSET(fd,fdsetp)	__FD_ISSET(fd,fdsetp)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> FD_ZERO(fdsetp)		__FD_ZERO(fdsetp)</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __FD_SET(fd, fdsetp) \</span></span><br><span class="line">		(((fd_set *)(fdsetp))-&gt;fds_bits[(fd) &gt;&gt; <span class="number">5</span>] |= (<span class="number">1</span>&lt;&lt;((fd) &amp; <span class="number">31</span>)))</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __FD_CLR(fd, fdsetp) \</span></span><br><span class="line">		(((fd_set *)(fdsetp))-&gt;fds_bits[(fd) &gt;&gt; <span class="number">5</span>] &amp;= ~(<span class="number">1</span>&lt;&lt;((fd) &amp; <span class="number">31</span>)))</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __FD_ISSET(fd, fdsetp) \</span></span><br><span class="line">		((((fd_set *)(fdsetp))-&gt;fds_bits[(fd) &gt;&gt; <span class="number">5</span>] &amp; (<span class="number">1</span>&lt;&lt;((fd) &amp; <span class="number">31</span>))) != <span class="number">0</span>)</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __FD_ZERO(fdsetp) \</span></span><br><span class="line">		(<span class="built_in">memset</span> (fdsetp, <span class="number">0</span>, <span class="keyword">sizeof</span> (*(fd_set *)(fdsetp))))</span><br></pre></td></tr></table></figure>

<p>SYSCALL_DEFINE5 ⇒ sys_select() 在这里</p>
<p>sys_select() 主要是对超时时间做处理, 从用户态copy到内核态, 然后将超时时间 转化为 纳秒</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE5(select, <span class="keyword">int</span>, n, fd_set __user *, inp, fd_set __user *, outp,</span><br><span class="line">        fd_set __user *, <span class="built_in">exp</span>, struct timeval __user *, tvp)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">end_time</span>, *<span class="title">to</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">tv</span>;</span></span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (tvp) &#123;</span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(&amp;tv, tvp, <span class="keyword">sizeof</span>(tv)))</span><br><span class="line">            <span class="keyword">return</span> -EFAULT;</span><br><span class="line"></span><br><span class="line">        to = &amp;end_time;</span><br><span class="line">        <span class="keyword">if</span> (poll_select_set_timeout(to,</span><br><span class="line">                tv.tv_sec + (tv.tv_usec / USEC_PER_SEC),</span><br><span class="line">                (tv.tv_usec % USEC_PER_SEC) * NSEC_PER_USEC))</span><br><span class="line">            <span class="keyword">return</span> -EINVAL;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    ret = core_sys_select(n, inp, outp, <span class="built_in">exp</span>, to);</span><br><span class="line">    ret = poll_select_copy_remaining(&amp;end_time, tvp, <span class="number">1</span>, ret);</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来调用 core_sys_select()</p>
<p>创建一个 256位 的数组, 然后获取当前进程的文件描述符表, 主要是做判断 不能让 select 监控的最大 文件描述符超过 该进程的文件描述符的上限, 接下来开辟空间 要开6个bitmap, in, out, ex 和其余三个对应的结果集合, 最后将其清空, 然后从用户态中拷贝到新创建的集合中, 调用 do_select() 去做真正的操作.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">core_sys_select</span><span class="params">(<span class="keyword">int</span> n, fd_set __user *inp, fd_set __user *outp,</span></span></span><br><span class="line"><span class="function"><span class="params">                 fd_set __user *<span class="built_in">exp</span>, struct timespec *end_time)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    fd_set_bits fds;</span><br><span class="line">    <span class="keyword">void</span> *bits;</span><br><span class="line">    <span class="keyword">int</span> ret, max_fds;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="built_in">size</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">fdtable</span> *<span class="title">fdt</span>;</span></span><br><span class="line"></span><br><span class="line">	  <span class="comment">// SELECT_STACK_ALLOC 为 256</span></span><br><span class="line">    <span class="keyword">long</span> stack_fds[SELECT_STACK_ALLOC/<span class="keyword">sizeof</span>(<span class="keyword">long</span>)];</span><br><span class="line"></span><br><span class="line">    rcu_read_lock();</span><br><span class="line">		<span class="comment">// 获取该进程的 文件描述符表</span></span><br><span class="line">    fdt = files_fdtable(current-&gt;files);</span><br><span class="line">    max_fds = fdt-&gt;max_fds;</span><br><span class="line">    rcu_read_unlock();</span><br><span class="line">		</span><br><span class="line">		<span class="comment">// select 可监控最大为 该进程的文件描述符的上限</span></span><br><span class="line">    <span class="keyword">if</span> (n &gt; max_fds)</span><br><span class="line">        n = max_fds;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 计算在位图中表示n需要多少字节, (32 + n - 1 / 32) * 4</span></span><br><span class="line">    <span class="built_in">size</span> = FDS_BYTES(n); </span><br><span class="line">    bits = stack_fds;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 6个 bitmap, in out ex, 和 三个对应的结果</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">size</span> &gt; <span class="keyword">sizeof</span>(stack_fds) / <span class="number">6</span>) &#123;</span><br><span class="line">        bits = kmalloc(<span class="number">6</span> * <span class="built_in">size</span>, GFP_KERNEL);</span><br><span class="line">    &#125;</span><br><span class="line">    fds.in      = bits;</span><br><span class="line">    fds.out     = bits +   <span class="built_in">size</span>;</span><br><span class="line">    fds.ex      = bits + <span class="number">2</span>*<span class="built_in">size</span>;</span><br><span class="line">    fds.res_in  = bits + <span class="number">3</span>*<span class="built_in">size</span>;</span><br><span class="line">    fds.res_out = bits + <span class="number">4</span>*<span class="built_in">size</span>;</span><br><span class="line">    fds.res_ex  = bits + <span class="number">5</span>*<span class="built_in">size</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 把用户态的三个集合拷贝到内核态</span></span><br><span class="line">    <span class="keyword">if</span> ((ret = get_fd_set(n, inp, fds.in)) ||</span><br><span class="line">            (ret = get_fd_set(n, outp, fds.out)) ||</span><br><span class="line">            (ret = get_fd_set(n, <span class="built_in">exp</span>, fds.ex)))</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">        </span><br><span class="line">    <span class="comment">// 清空结果集</span></span><br><span class="line">    zero_fd_set(n, fds.res_in);</span><br><span class="line">    zero_fd_set(n, fds.res_out);</span><br><span class="line">    zero_fd_set(n, fds.res_ex);</span><br><span class="line">		</span><br><span class="line">		<span class="comment">// 真正去做 select 的函数</span></span><br><span class="line">    ret = do_select(n, &amp;fds, end_time);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ret &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">goto</span> out;</span><br><span class="line">    <span class="keyword">if</span> (!ret) &#123;</span><br><span class="line">        ret = -ERESTARTNOHAND;</span><br><span class="line">        <span class="keyword">if</span> (signal_pending(current))</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        ret = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 将结果集 从内核态 拷贝到 用户态</span></span><br><span class="line">    <span class="keyword">if</span> (set_fd_set(n, inp, fds.res_in) ||</span><br><span class="line">            set_fd_set(n, outp, fds.res_out) ||</span><br><span class="line">            set_fd_set(n, <span class="built_in">exp</span>, fds.res_ex))</span><br><span class="line">        ret = -EFAULT;</span><br><span class="line"></span><br><span class="line">out:</span><br><span class="line">    <span class="keyword">if</span> (bits != stack_fds)</span><br><span class="line">        kfree(bits);</span><br><span class="line">out_nofds:</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>do_select()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_select</span><span class="params">(<span class="keyword">int</span> n, fd_set_bits *fds, struct timespec *end_time)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">ktime_t</span> expire, *to = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">poll_wqueues</span> <span class="title">table</span>;</span></span><br><span class="line">    poll_table *wait;</span><br><span class="line">    <span class="keyword">int</span> retval, i, timed_out = <span class="number">0</span>;</span><br><span class="line">    u64 slack = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    rcu_read_lock();</span><br><span class="line">    retval = max_select_fd(n, fds);</span><br><span class="line">    rcu_read_unlock();</span><br><span class="line">    n = retval;</span><br><span class="line"></span><br><span class="line">    poll_initwait(&amp;table);</span><br><span class="line">    wait = &amp;table.pt;</span><br><span class="line">    <span class="keyword">if</span> (end_time &amp;&amp; !end_time-&gt;tv_sec &amp;&amp; !end_time-&gt;tv_nsec) &#123;</span><br><span class="line">        wait-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">        timed_out = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (end_time &amp;&amp; !timed_out)</span><br><span class="line">        slack = select_estimate_accuracy(end_time);</span><br><span class="line"></span><br><span class="line">    retval = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="keyword">unsigned</span> <span class="keyword">long</span> *rinp, *routp, *rexp, *inp, *outp, *<span class="built_in">exp</span>;</span><br><span class="line">        <span class="keyword">bool</span> can_busy_loop = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        inp = fds-&gt;in; outp = fds-&gt;out; <span class="built_in">exp</span> = fds-&gt;ex;</span><br><span class="line">        rinp = fds-&gt;res_in; routp = fds-&gt;res_out; rexp = fds-&gt;res_ex;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n; ++rinp, ++routp, ++rexp) &#123;</span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> in, out, ex, all_bits, <span class="built_in">bit</span> = <span class="number">1</span>, mask, j;</span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> res_in = <span class="number">0</span>, res_out = <span class="number">0</span>, res_ex = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">            in = *inp++; out = *outp++; ex = *<span class="built_in">exp</span>++;</span><br><span class="line">            all_bits = in | out | ex;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">if</span> (all_bits == <span class="number">0</span>) &#123;</span><br><span class="line">                i += BITS_PER_LONG;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; BITS_PER_LONG; ++j, ++i, <span class="built_in">bit</span> &lt;&lt;= <span class="number">1</span>) &#123;</span><br><span class="line">                <span class="class"><span class="keyword">struct</span> <span class="title">fd</span> <span class="title">f</span>;</span></span><br><span class="line">                <span class="keyword">if</span> (i &gt;= n)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="keyword">if</span> (!(<span class="built_in">bit</span> &amp; all_bits))</span><br><span class="line">                    <span class="keyword">continue</span>;</span><br><span class="line">                f = fdget(i);</span><br><span class="line">                <span class="keyword">if</span> (f.file) &#123;</span><br><span class="line">                    <span class="keyword">const</span> <span class="class"><span class="keyword">struct</span> <span class="title">file_operations</span> *<span class="title">f_op</span>;</span></span><br><span class="line">                    f_op = f.file-&gt;f_op;</span><br><span class="line">                    mask = DEFAULT_POLLMASK;</span><br><span class="line">                    <span class="keyword">if</span> (f_op-&gt;poll) &#123;</span><br><span class="line">                        wait_key_set(wait, in, out, <span class="built_in">bit</span>, busy_flag);</span><br><span class="line">												<span class="comment">// 调用文件的 poll 操作</span></span><br><span class="line">                        mask = (*f_op-&gt;poll)(f.file, wait); </span><br><span class="line">                    &#125;</span><br><span class="line">                    fdput(f);</span><br><span class="line">                    <span class="comment">// 写入结果</span></span><br><span class="line">                    <span class="keyword">if</span> ((mask &amp; POLLIN_SET) &amp;&amp; (in &amp; <span class="built_in">bit</span>)) &#123;</span><br><span class="line">                        res_in |= <span class="built_in">bit</span>;</span><br><span class="line">                        retval++;</span><br><span class="line">                        wait-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> ((mask &amp; POLLOUT_SET) &amp;&amp; (out &amp; <span class="built_in">bit</span>)) &#123;</span><br><span class="line">                        res_out |= <span class="built_in">bit</span>;</span><br><span class="line">                        retval++;</span><br><span class="line">                        wait-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">if</span> ((mask &amp; POLLEX_SET) &amp;&amp; (ex &amp; <span class="built_in">bit</span>)) &#123;</span><br><span class="line">                        res_ex |= <span class="built_in">bit</span>;</span><br><span class="line">                        retval++;</span><br><span class="line">                        wait-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 当返回值不为零，则停止循环轮询</span></span><br><span class="line">                    <span class="keyword">if</span> (retval) &#123;</span><br><span class="line">                        can_busy_loop = <span class="literal">false</span>; </span><br><span class="line">                        busy_flag = <span class="number">0</span>;</span><br><span class="line">                    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (busy_flag &amp; mask)</span><br><span class="line">                        can_busy_loop = <span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (res_in)</span><br><span class="line">                *rinp = res_in;</span><br><span class="line">            <span class="keyword">if</span> (res_out)</span><br><span class="line">                *routp = res_out;</span><br><span class="line">            <span class="keyword">if</span> (res_ex)</span><br><span class="line">                *rexp = res_ex;</span><br><span class="line">            cond_resched(); <span class="comment">// 让出 CPU资源</span></span><br><span class="line">        &#125;</span><br><span class="line">        wait-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="comment">// 文件描述符准备就绪 超时 有信号 则退出循环</span></span><br><span class="line">        <span class="keyword">if</span> (retval || timed_out || signal_pending(current))</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">if</span> (table.error) &#123;</span><br><span class="line">            retval = table.error;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (can_busy_loop &amp;&amp; !need_resched()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!busy_end) &#123;</span><br><span class="line">                busy_end = busy_loop_end_time();</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (!busy_loop_timeout(busy_end))</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        busy_flag = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (end_time &amp;&amp; !to) &#123;</span><br><span class="line">            expire = timespec_to_ktime(*end_time);</span><br><span class="line">            to = &amp;expire;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (!poll_schedule_timeout(&amp;table, TASK_INTERRUPTIBLE,</span><br><span class="line">                         to, slack))</span><br><span class="line">            timed_out = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    poll_freewait(&amp;table);</span><br><span class="line">    <span class="keyword">return</span> retval;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到 do_select 主要遍历 集合, 通过集合中的文件描述符找到文件结构体, 然后调用 poll 函数, 最后将结果写入集合中, 比较蠢, 需要把集合中的全部扫一遍, 效率很低.</p>
<p>分三层遍历, 第一层死循环直到满足 超时, 文件描述符有监听事件发生, 中断, 第二层循环遍历文件描述符, 第三层遍历 集合中的每一个 bit.</p>
<ul>
<li>当有描述符发生所要监控的事件, 则会将其存下来, 返回到用户态</li>
<li>如果没有发生所要监控的事件, 如果已超时, 或者有待处理的信号, 也会回到用户态</li>
<li>即没有监控的事件, 又没有超时 或者 没有待处理的信号, 则会让出CPU, 等待被唤醒, 唤醒后再次进入循环</li>
</ul>
<p><code>mask = (*f_op-&gt;poll)(f.file, wait); </code></p>
<p>对于 socket 来说 应该是 sock_poll 会将当前进程 放入等待队列中(但并不是去睡眠), 等到这个 f_op 可读或可写时, 就会唤醒当前进程, 如果一直没人唤醒的话, 就会自己去睡眠, 并设置超时时间, 时间到了之后, 就会重新唤醒去重新遍历 fd.</p>
<h2 id="poll-的-优缺点-及-内核实现"><a href="#poll-的-优缺点-及-内核实现" class="headerlink" title="poll 的 优缺点 及 内核实现"></a>poll 的 优缺点 及 内核实现</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span>    fd;       <span class="comment">/* file descriptor */</span> <span class="comment">// 如果 &lt; 0 则不监听</span></span><br><span class="line">    short  events;   <span class="comment">/* events to look for */</span></span><br><span class="line">    short  revents;  <span class="comment">/* events returned */</span></span><br><span class="line"> &#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用于填写 events</span></span><br><span class="line"><span class="comment">// 可读事件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLIN     0x0001    <span class="comment">/* any readable data available */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLPRI    0x0002    <span class="comment">/* OOB/Urgent readable data */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLRDNORM 0x0040    <span class="comment">/* non-OOB/URG data available */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLRDBAND 0x0080    <span class="comment">/* OOB/Urgent readable data */</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 可写事件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLOUT    0x0004    <span class="comment">/* file descriptor is writeable */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLWRNORM POLLOUT   <span class="comment">/* no write type differentiation */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLWRBAND 0x0100    <span class="comment">/* OOB/Urgent data can be written */</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 异常事件 这些异常事件并不需要自己显式的传入, 内核源码中会自动帮我们 | 上去</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLERR    0x0008    <span class="comment">/* 一些错误发送 */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLHUP    0x0010    <span class="comment">/* 描述符挂起*/</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> POLLNVAL   0x0020    <span class="comment">/* 请求的事件无效*/</span></span></span><br></pre></td></tr></table></figure>

<h3 id="poll-使用方法"><a href="#poll-使用方法" class="headerlink" title="poll 使用方法"></a>poll 使用方法</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">poll</span><span class="params">(struct pollfd *fds, <span class="keyword">unsigned</span> <span class="keyword">long</span> nfds, <span class="keyword">int</span> timeout)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>fds: 监听事件数组</li>
<li>nfds: fds有多大(突破了select 1024的限制)</li>
<li>timeout: 超时事件 &lt; 0 则一直等待, 0立即返回, &gt;0 到时再返回</li>
</ul>
<h3 id="poll-优缺点"><a href="#poll-优缺点" class="headerlink" title="poll 优缺点"></a>poll 优缺点</h3><p>优点自然是 解决了 select 1024个文件描述符的限制</p>
<p>缺点和 select 一样</p>
<ol>
<li>每次都要从用户态拷贝 pollfd数组到 内核态</li>
<li>寻找发生事件的描述符也是 和 select 一样 进行遍历, 线性扫描.</li>
</ol>
<h3 id="poll-内核实现"><a href="#poll-内核实现" class="headerlink" title="poll 内核实现"></a>poll 内核实现</h3><p>sys_poll()</p>
<p>可以看到 poll 的系统调用 和 select 类似 都是先设置好超时时间</p>
<p>然后调用 do_sys_poll()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE3(poll, struct pollfd __user *, ufds, <span class="keyword">unsigned</span> <span class="keyword">int</span>, nfds,</span><br><span class="line">        <span class="keyword">int</span>, timeout_msecs)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">end_time</span>, *<span class="title">to</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    <span class="keyword">int</span> ret;</span><br><span class="line">		<span class="comment">// 老样子 如果有超时时间 先设置超时</span></span><br><span class="line">    <span class="keyword">if</span> (timeout_msecs &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        to = &amp;end_time;</span><br><span class="line">        poll_select_set_timeout(to, timeout_msecs / MSEC_PER_SEC,</span><br><span class="line">            NSEC_PER_MSEC * (timeout_msecs % MSEC_PER_SEC));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ret = do_sys_poll(ufds, nfds, to);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ret == -EINTR) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">restart_block</span> *<span class="title">restart_block</span>;</span></span><br><span class="line"></span><br><span class="line">        restart_block = &amp;current-&gt;restart_block;</span><br><span class="line">        restart_block-&gt;fn = do_restart_poll;</span><br><span class="line">        restart_block-&gt;poll.ufds = ufds;</span><br><span class="line">        restart_block-&gt;poll.nfds = nfds;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (timeout_msecs &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            restart_block-&gt;poll.tv_sec = end_time.tv_sec;</span><br><span class="line">            restart_block-&gt;poll.tv_nsec = end_time.tv_nsec;</span><br><span class="line">            restart_block-&gt;poll.has_timeout = <span class="number">1</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span></span><br><span class="line">            restart_block-&gt;poll.has_timeout = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        ret = -ERESTART_RESTARTBLOCK;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>do_sys_poll()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_sys_poll</span><span class="params">(struct pollfd __user *ufds, <span class="keyword">unsigned</span> <span class="keyword">int</span> nfds,</span></span></span><br><span class="line"><span class="function"><span class="params">        struct timespec *end_time)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">poll_wqueues</span> <span class="title">table</span>;</span></span><br><span class="line">    <span class="keyword">int</span> err = -EFAULT, fdcount, len, <span class="built_in">size</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建大小为256bit 的数组</span></span><br><span class="line">    <span class="keyword">long</span> stack_pps[POLL_STACK_ALLOC/<span class="keyword">sizeof</span>(<span class="keyword">long</span>)];</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">poll_list</span> *<span class="title">const</span> <span class="title">head</span> = (<span class="title">struct</span> <span class="title">poll_list</span> *)<span class="title">stack_pps</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">poll_list</span> *<span class="title">walk</span> = <span class="title">head</span>;</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> todo = nfds;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 上限默认为1024 (ulimit -n)</span></span><br><span class="line">    <span class="keyword">if</span> (nfds &gt; rlimit(RLIMIT_NOFILE))</span><br><span class="line">        <span class="keyword">return</span> -EINVAL;</span><br><span class="line"></span><br><span class="line">    len = <span class="keyword">min_t</span>(<span class="keyword">unsigned</span> <span class="keyword">int</span>, nfds, N_STACK_PPS);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 这个循环 负责将用户态的数据拷贝到内核态</span></span><br><span class="line">		<span class="comment">// 可见 select 和 poll的 性能开销大多用在了 内核和用户态的相互拷贝上</span></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        walk-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">        walk-&gt;len = len;</span><br><span class="line">        <span class="keyword">if</span> (!len)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (copy_from_user(walk-&gt;entries, ufds + nfds-todo,</span><br><span class="line">                    <span class="keyword">sizeof</span>(struct pollfd) * walk-&gt;len))</span><br><span class="line">            <span class="keyword">goto</span> out_fds;</span><br><span class="line"></span><br><span class="line">        todo -= walk-&gt;len;</span><br><span class="line">        <span class="keyword">if</span> (!todo)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        len = <span class="built_in">min</span>(todo, POLLFD_PER_PAGE);</span><br><span class="line">        <span class="built_in">size</span> = <span class="keyword">sizeof</span>(struct poll_list) + <span class="keyword">sizeof</span>(struct pollfd) * len;</span><br><span class="line">        walk = walk-&gt;next = kmalloc(<span class="built_in">size</span>, GFP_KERNEL);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 真正去做 do_poll()</span></span><br><span class="line">    poll_initwait(&amp;table);</span><br><span class="line">    fdcount = do_poll(nfds, head, &amp;table, end_time);</span><br><span class="line">    poll_freewait(&amp;table);</span><br><span class="line">		</span><br><span class="line">		<span class="comment">// 将结果拷贝回用户态</span></span><br><span class="line">    <span class="keyword">for</span> (walk = head; walk; walk = walk-&gt;next) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> *<span class="title">fds</span> = <span class="title">walk</span>-&gt;<span class="title">entries</span>;</span></span><br><span class="line">        <span class="keyword">int</span> j;</span><br><span class="line">        <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; walk-&gt;len; j++, ufds++)</span><br><span class="line">            <span class="keyword">if</span> (__put_user(fds[j].revents, &amp;ufds-&gt;revents))</span><br><span class="line">                <span class="keyword">goto</span> out_fds;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">    err = fdcount;</span><br><span class="line">out_fds:</span><br><span class="line">    walk = head-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (walk) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">poll_list</span> *<span class="title">pos</span> = <span class="title">walk</span>;</span></span><br><span class="line">        walk = walk-&gt;next;</span><br><span class="line">        kfree(pos);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> err;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>do_poll()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">do_poll</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">int</span> nfds,  struct poll_list *<span class="built_in">list</span>,</span></span></span><br><span class="line"><span class="function"><span class="params">           struct poll_wqueues *wait, struct timespec *end_time)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    poll_table* pt = &amp;wait-&gt;pt;</span><br><span class="line">    <span class="keyword">ktime_t</span> expire, *to = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">int</span> timed_out = <span class="number">0</span>, count = <span class="number">0</span>;</span><br><span class="line">    u64 slack = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> busy_flag = net_busy_loop_on() ? POLL_BUSY_LOOP : <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> busy_end = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (end_time &amp;&amp; !end_time-&gt;tv_sec &amp;&amp; !end_time-&gt;tv_nsec) &#123;</span><br><span class="line">        pt-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">        timed_out = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (end_time &amp;&amp; !timed_out)</span><br><span class="line">        slack = select_estimate_accuracy(end_time);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">poll_list</span> *<span class="title">walk</span>;</span></span><br><span class="line">        <span class="keyword">bool</span> can_busy_loop = <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (walk = <span class="built_in">list</span>; walk != <span class="literal">NULL</span>; walk = walk-&gt;next) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">pollfd</span> * <span class="title">pfd</span>, * <span class="title">pfd_end</span>;</span></span><br><span class="line"></span><br><span class="line">            pfd = walk-&gt;entries;</span><br><span class="line">            pfd_end = pfd + walk-&gt;len;</span><br><span class="line">            <span class="keyword">for</span> (; pfd != pfd_end; pfd++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (do_pollfd(pfd, pt, &amp;can_busy_loop,</span><br><span class="line">                          busy_flag)) &#123;</span><br><span class="line">                    count++;</span><br><span class="line">                    pt-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">                    busy_flag = <span class="number">0</span>;</span><br><span class="line">                    can_busy_loop = <span class="literal">false</span>; </span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        pt-&gt;_qproc = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">if</span> (!count) &#123;</span><br><span class="line">            count = wait-&gt;error;</span><br><span class="line">            <span class="keyword">if</span> (signal_pending(current))</span><br><span class="line">                count = -EINTR;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (count || timed_out)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (can_busy_loop &amp;&amp; !need_resched()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!busy_end) &#123;</span><br><span class="line">                busy_end = busy_loop_end_time();</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (!busy_loop_timeout(busy_end))</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        busy_flag = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (end_time &amp;&amp; !to) &#123;</span><br><span class="line">            expire = timespec_to_ktime(*end_time);</span><br><span class="line">            to = &amp;expire;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (!poll_schedule_timeout(wait, TASK_INTERRUPTIBLE, to, slack))</span><br><span class="line">            timed_out = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> count;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 又是通过 fd 找到 file 然后调用 file 的 poll 接口 将当前进程放入睡眠队列, 如果可读/可写</span></span><br><span class="line"><span class="comment">// 就唤醒当前进程, 要注意 这里放入睡眠队列不是说立刻进入睡眠</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> <span class="title">do_pollfd</span><span class="params">(struct pollfd *pollfd, </span></span></span><br><span class="line"><span class="function"><span class="params">                     poll_table *pwait,</span></span></span><br><span class="line"><span class="function"><span class="params">                     <span class="keyword">bool</span> *can_busy_poll,</span></span></span><br><span class="line"><span class="function"><span class="params">                     <span class="keyword">unsigned</span> <span class="keyword">int</span> busy_flag)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> mask;</span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line"></span><br><span class="line">    mask = <span class="number">0</span>;</span><br><span class="line">    fd = pollfd-&gt;fd;</span><br><span class="line">    <span class="keyword">if</span> (fd &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">fd</span> <span class="title">f</span> = <span class="title">fdget</span>(<span class="title">fd</span>);</span></span><br><span class="line">        mask = POLLNVAL;</span><br><span class="line">        <span class="keyword">if</span> (f.file) &#123;</span><br><span class="line">            mask = DEFAULT_POLLMASK;</span><br><span class="line">            <span class="keyword">if</span> (f.file-&gt;f_op-&gt;poll) &#123;</span><br><span class="line">                pwait-&gt;_key = pollfd-&gt;events|POLLERR|POLLHUP;</span><br><span class="line">                pwait-&gt;_key |= busy_flag;</span><br><span class="line">                mask = f.file-&gt;f_op-&gt;poll(f.file, pwait);</span><br><span class="line">                <span class="keyword">if</span> (mask &amp; busy_flag)</span><br><span class="line">                    *can_busy_poll = <span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            mask &amp;= pollfd-&gt;events | POLLERR | POLLHUP;</span><br><span class="line">            fdput(f);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    pollfd-&gt;revents = mask;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> mask;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>和 select 类似 来个无限循环, 然后 遍历 用户传进来的数组, 对数组中的文件描述符 进行 poll操作, 一样会将当前进程放入等待队列, 当文件描述符所代表的”文件”可读或者可写就会唤醒等待队列的进程, 最后将其放入 revents, 拷贝回用户态. 如果一直没有 文件可读或者可写, 就和select一样, 开个定时器, 让自己睡过去, 直到超时.</p>
<p>离开无限循环的条件 1. 有新事件 2. 超时, 3. 有信号发生, 发生中断</p>
<h2 id="epoll-的-优缺点-及-内核实现"><a href="#epoll-的-优缺点-及-内核实现" class="headerlink" title="epoll 的 优缺点 及 内核实现"></a>epoll 的 优缺点 及 内核实现</h2><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">union</span> epoll_data &#123;</span><br><span class="line">     <span class="keyword">void</span>        *ptr;</span><br><span class="line">     <span class="keyword">int</span>          fd;</span><br><span class="line">     <span class="keyword">uint32_t</span>     u32;</span><br><span class="line">     <span class="keyword">uint64_t</span>     u64;</span><br><span class="line"> &#125; <span class="keyword">epoll_data_t</span>;</span><br><span class="line"></span><br><span class="line"> <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> &#123;</span></span><br><span class="line">     <span class="keyword">uint32_t</span>     events;      <span class="comment">/* Epoll events */</span></span><br><span class="line">     <span class="keyword">epoll_data_t</span> data;        <span class="comment">/* User data variable */</span></span><br><span class="line"> &#125;;</span><br></pre></td></tr></table></figure>

<p>这里的 events 和 poll的 一样, 此外 epoll_data 中一般只用 fd.</p>
<h3 id="epoll-使用方法"><a href="#epoll-使用方法" class="headerlink" title="epoll 使用方法"></a>epoll 使用方法</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">epoll_create</span><span class="params">(<span class="keyword">int</span> <span class="built_in">size</span>)</span></span>;</span><br><span class="line"><span class="comment">// size 在 Linux 2.6.8 之后没用了, 但是还是要填写一个 大于 0 的数值</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">epoll_create1</span><span class="params">(<span class="keyword">int</span> flags)</span></span>;</span><br><span class="line"><span class="comment">// 创建 epollfd 可增加 额外选项</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">epoll_ctl</span><span class="params">(<span class="keyword">int</span> epfd, <span class="keyword">int</span> op, <span class="keyword">int</span> fd, struct epoll_event *event)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>epfd 就是 epoll_create 的返回值</li>
<li>op 就是 operator 操作<ul>
<li>EPOLL_CTL_ADD: 向 epoll 注册文件描述符的事件</li>
<li>EPOLL_CTL_DEL: 向 epoll 删除文件描述符的事件</li>
<li>EPOLL_CTL_MOD: 修改文件描述符的事件</li>
</ul>
</li>
<li>fd 监听的文件描述符</li>
<li>event 监听事件类型 和 用户自定义信息(大部分时候只放 fd)</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">epoll_wait</span><span class="params">(<span class="keyword">int</span> epfd, struct epoll_event *events, <span class="keyword">int</span> maxevents, <span class="keyword">int</span> timeout)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>events: 数组 返回需要处理的I/O事件</li>
<li>maxevents: 可以返回的最大事件值</li>
<li>timeout -1 不超时, 0 立即返回</li>
</ul>
<h3 id="条件触发-水平触发-与-边缘触发"><a href="#条件触发-水平触发-与-边缘触发" class="headerlink" title="条件触发(水平触发) 与 边缘触发"></a>条件触发(水平触发) 与 边缘触发</h3><p>通过设置 event | EPOLLET 设置为 边缘触发, 默认为水平触发.</p>
<p>用一句话来解释就是 条件触发的话 只要缓冲区有东西 就一直 从 epoll_wait 中提醒, 而边缘触发 只有在第一次 满足条件的时候才触发, 因此 边缘触发的效率比水平触发高, 不过 边缘触发的代码就不是很好写了.</p>
<p>题外话: select 和 poll 都是水平触发的模式.</p>
<h3 id="epoll-内核实现"><a href="#epoll-内核实现" class="headerlink" title="epoll 内核实现"></a>epoll 内核实现</h3><h4 id="sys-epoll-create"><a href="#sys-epoll-create" class="headerlink" title="sys_epoll_create()"></a>sys_epoll_create()</h4><p>epoll_create 会创建 匿名文件 和 文件描述符 同时将其绑定起来, 而且会将 该匿名文件 存入 eventpoll 结构体当中, 方便通过 epollfd 来找到 eventpoll 实例.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE1(epoll_create, <span class="keyword">int</span>, <span class="built_in">size</span>)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">size</span> &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> -EINVAL;</span><br><span class="line">    <span class="keyword">return</span> sys_epoll_create1(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE1(epoll_create1, <span class="keyword">int</span>, flags)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> error, fd;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">file</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建 eventpoll</span></span><br><span class="line">    error = ep_alloc(&amp;ep);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 找没用过的 fd</span></span><br><span class="line">    fd = get_unused_fd_flags(O_RDWR | (flags &amp; O_CLOEXEC));</span><br><span class="line">	  <span class="comment">// 创建 匿名文件 实例, 会将 eventpoll 存入 file 中的 private_data</span></span><br><span class="line">		<span class="comment">// 方便快速定位 到 eventpoll 对象</span></span><br><span class="line">    file = anon_inode_getfile(<span class="string">"[eventpoll]"</span>, &amp;eventpoll_fops, ep,</span><br><span class="line">                 O_RDWR | (flags &amp; O_CLOEXEC));</span><br><span class="line"></span><br><span class="line">    ep-&gt;file = file;</span><br><span class="line">		<span class="comment">// 将 fd 和 epoll 实例关联</span></span><br><span class="line">    fd_install(fd, file);</span><br><span class="line">    <span class="keyword">return</span> fd;</span><br><span class="line"></span><br><span class="line">out_free_fd:</span><br><span class="line">    put_unused_fd(fd);</span><br><span class="line">out_free_ep:</span><br><span class="line">    ep_free(ep);</span><br><span class="line">    <span class="keyword">return</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ep_alloc</span><span class="params">(struct eventpoll **pep)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> error;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">user_struct</span> *<span class="title">user</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span>;</span></span><br><span class="line">    user = get_current_user();</span><br><span class="line">    error = -ENOMEM;</span><br><span class="line">    ep = kzalloc(<span class="keyword">sizeof</span>(*ep), GFP_KERNEL);</span><br><span class="line"></span><br><span class="line">    spin_lock_init(&amp;ep-&gt;lock);</span><br><span class="line">    mutex_init(&amp;ep-&gt;mtx);</span><br><span class="line">    init_waitqueue_head(&amp;ep-&gt;wq); <span class="comment">// 执行 epoll_wait 而等待的进程队列</span></span><br><span class="line">    init_waitqueue_head(&amp;ep-&gt;poll_wait); <span class="comment">// eventloop 文件等待队列(epoll本身就是个文件)</span></span><br><span class="line">    INIT_LIST_HEAD(&amp;ep-&gt;rdllist);</span><br><span class="line">    ep-&gt;rbr = RB_ROOT;</span><br><span class="line">    ep-&gt;ovflist = EP_UNACTIVE_PTR;</span><br><span class="line">    ep-&gt;user = user;</span><br><span class="line">    *pep = ep;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">free_uid:</span><br><span class="line">    free_uid(user);</span><br><span class="line">    <span class="keyword">return</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * This structure is stored inside the "private_data" member of the file</span></span><br><span class="line"><span class="comment"> * structure and represents the main data structure for the eventpoll</span></span><br><span class="line"><span class="comment"> * interface.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> &#123;</span></span><br><span class="line">    <span class="comment">/* Protect the access to this structure */</span></span><br><span class="line">    <span class="keyword">spinlock_t</span> lock;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">mutex</span> <span class="title">mtx</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Wait queue used by sys_epoll_wait() */</span></span><br><span class="line">    <span class="comment">//这个队列里存放的是执行epoll_wait从而等待的进程队列</span></span><br><span class="line">    <span class="keyword">wait_queue_head_t</span> wq;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Wait queue used by file-&gt;poll() */</span></span><br><span class="line">		<span class="comment">// eventloop 文件等待队列(epoll本身就是个文件)</span></span><br><span class="line">    <span class="keyword">wait_queue_head_t</span> poll_wait;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* List of ready file descriptors */</span></span><br><span class="line">    <span class="comment">// 这里存放的是事件就绪的fd列表 链表的每个元素是下面的epitem</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">rdllist</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* RB tree root used to store monitored fd structs */</span></span><br><span class="line">    <span class="comment">// 红黑树 用来查找 fd</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">rb_root_cached</span> <span class="title">rbr</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * This is a single linked list that chains all the "struct epitem" that</span></span><br><span class="line"><span class="comment">     * happened while transferring ready events to userspace w/out</span></span><br><span class="line"><span class="comment">     * holding -&gt;lock.</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> *<span class="title">ovflist</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* wakeup_source used when ep_scan_ready_list is running */</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">wakeup_source</span> *<span class="title">ws</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* The user that created the eventpoll descriptor */</span></span><br><span class="line">		<span class="comment">// 描述 创建 epoll 的用户</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">user_struct</span> *<span class="title">user</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// epoll 实例文件</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">file</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* used to optimize loop detection check */</span></span><br><span class="line">    <span class="keyword">int</span> visited;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">visited_list_link</span>;</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 其实就是 红黑树的 一个节点</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> &#123;</span></span><br><span class="line">    <span class="keyword">union</span> &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">rb_node</span> <span class="title">rbn</span>;</span> <span class="comment">// RB树节点将此结构链接到eventpoll RB树</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">rcu_head</span> <span class="title">rcu</span>;</span> <span class="comment">// 用于释放epitem</span></span><br><span class="line">    &#125;;</span><br><span class="line">		<span class="comment">// 将这个eptiem连接到 eventpoll的 rdllist 的 list指针</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">rdllink</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> *<span class="title">next</span>;</span></span><br><span class="line">		<span class="comment">// epoll 监听的fd</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_filefd</span> <span class="title">ffd</span>;</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">// 一个文件被多个epoll实例监听时的监听数目</span></span><br><span class="line">    <span class="keyword">int</span> nwait;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">pwqlist</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span>;</span>  <span class="comment">// epitem 所属的 eventpoll</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> <span class="title">fllink</span>;</span> <span class="comment">// 链接到file条目列表的列表头</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">wakeup_source</span> __<span class="title">rcu</span> *<span class="title">ws</span>;</span> <span class="comment">// 设置EPOLLWAKEUP时使用的wakeup_source</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">event</span>;</span> <span class="comment">// 监控的事件和文件描述符</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> &#123;</span></span><br><span class="line">    __u32 events;</span><br><span class="line">    __u64 data;</span><br><span class="line">&#125; EPOLL_PACKED;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_filefd</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">file</span>;</span></span><br><span class="line">    <span class="keyword">int</span> fd;</span><br><span class="line">&#125; __packed;</span><br></pre></td></tr></table></figure>

<h4 id="sys-epoll-ctl"><a href="#sys-epoll-ctl" class="headerlink" title="sys_epoll_ctl()"></a>sys_epoll_ctl()</h4><p>先是根据 epollfd 来找到 匿名文件, 即 epoll 实例, 接着获取真正的 文件(fd传进来的), 然后取出 epoll,</p>
<p>接着 在红黑树中 找这个 fd.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE4(epoll_ctl, <span class="keyword">int</span>, epfd, <span class="keyword">int</span>, op, <span class="keyword">int</span>, fd,</span><br><span class="line">        struct epoll_event __user *, event)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> error;</span><br><span class="line">    <span class="keyword">int</span> full_check = <span class="number">0</span>;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">fd</span> <span class="title">f</span>, <span class="title">tf</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> *<span class="title">epi</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epoll_event</span> <span class="title">epds</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">tep</span> = <span class="title">NULL</span>;</span></span><br><span class="line"></span><br><span class="line">    error = -EFAULT;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ep_op_has_event(op) &amp;&amp;</span><br><span class="line">        copy_from_user(&amp;epds, event, <span class="keyword">sizeof</span>(struct epoll_event)))</span><br><span class="line"></span><br><span class="line">    f = fdget(epfd);</span><br><span class="line">    tf = fdget(fd);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!tf.file-&gt;f_op-&gt;poll)</span><br><span class="line">        <span class="keyword">goto</span> error_tgt_fput;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (ep_op_has_event(op))</span><br><span class="line">        ep_take_care_of_epollwakeup(&amp;epds);</span><br><span class="line"></span><br><span class="line">    ep = f.file-&gt;private_data; <span class="comment">// 取出epoll_create过程创建的eventpoll</span></span><br><span class="line"></span><br><span class="line">    mutex_lock_nested(&amp;ep-&gt;mtx, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    epi = ep_find(ep, tf.file, fd); <span class="comment">// 红黑树中 找 该 fd</span></span><br><span class="line">    <span class="keyword">switch</span> (op) &#123;</span><br><span class="line">    <span class="keyword">case</span> EPOLL_CTL_ADD:</span><br><span class="line">        <span class="keyword">if</span> (!epi) &#123;</span><br><span class="line">						<span class="comment">// 快看, 这里会自动 把 POLLERR 和 POLLHUP 带上 因此不需要显式传入</span></span><br><span class="line">            epds.events |= POLLERR | POLLHUP;</span><br><span class="line">            error = ep_insert(ep, &amp;epds, tf.file, fd, full_check);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (full_check)</span><br><span class="line">            clear_tfile_check_list();</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> EPOLL_CTL_DEL:</span><br><span class="line">        <span class="keyword">if</span> (epi)</span><br><span class="line">            error = ep_remove(ep, epi);</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> EPOLL_CTL_MOD:</span><br><span class="line">        <span class="keyword">if</span> (epi) &#123;</span><br><span class="line">            epds.events |= POLLERR | POLLHUP;</span><br><span class="line">            error = ep_modify(ep, epi, &amp;epds);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    mutex_unlock(&amp;ep-&gt;mtx);</span><br><span class="line">    fdput(tf);</span><br><span class="line">    fdput(f);</span><br><span class="line">    <span class="keyword">return</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>epi = ep_find(ep, tf.file, fd);</p>
<p>红黑树的 排序规则 采用 文件地址排序, 如果相同 就按照文件描述符进行排序</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">epoll_filefd</span> &#123;</span></span><br><span class="line">  <span class="class"><span class="keyword">struct</span> <span class="title">file</span> *<span class="title">file</span>;</span> <span class="comment">// pointer to the target file struct corresponding to the fd</span></span><br><span class="line">  <span class="keyword">int</span> fd; <span class="comment">// target file descriptor number</span></span><br><span class="line">&#125; __packed;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Compare RB tree keys */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ep_cmp_ffd</span><span class="params">(struct epoll_filefd *p1,</span></span></span><br><span class="line"><span class="function"><span class="params">                            struct epoll_filefd *p2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (p1-&gt;file &gt; p2-&gt;file ? +<span class="number">1</span>:</span><br><span class="line">       (p1-&gt;file &lt; p2-&gt;file ? <span class="number">-1</span> : p1-&gt;fd - p2-&gt;fd));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ep_insert()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ep_insert</span><span class="params">(struct eventpoll *ep, struct epoll_event *event,</span></span></span><br><span class="line"><span class="function"><span class="params">             struct file *tfile, <span class="keyword">int</span> fd, <span class="keyword">int</span> full_check)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> error, revents, pwake = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> flags;</span><br><span class="line">    <span class="keyword">long</span> user_watches;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> *<span class="title">epi</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">ep_pqueue</span> <span class="title">epq</span>;</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">// 判断 当前监控的文件值 是否超过了 /proc/sys/fs/epoll/max_user_watches 的预设最大值</span></span><br><span class="line">    user_watches = atomic_long_read(&amp;ep-&gt;user-&gt;epoll_watches);</span><br><span class="line">    <span class="keyword">if</span> (unlikely(user_watches &gt;= max_user_watches))</span><br><span class="line">        <span class="keyword">return</span> -ENOSPC;</span><br><span class="line">    <span class="keyword">if</span> (!(epi = kmem_cache_alloc(epi_cache, GFP_KERNEL)))</span><br><span class="line">        <span class="keyword">return</span> -ENOMEM;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化</span></span><br><span class="line">    INIT_LIST_HEAD(&amp;epi-&gt;rdllink);</span><br><span class="line">    INIT_LIST_HEAD(&amp;epi-&gt;fllink);</span><br><span class="line">    INIT_LIST_HEAD(&amp;epi-&gt;pwqlist);</span><br><span class="line">    epi-&gt;ep = ep;</span><br><span class="line">    ep_set_ffd(&amp;epi-&gt;ffd, tfile, fd); <span class="comment">// 将tfile和fd都赋值给ffd</span></span><br><span class="line">    epi-&gt;event = *event;</span><br><span class="line">    epi-&gt;nwait = <span class="number">0</span>;</span><br><span class="line">    epi-&gt;next = EP_UNACTIVE_PTR;</span><br><span class="line">    <span class="keyword">if</span> (epi-&gt;event.events &amp; EPOLLWAKEUP) &#123;</span><br><span class="line">        error = ep_create_wakeup_source(epi);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        RCU_INIT_POINTER(epi-&gt;ws, <span class="literal">NULL</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    epq.epi = epi;</span><br><span class="line">    <span class="comment">// 设置轮询的回调函数</span></span><br><span class="line">    init_poll_funcptr(&amp;epq.pt, ep_ptable_queue_proc);</span><br><span class="line">		<span class="comment">// ep_ptable_queue_proc -&gt; 会去设置回调函数 ep_poll_callback</span></span><br><span class="line">		<span class="comment">// 当有事件发生的时候 就会调用这个函数</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 执行poll方法</span></span><br><span class="line">    revents = ep_item_poll(epi, &amp;epq.pt);</span><br><span class="line"></span><br><span class="line">    spin_lock(&amp;tfile-&gt;f_lock);</span><br><span class="line">    list_add_tail_rcu(&amp;epi-&gt;fllink, &amp;tfile-&gt;f_ep_links);</span><br><span class="line">    spin_unlock(&amp;tfile-&gt;f_lock);</span><br><span class="line"></span><br><span class="line">		<span class="comment">// 将创建的epi添加到RB树</span></span><br><span class="line">    ep_rbtree_insert(ep, epi); </span><br><span class="line"></span><br><span class="line">    spin_lock_irqsave(&amp;ep-&gt;lock, flags);</span><br><span class="line">    <span class="comment">// 监听事件就绪 并且 epi的就绪队列有数据</span></span><br><span class="line">    <span class="keyword">if</span> ((revents &amp; event-&gt;events) &amp;&amp; !ep_is_linked(&amp;epi-&gt;rdllink)) &#123;</span><br><span class="line">        list_add_tail(&amp;epi-&gt;rdllink, &amp;ep-&gt;rdllist);</span><br><span class="line">        ep_pm_stay_awake(epi);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 唤醒调用epoll_wait的进程</span></span><br><span class="line">        <span class="keyword">if</span> (waitqueue_active(&amp;ep-&gt;wq))</span><br><span class="line">            wake_up_locked(&amp;ep-&gt;wq);</span><br><span class="line">        <span class="keyword">if</span> (waitqueue_active(&amp;ep-&gt;poll_wait))</span><br><span class="line">            pwake++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    spin_unlock_irqrestore(&amp;ep-&gt;lock, flags);</span><br><span class="line">    atomic_long_inc(&amp;ep-&gt;user-&gt;epoll_watches);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (pwake)</span><br><span class="line">        ep_poll_safewake(&amp;ep-&gt;poll_wait); <span class="comment">// 唤醒等待eventpoll文件就绪的进程</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ep_poll_callback()</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ep_poll_callback</span><span class="params">(<span class="keyword">wait_queue_t</span> *wait, <span class="keyword">unsigned</span> mode, <span class="keyword">int</span> sync, <span class="keyword">void</span> *key)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> pwake = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> flags;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">epitem</span> *<span class="title">epi</span> = <span class="title">ep_item_from_wait</span>(<span class="title">wait</span>);</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span> = <span class="title">epi</span>-&gt;<span class="title">ep</span>;</span></span><br><span class="line"></span><br><span class="line">    spin_lock_irqsave(&amp;ep-&gt;lock, flags);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (unlikely(ep-&gt;ovflist != EP_UNACTIVE_PTR)) &#123;</span><br><span class="line">        <span class="keyword">if</span> (epi-&gt;next == EP_UNACTIVE_PTR) &#123;</span><br><span class="line">            epi-&gt;next = ep-&gt;ovflist;</span><br><span class="line">            ep-&gt;ovflist = epi;</span><br><span class="line">            <span class="keyword">if</span> (epi-&gt;ws) &#123;</span><br><span class="line">                __pm_stay_awake(ep-&gt;ws);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">goto</span> out_unlock;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 事件发生 但不在已完成队列 就放进去</span></span><br><span class="line">    <span class="keyword">if</span> (!ep_is_linked(&amp;epi-&gt;rdllink)) &#123;</span><br><span class="line">        <span class="comment">// 将epi就绪事件 插入到ep就绪队列</span></span><br><span class="line">        list_add_tail(&amp;epi-&gt;rdllink, &amp;ep-&gt;rdllist);</span><br><span class="line">        ep_pm_stay_awake_rcu(epi);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 唤醒 eventpoll上的等待进程</span></span><br><span class="line">    <span class="keyword">if</span> (waitqueue_active(&amp;ep-&gt;wq))</span><br><span class="line">				<span class="comment">// 当队列不为空，则唤醒进程</span></span><br><span class="line">        wake_up_locked(&amp;ep-&gt;wq);</span><br><span class="line">    <span class="keyword">if</span> (waitqueue_active(&amp;ep-&gt;poll_wait))</span><br><span class="line">        pwake++;</span><br><span class="line"></span><br><span class="line">out_unlock:</span><br><span class="line">    spin_unlock_irqrestore(&amp;ep-&gt;lock, flags);</span><br><span class="line">    <span class="keyword">if</span> (pwake)</span><br><span class="line">        ep_poll_safewake(&amp;ep-&gt;poll_wait);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> ((<span class="keyword">unsigned</span> <span class="keyword">long</span>)key &amp; POLLFREE) &#123;</span><br><span class="line">        list_del_init(&amp;wait-&gt;task_list);</span><br><span class="line">        smp_store_release(&amp;ep_pwq_from_wait(wait)-&gt;whead, <span class="literal">NULL</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="sys-epoll-wait"><a href="#sys-epoll-wait" class="headerlink" title="sys_epoll_wait()"></a>sys_epoll_wait()</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">SYSCALL_DEFINE4(epoll_wait, <span class="keyword">int</span>, epfd, struct epoll_event __user *, events,</span><br><span class="line">        <span class="keyword">int</span>, maxevents, <span class="keyword">int</span>, timeout)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> error;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">fd</span> <span class="title">f</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">eventpoll</span> *<span class="title">ep</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (maxevents &lt;= <span class="number">0</span> || maxevents &gt; EP_MAX_EVENTS)</span><br><span class="line">        <span class="keyword">return</span> -EINVAL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!access_ok(VERIFY_WRITE, events, maxevents * <span class="keyword">sizeof</span>(struct epoll_event)))</span><br><span class="line">        <span class="keyword">return</span> -EFAULT;</span><br><span class="line"></span><br><span class="line">    f = fdget(epfd);  <span class="comment">// 获取 epollfd 对应的文件</span></span><br><span class="line">    ep = f.file-&gt;private_data;</span><br><span class="line"></span><br><span class="line">    ****error = ep_poll(ep, events, maxevents, timeout);</span><br><span class="line"></span><br><span class="line">error_fput:</span><br><span class="line">    fdput(f);</span><br><span class="line">    <span class="keyword">return</span> error;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ep_poll</span><span class="params">(struct eventpoll *ep, struct epoll_event __user *events,</span></span></span><br><span class="line"><span class="function"><span class="params">           <span class="keyword">int</span> maxevents, <span class="keyword">long</span> timeout)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> res = <span class="number">0</span>, eavail, timed_out = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> flags;</span><br><span class="line">    <span class="keyword">long</span> slack = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">wait_queue_t</span> wait;</span><br><span class="line">    <span class="keyword">ktime_t</span> expires, *to = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (timeout &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">timespec</span> <span class="title">end_time</span> = <span class="title">ep_set_mstimeout</span>(<span class="title">timeout</span>);</span></span><br><span class="line">        slack = select_estimate_accuracy(&amp;end_time);</span><br><span class="line">        to = &amp;expires;</span><br><span class="line">        *to = timespec_to_ktime(end_time);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (timeout == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// timeout 等于0为非阻塞 直接跳过去</span></span><br><span class="line">        timed_out = <span class="number">1</span>;</span><br><span class="line">        spin_lock_irqsave(&amp;ep-&gt;lock, flags);</span><br><span class="line">        <span class="keyword">goto</span> check_events;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">fetch_events:</span><br><span class="line">    spin_lock_irqsave(&amp;ep-&gt;lock, flags);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!ep_events_available(ep)) &#123;</span><br><span class="line">        <span class="comment">// 没有事件就绪则进入睡眠状态，当事件就绪后可通过ep_poll_callback()来唤醒</span></span><br><span class="line">        <span class="comment">// 将当前进程放入wait等待队列</span></span><br><span class="line">        init_waitqueue_entry(&amp;wait, current);</span><br><span class="line">        <span class="comment">// 将当前进程加入eventpoll等待队列，等待文件就绪、超时或中断信号</span></span><br><span class="line">        __add_wait_queue_exclusive(&amp;ep-&gt;wq, &amp;wait);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            set_current_state(TASK_INTERRUPTIBLE);</span><br><span class="line">						<span class="comment">// 有就绪事件 或者 超时 或者有信号 则退出循环</span></span><br><span class="line">            <span class="keyword">if</span> (ep_events_available(ep) || timed_out)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">if</span> (signal_pending(current)) &#123;</span><br><span class="line">                res = -EINTR;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            spin_unlock_irqrestore(&amp;ep-&gt;lock, flags);</span><br><span class="line">            <span class="comment">// 让出 CPU 进入睡眠</span></span><br><span class="line">            <span class="keyword">if</span> (!freezable_schedule_hrtimeout_range(to, slack,</span><br><span class="line">                                HRTIMER_MODE_ABS))</span><br><span class="line">                timed_out = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">            spin_lock_irqsave(&amp;ep-&gt;lock, flags);</span><br><span class="line">        &#125;</span><br><span class="line">				<span class="comment">// 被唤醒 将当前进程从等待队列中删除</span></span><br><span class="line">        __remove_wait_queue(&amp;ep-&gt;wq, &amp;wait);</span><br><span class="line">				<span class="comment">// 设置当前进程状态</span></span><br><span class="line">        set_current_state(TASK_RUNNING);</span><br><span class="line">    &#125;</span><br><span class="line">check_events:</span><br><span class="line">    eavail = ep_events_available(ep);</span><br><span class="line">    spin_unlock_irqrestore(&amp;ep-&gt;lock, flags);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将就绪事件传到 用户态</span></span><br><span class="line">    <span class="keyword">if</span> (!res &amp;&amp; eavail &amp;&amp;</span><br><span class="line">        !(res = ep_send_events(ep, events, maxevents)) &amp;&amp; !timed_out)</span><br><span class="line">        <span class="keyword">goto</span> fetch_events;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> res;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>ep_send_events() 还会在里面再次检测是否真的就绪, 因为很有可能在窗口时间(就是处理的过程中) 被用户处理掉.</p>
<h3 id="LT-与-ET-是怎么实现的"><a href="#LT-与-ET-是怎么实现的" class="headerlink" title="LT 与 ET 是怎么实现的?"></a>LT 与 ET 是怎么实现的?</h3><p>其实很简单, 如果是 LT 的话, 每次 处理完 都将 epoll_item 重新加入 eventpoll 就绪队列中, 这样就能再次被重新处理.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>经过以上洗礼, 我们可以得出结论, epoll 效率比 select 或者 poll 高的原因是, 因为, 它内部采用了红黑树 来存储 事件, 这样就不需要每次都从 用户态拷贝到内核态 节约了一层的性能开销.</p>
<p>此外, 红黑树能用来快速搜索 fd, fd 又直接关联 eventpoll 对象, 可以直接将 fd加入到 eventpoll的就绪队列中, 不用像 select 或者 poll 一样, 发生了事件, 傻傻的去遍历 到底是哪个 fd 发生了事件.</p>
<p>同时, 返回给用户的事件的方式又有很大改善, select 和 poll 一股脑全反回去, 你还要自己进行遍历, 筛选掉很多没用的事件, 而 epoll 则不同, 它直接给你发生了事件的, 没发生事件的不给你, 省的你去遍历, 以上三层, 奠定了 epoll 在 I/O多路复用中的地位.</p>
]]></content>
      <categories>
        <category>Linux网络编程</category>
      </categories>
      <tags>
        <tag>Linux</tag>
        <tag>系统编程</tag>
        <tag>网络编程</tag>
        <tag>I/O多路复用</tag>
        <tag>epoll</tag>
        <tag>select</tag>
        <tag>poll</tag>
      </tags>
  </entry>
  <entry>
    <title>SkipList 原理及在游戏排行榜中的应用</title>
    <url>/SkipList-%E5%8E%9F%E7%90%86%E5%8F%8A%E5%9C%A8%E6%B8%B8%E6%88%8F%E6%8E%92%E8%A1%8C%E6%A6%9C%E4%B8%AD%E7%9A%84%E5%BA%94%E7%94%A8/</url>
    <content><![CDATA[<p>近期工作的内容主要是设计一个通用的游戏排行榜服务器, 通用的含义指的是同时为多个项目组提供一套通用的解决方案, 经过不断的调研, 最终写下此文.</p>
<p>本文将会讲述 排行榜的多种实现方式, 不过最终我选择了 SkipList 作为底层的数据结构, 因此会着重讲SkipList.</p>
<p>先来讲一下排行榜的主要功能.</p>
<h2 id="排行榜主要功能"><a href="#排行榜主要功能" class="headerlink" title="排行榜主要功能"></a>排行榜主要功能</h2><ul>
<li>更新/删除/获取 一个 Key 在某个 排行榜 上的 排名, 及展示信息</li>
<li>获取 一个排行榜 任意区间(例如排名区间) 的展示信息</li>
</ul>
<h2 id="排行榜排序方案"><a href="#排行榜排序方案" class="headerlink" title="排行榜排序方案"></a>排行榜排序方案</h2><p>游戏中的排行榜的排序方案无非就两种</p>
<ul>
<li>实时排序, 即玩家数值变化的瞬间, 就完成了对它的排序</li>
<li>非实时排序, 亦可称之为定时排序, 玩家数据变动不立即排序, 而是直到策划所配的时间点到的时候才进行排序</li>
</ul>
<p>这两种排序方案没有好坏之分, 主要是看策划他想要什么? <del>(或许他自己也不知道想要什么, 他都想试试看, 他只顾着他自己)</del></p>
<a id="more"></a>

<h2 id="数据结构的抉择"><a href="#数据结构的抉择" class="headerlink" title="数据结构的抉择"></a>数据结构的抉择</h2><p>首先, 因为我们要提供给多个项目组一套解决方案, 每个项目组需要的排序方案大概率是不同的, 因此 排除了定时排序, 如果一开始就往定时排序的思路上想, 到时候哪个项目组突然说要实时排序, 改动起来会非常痛苦, 而且定时排序实现起来比较简单, 直接到点快排就完事了.</p>
<p>那么 排除了 定时排序, 就只能朝着实时排序的思路上走.</p>
<p>业界一般采用两种数据结构进行实现</p>
<ul>
<li>RB Tree 红黑树<ul>
<li>优势:<ul>
<li>时间复杂度的常数相对 SkipList 低</li>
<li>内存占用也相对较低</li>
</ul>
</li>
<li>不足:<ul>
<li>实现复杂</li>
<li>不能方便的获取到某个区间的信息</li>
</ul>
</li>
</ul>
</li>
<li>SkipList 跳表<ul>
<li>和 红黑树 相反</li>
</ul>
</li>
</ul>
<p>或许 堆也可以加进去, 取topK, 但是超出了k项, 就不知道排名.</p>
<p>SkipList 在 redis 的有序集合中也有用到, 这时候可能会有人问 为什么你不考虑直接用 redis 呢?</p>
<p>我考虑的点主要有以下几点</p>
<ol>
<li>引入 redis 会增加部署难度</li>
<li>引入 redis 仅仅只用来做排序, 是不是太笨重了?</li>
<li>redis 的默认 SkipList 实现是从小到大, 而我们知道一般是分值越靠前的越经常访问, 如果从小到大 查找靠前的效率可能不如从大到小, 但是 redis 写死排序顺序了, 我不开心</li>
<li>现在游戏服务器为了避免宕机的风险, 都会把服务器拆的特别细, 但是这就会带来一个问题, 本来一件事情能在一个地方集中处理完, 现在要多走几条链路才能做完, 效率会变低, 如果再加入一个 redis, 链路只会更长</li>
</ol>
<p>不过, 我们实现的时候可以参考 redis 的 SkipList.</p>
<h2 id="排行榜服务实现"><a href="#排行榜服务实现" class="headerlink" title="排行榜服务实现"></a>排行榜服务实现</h2><p>首先 我们要抽象出 key 和 value 这两个概念, key 你可以理解为 playerID, 那之所以叫 key 是因为 有的排行榜它不是以角色为单位, 它也有可能是以宠物为单位, 此时的 key 就是 petID. value 就是分值, 不过这里不能简单地就一个分值, 试想一下, 如果分值相同了怎么办?</p>
<p>以下代码均为伪代码, 随手一写.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Value</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint64_t</span> score;</span><br><span class="line">    <span class="comment">// 加入操作时间这一概念, 如果分值相同, 先来排前</span></span><br><span class="line">    <span class="keyword">int64_t</span> operatorTime;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Data</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint64_t</span> key;</span><br><span class="line">    Value value;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>Data 表示的就是 这个 key 用于排序的信息, 那么这时候有个问题 如果 这个 key 同时出现在 A, B 两个排行榜, 那么 key 就可能造成数据冗余, 占用内存, 这个时候 就要改造 Data 这一 结构.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Data</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint64_t</span> key;</span><br><span class="line">    <span class="comment">// Value value;</span></span><br><span class="line">    <span class="comment">// [leaderBoardID, value]</span></span><br><span class="line">    Value <span class="built_in">unordered_map</span>&lt;<span class="keyword">uint32_t</span>, Value&gt; value;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>这时候 如果一个 key 出现在多个排行榜 只需要在 这个 map 里 通过 排行榜ID 去找属于这个排行榜的分值就行了.</p>
<p>此时又出现了一个问题, player key 和 pet key 有可能相同 通过 key 来查找数据 显然是不合理的.</p>
<p>这时候就要引入一个 DataType 可以理解为 key 的类型, 它与 排行榜LeaderBoard 绑定在一起.</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">uint32_t</span> DataType;</span><br><span class="line"><span class="built_in">vector</span>&lt;Data&gt; DataArray;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">LeaderBoard</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> leaderBoardID;</span><br><span class="line">    DataType dataType;</span><br><span class="line">    <span class="comment">// 真正去排序的 SkipList</span></span><br><span class="line">    SkipList <span class="built_in">list</span>;</span><br><span class="line">&#125;;</span><br><span class="line">score = DataArray[leaderBoard.dataType].value[leaderboard.leaderBoard.ID].score;</span><br></pre></td></tr></table></figure>

<p>这样就能通过 排行榜 反查到其旗下的排行信息.</p>
<p>讲清楚了排行榜的设计, 接下来就是重头戏 SkipList 的实现, 这里我 拿出 Redis 的源码来讲解.</p>
<h2 id="SkipList-原理"><a href="#SkipList-原理" class="headerlink" title="SkipList 原理"></a>SkipList 原理</h2><p>其实你可以这么理解, 普通的链表 搜索一个东西 要遍历一次 时间复杂度为 O(n), 要想提高搜索速度, 就要尽可能跳过多的节点, 那么给它建多条索引链就行了.</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">┌────────┐    .─.                .─.               .─.</span><br><span class="line">│  Head  ├──▶(Min───────────────▶ <span class="number">7</span> ──────────────▶<span class="number">99</span> )  Level <span class="number">3</span></span><br><span class="line">└────┬───┘    `─'                `┬'               `┬'</span><br><span class="line">     │                            │                 │</span><br><span class="line">┌────▼───┐    .─.    .─.         .▼.   .─.         .▼.</span><br><span class="line">│  Head  ├──▶(Min──▶( <span class="number">3</span> ────────▶ <span class="number">7</span> ──▶<span class="number">30</span> ────────▶<span class="number">99</span> )  Level <span class="number">2</span></span><br><span class="line">└────┬───┘    `─'    `┬'         `┬'   `┬'         `┬'</span><br><span class="line">     │                │           │     │           │</span><br><span class="line">┌────▼───┐    .─.    .▼.   .─.   .▼.   .▼.   .─.   .▼.</span><br><span class="line">│  Head  ├──▶(Min──▶( <span class="number">3</span> ──▶ <span class="number">9</span> ──▶ <span class="number">7</span> ──▶<span class="number">30</span> ──▶<span class="number">90</span> ──▶<span class="number">99</span> )  Level <span class="number">1</span></span><br><span class="line">└────────┘    `─'    `─'   `─'   `─'   `─'   `─'   `─'</span><br></pre></td></tr></table></figure>

<p>从最上面的索引链表开始搜 30 的排名 可以像下面这样搜索</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">┌────────┐    .─.                .─.               .─.</span><br><span class="line">│  Head  ├──▶(Min──────<span class="number">3</span>────────▶ <span class="number">7</span> ──────────────▶<span class="number">99</span> )  Level <span class="number">3</span></span><br><span class="line">└────┬───┘    `─'                `┬'               `┬'</span><br><span class="line">	 │                            │                 │</span><br><span class="line">┌────▼───┐    .─.    .─.         .▼.   .─.         .▼.</span><br><span class="line">│  Head  ├──▶(Min──▶( <span class="number">3</span> ────────▶ <span class="number">7</span>  <span class="number">1</span>▶<span class="number">30</span> ────────▶<span class="number">99</span> )  Level <span class="number">2</span></span><br><span class="line">└────┬───┘    `─'    `┬'         `┬'   `┬'         `┬'</span><br><span class="line">     │                │           │     │           │</span><br><span class="line">┌────▼───┐    .─.    .▼.   .─.   .▼.   .▼.   .─.   .▼.</span><br><span class="line">│  Head  ├──▶(Min──▶( <span class="number">3</span> ──▶ <span class="number">9</span> ──▶ <span class="number">7</span> ──▶<span class="number">30</span> ──▶<span class="number">90</span> ──▶<span class="number">99</span> )  Level <span class="number">1</span></span><br><span class="line">└────────┘    `─'    `─'   `─'   `─'   `─'   `─'   `─'</span><br></pre></td></tr></table></figure>

<p>min 后面 要跨越 span = 3 才能到 7,  7要跨越 span = 1 才能到 30, 因此 30 从小到大为第四名.</p>
<p>至此, 我们知道 skiplist 本质就是多个索引 快速查找, 同时维护了一个 span值, 来快速得到 排名</p>
<h3 id="Redis-中-SkipList-的实现"><a href="#Redis-中-SkipList-的实现" class="headerlink" title="Redis 中 SkipList 的实现"></a>Redis 中 SkipList 的实现</h3><p>首先是要确定, 我们提升一个节点到索引链的概率, 和 要建多少条索引链, redis 中 最大层级为32层, 每次提升节点的概率为 0.25.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZSKIPLIST_MAXLEVEL 32</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ZSKIPLIST_P 0.25</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">zslRandomLevel</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> level = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> ((<span class="built_in">random</span>()&amp;<span class="number">0xFFFF</span>) &lt; (ZSKIPLIST_P * <span class="number">0xFFFF</span>))</span><br><span class="line">        level += <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> (level&lt;ZSKIPLIST_MAXLEVEL) ? level : ZSKIPLIST_MAXLEVEL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过这个随机算法, 可以粗略的计算</p>
<ul>
<li>层数为 1, 概率为 1 - p</li>
<li>层数为 2, 概率为 (1 - p)*p</li>
<li>层数为 3, 概率为 (1 - p)<em>p</em>p</li>
</ul>
<p>平均层数为 (1-p)+ (1-p)<em>p + (1-p)</em>p*p + … = 1 / 1 - p</p>
<p>如果按照 redis 的提升概率 0.25 来算, 那么空间上膨胀 (4 / 3) 即 1.33倍.</p>
<p>下面来看 SkipList 的定义.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> &#123;</span></span><br><span class="line">    <span class="comment">// 一个对象, 当分值相同时, 它决定了先后顺序, 可以忽视</span></span><br><span class="line">    robj *obj;</span><br><span class="line">    <span class="comment">// 分值</span></span><br><span class="line">    <span class="keyword">double</span> score;</span><br><span class="line">    <span class="comment">// 回退指针, 其实基本用不上</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">backward</span>;</span></span><br><span class="line">    <span class="comment">// 每一层</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistLevel</span> &#123;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 前进指针</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">forward</span>;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 距离后一个节点要走几步, 用来算 排名</span></span><br><span class="line">        <span class="keyword">unsigned</span> <span class="keyword">int</span> span;</span><br><span class="line"></span><br><span class="line">    &#125; level[];</span><br><span class="line"></span><br><span class="line">&#125; zskiplistNode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">zskiplist</span> &#123;</span></span><br><span class="line">    <span class="comment">// 头和尾节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">zskiplistNode</span> *<span class="title">header</span>, *<span class="title">tail</span>;</span></span><br><span class="line">    <span class="comment">// 表中节点的数量, 不然它从小到大排序, 你怎么找最大的?</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> length;</span><br><span class="line">    <span class="comment">// 最大层数 不超过 32层</span></span><br><span class="line">    <span class="keyword">int</span> level;</span><br><span class="line"></span><br><span class="line">&#125; zskiplist;</span><br></pre></td></tr></table></figure>

<h3 id="Insert-插入操作"><a href="#Insert-插入操作" class="headerlink" title="Insert 插入操作"></a>Insert 插入操作</h3><p>主要是从最高层, 依次向下找 新来的值应该插入到哪, 然后 保存到 update 这个数组中, 这样就不用每找一层就改一层.</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">zskiplistNode *<span class="title">zslInsert</span><span class="params">(zskiplist *zsl, <span class="keyword">double</span> score, robj *obj)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *update[<span class="number">32</span>], *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> rank[<span class="number">32</span>];</span><br><span class="line">    <span class="keyword">int</span> i, level;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 最顶层的起始 rank值当然是 0</span></span><br><span class="line">        rank[i] = i == (zsl-&gt;level<span class="number">-1</span>) ? <span class="number">0</span> : rank[i+<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 从小到大排</span></span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp;</span><br><span class="line">            (x-&gt;level[i].forward-&gt;score &lt; score ||</span><br><span class="line">                (x-&gt;level[i].forward-&gt;score == score &amp;&amp;</span><br><span class="line">                compareStringObjects(x-&gt;level[i].forward-&gt;obj,obj) &lt; <span class="number">0</span>))) &#123;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 跨度就是 排名</span></span><br><span class="line">            rank[i] += x-&gt;level[i].span;</span><br><span class="line"></span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line">        update[i] = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    level = zslRandomLevel();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化未使用层级</span></span><br><span class="line">    <span class="keyword">if</span> (level &gt; zsl-&gt;level) &#123;</span><br><span class="line">        <span class="keyword">for</span> (i = zsl-&gt;level; i &lt; level; i++) &#123;</span><br><span class="line">            rank[i] = <span class="number">0</span>;</span><br><span class="line">            update[i] = zsl-&gt;header;</span><br><span class="line">            update[i]-&gt;level[i].span = zsl-&gt;length;</span><br><span class="line">        &#125;</span><br><span class="line">        zsl-&gt;level = level;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    x = zslCreateNode(level,score,obj);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 把 update 记录的位置, 拿来做修正</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; level; i++) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 简单的链表插入操作, 你只需要记住, update[i]-&gt;x-&gt;update[i].forward</span></span><br><span class="line">        x-&gt;level[i].forward = update[i]-&gt;level[i].forward;</span><br><span class="line">        update[i]-&gt;level[i].forward = x;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// rank[0] 是最底层链表的 x 的前一个节点</span></span><br><span class="line">        <span class="comment">// rank[i] 是第i层链表 距离 x 的前一个节点</span></span><br><span class="line">        <span class="comment">// 相当于修复一下 第i层链表距离x的前一个节点的span值</span></span><br><span class="line">        x-&gt;level[i].span = update[i]-&gt;level[i].span - (rank[<span class="number">0</span>] - rank[i]);</span><br><span class="line">        update[i]-&gt;level[i].span = (rank[<span class="number">0</span>] - rank[i]) + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = level; i &lt; zsl-&gt;level; i++) &#123;</span><br><span class="line">        update[i]-&gt;level[i].span++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    x-&gt;backward = (update[<span class="number">0</span>] == zsl-&gt;header) ? <span class="literal">NULL</span> : update[<span class="number">0</span>];</span><br><span class="line">    <span class="keyword">if</span> (x-&gt;level[<span class="number">0</span>].forward)</span><br><span class="line">        x-&gt;level[<span class="number">0</span>].forward-&gt;backward = x;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        zsl-&gt;tail = x;</span><br><span class="line"></span><br><span class="line">    zsl-&gt;length++;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> x;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="GetRank-获取排名"><a href="#GetRank-获取排名" class="headerlink" title="GetRank 获取排名"></a>GetRank 获取排名</h3><p>一样从最上层开始找… 然后累计 span 值</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">zslGetRank</span><span class="params">(zskiplist *zsl, <span class="keyword">double</span> score, robj *o)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> rank = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp;</span><br><span class="line">            (x-&gt;level[i].forward-&gt;score &lt; score ||</span><br><span class="line">                (x-&gt;level[i].forward-&gt;score == score &amp;&amp;</span><br><span class="line">                compareStringObjects(x-&gt;level[i].forward-&gt;obj,o) &lt;= <span class="number">0</span>))) &#123;</span><br><span class="line"></span><br><span class="line">            rank += x-&gt;level[i].span;</span><br><span class="line"></span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (x-&gt;obj &amp;&amp; equalStringObjects(x-&gt;obj,o)) &#123;</span><br><span class="line">            <span class="keyword">return</span> rank;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="GetElementByRank-根据排名找对象"><a href="#GetElementByRank-根据排名找对象" class="headerlink" title="GetElementByRank 根据排名找对象"></a>GetElementByRank 根据排名找对象</h3><p>用 traversed 统计意境走过的步长, 这个思路还是比较清奇的…</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">zskiplistNode* <span class="title">zslGetElementByRank</span><span class="params">(zskiplist *zsl, <span class="keyword">unsigned</span> <span class="keyword">long</span> rank)</span> </span>&#123;</span><br><span class="line">    zskiplistNode *x;</span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">long</span> traversed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line"></span><br><span class="line">    x = zsl-&gt;header;</span><br><span class="line">    <span class="keyword">for</span> (i = zsl-&gt;level<span class="number">-1</span>; i &gt;= <span class="number">0</span>; i--) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (x-&gt;level[i].forward &amp;&amp; (traversed + x-&gt;level[i].span) &lt;= rank)</span><br><span class="line">        &#123;</span><br><span class="line">            traversed += x-&gt;level[i].span;</span><br><span class="line">            x = x-&gt;level[i].forward;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (traversed == rank) &#123;</span><br><span class="line">            <span class="keyword">return</span> x;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 没找到目标节点</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>至此, Redis 中的 SkipList 已经基本了解完了.</p>
<h2 id="排行榜的另类做法"><a href="#排行榜的另类做法" class="headerlink" title="排行榜的另类做法"></a>排行榜的另类做法</h2><p>其实如果你知道 玩家的分值的区间, 例如皇室战争的皇冠杯数大致的范围你是知道的, 那你其实可以这么来做</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">vector</span>&lt;<span class="built_in">vector</span>&lt;<span class="keyword">uint32_t</span>&gt;&gt; <span class="built_in">array</span>;</span><br><span class="line"><span class="built_in">array</span>[皇冠数].push_back(👑的key);</span><br><span class="line">这样要取出其排名, 只需要累加前面的 数组就可以了..</span><br><span class="line"></span><br><span class="line">假设 分值为 <span class="number">1</span> - <span class="number">5</span> </span><br><span class="line">[<span class="number">5</span>] [<span class="number">4</span>] [<span class="number">3</span>] [<span class="number">2</span>] [<span class="number">1</span>]</span><br><span class="line">k1  k4  k2  k5  k7</span><br><span class="line">k3      k6</span><br><span class="line"></span><br><span class="line">那你要得到 k2 的排名, 你只需要 累加 [<span class="number">5</span>] + [<span class="number">4</span>] 的大小就能算出来</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>游戏设计</category>
      </categories>
      <tags>
        <tag>Redis</tag>
        <tag>SkipList</tag>
        <tag>游戏服务器</tag>
        <tag>排行榜</tag>
      </tags>
  </entry>
  <entry>
    <title>使用 Github Webhook 自动部署 博客</title>
    <url>/%E4%BD%BF%E7%94%A8Github-Webhook%E8%87%AA%E5%8A%A8%E9%83%A8%E7%BD%B2%E5%8D%9A%E5%AE%A2/</url>
    <content><![CDATA[<h3 id="使用-Github-Webhook-前"><a href="#使用-Github-Webhook-前" class="headerlink" title="使用 Github Webhook 前"></a>使用 Github Webhook 前</h3><p>在搭建 这个 Blog 的早期 还没有用到自动部署<br>因此 每次发布 一篇新的文章 亦或是 修改了 Blog 的一些设置或者内容时 至少要经历以下几件事情</p>
<ol>
<li>生成 Blog 静态页面</li>
<li>将其 <code>git push</code> 到 git 仓库</li>
<li>ssh 登录 服务器</li>
<li>服务器 <code>git pull</code> 更新 Blog 静态页面</li>
</ol>
<p>每次都要经历以上四件事情 实在是太繁琐了 因此决定使用 Github Webhook 进行自动部署</p>
<a id="more"></a>

<h3 id="使用-Github-Webhook-后"><a href="#使用-Github-Webhook-后" class="headerlink" title="使用 Github Webhook 后"></a>使用 Github Webhook 后</h3><p>当使用了 Github Webhook 后 更新 Blog 就变成了以下 两件事情</p>
<ol>
<li>生成 Blog 静态页面</li>
<li>将其 <code>git push</code> 到 git 仓库 </li>
</ol>
<p>方便了不少 具体怎么做呢?</p>
<h4 id="部署-部署服务器"><a href="#部署-部署服务器" class="headerlink" title="部署 部署服务器"></a>部署 部署服务器</h4><p>服务器首先是要有一个 一个部署脚本 和 自动部署服务器(我用 Golang 简易实现了一个)</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/bin/sh</span></span><br><span class="line"><span class="built_in">cd</span> ~/~~~~</span><br><span class="line">git pull ~~~~</span><br></pre></td></tr></table></figure>
<figure class="highlight golang"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">	<span class="string">"io/ioutil"</span></span><br><span class="line">	<span class="string">"encoding/hex"</span></span><br><span class="line">	<span class="string">"crypto/sha1"</span></span><br><span class="line">	<span class="string">"io"</span></span><br><span class="line">	<span class="string">"crypto/hmac"</span></span><br><span class="line">	<span class="string">"log"</span></span><br><span class="line">	<span class="string">"os/exec"</span></span><br><span class="line">	<span class="string">"net/http"</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">reLaunch</span><span class="params">()</span></span> &#123;</span><br><span class="line">	cmd := exec.Command(<span class="string">"sh"</span>, <span class="string">"./deploy.sh"</span>)</span><br><span class="line">	err := cmd.Start()</span><br><span class="line">	<span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">		log.Fatal(err)</span><br><span class="line">	&#125;</span><br><span class="line">	err = cmd.Wait()</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">index</span><span class="params">(w http.ResponseWriter, r *http.Request)</span></span> &#123;</span><br><span class="line"></span><br><span class="line">	signature := r.Header.Get(<span class="string">"X-Hub-Signature"</span>)</span><br><span class="line">	<span class="keyword">if</span> <span class="built_in">len</span>(signature) &lt;= <span class="number">0</span> &#123;</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">	payload, _ := ioutil.ReadAll(r.Body)</span><br><span class="line">	mac := hmac.New(sha1.New, []<span class="keyword">byte</span>(<span class="string">"这里填写你的 Secret"</span>))</span><br><span class="line">	_, _ = mac.Write(payload)</span><br><span class="line">	expectedMac := hex.EncodeToString(mac.Sum(<span class="literal">nil</span>))</span><br><span class="line">	<span class="keyword">if</span> !hmac.Equal([]<span class="keyword">byte</span>(signature[<span class="number">5</span>:]), []<span class="keyword">byte</span>(expectedMac)) &#123;</span><br><span class="line">		io.WriteString(w, <span class="string">"&lt;h1&gt;401 Signature is error!&lt;/h1&gt;"</span>)</span><br><span class="line">		<span class="keyword">return</span></span><br><span class="line">	&#125;</span><br><span class="line">	io.WriteString(w, <span class="string">"&lt;h1&gt;200 Deploy server is running!&lt;/h1&gt;"</span>)</span><br><span class="line">	reLaunch()</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">	http.HandleFunc(<span class="string">"/"</span>, index)</span><br><span class="line">	http.ListenAndServe(<span class="string">":1111"</span>, <span class="literal">nil</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="设置-Github"><a href="#设置-Github" class="headerlink" title="设置 Github"></a>设置 Github</h4><ol>
<li>先在 Github 上 找到 自己的仓库 的 Settings</li>
<li>在 Payload URL 中设置 自己的 自动部署服务器 URL</li>
<li>Secret 设置一个密码 (如果不设置的话 就没办法判断是 Github 访问了你的部署服务器 还是 其他不怀好意的人)</li>
<li>选择 只有 Push 事件时 才触发 Hook</li>
<li>添加</li>
</ol>
<p><img src="/images/Github_Webhook.png" alt="Github_Webhook"></p>
<p>Github Webhook 每当 Push 事件发生时 就会 Post 请求访问 我们设置的 Payload URL (请求的 header 会有一个 X-Hub-Signature=’sha1=xxxx’ 的值 它是由 我们设置的 Secret sha1 加密 再加上 请求的 Body sha1 加密而来) 只要判断 这个 X-Hub-Signature 的值是否和我们预想的一样 就能知道这次请求是不是真的由 Github Webhook 发起的.</p>
<p>若判定通过 则调用我们的 部署脚本 进行部署</p>
]]></content>
      <categories>
        <category>搞东搞西</category>
      </categories>
      <tags>
        <tag>Github</tag>
        <tag>Webhook</tag>
      </tags>
  </entry>
  <entry>
    <title>关于 IEEE 754 浮点数</title>
    <url>/%E5%85%B3%E4%BA%8E-IEEE-754-%E6%B5%AE%E7%82%B9%E6%95%B0/</url>
    <content><![CDATA[<p>首先讲讲数在计算机上的表示</p>
<h2 id="数的机器表示"><a href="#数的机器表示" class="headerlink" title="数的机器表示"></a>数的机器表示</h2><p>众所周知 机器很笨 它是不认识什么 有符号数 和 无符号数的 它只认识 二进制串 那么在计算机上 是如何表示 有符号数和无符号数的呢?</p>
<h3 id="计算机不区分有符号数和无符号数"><a href="#计算机不区分有符号数和无符号数" class="headerlink" title="计算机不区分有符号数和无符号数"></a>计算机不区分有符号数和无符号数</h3><p>首先在计算机眼中 它确确实实只认识 二进制串 二进制串所表示的数 是有符号数还是无符号数 取决于 用户(编码者)怎么看 当你认为 <code>0xffff</code> 为正数时 它就是 <code>65535</code> 当你认为 <code>0xffff</code> 为负数时 就为 <code>-1</code></p>
<p>其次 在计算机中 它会将送进来的数 统一用补码计算 使用补码计算就不用区分 是有符号数还是无符号数 因为补码的加减法的规则相同</p>
<a id="more"></a>

<p>比如 一个二进制为: <code>1000 0001</code> 当你对其 <code>+1</code> 后 则为 <code>1000 0010</code> </p>
<ul>
<li>若你将 <code>1000 0001</code> 当做无符号数 则为 <code>129 + 1 = 130</code> 此时 <code>1000 0010</code> 为无符号数的 <code>130</code></li>
<li>若视为 有符号数 则为 <code>-127 + 1 = -126</code> 此时 <code>1000 0010</code> 为有符号数的 <code>-126</code></li>
</ul>
<p>也就是说 补码的存在 使得CPU 计算有符号数和无符号数的 加法过程统一 此外为了方便用户 CPU计算完成后 还会设置 eflags 状态寄存器 根据状态位即可同时知道 有符号数和无符号数的运算结果</p>
<p>至于 最高有效位 是 0 还是 1 来表示的正负数 是程序员自己认为的 机器不认识 也不懂 它只知道执行指令</p>
<h3 id="浮点数的表示"><a href="#浮点数的表示" class="headerlink" title="浮点数的表示"></a>浮点数的表示</h3><p>根据 IEEE 754 标准<br>二进制浮点数V 可以表示为 <code>V = (-1)^S * M * 2^E</code><br>计算机中浮点数二进制存储结构可以表示为 <code>[sign|      exp      |            frac            ]</code><br>其中 s 为符号位 和整数的二进制表示不同的是 它只表示符号位 不带任何权重</p>
<ul>
<li>单精度浮点数: exp宽为 8位 frac宽为 23位</li>
<li>双精度浮点数: exp宽为 11位 frac宽为52位</li>
</ul>
<h4 id="规格化浮点数"><a href="#规格化浮点数" class="headerlink" title="规格化浮点数"></a>规格化浮点数</h4><p><code>exp != 000...0 &amp;&amp; exp != 111...1</code> 则为规格化浮点数</p>
<p>此时 E + Bias = Exp</p>
<ul>
<li>Bias取值<ul>
<li>Bias = 2^e-1 e = exp位数</li>
<li>单精度数: 127</li>
<li>双精度数: 1023</li>
</ul>
</li>
</ul>
<p>之所以 E 要 Exp 减去 Bias 的原因是 E是一个 <code>unsigned int</code> 而科学计数法中的 E 是有正有负的 因此 E要减去一个中间数</p>
<p>frac域的第一位隐含为 1 在 32位浮点数中 frac的宽为 23位 省略了第一位以后 则多了一位 可以保存24位</p>
<h5 id="规格化浮点数实例"><a href="#规格化浮点数实例" class="headerlink" title="规格化浮点数实例"></a>规格化浮点数实例</h5><p>Float F = 15213.0</p>
<ul>
<li>15213D = 11101101101101B = 1.1101101101101B X 2^13</li>
<li>M = 1.1101101101101B</li>
<li>frac= 1101101101101B(规格化浮点数 舍弃第一位)</li>
<li>E = 13</li>
<li>Bias = 127(因为是32位浮点数)</li>
<li>Exp = 13 + 127 = 140 = 10001100B</li>
</ul>
<p>最终在机器中表示为 <code>[0|1000 1100|1101 1011 0110 1]</code></p>
<h4 id="非规格化浮点数"><a href="#非规格化浮点数" class="headerlink" title="非规格化浮点数"></a>非规格化浮点数</h4><p><code>exp = 000...0</code></p>
<ul>
<li>E = -Bias + 1 Bias = 2^e-1 e = exp位数</li>
<li>M = 0.xxxxx</li>
<li>当 exp = 000…0, frac = 000…0<ul>
<li>表示 0</li>
<li>有 +0 与 -0</li>
</ul>
</li>
<li>当 exp = 000…0, frac != 000…0<ul>
<li>表示 非常接近于 0</li>
<li>会逐步丧失精度</li>
</ul>
</li>
<li>当 exp = 111…1, frac = 000…0<ul>
<li>表示无穷</li>
<li>1.0/0.0 = +无穷 -1.0/0.0 = -无穷</li>
</ul>
</li>
<li>当 exp = 111…1, frac != 000…0<ul>
<li>NaN</li>
</ul>
</li>
</ul>
<h5 id="举个例子-Int-to-Float"><a href="#举个例子-Int-to-Float" class="headerlink" title="举个例子 Int to Float"></a>举个例子 Int to Float</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> num = <span class="number">8</span>;</span><br><span class="line"><span class="keyword">float</span>* pfloat = &amp;num;</span><br></pre></td></tr></table></figure>
<p><code>*pfloat</code> 的值为多少?</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">首先 num 为 0x00000008</span><br><span class="line">第一位 为符号位 为 0 因为是正数</span><br><span class="line">其次 符号位后面八位 为exp域 为 0000 0000 全0 则 E &#x3D; -Bias + 1 &#x3D; 2^e-1 + 1 &#x3D; -126</span><br><span class="line">最后 M 23位 &#x3D; 000 0000 0000 0000 0000 1000</span><br><span class="line">(省略掉最前面的0 因为是非规格化浮点数 若为规格化浮点数则省略最前面的1)</span><br><span class="line">得到</span><br><span class="line">V &#x3D; (-1)^S * M * 2^E &#x3D; 0x0.00000000000000000001000 * 2^(-126) &#x3D; 1.000 * 2^(-146)</span><br><span class="line">很接近于0 因此 *pfloat 为 0.000000</span><br></pre></td></tr></table></figure>

<h5 id="再举个例子-Float-to-Int"><a href="#再举个例子-Float-to-Int" class="headerlink" title="再举个例子 Float to Int"></a>再举个例子 Float to Int</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> num = <span class="number">8</span>;</span><br><span class="line"><span class="keyword">float</span>* pfloat = &amp;num;</span><br><span class="line">*pfloat = <span class="number">8.0</span>;</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">num 的值为多少?</span><br></pre></td></tr></table></figure>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">首先 8.0 是一个正数 符号位 为 0</span><br><span class="line">其次 8.0 &#x3D; 1000.0B &#x3D; 1.000 * 2^3 E &#x3D; 3 exp &#x3D; E + Bias &#x3D; 3 + 127 &#x3D; 130 &#x3D; 1000 0010B</span><br><span class="line">M &#x3D; 000 补到 23位</span><br><span class="line">因此 在二进制为 [0|1000 0010|000 0000 0000 0000 0000] 转换为十进制 为 1107296256</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>计算机基础</category>
      </categories>
      <tags>
        <tag>浮点数</tag>
        <tag>计算机基础</tag>
      </tags>
  </entry>
  <entry>
    <title>实现 SOCKS5 网络混淆代理</title>
    <url>/%E5%AE%9E%E7%8E%B0-SOCKS5-%E7%BD%91%E7%BB%9C%E6%B7%B7%E6%B7%86%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<p>在天朝这么久 你曾为网络状况不佳而烦恼过吗? 平时使用的 Shadowsocks 是怎么实现?<br>这篇讲的就是 如何用 Golang 实现一个最为基础的 Shdowsocks 的功能的软件 我将本文实现的软件取名叫 GoSocksProxy</p>
<p>老规矩先介绍一遍 什么是 GoSocksProxy? 它是怎么来的?</p>
<h2 id="GoSocksProxy"><a href="#GoSocksProxy" class="headerlink" title="GoSocksProxy"></a><a href="https://github.com/Yu2erer/GoSocksProxy" target="_blank" rel="noopener">GoSocksProxy</a></h2><p>一个 Golang 所写的网络混淆代理 是学习 SOCKS5 协议时的副产品 为此还做了一张图</p>
<p><img src="/images/GoSocksProxy.jpg" alt="GoSocksProxy"></p>
<a id="more"></a>

<h2 id="Shdowsocks大致原理"><a href="#Shdowsocks大致原理" class="headerlink" title="Shdowsocks大致原理"></a>Shdowsocks大致原理</h2><p>酸酸最主要是由两部分构成</p>
<h4 id="ss-local"><a href="#ss-local" class="headerlink" title="ss-local"></a>ss-local</h4><p>在本地监听的一个服务 本地主机的网络请求经过 ss-local 并加密(让防火墙不知道你在访问什么) 传输到 ss-server</p>
<h4 id="ss-server"><a href="#ss-server" class="headerlink" title="ss-server"></a>ss-server</h4><p>跑在 墙外的服务器 且监听来自本地的请求 对 ss-local 发来的数据进行解密 访问真实的目标服务地址 将其读出 并加密(目的同上) 发回本地</p>
<h2 id="SOCKS协议简介"><a href="#SOCKS协议简介" class="headerlink" title="SOCKS协议简介"></a>SOCKS协议简介</h2><p>一种网络传输协议 主要用于客户端与外网服务器之间通讯的中间传递 </p>
<ul>
<li>是SOCKetS的简称 </li>
<li>是会话层的协议 位于表示层与传输层之间<ul>
<li>Shdowsocks的数据传输建立于SOCKS5协议</li>
</ul>
</li>
</ul>
<p>最初由David Koblas开发，而后由NEC的Ying-Da Lee将其扩展到版本4。最新协议是版本5</p>
<ul>
<li>支持UDP协议</li>
<li>支持用户身份验证和通信加密方式</li>
<li>支持IPv6</li>
</ul>
<h3 id="SOCKS5协议详解"><a href="#SOCKS5协议详解" class="headerlink" title="SOCKS5协议详解"></a>SOCKS5协议详解</h3><p>源自RFC:</p>
<ul>
<li><a href="https://www.ietf.org/rfc/rfc1928.txt" target="_blank" rel="noopener">RFC1928</a></li>
<li><a href="https://www.ietf.org/rfc/rfc1929.txt" target="_blank" rel="noopener">RFC1929</a></li>
</ul>
<p>RFC 1928 前半截给你介绍了 SOCKS产生的背景 和 现有 SOCKS4 的缺陷 然后引出了 SOCKS5</p>
<h4 id="建立连接"><a href="#建立连接" class="headerlink" title="建立连接"></a>建立连接</h4><p>当一个SOCKS协议的客户端连接到服务端时 会发送 <code>版本信息</code> 和 <code>认证方式</code>的消息到服务器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+----+----------+----------+</span><br><span class="line">|VER | NMETHODS | METHODS  |</span><br><span class="line">+----+----------+----------+</span><br><span class="line">| 1  |    1     | 1 to 255 |</span><br><span class="line">+----+----------+----------+</span><br></pre></td></tr></table></figure>
<ul>
<li>VER指的是 SOCKS版本 这里是SOCKS5 故为 0x05</li>
<li>NMETHODS是 METHODS 的长度</li>
<li>METHODS为 客户端锁支持的认证方式</li>
</ul>
<p>NMETHODS 原文解释为 </p>
<blockquote>
<p><code>The NMETHODS field contains the number of method identifier octets that appear in the METHODS field.</code></p>
</blockquote>
<p>里面的 octets 为 八位元组 相当于是 八位 一字节 为什么不直接用byte呢?<br>经过 Wiki 得知 不同计算机中的字节长度不同 使用 octets 是为了不引起歧义</p>
<p>接着服务端从中挑选一个认证方式 并将消息发回客户端</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+----+--------+</span><br><span class="line">|VER | METHOD |</span><br><span class="line">+----+--------+</span><br><span class="line">| 1  |   1    |</span><br><span class="line">+----+--------+</span><br></pre></td></tr></table></figure>
<ul>
<li>X’00’ NO AUTHENTICATION REQUIRED(不需要认证)</li>
<li>X’01’ GSSAPI</li>
<li>X’02’ USERNAME/PASSWORD</li>
<li>X’03’ to X’7F’ IANA ASSIGNED</li>
<li>X’80’ to X’FE’ RESERVED FOR PRIVATE METHODS(私人方法保留)</li>
<li>X’FF’ NO ACCEPTABLE METHODS(都不支持)</li>
</ul>
<p>本地监听服务器整个握手过程具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">buf := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">263</span>)</span><br><span class="line">n, _ := io.ReadAtLeast(conn, buf, <span class="number">2</span>) <span class="comment">// 读入两个字节</span></span><br><span class="line"><span class="keyword">if</span> buf[<span class="number">0</span>] != <span class="number">0x05</span> &#123; <span class="comment">// 判断SOCKS版本 只支持 SOCKS5</span></span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line">conn.Write([]<span class="keyword">byte</span>&#123;<span class="number">0x05</span>, <span class="number">0x00</span>&#125;) <span class="comment">// 告诉客户端 不需要认证</span></span><br></pre></td></tr></table></figure>

<h4 id="发起请求"><a href="#发起请求" class="headerlink" title="发起请求"></a>发起请求</h4><p>客户端正式发起请求 给 服务端 告诉服务端我现在访问的地址是哪里 你替我访问</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+----+-----+-------+------+----------+----------+</span><br><span class="line">|VER | CMD |  RSV  | ATYP | DST.ADDR | DST.PORT |</span><br><span class="line">+----+-----+-------+------+----------+----------+</span><br><span class="line">| 1  |  1  | X&#39;00&#39; |  1   | Variable |    2     |</span><br><span class="line">+----+-----+-------+------+----------+----------+</span><br></pre></td></tr></table></figure>
<ul>
<li>VER protocol version: X’05’</li>
<li>CMD(命令码 指示操作)<ul>
<li>CONNECT X’01’</li>
<li>BIND X’02’</li>
<li>UDP ASSOCIATE X’03’</li>
</ul>
</li>
<li>RSV RESERVED(保留 为0x00)</li>
<li>ATYP address type of following address(表示 DST.ADDR的类型)<ul>
<li>IP V4 address: X’01’</li>
<li>DOMAINNAME: X’03’</li>
<li>IP V6 address: X’04’</li>
</ul>
</li>
<li>DST.ADDR desired destination address(目的地址)</li>
<li>DST.PORT desired destination port in network octet order(目的端口)</li>
</ul>
<p>到了这里 就可以将这个请求 转发 给墙外的服务器 由墙外的服务器来处理这个请求</p>
<p>服务端收到以后进行回复</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">+----+-----+-------+------+----------+----------+</span><br><span class="line">|VER | REP |  RSV  | ATYP | BND.ADDR | BND.PORT |</span><br><span class="line">+----+-----+-------+------+----------+----------+</span><br><span class="line">| 1  |  1  | X&#39;00&#39; |  1   | Variable |    2     |</span><br><span class="line">+----+-----+-------+------+----------+----------+</span><br></pre></td></tr></table></figure>
<ul>
<li>VER protocol version: X’05’</li>
<li>REP Reply field:<ul>
<li>X’00’ succeeded(成功)</li>
<li>X’01’ general SOCKS server failure(普通SOCKS服务器连接失败)</li>
<li>X’02’ connection not allowed by ruleset(现有规则不允许连接)</li>
<li>X’03’ Network unreachable(网络不可达)</li>
<li>X’04’ Host unreachable(主机不可达)</li>
<li>X’05’ Connection refused(拒绝连接)</li>
<li>X’06’ TTL expired(TTL 超时)</li>
<li>X’07’ Command not supported(不支持的命令)</li>
<li>X’08’ Address type not supported(地址类型不支持)</li>
<li>X’09’ to X’FF’ unassigned(未定义)</li>
</ul>
</li>
<li>RSV RESERVED</li>
<li>ATYP address type of following address<ul>
<li>IP V4 address: X’01’</li>
<li>DOMAINNAME: X’03’</li>
<li>IP V6 address: X’04’</li>
</ul>
</li>
<li>BND.ADDR server bound address(服务器绑定地址)</li>
<li>BND.PORT server bound port in network octet order(服务器绑定端口)</li>
</ul>
<p>本地监听服务器 将请求转发给 墙外服务器后 墙外服务器再做出相应操作</p>
<p>本地监听服务器具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    直接转发 socks5协议的请求的一部分到墙外服务器 从AYTR开始</span></span><br><span class="line"><span class="comment">    +------+----------+----------+</span></span><br><span class="line"><span class="comment">    | ATYP | DST.ADDR | DST.PORT |</span></span><br><span class="line"><span class="comment">    +------+----------+----------+</span></span><br><span class="line"><span class="comment">    |  1   | Variable |    2    |</span></span><br><span class="line"><span class="comment">    +----+-----+-------+--------+</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">dstServer, _ := s.DialServer()</span><br><span class="line"><span class="comment">// 加密转发到墙外服务器</span></span><br><span class="line">dstServer.Write(buf[<span class="number">3</span>:n])</span><br><span class="line"><span class="comment">// 直接回应SOCKS客户端说请求成功</span></span><br><span class="line">conn.Write([]<span class="keyword">byte</span>&#123;<span class="number">0x05</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x01</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>, <span class="number">0x00</span>&#125;)</span><br><span class="line"><span class="comment">// 接着就是不断的将墙外服务器的信息解密后传给SOCKS</span></span><br><span class="line"><span class="comment">// SOCKS传来的信息加密后转发给墙外服务器</span></span><br><span class="line"><span class="keyword">go</span> io.Copy(conn, dstServer)</span><br><span class="line">io.Copy(dstServer, conn)</span><br></pre></td></tr></table></figure>

<p>墙外服务器具体实现</p>
<figure class="highlight go"><table><tr><td class="code"><pre><span class="line">buf := <span class="built_in">make</span>([]<span class="keyword">byte</span>, <span class="number">263</span>)</span><br><span class="line">n, _ := io.ReadAtLeast(conn, buf, <span class="number">5</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> dstIP []<span class="keyword">byte</span></span><br><span class="line"><span class="comment">// 根据传入进来的第一个字节判断访问地址的类型</span></span><br><span class="line"><span class="keyword">switch</span> buf[<span class="number">0</span>] &#123;</span><br><span class="line"><span class="keyword">case</span> <span class="number">0x01</span>: <span class="comment">// ipv4</span></span><br><span class="line">    dstIP = buf[<span class="number">1</span> : net.IPv4len+<span class="number">1</span>]</span><br><span class="line"><span class="keyword">case</span> <span class="number">0x03</span>: <span class="comment">// domainname</span></span><br><span class="line">    ipAddr, err := net.ResolveIPAddr(<span class="string">"ip"</span>, <span class="keyword">string</span>(buf[<span class="number">2</span>:n<span class="number">-2</span>]))</span><br><span class="line">    <span class="keyword">if</span> err != <span class="literal">nil</span> &#123;</span><br><span class="line">        <span class="keyword">return</span></span><br><span class="line">    &#125;</span><br><span class="line">    dstIP = ipAddr.IP</span><br><span class="line"><span class="keyword">case</span> <span class="number">0x04</span>: <span class="comment">// ipv6</span></span><br><span class="line">    dstIP = buf[<span class="number">1</span> : net.IPv6len+<span class="number">1</span>]</span><br><span class="line"><span class="keyword">default</span>:</span><br><span class="line">    <span class="keyword">return</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 传入进来的请求的最后两位为访问地址的端口</span></span><br><span class="line">dstPort := buf[n<span class="number">-2</span>:]</span><br><span class="line">dstAddr := &amp;net.TCPAddr&#123;</span><br><span class="line">    IP:   dstIP,</span><br><span class="line">    Port: <span class="keyword">int</span>(binary.BigEndian.Uint16(dstPort)),</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 墙外服务端另起一个请求 去访问真正要访问的地址 例如 google.com</span></span><br><span class="line">client, _ := net.DialTCP(<span class="string">"tcp"</span>, <span class="literal">nil</span>, dstAddr)</span><br><span class="line"><span class="comment">// 最后源源不断的从 真正的请求中读出数据并加密转发给 本地监听服务器</span></span><br><span class="line"><span class="keyword">go</span> io.Copy(conn, client)</span><br><span class="line"><span class="comment">// 从本地监听服务器读出要访问的地址并解密转发给 client做真正的请求操作</span></span><br><span class="line">io.Copy(client, conn)</span><br></pre></td></tr></table></figure>

<h2 id="至此"><a href="#至此" class="headerlink" title="至此"></a>至此</h2><p>最主要的功能就实现完了 完整的源码 已经放到 <a href="https://github.com/Yu2erer/GoSocksProxy" target="_blank" rel="noopener">Github: GoSocksProxy</a></p>
]]></content>
      <categories>
        <category>搞东搞西</category>
        <category>计算机网络</category>
      </categories>
      <tags>
        <tag>网络协议</tag>
        <tag>计算机网络</tag>
        <tag>SOCKS5</tag>
        <tag>GoSocksProxy</tag>
      </tags>
  </entry>
  <entry>
    <title>微信摇一摇签到 解决方案</title>
    <url>/%E5%BE%AE%E4%BF%A1%E6%91%87%E4%B8%80%E6%91%87%E7%AD%BE%E5%88%B0-%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<p>所在学校在2016年的时候 使用了 微信摇一摇作为签到的系统</p>
<p>摇一摇签到是基于 iBeacon(低功耗蓝牙通信)</p>
<h3 id="iBeacon-组成"><a href="#iBeacon-组成" class="headerlink" title="iBeacon 组成"></a>iBeacon 组成</h3><p>iBeacon 由 UUID Major Minor 组成<br>那么 理论上我们只要获取到了以上信息 就能在任何地方(一般都会进行定位认证 所以任何地方是有条件的)发射出一个教室的信号 进行摇一摇签到</p>
<a id="more"></a>
<ol>
<li>获取微信摇一摇的 UUID(唯一标识符) 能从微信摇一摇的开发页面上得到</li>
</ol>
<p><code>UUID = FDA50693-A4E2-4FB1-AFCF-C6EB07647825</code></p>
<ol start="2">
<li>Major 开发者决定 一般是指区域信息</li>
</ol>
<p><code>Major = 10099</code></p>
<ol start="3">
<li>Minor 开发者决定 在这篇文章中指的是 学校的教室号</li>
</ol>
<p><code>Minor = 随便你写一个就好</code></p>
<p>实际测试得知 学校的签到系统 没有对签到的教室进行认证 换句话说 你爱签哪个教室都行<br>只要你签到了 就显示签到成功了</p>
<p>另外 学校的这套系统并没有定位认证 一般做这种摇一摇的系统 都是要进行定位确认的<br>抓包得知 只是假模假样的 post 个 Position 但并没有后台确认(真的很会偷懒)</p>
<h3 id="代码实现"><a href="#代码实现" class="headerlink" title="代码实现"></a>代码实现</h3><p>以下为 Swift 的代码实现(大一时所写) 直接做成 App 装在 iPhone 上面 一打开就能摇一摇签到(自己摇不到 只能给别人摇)</p>
<figure class="highlight swift"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> UIKit</span><br><span class="line"><span class="keyword">import</span> CoreLocation</span><br><span class="line"><span class="keyword">import</span> CoreBluetooth</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">let</span> uuid = <span class="type">UUID</span>(uuidString: <span class="string">"FDA50693-A4E2-4FB1-AFCF-C6EB07647825"</span>)</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ViewController</span>: <span class="title">UIViewController</span> </span>&#123;</span><br><span class="line">    <span class="keyword">fileprivate</span> <span class="built_in">lazy</span> <span class="keyword">var</span> data = <span class="type">NSMutableDictionary</span>()</span><br><span class="line">    <span class="keyword">fileprivate</span> <span class="built_in">lazy</span> <span class="keyword">var</span> manager = <span class="type">CBPeripheralManager</span>()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">override</span> <span class="function"><span class="keyword">func</span> <span class="title">viewDidLoad</span><span class="params">()</span></span> &#123;</span><br><span class="line">        <span class="keyword">super</span>.viewDidLoad()</span><br><span class="line">        <span class="keyword">let</span> major: <span class="type">CLBeaconMajorValue</span> = <span class="number">10099</span></span><br><span class="line">        <span class="keyword">let</span> minor: <span class="type">CLBeaconMinorValue</span> = <span class="number">21920</span></span><br><span class="line">        <span class="keyword">let</span> beaconRegion = <span class="type">CLBeaconRegion</span>(proximityUUID: uuid!, major: major, minor: minor, identifier: <span class="string">"id"</span>)</span><br><span class="line">        data = beaconRegion.peripheralData(withMeasuredPower: <span class="literal">nil</span>)</span><br><span class="line">        manager = <span class="type">CBPeripheralManager</span>(delegate: <span class="keyword">self</span>, queue: <span class="literal">nil</span>)</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">extension</span> <span class="title">ViewController</span>: <span class="title">CBPeripheralManagerDelegate</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">func</span> <span class="title">peripheralManagerDidUpdateState</span><span class="params">(<span class="number">_</span> peripheral: CBPeripheralManager)</span></span> &#123;</span><br><span class="line">        <span class="keyword">self</span>.manager.startAdvertising(<span class="keyword">self</span>.data <span class="keyword">as</span>? [<span class="type">String</span> : <span class="type">Any</span>])</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>搞东搞西</category>
      </categories>
      <tags>
        <tag>微信签到</tag>
      </tags>
  </entry>
  <entry>
    <title>急性肠胃炎的一周</title>
    <url>/%E6%80%A5%E6%80%A7%E8%82%A0%E8%83%83%E7%82%8E%E7%9A%84%E4%B8%80%E5%91%A8/</url>
    <content><![CDATA[<p>这一周原计划是入门一下 <code>编译原理</code> 然后就准备过年了 结果不知道是吃了什么?(酸菜鱼? 杭州菜?) 中了个急性肠胃炎 第一天吐了一整天 第二天啥事没有 第三天疼了一整天 第四天啥事没有 今天是第五天了 真是可怕的疾病啊!</p>
]]></content>
      <categories>
        <category>胡言乱语</category>
      </categories>
      <tags>
        <tag>急性肠胃炎</tag>
        <tag>生病</tag>
      </tags>
  </entry>
  <entry>
    <title>我所使用的工具们</title>
    <url>/%E6%88%91%E6%89%80%E4%BD%BF%E7%94%A8%E7%9A%84%E5%B7%A5%E5%85%B7%E4%BB%AC/</url>
    <content><![CDATA[<h3 id="Retina-Macbook-Pro-2016-13-inch"><a href="#Retina-Macbook-Pro-2016-13-inch" class="headerlink" title="Retina Macbook Pro 2016 13-inch"></a>Retina Macbook Pro 2016 13-inch</h3><p>懂的都懂 不多说</p>
<h3 id="HHKB-白无刻BT"><a href="#HHKB-白无刻BT" class="headerlink" title="HHKB 白无刻BT"></a>HHKB 白无刻BT</h3><p>HHKB(Happy Hacking Keyboard) 属于一把静电容键盘</p>
<p><img src="/images/hhkb.jpg" alt="hhkb"></p>
<a id="more"></a>

<h4 id="购买原因"><a href="#购买原因" class="headerlink" title="购买原因"></a>购买原因</h4><p>在很久很久以前 我觉得 RMBP(2016 13-inch) 的蝶式键盘 手感非常好 键程短 不拖泥带水</p>
<p>直到 有一天 在网上闲逛 看到大家都对 HHKB 赞不绝口… 于是 下了一单 2300RMB</p>
<h4 id="手感"><a href="#手感" class="headerlink" title="手感"></a>手感</h4><p>一摸 一发不可收拾 敲下去的感觉很脆 又很软 但又不是软到 像肉一样的感觉</p>
<p>从此无比嫌弃 RMBP的蝶式键盘 像手指在钢板上💃</p>
<h3 id="DPT-CP1"><a href="#DPT-CP1" class="headerlink" title="DPT-CP1"></a>DPT-CP1</h3><p>索尼家出的 一款 A4大小 电子阅读器</p>
<p><img src="/images/dpt-cp1.jpg" alt="dpt-cp1"></p>
<h4 id="购买原因-1"><a href="#购买原因-1" class="headerlink" title="购买原因"></a>购买原因</h4><p>上课不想听课 没事做只能看书 用 iPad 看久了 眼睛不舒服 于是换了这个</p>
<h3 id="Sayl"><a href="#Sayl" class="headerlink" title="Sayl"></a>Sayl</h3><p>Sayl 一把人体工程学椅</p>
<p><img src="/images/sayl_1.jpg" alt="sayl_1"></p>
<p><img src="/images/sayl_2.jpg" alt="sayl_2"></p>
<h4 id="购买原因-2"><a href="#购买原因-2" class="headerlink" title="购买原因"></a>购买原因</h4><p>看到网络上越来越多的人腰椎盘突出 刚好那段时间在机房坐着板凳 腰酸的很 就觉得自己有必要对自己的腰椎好一些</p>
<h4 id="使用感受"><a href="#使用感受" class="headerlink" title="使用感受"></a>使用感受</h4><p>刚看到快递的时候 我是震惊的 包装快比沙发还大了 真是可怕</p>
<p>拆封以后 坐在椅子上 恩~不错 我感受到我的腰被这把椅子顶起 有点舒服</p>
<p>不到二十分钟… ?? 我居然感觉到腰酸了?? 5000+RMB 的椅子 也会让人腰酸??</p>
<p>随后不断的调试这把椅子 终于在第三周的时候 调试到自己合适的感觉 就再也没腰酸过了</p>
<h3 id="AOC-U2790PQU"><a href="#AOC-U2790PQU" class="headerlink" title="AOC U2790PQU"></a>AOC U2790PQU</h3><p>双十一 上市的一款 27-inch 4k 显示器</p>
<p>图上显示器已调色温</p>
<p><img src="/images/aoc_u2790pqu.jpg" alt="aoc_u2790pqu"></p>
<h4 id="购买原因-3"><a href="#购买原因-3" class="headerlink" title="购买原因"></a>购买原因</h4><p>早在初中的时候 由于长期使用笔记本电脑 颈椎就不是很舒服 一直想外接一个显示器 让自己避免低头</p>
<p>一直心水 DELL U2718q 但其 高昂的价格(近4000RMB) 让我抬不起头</p>
<p>后 又对 LG 27uk850(因为它自带 Type-C 接口 可以给 RMBP 供电) 示爱 依然是其高昂的价格(双十一的时候 3600RMB 双十二的时候 5000RMB) 这个价格走势 一度让我怀疑人生</p>
<p>当我在🐶东上看到 一款 2000元 的 27-inch 4k 时 第一件事 就是去淘宝搜同款 却没找到 被迫只能在🐶东上下单了</p>
<h4 id="使用感受-1"><a href="#使用感受-1" class="headerlink" title="使用感受"></a>使用感受</h4><p>大 很大 非常大 除了颜色有些不准之外 其它都还好</p>
<p>有个尴尬的地方 在macOS上 选择 1080p 分辨率又嫌太大 选 2k分辨率又嫌太小 真是难以抉择啊!</p>
]]></content>
      <categories>
        <category>胡言乱语</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>我所理解的子网掩码</title>
    <url>/%E6%88%91%E6%89%80%E7%90%86%E8%A7%A3%E7%9A%84%E5%AD%90%E7%BD%91%E6%8E%A9%E7%A0%81/</url>
    <content><![CDATA[<p>首先 IPv4 资源是非常稀少的 大约只有 43亿 而且这还是包括了私有IP地址段的 其次 IP地址分为了五类 其中 D类地址没有主机标识 常被用于广播 E类被保留 我们不去管它</p>
<p>那就只剩下了 A B C 三类 问题是 A类和B类的链路允许非常多的计算机连接 实际网络架构上 根本用不到那么多 这就造成了IPv4的浪费 这个时候 子网掩码出现了</p>
<h3 id="子网掩码的意义"><a href="#子网掩码的意义" class="headerlink" title="子网掩码的意义"></a>子网掩码的意义</h3><p>将 A B C类网络进行更细的划分 避免浪费 IPv4地址 也可以把小的网络归并成大的网络即超网</p>
<h3 id="子网掩码的理解"><a href="#子网掩码的理解" class="headerlink" title="子网掩码的理解"></a>子网掩码的理解</h3><p>假设有一个财主 他有256间房子 房子的编号是 <code>0~255</code> 然后它的房子都在<code>192.168.0</code>这一条街上 现在他有<code>8</code>个儿子 要分家 则相当于 每个人都分得<code>32</code>间房子</p>
<a id="more"></a>

<p>以下为每个儿子分得的房子地址</p>
<ul>
<li>一: 192.168.0.0-192.168.0.31</li>
<li>二: 192.168.0.32-192.168.0.63</li>
<li>三: 192.168.0.64-192.168.0.95</li>
<li>四: 192.168.0.96-192.168.0.127</li>
<li>五: 192.168.0.128-192.168.0.159</li>
<li>六: 192.168.0.160-192.168.0.191</li>
<li>七: 192.168.0.192-192.168.0.223</li>
<li>八: 192.168.0.224-192.168.0.255</li>
</ul>
<p>划分出来的各个儿子房子地址的范围 就是这个财主的子网 它们原本是互通的(因为都属于财主一个人) 现在分家了 它们不再是互通的了(不可能你大儿子结婚以后 二儿子还没事去你家看看)</p>
<p>现在我们来看一号儿子的其中一个房子地址</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">     192.     168.       0.       20</span><br><span class="line">11000000|10101000|00000000|000|10100</span><br><span class="line">   该类本身的网络标识为24位   |扩展|主机部分</span><br><span class="line">      经过分家以后这个IP的网络标识为27位</span><br></pre></td></tr></table></figure>
<p>为什么分家以后网络标识为27位?</p>
<p>首先 我们知道 IPv4的地址为32位 然后这是一个C类地址 网络标识为 24位 剩下 8位做主机号 但是 如果主机号为8位的话 那么也就是说 这个IP的地址的主机号从0-255都是一个子网的 </p>
<p>现在我们是有8个儿子分家 所以每个儿子只能有32个房子 也就是说 主机号只能留到刚好能够表示到十进制31的比特位数(0~31) 显而易见 是5位做主机号 那么 32位 - 5位 = 27位 这就是为什么分家以后网络标识为27位 有3位是因为分家以后扩展了的网络标识</p>
<p>由于 <code>192.168.0.0-192.168.0.31</code> 这样表示实在太复杂了 于是乎 产生了下面一种表示方式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">192.168.0.0&#x2F;27</span><br><span class="line">192.168.0.32&#x2F;27</span><br><span class="line">.           .</span><br><span class="line">.  以此类推  .</span><br><span class="line">.           .</span><br><span class="line">192.168.0.224&#x2F;27</span><br><span class="line">&#x2F;27 可以理解为 主机号只有 32-27 &#x3D; 5位 </span><br><span class="line">也就是说 这个网段是每32个地址为一个子网划分的</span><br></pre></td></tr></table></figure>

<p>上面这种表示方式虽然人能看得懂 但是计算机不认识 于是产生了 <code>子网掩码</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">     255.     255.     255.     224</span><br><span class="line">11111111|11111111|11111111|11100000</span><br><span class="line">大概意思就是 网络标识部分为1 主机号标识部分为0</span><br></pre></td></tr></table></figure>

<p>这样 财主的房子分配后的子网掩码就是 255.255.255.224</p>
<h3 id="一道有趣的子网掩码问题"><a href="#一道有趣的子网掩码问题" class="headerlink" title="一道有趣的子网掩码问题"></a>一道有趣的子网掩码问题</h3><p>两台服务器 A 和 B 的网络配置如下，B 的子网掩码本应该是255.255.255.0，被不小心配成了 255.255.255.224。它们还能正常通信吗？</p>
<p><img src="/images/Subnet_mask.png" alt="Subnet_mask"></p>
<p>这道题是我从 林沛满先生的《Wireshark网络分析就这么简单》摘取的 林沛满先生的两本书在我看来都是很不错的(虽然我的水平暂时还没资格评论别人的书) 就是名字取得差了一些 如果能把名字改成   《从Wireshark的角度看网络》 我想会卖的更好!</p>
<p>书中的解法是直接用Wireshark抓一次包</p>
<p>我个人的分析是这样的</p>
<ul>
<li>当B访问A时</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">B的IP是 192.168.26.3 它的子网掩码是 255.255.255.224</span><br><span class="line">A的IP是 192.168.26.129 因为此时是 B访问A 所以A的子网掩码用不上</span><br><span class="line">将 A的IP的二进制与B的子网掩码的二进制进行与运算</span><br><span class="line">11000000|10101000|00011010|10000001</span><br><span class="line">11111111|11111111|11111111|11100000</span><br><span class="line">得到</span><br><span class="line">11000000|10101000|00011010|10000000</span><br><span class="line">转换为十进制 为 192.168.26.128</span><br><span class="line">因此A的IP处于B的192.168.26.128的子网中</span><br><span class="line">其实 我个人是这么算的 255-224 &#x3D; 31 说明 每32位为一个子网</span><br><span class="line">接着 129 &#x2F; 32 约等于 4.03 也就是说比4稍微大一点</span><br><span class="line">也就是说处于第五个子网中 32*4&#x3D;128 </span><br><span class="line">说明 A的IP处于主机号起始为128的子网中</span><br><span class="line">接下来再用相同的方法分析B的IP处于哪个子网中</span><br><span class="line">3 &#x2F; 32 &#x3D; 0 说明在第一个子网中 A和B处于不同子网</span><br><span class="line">于是B访问A 需要跨子网通信 所以借助于网关 192.168.26.2 将其转发到 A 去</span><br></pre></td></tr></table></figure>

<ul>
<li>当A访问B时</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A的子网掩码为 255.255.255.0</span><br><span class="line">用上面的方法 可以知道 这整个0~255都在一个子网中</span><br><span class="line">B的IP是 192.168.26.3</span><br><span class="line">3 &#x2F; 0 &#x3D; 0 所以处于第一个子网也是唯一一个子网</span><br><span class="line">A的IP是 192.168.26.129</span><br><span class="line">129 &#x2F; 0 &#x3D; 0 也处于第一个子网也是唯一一个子网</span><br><span class="line">因此 A访问B是同一个网络 不需要借助网关 因此A能直接访问B</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>计算机网络</category>
      </categories>
      <tags>
        <tag>计算机网络</tag>
        <tag>子网掩码</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 1 含 Challenge</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-1/</url>
    <content><![CDATA[<h3 id="练习1：理解通过make生成执行文件的过程"><a href="#练习1：理解通过make生成执行文件的过程" class="headerlink" title="练习1：理解通过make生成执行文件的过程"></a>练习1：理解通过make生成执行文件的过程</h3><ol>
<li>操作系统镜像文件ucore.img是如何一步一步生成的？</li>
</ol>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line"><span class="variable">$(UCOREIMG)</span>: <span class="variable">$(kernel)</span> <span class="variable">$(bootblock)</span></span><br><span class="line">	<span class="variable">$(V)</span>dd if=/dev/zero of=<span class="variable">$@</span> count=10000</span><br><span class="line">	<span class="variable">$(V)</span>dd if=<span class="variable">$(bootblock)</span> of=<span class="variable">$@</span> conv=notrunc</span><br><span class="line">	<span class="variable">$(V)</span>dd if=<span class="variable">$(kernel)</span> of=<span class="variable">$@</span> seek=1 conv=notrunc</span><br><span class="line">从 MakeFile 里面 可以看出 生成 ucore.img 首先需要生成 大小为 10000字节 的空间</span><br><span class="line">然后 将 bootblock 和 kernel 依次写入到 那块空间之中</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>生成 Bootblock</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">bootfiles = <span class="variable">$(<span class="built_in">call</span> listf_cc,boot)</span></span><br><span class="line"><span class="variable">$(<span class="built_in">foreach</span> f,<span class="variable">$(bootfiles)</span>,$(<span class="built_in">call</span> cc_compile,<span class="variable">$(f)</span>,<span class="variable">$(CC)</span>,<span class="variable">$(CFLAGS)</span> -Os -nostdinc)</span>)</span><br><span class="line">这里遍历 boot 目录下的所有文件 asm.h bootasm.S bootmain.c</span><br><span class="line">bootblock = <span class="variable">$(<span class="built_in">call</span> totarget,bootblock)</span></span><br><span class="line">生成目标文件 asm.o bootasm.o bootmain.o sign.o</span><br><span class="line"><span class="variable">$(bootblock)</span>: <span class="variable">$(<span class="built_in">call</span> toobj,<span class="variable">$(bootfiles)</span>)</span> | <span class="variable">$(<span class="built_in">call</span> totarget,sign)</span></span><br><span class="line">	@echo + ld <span class="variable">$@</span></span><br><span class="line">	<span class="variable">$(V)</span><span class="variable">$(LD)</span> <span class="variable">$(LDFLAGS)</span> -N -e start -Ttext 0x7C00 <span class="variable">$^</span> -o <span class="variable">$(<span class="built_in">call</span> toobj,bootblock)</span></span><br><span class="line">	@<span class="variable">$(OBJDUMP)</span> -S <span class="variable">$(<span class="built_in">call</span> objfile,bootblock)</span> &gt; <span class="variable">$(<span class="built_in">call</span> asmfile,bootblock)</span></span><br><span class="line">	@<span class="variable">$(OBJDUMP)</span> -t <span class="variable">$(<span class="built_in">call</span> objfile,bootblock)</span> | <span class="variable">$(SED)</span> '1,/SYMBOL TABLE/d; s/ .* / /; /^$$/d' &gt; <span class="variable">$(<span class="built_in">call</span> symfile,bootblock)</span></span><br><span class="line">	@<span class="variable">$(OBJCOPY)</span> -S -O binary <span class="variable">$(<span class="built_in">call</span> objfile,bootblock)</span> <span class="variable">$(<span class="built_in">call</span> outfile,bootblock)</span></span><br><span class="line">	@<span class="variable">$(<span class="built_in">call</span> totarget,sign)</span> <span class="variable">$(<span class="built_in">call</span> outfile,bootblock)</span> <span class="variable">$(bootblock)</span></span><br><span class="line">将目标文件 链接起来 同时指定代码段开始地址 为 0x7c00</span><br><span class="line"><span class="variable">$(<span class="built_in">call</span> create_target,bootblock)</span></span><br></pre></td></tr></table></figure>

<p><img src="/images/lab1_gcc.png"></p>
<p><img src="/images/lab1_ld_bootblock.png"></p>
<p>生成 Kernel</p>
<figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">KOBJS	= <span class="variable">$(<span class="built_in">call</span> read_packet,kernel libs)</span></span><br><span class="line"></span><br><span class="line">kernel = <span class="variable">$(<span class="built_in">call</span> totarget,kernel)</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$(kernel)</span>: tools/kernel.ld</span><br><span class="line"></span><br><span class="line"><span class="variable">$(kernel)</span>: <span class="variable">$(KOBJS)</span></span><br><span class="line">	@echo + ld <span class="variable">$@</span></span><br><span class="line">	<span class="variable">$(V)</span><span class="variable">$(LD)</span> <span class="variable">$(LDFLAGS)</span> -T tools/kernel.ld -o <span class="variable">$@</span> <span class="variable">$(KOBJS)</span></span><br><span class="line">	@<span class="variable">$(OBJDUMP)</span> -S <span class="variable">$@</span> &gt; <span class="variable">$(<span class="built_in">call</span> asmfile,kernel)</span></span><br><span class="line">	@<span class="variable">$(OBJDUMP)</span> -t <span class="variable">$@</span> | <span class="variable">$(SED)</span> '1,/SYMBOL TABLE/d; s/ .* / /; /^$$/d' &gt; <span class="variable">$(<span class="built_in">call</span> symfile,kernel)</span></span><br><span class="line"></span><br><span class="line"><span class="variable">$(<span class="built_in">call</span> create_target,kernel)</span></span><br><span class="line">将 kern 下面的所有文件 编译 生成 目标文件 再进行链接</span><br></pre></td></tr></table></figure>
<p><img src="/images/lab1_ld_kernel.png"></p>
<ul>
<li>-ggdb 生成可供gdb使用的调试信息 </li>
<li>-m32 生成适用于32位环境的代码 </li>
<li>-gstabs 生成stabs格式的调试信息 </li>
<li>-nostdinc 不使用标准库 </li>
<li>-fno-stack-protector 不生成用于检测缓冲区溢出的代码 </li>
<li>-Os 为减小代码大小而进行优化 </li>
<li>-I添加搜索头文件的路径 </li>
<li>-fno-builtin 不进行builtin函数的优化</li>
<li>-m 模拟为i386上的连接器 </li>
<li>-N 设置代码段和数据段均可读写 </li>
<li>-e 指定入口 </li>
<li>-Ttext 指定代码段开始位置</li>
</ul>
<ol start="2">
<li>一个被系统认为是符合规范的硬盘主引导扇区的特征是什么？</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">在 tools/sign.c 里面  有以下两句 说明 符合规范的硬盘主引导扇区特征是 最后两个字节 为 <span class="number">0x55</span> <span class="number">0xAA</span> 同时 主引导扇区的大小应为 <span class="number">512</span> 字节</span><br><span class="line">    buf[<span class="number">510</span>] = <span class="number">0x55</span>;</span><br><span class="line">    buf[<span class="number">511</span>] = <span class="number">0xAA</span>;</span><br></pre></td></tr></table></figure>

<h3 id="练习2：使用qemu执行并调试lab1中的软件"><a href="#练习2：使用qemu执行并调试lab1中的软件" class="headerlink" title="练习2：使用qemu执行并调试lab1中的软件"></a>练习2：使用qemu执行并调试lab1中的软件</h3><ol>
<li>从CPU加电后执行的第一条指令开始，单步跟踪BIOS的执行。</li>
<li>在初始化位置0x7c00设置实地址断点,测试断点正常。</li>
<li>从0x7c00开始跟踪代码运行,将单步跟踪反汇编得到的代码与bootasm.S和 bootblock.asm进行比较。</li>
<li>自己找一个bootloader或内核中的代码位置，设置断点并进行测试。</li>
</ol>
<p>在一个终端中先执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">qemu-system-i386 -S -s -d in_asm -D bin&#x2F;q.log -monitor stdio -hda bin&#x2F;ucore.img</span><br></pre></td></tr></table></figure>
<p>后在另一个终端执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">i386-elf-gdb</span><br><span class="line">file bin&#x2F;kernel</span><br><span class="line">target remote :1234</span><br><span class="line">查看 CS:EIP 由于此时在实际模式下 CPU 在加电后执行的第一条指令的地址 为 0xf000:0xfff0 &#x3D;&gt; 0xffff0</span><br><span class="line">(gdb) x&#x2F;i $cs</span><br><span class="line">   0xf000:	add    %al,(%eax)</span><br><span class="line">(gdb) x&#x2F;i $eip</span><br><span class="line">&#x3D;&gt; 0xfff0:	add    %al,(%eax)</span><br><span class="line">再来看看这个地址的指令是什么</span><br><span class="line">(gdb) x&#x2F;2i 0xffff0</span><br><span class="line">   0xffff0:	ljmp   $0x3630,$0xf000e05b</span><br><span class="line">可以看到 第一条指令执行完以后 会跳转到 0xf000e05b 也就是说 BIOS 开始的地址是 0xfe05b</span><br><span class="line"></span><br><span class="line">打上断点</span><br><span class="line">b *0x7c00</span><br><span class="line">(gdb) b *0x7c00</span><br><span class="line">Breakpoint 1 at 0x7c00</span><br><span class="line">(gdb) c</span><br><span class="line">Continuing.</span><br><span class="line"></span><br><span class="line">Breakpoint 1, 0x00007c00 in ?? ()</span><br></pre></td></tr></table></figure>

<h3 id="练习3：分析bootloader进入保护模式的过程"><a href="#练习3：分析bootloader进入保护模式的过程" class="headerlink" title="练习3：分析bootloader进入保护模式的过程"></a>练习3：分析bootloader进入保护模式的过程</h3><p>BIOS将通过读取硬盘主引导扇区到内存，并转跳到对应内存中的位置执行bootloader。请分析bootloader是如何完成从实模式进入保护模式的。</p>
<figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line">#include &lt;asm<span class="number">.</span>h&gt;</span><br><span class="line"></span><br><span class="line"># Start the <span class="meta">CPU</span>: switch to <span class="number">32</span>-bit protected mode, jump <span class="keyword">into</span> C.</span><br><span class="line"># The BIOS loads this code from the first sector of the hard disk <span class="keyword">into</span></span><br><span class="line"># memory <span class="meta">at</span> physical address <span class="number">0x7c00</span> <span class="keyword">and</span> starts executing <span class="keyword">in</span> real mode</span><br><span class="line"># with %cs=<span class="number">0</span> %ip=7c00.</span><br><span class="line"></span><br><span class="line"><span class="meta">.set</span> PROT_MODE_CSEG,        <span class="number">0x8</span>                     # kernel code <span class="meta">segment</span> selector</span><br><span class="line"><span class="meta">.set</span> PROT_MODE_DSEG,        <span class="number">0x10</span>                    # kernel data <span class="meta">segment</span> selector</span><br><span class="line"><span class="meta">.set</span> CR0_PE_ON,             <span class="number">0x1</span>                     # protected mode enable flag</span><br><span class="line"></span><br><span class="line"># start address should be <span class="number">0</span>:7c00, <span class="keyword">in</span> real mode, the beginning address of the running bootloader</span><br><span class="line"><span class="meta">.globl</span> start</span><br><span class="line"><span class="symbol">start:</span></span><br><span class="line"><span class="meta">.code16</span>                                             # Assemble for <span class="number">16</span>-bit mode</span><br><span class="line">    <span class="keyword">cli</span>                                             # Disable interrupts</span><br><span class="line">    <span class="keyword">cld</span>                                             # String operations increment</span><br><span class="line"></span><br><span class="line">    # Set <span class="meta">up</span> the important data <span class="meta">segment</span> registers (<span class="built_in">DS</span>, <span class="built_in">ES</span>, <span class="built_in">SS</span>).</span><br><span class="line">    xorw %ax, %ax                                   # <span class="meta">Segment</span> number <span class="meta">zero</span></span><br><span class="line">    movw %ax, %ds                                   # -&gt; Data <span class="meta">Segment</span></span><br><span class="line">    movw %ax, %es                                   # -&gt; Extra <span class="meta">Segment</span></span><br><span class="line">    movw %ax, %ss                                   # -&gt; Stack <span class="meta">Segment</span></span><br><span class="line"></span><br><span class="line">    # Enable A20:</span><br><span class="line">    #  For backwards compatibility with the earliest PCs, physical</span><br><span class="line">    #  address line <span class="number">20</span> is tied low, so that addresses higher than</span><br><span class="line">    #  1MB wrap around to <span class="meta">zero</span> by <span class="meta">default</span>. This code undoes this.</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">A20 开启方法是 将 <span class="number">0x64</span> 端口读入一个字节 到 <span class="built_in">al</span>中 然后 testb 即做一个 <span class="keyword">and</span> 运算 只不过不保存结果 判断一下 <span class="number">0x64</span>端口的第二位是否为<span class="number">0</span> 即 <span class="number">8042</span> 键盘缓冲区是否为空 若不为空 则循环 直至为空</span><br><span class="line">后 将 0xdl(写入数据到<span class="number">8042</span>的p2端口) 写入到 <span class="number">0x64</span>端口中</span><br><span class="line"></span><br><span class="line">后面的也很类似</span><br><span class="line">还是判断 <span class="number">0x64</span>端口的第二位是不是为<span class="number">0</span> 不是就循环</span><br><span class="line">然后把 <span class="number">0xdf</span>(<span class="number">11011111</span>) 写入到 <span class="number">0x60</span> 设置了 P2的 A20位 即第一位为 <span class="number">1</span> 开启 A20地址线</span><br><span class="line">--------------------------------------------------------</span><br><span class="line"><span class="symbol">seta20.1:</span></span><br><span class="line">    inb <span class="number">$0</span>x64, %al                                  # Wait for <span class="keyword">not</span> busy(<span class="number">8042</span> input buffer empty).</span><br><span class="line">    testb <span class="number">$0</span>x2, %al</span><br><span class="line">    <span class="keyword">jnz</span> seta20<span class="number">.1</span></span><br><span class="line"></span><br><span class="line">    movb <span class="number">$0</span>xd1, %al                                 # <span class="number">0xd1</span> -&gt; port <span class="number">0x64</span></span><br><span class="line">    outb %al, <span class="number">$0</span>x64                                 # <span class="number">0xd1</span> means: write data to <span class="number">8042</span><span class="string">'s P2 port</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">seta20.2:</span></span><br><span class="line"><span class="string">    inb $0x64, %al                                  # Wait for not busy(8042 input buffer empty).</span></span><br><span class="line"><span class="string">    testb $0x2, %al</span></span><br><span class="line"><span class="string">    jnz seta20.2</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    movb $0xdf, %al                                 # 0xdf -&gt; port 0x60</span></span><br><span class="line"><span class="string">    outb %al, $0x60                                 # 0xdf = 11011111, means set P2'</span>s A20 bit(the <span class="number">1</span> bit) to <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    # Switch from real to protected mode, using a bootstrap GDT</span><br><span class="line">    # <span class="keyword">and</span> <span class="meta">segment</span> translation that makes virtual addresses</span><br><span class="line">    # identical to physical addresses, so that the</span><br><span class="line">    # effective memory map does <span class="keyword">not</span> change during the switch.</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">加载 GDT 全局描述符表</span><br><span class="line">打开 保护模式 需要将 <span class="built_in">cr0</span> 控制寄存器的 第<span class="number">0</span>位 PE位 置<span class="number">1</span></span><br><span class="line">--------------------------------------------------------</span><br><span class="line">    <span class="keyword">lgdt</span> gdtdesc</span><br><span class="line">    movl %cr0, %eax</span><br><span class="line">    orl $CR0_PE_ON, %eax</span><br><span class="line">    movl %eax, %cr0</span><br><span class="line"></span><br><span class="line">    # Jump to next instruction, but <span class="keyword">in</span> <span class="number">32</span>-bit code <span class="meta">segment</span>.</span><br><span class="line">    # Switches processor <span class="keyword">into</span> <span class="number">32</span>-bit mode.</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">刷新流水线 进入 <span class="number">32</span>位模式</span><br><span class="line">将 PROT_MODE_CSEG = <span class="number">0x8</span> 此时 指向的是 GDT 中的第一个段描述符 加载到 <span class="built_in">CS</span> 后 protcseg 加载到 <span class="built_in">IP</span></span><br><span class="line">--------------------------------------------------------</span><br><span class="line">    ljmp $PROT_MODE_CSEG, $protcseg</span><br><span class="line"></span><br><span class="line"><span class="meta">.code32</span>                                             # Assemble for <span class="number">32</span>-bit mode</span><br><span class="line"><span class="symbol">protcseg:</span></span><br><span class="line">    # Set <span class="meta">up</span> the protected-mode data <span class="meta">segment</span> registers</span><br><span class="line">    movw $PROT_MODE_DSEG, %ax                       # Our data <span class="meta">segment</span> selector</span><br><span class="line">    movw %ax, %ds                                   # -&gt; <span class="built_in">DS</span>: Data <span class="meta">Segment</span></span><br><span class="line">    movw %ax, %es                                   # -&gt; <span class="built_in">ES</span>: Extra <span class="meta">Segment</span></span><br><span class="line">    movw %ax, %fs                                   # -&gt; <span class="built_in">FS</span></span><br><span class="line">    movw %ax, %gs                                   # -&gt; <span class="built_in">GS</span></span><br><span class="line">    movw %ax, %ss                                   # -&gt; <span class="built_in">SS</span>: Stack <span class="meta">Segment</span></span><br><span class="line"></span><br><span class="line">    # Set <span class="meta">up</span> the stack pointer <span class="keyword">and</span> <span class="keyword">call</span> <span class="keyword">into</span> C. The stack region is from <span class="number">0</span>--start(<span class="number">0x7c00</span>)</span><br><span class="line">    movl <span class="number">$0</span>x0, %ebp</span><br><span class="line">    movl $start, %esp</span><br><span class="line">    <span class="keyword">call</span> bootmain</span><br><span class="line"></span><br><span class="line">    # If bootmain returns (it shouldn<span class="string">'t), loop.</span></span><br><span class="line"><span class="string">spin:</span></span><br><span class="line"><span class="string">    jmp spin</span></span><br><span class="line"><span class="string">--------------------------------------------------------</span></span><br><span class="line"><span class="string">GDT 构建一个代码段描述符 和 一个数据段描述符 使用的平坦模型</span></span><br><span class="line"><span class="string">--------------------------------------------------------</span></span><br><span class="line"><span class="string"># Bootstrap GDT</span></span><br><span class="line"><span class="string">.p2align 2                                          # force 4 byte alignment</span></span><br><span class="line"><span class="string">gdt:</span></span><br><span class="line"><span class="string">第0个描述符项不可用</span></span><br><span class="line"><span class="string">    SEG_NULLASM                                     # null seg</span></span><br><span class="line"><span class="string">    SEG_ASM(STA_X|STA_R, 0x0, 0xffffffff)           # code seg for bootloader and kernel</span></span><br><span class="line"><span class="string">    SEG_ASM(STA_W, 0x0, 0xffffffff)                 # data seg for bootloader and kernel</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">gdtdesc:</span></span><br><span class="line"><span class="string">    .word 0x17                                      # sizeof(gdt) - 1</span></span><br><span class="line"><span class="string">    .long gdt                                       # address gdt</span></span><br></pre></td></tr></table></figure>

<ul>
<li>为何开启A20，以及如何开启A20</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">实模式下内存访问采取的 段基址:段内偏移地址 的形式 段基址要左移4位再加上段内偏移地址来访问 实模式下寄存器都是16位的 如果段基址和段内便宜地址都为16位的最大值 0xFFFF:0xFFFF 即 0x10FFEF 当实模式下的地址总线为20位 最大寻址空间为 2的20次方&#x3D; 1M 的内存 超出了 1M的内存 若不打开 A20地址线 CPU将采用 8086&#x2F;8088 的地址回绕</span><br></pre></td></tr></table></figure>

<ul>
<li>如何初始化GDT表</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">先提前创建好 GDT 里面的 代码段选择子 数据段选择子 然后 通过 调用 lgdt 将 GDT的界限和内存起始地址存入 GDTR 寄存器中</span><br></pre></td></tr></table></figure>

<ul>
<li>如何使能和进入保护模式</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">将 cr0 控制寄存器的 第0位 PE位 置1</span><br><span class="line">同时 使用长跳转 刷新流水线 进入 32位模式</span><br></pre></td></tr></table></figure>

<h3 id="练习4：分析bootloader加载ELF格式的OS的过程"><a href="#练习4：分析bootloader加载ELF格式的OS的过程" class="headerlink" title="练习4：分析bootloader加载ELF格式的OS的过程"></a>练习4：分析bootloader加载ELF格式的OS的过程</h3><p>通过阅读bootmain.c，了解bootloader如何加载ELF文件。通过分析源代码和通过qemu来运行并调试bootloader&amp;OS，</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> SECTSIZE        512</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> ELFHDR          ((struct elfhdr *)0x10000)      <span class="comment">// scratch space</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* waitdisk - wait for disk ready */</span></span><br><span class="line">检查 <span class="number">0x1F7</span> 端口 的 第<span class="number">7</span>位为<span class="number">0</span>则硬盘不忙</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">waitdisk</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">while</span> ((inb(<span class="number">0x1F7</span>) &amp; <span class="number">0xC0</span>) != <span class="number">0x40</span>)</span><br><span class="line">        <span class="comment">/* do nothing */</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* readsect - read a single sector at @secno into @dst */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">readsect</span><span class="params">(<span class="keyword">void</span> *dst, <span class="keyword">uint32_t</span> secno)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// wait for disk to be ready</span></span><br><span class="line">    waitdisk();</span><br><span class="line">--------------------------------------------------------</span><br><span class="line"><span class="number">0x1F2</span> 写入扇区数</span><br><span class="line"><span class="number">0x1F3</span> LBA <span class="number">0</span>~<span class="number">7</span></span><br><span class="line"><span class="number">0x1F4</span> LBA <span class="number">8</span>~<span class="number">15</span></span><br><span class="line"><span class="number">0x1F5</span> LBA <span class="number">16</span>~<span class="number">23</span></span><br><span class="line"><span class="number">0x1F6</span> LBA <span class="number">7</span>~<span class="number">4</span>位为<span class="number">1110</span> 表示LBA模式 <span class="number">24</span>~<span class="number">27</span> </span><br><span class="line"><span class="number">0x1F7</span> <span class="number">0x20</span> 读命令</span><br><span class="line">一次从 <span class="number">0x1F0</span> 读入 <span class="number">2</span>个字 <span class="number">4</span>个字节 读入<span class="number">512</span>字节 需要 <span class="number">128</span>次</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">    outb(<span class="number">0x1F2</span>, <span class="number">1</span>);                         <span class="comment">// count = 1</span></span><br><span class="line">    outb(<span class="number">0x1F3</span>, secno &amp; <span class="number">0xFF</span>);</span><br><span class="line">    outb(<span class="number">0x1F4</span>, (secno &gt;&gt; <span class="number">8</span>) &amp; <span class="number">0xFF</span>);</span><br><span class="line">    outb(<span class="number">0x1F5</span>, (secno &gt;&gt; <span class="number">16</span>) &amp; <span class="number">0xFF</span>);</span><br><span class="line">    outb(<span class="number">0x1F6</span>, ((secno &gt;&gt; <span class="number">24</span>) &amp; <span class="number">0xF</span>) | <span class="number">0xE0</span>);</span><br><span class="line">    outb(<span class="number">0x1F7</span>, <span class="number">0x20</span>);                      <span class="comment">// cmd 0x20 - read sectors</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// wait for disk to be ready</span></span><br><span class="line">    waitdisk();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// read a sector</span></span><br><span class="line">    insl(<span class="number">0x1F0</span>, dst, SECTSIZE / <span class="number">4</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* *</span></span><br><span class="line"><span class="comment"> * readseg - read @count bytes at @offset from kernel into virtual address @va,</span></span><br><span class="line"><span class="comment"> * might copy more than asked.</span></span><br><span class="line"><span class="comment"> * */</span></span><br><span class="line"> 包装一下 可以读取任意长度的内容 </span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">readseg</span><span class="params">(<span class="keyword">uintptr_t</span> va, <span class="keyword">uint32_t</span> count, <span class="keyword">uint32_t</span> offset)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">uintptr_t</span> end_va = va + count;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// round down to sector boundary</span></span><br><span class="line">    va -= offset % SECTSIZE;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// translate from bytes to sectors; kernel starts at sector 1</span></span><br><span class="line">    <span class="keyword">uint32_t</span> secno = (offset / SECTSIZE) + <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// If this is too slow, we could read lots of sectors at a time.</span></span><br><span class="line">    <span class="comment">// We'd write more to memory than asked, but it doesn't matter --</span></span><br><span class="line">    <span class="comment">// we load in increasing order.</span></span><br><span class="line">    <span class="keyword">for</span> (; va &lt; end_va; va += SECTSIZE, secno ++) &#123;</span><br><span class="line">        readsect((<span class="keyword">void</span> *)va, secno);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* bootmain - the entry of bootloader */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">bootmain</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// read the 1st page off disk</span></span><br><span class="line">这里读入了 <span class="number">512</span> * <span class="number">8</span> 个字节 ELF头</span><br><span class="line">    readseg((<span class="keyword">uintptr_t</span>)ELFHDR, SECTSIZE * <span class="number">8</span>, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// is this a valid ELF?</span></span><br><span class="line">根据读进来的文件的头部 魔数 判断是否为合法 ELF文件</span><br><span class="line">    <span class="keyword">if</span> (ELFHDR-&gt;e_magic != ELF_MAGIC) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proghdr</span> *<span class="title">ph</span>, *<span class="title">eph</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// load each program segment (ignores ph flags)</span></span><br><span class="line">读取每个代码段 到指定的地方</span><br><span class="line">    ph = (struct proghdr *)((<span class="keyword">uintptr_t</span>)ELFHDR + ELFHDR-&gt;e_phoff);</span><br><span class="line">    eph = ph + ELFHDR-&gt;e_phnum;</span><br><span class="line">    <span class="keyword">for</span> (; ph &lt; eph; ph ++) &#123;</span><br><span class="line">        readseg(ph-&gt;p_va &amp; <span class="number">0xFFFFFF</span>, ph-&gt;p_memsz, ph-&gt;p_offset);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// call the entry point from the ELF header</span></span><br><span class="line">    <span class="comment">// note: does not return</span></span><br><span class="line">进入内核</span><br><span class="line">    ((<span class="keyword">void</span> (*)(<span class="keyword">void</span>))(ELFHDR-&gt;e_entry &amp; <span class="number">0xFFFFFF</span>))();</span><br><span class="line"></span><br><span class="line">bad:</span><br><span class="line">真实硬件中 并不会有设备连接到 <span class="number">0x8A00</span> 端口 故相当于啥也没做</span><br><span class="line">    outw(<span class="number">0x8A00</span>, <span class="number">0x8A00</span>);</span><br><span class="line">    outw(<span class="number">0x8A00</span>, <span class="number">0x8E00</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* do nothing */</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>bootloader如何读取硬盘扇区的？</li>
<li>bootloader是如何加载ELF格式的OS？<br>首先 bootloader 调用 bootmain 函数 然后读取第一个扇区的内容 到 0x10000 地址处 读取硬盘扇区的过程<br>0x1F2 写入扇区数<br>0x1F3 LBA 0<del>7<br>0x1F4 LBA 8</del>15<br>0x1F5 LBA 16<del>23<br>0x1F6 LBA 7</del>4位为1110 表示LBA模式 24~27<br>0x1F7 0x20 读命令<br>一次从 0x1F0 读入 2个字 4个字节 读入512字节 需要 128次<br>然后 判断 他的魔数是否为 ELF文件 若不是 则走向死循环 若是 则读 ELF 文件的程序段 到 该段的起始虚拟地址中去</li>
</ul>
<h3 id="练习5：实现函数调用堆栈跟踪函数"><a href="#练习5：实现函数调用堆栈跟踪函数" class="headerlink" title="练习5：实现函数调用堆栈跟踪函数"></a>练习5：实现函数调用堆栈跟踪函数</h3><p>我们需要在lab1中完成kdebug.c中函数print_stackframe的实现，可以通过函数print_stackframe来跟踪函数调用堆栈中记录的返回地址。在如果能够正确实现此函数，可在lab1中执行 “make qemu”后，在qemu模拟器中得到类似如下的输出</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">print_stackframe</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">uint32_t</span> ebp = read_ebp(), eip = read_eip();</span><br><span class="line">    <span class="keyword">int</span> i, j;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; ebp != <span class="number">0</span> &amp;&amp; i &lt; STACKFRAME_DEPTH; i++) &#123;</span><br><span class="line">        cprintf(<span class="string">"ebp:0x%08x eip:0x%08x args:"</span>, ebp, eip);</span><br><span class="line">        <span class="keyword">uint32_t</span> *args = (<span class="keyword">uint32_t</span>*)ebp + <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; <span class="number">4</span>; j ++ ) &#123;</span><br><span class="line">            cprintf(<span class="string">"0x%08x "</span>, args[j]);</span><br><span class="line">        &#125;</span><br><span class="line">        cprintf(<span class="string">"\n"</span>);</span><br><span class="line">        print_debuginfo(eip - <span class="number">1</span>);</span><br><span class="line">        eip = ((<span class="keyword">uint32_t</span>*)ebp)[<span class="number">1</span>];</span><br><span class="line">取得 调用本函数的函数的返回地址</span><br><span class="line">        ebp = ((<span class="keyword">uint32_t</span>*)ebp)[<span class="number">0</span>];</span><br><span class="line">取得 调用本函数的 之前的 ebp 地址</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">最后一行输出的 ebp为 <span class="number">0x00007bf8</span> 但是 bootloader 起始地址是 <span class="number">0x7c00</span> 说明 压入了 两个东西 其中一个是 返回地址 另一个是 ebp 最后将 esp 赋给 ebp</span><br></pre></td></tr></table></figure>

<p><img src="/images/stack.png"><br>esp 栈顶指针<br>ebp 栈底指针<br>eip 寄存器存放的CPU下一条指令的地址<br>首先读取 ebp 和 eip 的值 然后 在ebp的地址上 + 2 就是第一个参数的位置</p>
<h3 id="练习6：完善中断初始化和处理"><a href="#练习6：完善中断初始化和处理" class="headerlink" title="练习6：完善中断初始化和处理"></a>练习6：完善中断初始化和处理</h3><p>请完成编码工作和回答如下问题：</p>
<ol>
<li>中断描述符表（也可简称为保护模式下的中断向量表）中一个表项占多少字节？其中哪几位代表中断处理代码的入口？</li>
</ol>
<p><img src="/images/trapgate.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Gate descriptors for interrupts and traps */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">gatedesc</span> &#123;</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_off_15_0 : <span class="number">16</span>;        <span class="comment">// low 16 bits of offset in segment</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_ss : <span class="number">16</span>;            <span class="comment">// segment selector</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_args : <span class="number">5</span>;            <span class="comment">// # args, 0 for interrupt/trap gates</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_rsv1 : <span class="number">3</span>;            <span class="comment">// reserved(should be zero I guess)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_type : <span class="number">4</span>;            <span class="comment">// type(STS_&#123;TG,IG32,TG32&#125;)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_s : <span class="number">1</span>;                <span class="comment">// must be 0 (system)</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_dpl : <span class="number">2</span>;            <span class="comment">// descriptor(meaning new) privilege level</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_p : <span class="number">1</span>;                <span class="comment">// Present</span></span><br><span class="line">    <span class="keyword">unsigned</span> gd_off_31_16 : <span class="number">16</span>;        <span class="comment">// high bits of offset in segment</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="number">8</span> 个字节  <span class="number">0</span>~<span class="number">15</span> + <span class="number">48</span>~<span class="number">63</span> 组成段偏移 + <span class="number">16</span>~<span class="number">31</span> 组成段描述符选择子 通过段描述符选择子 和 段偏移 找到 中断程序的入口</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>请编程完善kern/trap/trap.c中对中断向量表进行初始化的函数idt_init。在idt_init函数中，依次对所有中断入口进行初始化。使用mmu.h中的SETGATE宏，填充idt数组内容。每个中断的入口由tools/vectors.c生成，使用trap.c中声明的vectors数组即可。</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">extern</span> <span class="keyword">uintptr_t</span> __vectors[];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">idt_init</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="keyword">sizeof</span>(idt) / <span class="keyword">sizeof</span>(struct gatedesc); i++) &#123;</span><br><span class="line">        SETGATE(idt[i], <span class="number">0</span>, GD_KTEXT, __vectors[i], DPL_KERNEL);</span><br><span class="line">    &#125;</span><br><span class="line">	用于系统调用 专门给用户使用的</span><br><span class="line">	SETGATE(idt[T_SWITCH_TOK], <span class="number">0</span>, GD_KTEXT, __vectors[T_SWITCH_TOK], DPL_USER);</span><br><span class="line">	将 中断向量表加载到 ldtr 寄存器中</span><br><span class="line">    lidt(&amp;idt_pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>请编程完善trap.c中的中断处理函数trap，在对时钟中断进行处理的部分填写trap函数中处理时钟中断的部分，使操作系统每遇到100次时钟中断后，调用print_ticks子程序，向屏幕上打印一行文字”100 ticks”。</li>
</ol>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kern/trap/trap.c <span class="number">138</span>:</span><br><span class="line">    <span class="keyword">switch</span> (tf-&gt;tf_trapno) &#123;</span><br><span class="line">    <span class="keyword">case</span> IRQ_OFFSET + IRQ_TIMER:</span><br><span class="line">        <span class="keyword">if</span> (++ticks % TICK_NUM == <span class="number">0</span>) &#123;</span><br><span class="line">            print_ticks();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure>

<p><img src="/images/lab1.png"></p>
<h3 id="扩展练习"><a href="#扩展练习" class="headerlink" title="扩展练习"></a>扩展练习</h3><h4 id="扩展练习-Challenge-1"><a href="#扩展练习-Challenge-1" class="headerlink" title="扩展练习 Challenge 1"></a>扩展练习 Challenge 1</h4><p>扩展proj4,增加syscall功能，即增加一用户态函数（可执行一特定系统调用：获得时钟计数值），当内核初始完毕后，可从内核态返回到用户态的函数，而用户态的函数又通过系统调用得到内核态的服务。需写出详细的设计和分析报告。完成出色的可获得适当加分。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kern/init/init.c</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">lab1_switch_to_user</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">	<span class="string">"sub $0x8, %%esp \n"</span> </span><br><span class="line">	让 SS 和 ESP 这两个寄存器 有机会 POP 出时 更新 SS 和 ESP</span><br><span class="line">	因为 从内核态进入中断 它的特权级没有改变 是不会 push 进 SS 和 ESP的 但是我们又需要通过 POP SS 和 ESP 去修改它们</span><br><span class="line">	进入 T_SWITCH_TOU(<span class="number">120</span>) 中断</span><br><span class="line">	将原来的栈顶指针还给esp栈底指针</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"sub $0x8, %%esp \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"int %0 \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"movl %%ebp, %%esp"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    : </span></span></span><br><span class="line"><span class="function"><span class="params">	    : <span class="string">"i"</span>(T_SWITCH_TOU)</span></span></span><br><span class="line"><span class="function"><span class="params">	)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">lab1_switch_to_kernel</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">	进入 T_SWITCH_TOK(<span class="number">121</span>) 中断</span><br><span class="line">	将原来的栈顶指针还给esp栈底指针</span><br><span class="line">--------------------------------------------------------</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"int %0 \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"movl %%ebp, %%esp \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    : </span></span></span><br><span class="line"><span class="function"><span class="params">	    : <span class="string">"i"</span>(T_SWITCH_TOK)</span></span></span><br><span class="line"><span class="function"><span class="params">	)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kern/trap/trap.c :</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">trap_dispatch</span><span class="params">(struct trapframe *tf)</span></span></span><br><span class="line">通过"改造"一个中断 来进入我们想进入的用户态或者内核态</span><br><span class="line">    <span class="keyword">case</span> T_SWITCH_TOU:</span><br><span class="line">        <span class="keyword">if</span> (tf-&gt;tf_cs != USER_CS) &#123;</span><br><span class="line">            switchk2u = *tf;</span><br><span class="line">            switchk2u.tf_cs = USER_CS;</span><br><span class="line">            switchk2u.tf_ds = switchk2u.tf_es = switchk2u.tf_ss = USER_DS;</span><br><span class="line">            switchk2u.tf_eflags |= FL_IOPL_MASK; <span class="comment">// IOPL 改为 0</span></span><br><span class="line">            switchk2u.tf_esp = (<span class="keyword">uint32_t</span>)tf + <span class="keyword">sizeof</span>(struct trapframe) - <span class="number">8</span>; <span class="comment">// tf-&gt;esp的位置</span></span><br><span class="line">            <span class="comment">// iret 回到用户栈</span></span><br><span class="line">            *((<span class="keyword">uint32_t</span> *)tf - <span class="number">1</span>) = (<span class="keyword">uint32_t</span>)&amp;switchk2u;</span><br><span class="line">        &#125;</span><br><span class="line">		<span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> T_SWITCH_TOK:</span><br><span class="line">        <span class="keyword">if</span> (tf-&gt;tf_cs != KERNEL_CS) &#123;</span><br><span class="line">            tf-&gt;tf_cs = KERNEL_CS;</span><br><span class="line">            tf-&gt;tf_ds = tf-&gt;tf_es = KERNEL_DS;</span><br><span class="line">            tf-&gt;tf_eflags &amp;= ~FL_IOPL_MASK;</span><br><span class="line">            switchu2k = (struct trapframe *)(tf-&gt;tf_esp - (<span class="keyword">sizeof</span>(struct trapframe) - <span class="number">8</span>));</span><br><span class="line">            memmove(switchu2k, tf, <span class="keyword">sizeof</span>(struct trapframe) - <span class="number">8</span>);</span><br><span class="line">            *((<span class="keyword">uint32_t</span> *)tf - <span class="number">1</span>) = (<span class="keyword">uint32_t</span>)switchu2k;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure>

<p>根据这张图 可以看出 内核态和用户态的转换 首先是留下 SS 和 ESP 的位置 然后 调用中断 改中断栈里面的内容 最后退出中断的时候 跳到内核态中 最后将 ebp 赋给 esp 修复 esp 的位置<br><img src="/images/pcb.png"></p>
<h4 id="扩展练习-Challenge-2"><a href="#扩展练习-Challenge-2" class="headerlink" title="扩展练习 Challenge 2"></a>扩展练习 Challenge 2</h4><p>用键盘实现用户模式内核模式切换。具体目标是：“键盘输入3时切换到用户模式，键盘输入0时切换到内核模式”。 基本思路是借鉴软中断(syscall功能)的代码，并且把trap.c中软中断处理的设置语句拿过来。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kern/trap/trap.c</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">switch_to_user</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"sub $0x8, %%esp \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"int %0 \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"movl %%ebp, %%esp"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    : </span></span></span><br><span class="line"><span class="function"><span class="params">	    : <span class="string">"i"</span>(T_SWITCH_TOU)</span></span></span><br><span class="line"><span class="function"><span class="params">	)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">switch_to_kernel</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">asm</span> <span class="title">volatile</span> <span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"int %0 \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    <span class="string">"movl %%ebp, %%esp \n"</span></span></span></span><br><span class="line"><span class="function"><span class="params">	    : </span></span></span><br><span class="line"><span class="function"><span class="params">	    : <span class="string">"i"</span>(T_SWITCH_TOK)</span></span></span><br><span class="line"><span class="function"><span class="params">	)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">case</span> IRQ_OFFSET + IRQ_KBD:</span><br><span class="line">        c = cons_getc();</span><br><span class="line">        <span class="keyword">if</span> (c == <span class="string">'3'</span>) &#123;</span><br><span class="line">            switch_to_user();</span><br><span class="line">            print_trapframe(tf);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (c == <span class="string">'0'</span>) &#123;</span><br><span class="line">            switch_to_kernel();</span><br><span class="line">            print_trapframe(tf);</span><br><span class="line">        &#125;</span><br><span class="line">        cprintf(<span class="string">"kbd [%03d] %c\n"</span>, c, c);</span><br><span class="line">        <span class="keyword">break</span>;</span><br></pre></td></tr></table></figure>

<p>按键的中断在 IRQ_KBD 处</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 2</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-2/</url>
    <content><![CDATA[<h3 id="x86-特权级-Privilege-Levels"><a href="#x86-特权级-Privilege-Levels" class="headerlink" title="x86 特权级 (Privilege Levels)"></a>x86 特权级 (Privilege Levels)</h3><ul>
<li>RING 0(内核) </li>
<li>RING 1(服务) </li>
<li>RING 2(服务) </li>
<li>RING 3(应用程序)</li>
</ul>
<p>当前操作系统 一般只用到了 RING 0 和 RING 3 比如 Linux<br>在访问数据段 页表 进入中断服务例程 (ISRs) CPU 会检查特权级</p>
<h4 id="x86-特权级检查失败"><a href="#x86-特权级检查失败" class="headerlink" title="x86 特权级检查失败"></a>x86 特权级检查失败</h4><p>会提示 General Protection Fault 一般保护错误</p>
<a id="more"></a>
<h4 id="x86-特权级检查"><a href="#x86-特权级检查" class="headerlink" title="x86 特权级检查"></a>x86 特权级检查</h4><p>RPL 请求特权级 DS ES GS FS 数据段</p>
<p>CPL 当前特权级 存在于 CS / SS 的低2位</p>
<p><img src="/images/segmentregister.png"></p>
<p>DPL 段或者门的特权级</p>
<p><img src="/images/segmentdescriptor.png"></p>
<p>访问门时 CPL &lt;= DPL[门] &amp;&amp; CPL &gt;= DPL[段]<br>访问段时 MAX(CPL, RPL) &lt;= DPL[段]</p>
<h4 id="x86-通过中断切换特权级"><a href="#x86-通过中断切换特权级" class="headerlink" title="x86 通过中断切换特权级"></a>x86 通过中断切换特权级</h4><p>首先在中断描述符表里 建立好 中断门 来实现中断切换特权级</p>
<p><img src="/images/interruptgate.png"></p>
<h5 id="RING-0-to-RING-3"><a href="#RING-0-to-RING-3" class="headerlink" title="RING 0 to RING 3"></a>RING 0 to RING 3</h5><p>当 ring 0 内核态发生中断的时候 首先因为发生中断的时候还是在 ring 0 所以不会将 ss esp 压入堆栈中 只会压入 Eflags cs eip 和 中断错误码</p>
<p>因此 为了实现从 ring 0 到 ring 3 的特权级转换 将其 ss 改成特权级3的栈 cs 改为用户代码段 最后通过 IRET 将这些信息 POP 出栈 此时 运行环境就进入了用户态中了 </p>
<p><img src="/images/ring0toring3.png"></p>
<h5 id="RING-3-to-RING-0"><a href="#RING-3-to-RING-0" class="headerlink" title="RING 3 to RING 0"></a>RING 3 to RING 0</h5><p>当 ring 3 用户态发生中断时 会将 ss esp 压入堆栈中 这是为了 跳出中断的时候 还能返回到这个用户态中 但是我们是为了实现 从 ring 3 到 ring 0 的特权级转换<br>因此 ss esp 是不需要的 将它们给去掉 同时将 cs 改为 内核态代码段 最后 还是 通过 IRET 将这些信息 POP 出栈 就回到了 ring 0 内核态中</p>
<p><img src="/images/ring3toring0.png"></p>
<h5 id="TSS-任务状态段-Task-State-Segment"><a href="#TSS-任务状态段-Task-State-Segment" class="headerlink" title="TSS 任务状态段 (Task State Segment)"></a>TSS 任务状态段 (Task State Segment)</h5><p>TSS 的位置 可以从 全局描述符表 中的 任务状态描述符 (Task State Descriptor) 中找到</p>
<p>因为 IDT 中断描述符表 中的 中断门 有 代码段选择子 可以用它作为索引从 GDT 全局描述符表中 找到实际的代码段的内存地址 但是 ss 和 esp 是不存在于 中断门中的<br>它们 存在于 TSS 中. TR(Task Register) 寄存器会缓存 TSS 从而实现 任务的切换</p>
<p><img src="/images/tss.png"></p>
<p>tss 在里面 只会保存 ring 0 ~ ring 2 的 ss 和 esp 之所以不保存 ring 3 的 ss 和 esp 是因为 CPU 默认只支持 从低特权级跳到高特权级 而 ring 3 是最低的特权级 不会有其他的特权级跳过去了 因此 不保存 ring 3 的 ss 和 esp</p>
<h4 id="x86-内存管理单元-MMU"><a href="#x86-内存管理单元-MMU" class="headerlink" title="x86 内存管理单元 MMU"></a>x86 内存管理单元 MMU</h4><h5 id="段机制"><a href="#段机制" class="headerlink" title="段机制"></a>段机制</h5><p>首先通过段选择子作为索引 在 GDT 全局描述符表中找到 段描述符 若没启动页机制的话 那么现在就找到 线性地址 </p>
<p><img src="/images/segment_based.png"></p>
<p>GDT 存在于内存当中 因为它所占空间比较大 但是由于内存比较慢 每次去访问 段表的时候 耗费比较大 因此 硬件会将 GDT 中的描述信息(Base Address, Limit…) 放在 CPU 来加快段的映射过程 </p>
<h5 id="页机制"><a href="#页机制" class="headerlink" title="页机制"></a>页机制</h5><ol>
<li>线性地址 的 高十位 + cr3 中的 PDE 页目标表的地址 找到 PTE 页表的 物理地址 </li>
<li>PTE 页表物理地址+ 线性地址中间 10位 找到 物理页基址 </li>
<li>物理页基址 加上 线性地址的 低 12位 找到物理地址</li>
</ol>
<p><img src="/images/coarsepagetable.png"></p>
<p>页目录表项和页表项的高20位为物理页表地址/物理页地址 之所以只用到 20 位 是因为页是以 4K 为单位 地址都是 4K的倍数 后面12位都为 0 所以 可以将多余的 12 位用作属性位</p>
<ul>
<li>AVL CPU 不理会这个属性 可以不管 (有可能在32位系统使用大过 4G内存的时候 用到这几位) </li>
<li>G Global 全局位 表示是否将虚拟地址与物理地址的转换结果缓存到 TLB 中</li>
<li>D Dirty 脏页位 当 CPU 对这个页进行写操作时 会置 1</li>
<li>PAT Page Attribute Table 页属性表位 置 0</li>
<li>A Accessed 访问位 若为 1 则 说明 CPU 访问过了 CPU 会定时清 0 记录被置 1 的频率 当内存不足时 会将 使用频率较低的页面换出到外存 同时将 P位 置 0 下次访问 该页时 会引起 Pagefault 异常 中断处理程序再将此页换上</li>
<li>PCD Page-level Cache Disable 页级高速缓存位 置 0 即可 读的时候 高速缓存是否有效 若有效则直接从高速缓存中读出 若无效的话 则必须实实在在的从 I/O 端口去读数据</li>
<li>PWT Page-level Write-Through 页级通写位 控制是先写到高速缓存里再慢慢回写到内存里 还是 直接慢慢写到内存里</li>
<li>US User/Superviosr 普通用户/超级用户位</li>
<li>RW Read/Write 读写位</li>
<li>P Present 存在位 (虚拟页式存储的关键位 若为 0 则发起缺页异常)</li>
</ul>
<p><img src="/images/pagetableentries.png"></p>
<h5 id="如何开启页机制"><a href="#如何开启页机制" class="headerlink" title="如何开启页机制"></a>如何开启页机制</h5><ol>
<li>准备好页目录表和页表</li>
<li>页目录表物理地址 写入 cr3 寄存器</li>
<li>cr0 最高位 PG位 置 1</li>
</ol>
<h5 id="段机制和页机制都能作为映射机制-应该如何选择"><a href="#段机制和页机制都能作为映射机制-应该如何选择" class="headerlink" title="段机制和页机制都能作为映射机制 应该如何选择?"></a>段机制和页机制都能作为映射机制 应该如何选择?</h5><p>选择页机制有助于硬件机制对其进行有效的处理</p>
<p>使用段机制的安全保护手段 来保护系统的安全 但是弱化了 段机制的映射 使用页机制的映射</p>
<p>段机制和页机制结合成段页式的存储管理 从而 既能便于程序的共享和保护 又能高效率利用存储空间</p>
<h4 id="线性地址-虚拟地址-逻辑地址-物理地址-有效地址-区别"><a href="#线性地址-虚拟地址-逻辑地址-物理地址-有效地址-区别" class="headerlink" title="线性地址 虚拟地址 逻辑地址 物理地址 有效地址 区别"></a>线性地址 虚拟地址 逻辑地址 物理地址 有效地址 区别</h4><ul>
<li>保护模式 段基址+段偏移 = 线性地址 若不开分页 == 物理地址</li>
<li>实模式/保护模式 段偏移 = 有效地址 也称 逻辑地址</li>
<li>开启分页后 线性地址 == 虚拟地址</li>
</ul>
<h2 id="Ucore-Lab-2"><a href="#Ucore-Lab-2" class="headerlink" title="Ucore Lab 2"></a>Ucore Lab 2</h2><h3 id="练习0-填写已有实验"><a href="#练习0-填写已有实验" class="headerlink" title="练习0: 填写已有实验"></a>练习0: 填写已有实验</h3><p>本实验依赖实验1。请把你做的实验1的代码填入本实验中代码中有“LAB1”的注释相应部分。提示：可采用diff和patch工具进行半自动的合并（merge），也可用一些图形化的比较/merge工具来手动合并，比如meld，eclipse中的diff/merge工具，understand中的diff/merge工具等。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">其实 就 kdebug.c init.c(Lab 1 Challenge 可以不管) trap.c 这三个文件自己复制粘贴下完事</span><br></pre></td></tr></table></figure>

<h3 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h3><h4 id="int-0x15-0xE820-物理内存探测"><a href="#int-0x15-0xE820-物理内存探测" class="headerlink" title="int 0x15 0xE820 物理内存探测"></a>int 0x15 0xE820 物理内存探测</h4><p>首先 Lab 2 我们知道是要实现连续物理内存的分配 那么就要知道 我们的物理内存有多少<br>Linux 采用 实模式下 int 0x15 中断来探测 物理内存的大小 它 和 ucore 不同的是 Linux 通过三种不同的方法来获取 物理内存大小 分别是<br>(E820h E801h 88h) 功能依次减弱 当一种方法不能使用时 则采用更弱的一种方法去获取 而 ucore 直接采用最强大的 E820h 方法 来获取物理内存</p>
<p>下面是通过中断将物理内存信息 存放到 0x8000 的代码实现</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">e820map</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> nr_map;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">        <span class="keyword">long</span> <span class="keyword">long</span> addr;</span><br><span class="line">        <span class="keyword">long</span> <span class="keyword">long</span> <span class="built_in">size</span>;</span><br><span class="line">        <span class="keyword">long</span> type;</span><br><span class="line">    &#125; <span class="built_in">map</span>[E820MAX];</span><br><span class="line">&#125;;</span><br><span class="line">probe_memory:</span><br><span class="line"><span class="comment">//对0x8000处的32位单元清零,即给位于0x8000处的</span></span><br><span class="line"><span class="comment">//struct e820map的成员变量nr_map清零</span></span><br><span class="line">                  movl $<span class="number">0</span>, <span class="number">0x8000</span></span><br><span class="line">                  xorl %ebx, %ebx</span><br><span class="line"><span class="comment">//表示设置调用INT 15h BIOS中断后，BIOS返回的映射地址描述符的起始地址</span></span><br><span class="line">                  movw $<span class="number">0x8004</span>, %di</span><br><span class="line">start_probe:</span><br><span class="line">                  movl $<span class="number">0xE820</span>, %eax <span class="comment">// INT 15的中断调用参数</span></span><br><span class="line"><span class="comment">//设置地址范围描述符的大小为20字节，其大小等于struct e820map的成员变量map的大小</span></span><br><span class="line">                  movl $<span class="number">20</span>, %ecx</span><br><span class="line"><span class="comment">//设置edx为534D4150h (即4个ASCII字符“SMAP”)，这是一个约定</span></span><br><span class="line">                  movl $SMAP, %edx</span><br><span class="line"><span class="comment">//调用int 0x15中断，要求BIOS返回一个用地址范围描述符表示的内存段信息</span></span><br><span class="line">                  <span class="keyword">int</span> $<span class="number">0x15</span></span><br><span class="line"><span class="comment">//如果eflags的CF位为0，则表示还有内存段需要探测</span></span><br><span class="line">                  jnc cont</span><br><span class="line"><span class="comment">//探测有问题，结束探测</span></span><br><span class="line">                  movw $<span class="number">12345</span>, <span class="number">0x8000</span> <span class="comment">// 12345 这个数是给 ucore 检测错误用的</span></span><br><span class="line">                  jmp finish_probe</span><br><span class="line">cont:</span><br><span class="line"><span class="comment">//设置下一个BIOS返回的映射地址描述符的起始地址</span></span><br><span class="line">                  addw $<span class="number">20</span>, %di</span><br><span class="line"><span class="comment">//递增struct e820map的成员变量nr_map</span></span><br><span class="line">                  incl <span class="number">0x8000</span></span><br><span class="line"><span class="comment">//如果INT0x15返回的ebx为零，表示探测结束，否则继续探测</span></span><br><span class="line">                  cmpl $<span class="number">0</span>, %ebx</span><br><span class="line">                  jnz start_probe</span><br><span class="line">finish_probe:</span><br></pre></td></tr></table></figure>

<h4 id="系统执行中的地址映射四阶段"><a href="#系统执行中的地址映射四阶段" class="headerlink" title="系统执行中的地址映射四阶段"></a>系统执行中的地址映射四阶段</h4><p>第一阶段 bootloader 阶段 也就是此时 kernel 还没有载入 从bootloader的start函数（在boot/bootasm.S中）到执行ucore kernel的kern_\entry函数之前 和 Lab 1一样 </p>
<p><code>virt addr = linear addr = phy addr</code></p>
<p>第二阶段 从kern_\entry函数开始，到执行enable_page函数（在kern/mm/pmm.c中）之前再次更新了段映射，还没有启动页映射机制 而 Lab 2 通过 ld 工具将 ucore 起始内核虚拟地址设置为了 0xC0100000 但其实际 物理内存地址仍是 0x100000(换句话说就是 我链接的 ucore的内核虚拟地址 是假设开了分页后的特意算好的地址 但是我进入内核的时候 还没有开好分页 所以只能从段机制上做文章 也就是让段的起始地址 从 -0xc0000000开始) 为了使ucore正确运行 映射关系应为</p>
<p><code>virt addr - 0xC0000000 = linear addr = phy addr</code></p>
<p>第三阶段 从enable_page函数开始，到执行gdt_init函数（在kern/mm/pmm.c中）之前，启动了页映射机制 然而之前第二阶段的段机制还没有修改<br>此时的映射关系为</p>
<p><code>virt addr = linear addr + 0xC0000000 = phy addr + 2 * 0xC0000000</code></p>
<p>这肯定是错的 我们想要的 映射 应该是</p>
<p><code>virt addr = linear addr + 0xC0000000 = phy addr + 0xC0000000</code></p>
<p>ucore 采用一个小技巧 就是建立临时的页映射 线性地址 0xC0000000<del>0xC0400000(4MB) 映射到物理地址 0x00000000</del>0x00400000(4MB) 按照我们想要的映射关系映射 4MB 之外的内存地址 依然保留错误的映射关系 但也足够了 因为 ucore 的内核大小也就 3MB</p>
<p>第四阶段 可喜可贺 从gdt_init函数开始 此时重设了 GDT 的段起始地址 又改回了 0 然后再将第三阶段的临时 映射关系给取消了 终于得到了想要的 映射关系</p>
<p><code>virt addr = linear addr = phy addr + 0xC0000000</code></p>
<h3 id="练习1-实现-first-fit-连续物理内存分配算法"><a href="#练习1-实现-first-fit-连续物理内存分配算法" class="headerlink" title="练习1: 实现 first-fit 连续物理内存分配算法"></a>练习1: 实现 first-fit 连续物理内存分配算法</h3><p>在实现first fit 内存分配算法的回收函数时，要考虑地址连续的空闲块之间的合并操作。提示:在建立空闲页块链表时，需要按照空闲页块起始地址来排序，形成一个有序的链表。可能会修改default_pmm.c中的default_init，default_init_memmap，default_alloc_pages， default_free_pages等相关函数。请仔细查看和理解default_pmm.c中的注释。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Page</span> &#123;</span></span><br><span class="line">    <span class="comment">// 页帧的 引用计数</span></span><br><span class="line">    <span class="keyword">int</span> ref;</span><br><span class="line">    <span class="comment">// 页帧的状态 Reserve 表示是否被内核保留 另一个是 表示是否 可分配</span></span><br><span class="line">    <span class="keyword">uint32_t</span> flags;</span><br><span class="line">    <span class="comment">// 记录连续空闲页块的数量 只在第一块进行设置</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> property;</span><br><span class="line">    <span class="comment">// 用于将所有的页帧串在一个双向链表中 这个地方很有趣 直接将 Page 这个结构体加入链表中会有点浪费空间 因此在 Page 中设置一个链表的结点 将其结点加入到链表中 还原的方法是将 链表中的 page_link 的地址 减去它所在的结构体中的偏移 就得到了 Page 的起始地址</span></span><br><span class="line">    <span class="keyword">list_entry_t</span> page_link;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化空闲页块链表</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">default_init</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    list_init(&amp;free_list);</span><br><span class="line">    nr_free = <span class="number">0</span>; <span class="comment">// 空闲页块一开始是0个</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 初始化n个空闲页块</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">default_init_memmap</span><span class="params">(struct Page *base, <span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">    assert(n &gt; <span class="number">0</span>);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">p</span> = <span class="title">base</span>;</span></span><br><span class="line">    <span class="keyword">for</span> (; p != base + n; p ++) &#123;</span><br><span class="line">        assert(PageReserved(p)); <span class="comment">// 看看这个页是不是被内核保留的</span></span><br><span class="line">        p-&gt;flags = p-&gt;property = <span class="number">0</span>;</span><br><span class="line">        set_page_ref(p, <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    base-&gt;property = n; <span class="comment">// 头一个空闲页块 要设置数量</span></span><br><span class="line">    SetPageProperty(base);</span><br><span class="line">    nr_free += n;</span><br><span class="line">    <span class="comment">// 初始化玩每个空闲页后 将其要插入到链表每次都插入到节点前面 因为是按地址排序</span></span><br><span class="line">    list_add_before(&amp;free_list, &amp;(base-&gt;page_link));</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 分配n个页块</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct Page * <span class="title">default_alloc_pages</span><span class="params">(<span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">    assert(n &gt; <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">if</span> (n &gt; nr_free) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">page</span> = <span class="title">NULL</span>;</span></span><br><span class="line">    <span class="keyword">list_entry_t</span> *le = &amp;free_list;</span><br><span class="line">    <span class="comment">// 查找 n 个或以上 空闲页块 若找到 则判断是否大过 n 则将其拆分 并将拆分后的剩下的空闲页块加回到链表中</span></span><br><span class="line">    <span class="keyword">while</span> ((le = list_next(le)) != &amp;free_list) &#123;</span><br><span class="line">        <span class="comment">// 此处 le2page 就是将 le 的地址 - page_link 在 Page 的偏移 从而找到 Page 的地址</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">p</span> = <span class="title">le2page</span>(<span class="title">le</span>, <span class="title">page_link</span>);</span></span><br><span class="line">        <span class="keyword">if</span> (p-&gt;property &gt;= n) &#123;</span><br><span class="line">            page = p;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (page != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> (page-&gt;property &gt; n) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">p</span> = <span class="title">page</span> + <span class="title">n</span>;</span></span><br><span class="line">            p-&gt;property = page-&gt;property - n;</span><br><span class="line">            SetPageProperty(p);</span><br><span class="line">            <span class="comment">// 将多出来的插入到 被分配掉的页块 后面</span></span><br><span class="line">            list_add(&amp;(page-&gt;page_link), &amp;(p-&gt;page_link));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 最后在空闲页链表中删除掉原来的空闲页</span></span><br><span class="line">        list_del(&amp;(page-&gt;page_link));</span><br><span class="line">        nr_free -= n;</span><br><span class="line">        ClearPageProperty(page);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> page;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 释放掉 n 个 页块</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">default_free_pages</span><span class="params">(struct Page *base, <span class="keyword">size_t</span> n)</span> </span>&#123;</span><br><span class="line">    assert(n &gt; <span class="number">0</span>);</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">p</span> = <span class="title">base</span>;</span></span><br><span class="line">    <span class="keyword">for</span> (; p != base + n; p ++) &#123;</span><br><span class="line">        assert(!PageReserved(p) &amp;&amp; !PageProperty(p));</span><br><span class="line">        p-&gt;flags = <span class="number">0</span>;</span><br><span class="line">        set_page_ref(p, <span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    base-&gt;property = n;</span><br><span class="line">    SetPageProperty(base);</span><br><span class="line">    <span class="keyword">list_entry_t</span> *le = list_next(&amp;free_list);</span><br><span class="line">    <span class="comment">// 合并到合适的页块中</span></span><br><span class="line">    <span class="keyword">while</span> (le != &amp;free_list) &#123;</span><br><span class="line">        p = le2page(le, page_link);</span><br><span class="line">        le = list_next(le);</span><br><span class="line">        <span class="keyword">if</span> (base + base-&gt;property == p) &#123;</span><br><span class="line">            base-&gt;property += p-&gt;property;</span><br><span class="line">            ClearPageProperty(p);</span><br><span class="line">            list_del(&amp;(p-&gt;page_link));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (p + p-&gt;property == base) &#123;</span><br><span class="line">            p-&gt;property += base-&gt;property;</span><br><span class="line">            ClearPageProperty(base);</span><br><span class="line">            base = p;</span><br><span class="line">            list_del(&amp;(p-&gt;page_link));</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    nr_free += n;</span><br><span class="line">    le = list_next(&amp;free_list);</span><br><span class="line">    <span class="comment">// 将合并好的合适的页块添加回空闲页块链表</span></span><br><span class="line">    <span class="keyword">while</span> (le != &amp;free_list) &#123;</span><br><span class="line">        p = le2page(le, page_link);</span><br><span class="line">        <span class="keyword">if</span> (base + base-&gt;property &lt;= p) &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        le = list_next(le);</span><br><span class="line">    &#125;</span><br><span class="line">    list_add_before(le, &amp;(base-&gt;page_link));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请在实验报告中简要说明你的设计实现过程。请回答如下问题：</p>
<ul>
<li>你的first fit算法是否有进一步的改进空间</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">可以像 Buddy System 用树来管理空闲页块 搜索的时间复杂度 为 O(logn)</span><br></pre></td></tr></table></figure>

<h3 id="练习2：实现寻找虚拟地址对应的页表项"><a href="#练习2：实现寻找虚拟地址对应的页表项" class="headerlink" title="练习2：实现寻找虚拟地址对应的页表项"></a>练习2：实现寻找虚拟地址对应的页表项</h3><p>通过设置页表和对应的页表项，可建立虚拟内存地址和物理内存地址的对应关系。其中的get_pte函数是设置页表项环节中的一个重要步骤。此函数找到一个虚地址对应的二级页表项的内核虚地址，如果此二级页表项不存在，则分配一个包含此项的二级页表。本练习需要补全get_pte函数 in kern/mm/pmm.c，实现其功能。请仔细查看和理解get_pte函数中的注释。get_pte函数的调用关系图如下所示：</p>
<p><img src="/images/lab2_ex.2.png"></p>
<p>这道题和下面一道题比较简单<br>原理就是 给我一个虚拟地址 然后我根据这个虚拟地址 的 高 10 位 找到 页目录表 中的 PDE项 前20位是页表项 (二级页表)的线性地址 后 12位 为属性 然后 判断一下 PDE 是否存在(就是判断 P位) 不存在 则 获取一个物理页 然后将这个物理页的线性地址写入到 PDE 中 最后返回 PTE 项<br>换句话说 就是 根据 给的 虚拟地址 构造一个 PTE 项 跟着注释来很容易就解决了</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">pte_t</span> *<span class="title">get_pte</span><span class="params">(<span class="keyword">pde_t</span> *pgdir, <span class="keyword">uintptr_t</span> la, <span class="keyword">bool</span> create)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">pde_t</span> *pdep = &amp;pgdir[PDX(la)]; <span class="comment">// 找到 PDE 这里的 pgdir 可以看做是 页目录表的基址</span></span><br><span class="line">    <span class="keyword">if</span> (!(*pdep &amp; PTE_P)) &#123;         <span class="comment">// 看看 PDE 指向的页表 是否存在</span></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">Page</span>* <span class="title">page</span> = <span class="title">alloc_page</span>();</span> <span class="comment">// 不存在就申请一页物理页</span></span><br><span class="line">        <span class="comment">/* 这里说多几句 通过 default_alloc_pages() 分配的页 的地址 并不是真正的页分配的地址</span></span><br><span class="line"><span class="comment">            实际上只是 Page 这个结构体所在的地址而已 故而需要 通过使用 page2pa() 将 Page 这个结构体</span></span><br><span class="line"><span class="comment">            的地址 转换成真正的物理页地址的线性地址 然后需要注意的是 无论是 * 或是 memset 都是对虚拟地址进行操作的</span></span><br><span class="line"><span class="comment">            所以需要将 真正的物理页地址再转换成 内核虚拟地址</span></span><br><span class="line"><span class="comment">            */</span></span><br><span class="line">        <span class="keyword">if</span> (!create || page == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        set_page_ref(page, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">uintptr_t</span> pa = page2pa(page);</span><br><span class="line">        <span class="built_in">memset</span>(KADDR(pa), <span class="number">0</span>, PGSIZE); <span class="comment">// 将这一页清空 此时将 线性地址转换为内核虚拟地址</span></span><br><span class="line">        *pdep = pa | PTE_U | PTE_W | PTE_P; <span class="comment">// 设置 PDE 权限</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> &amp;((<span class="keyword">pte_t</span> *)KADDR(PDE_ADDR(*pdep)))[PTX(la)];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请在实验报告中简要说明你的设计实现过程。请回答如下问题：</p>
<ul>
<li>请描述页目录项（Page Directory Entry）和页表项（Page Table Entry）中每个组成部分的含义以及对ucore而言的潜在用处。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">PDE 和 PTE 的组成部分含义在上面</span><br></pre></td></tr></table></figure>

<ul>
<li>如果ucore执行过程中访问内存，出现了页访问异常，请问硬件要做哪些事情？</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">进行换页操作 首先 CPU 将产生页访问异常的线性地址 放到 cr2 寄存器中 </span><br><span class="line">然后就是和普通的中断一样 保护现场 将寄存器的值压入栈中 </span><br><span class="line">然后压入 error_code 中断服务例程将外存的数据换到内存中来 </span><br><span class="line">最后 退出中断 回到进入中断前的状态</span><br></pre></td></tr></table></figure>

<h3 id="练习3：释放某虚地址所在的页并取消对应二级页表项的映射"><a href="#练习3：释放某虚地址所在的页并取消对应二级页表项的映射" class="headerlink" title="练习3：释放某虚地址所在的页并取消对应二级页表项的映射"></a>练习3：释放某虚地址所在的页并取消对应二级页表项的映射</h3><p>当释放一个包含某虚地址的物理内存页时，需要让对应此物理内存页的管理数据结构Page做相关的清除处理，使得此物理内存页成为空闲；另外还需把表示虚地址与物理地址对应关系的二级页表项清除。请仔细查看和理解page_remove_pte函数中的注释。为此，需要补全在 kern/mm/pmm.c中的page_remove_pte函数。page_remove_pte函数的调用关系图如下所示：</p>
<p><img src="/images/lab2_ex.3.png"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">page_remove_pte</span><span class="params">(<span class="keyword">pde_t</span> *pgdir, <span class="keyword">uintptr_t</span> la, <span class="keyword">pte_t</span> *ptep)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ((*ptep &amp; PTE_P)) &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">page</span> = <span class="title">pte2page</span>(*<span class="title">ptep</span>);</span></span><br><span class="line">        <span class="keyword">if</span> (page_ref_dec(page) == <span class="number">0</span>) &#123; <span class="comment">// 若引用计数减一后为0 则释放该物理页</span></span><br><span class="line">            free_page(page);</span><br><span class="line">        &#125;</span><br><span class="line">        *ptep = <span class="number">0</span>; <span class="comment">// 清空 PTE</span></span><br><span class="line">        tlb_invalidate(pgdir, la); <span class="comment">// 刷新 tlb</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后测试一下是不是都通过了</p>
<p><img src="/images/lab2_finish.png" alt="lab2_finish"></p>
<p>请在实验报告中简要说明你的设计实现过程。请回答如下问题：</p>
<ul>
<li>数据结构Page的全局变量（其实是一个数组）的每一项与页表中的页目录项和页表项有无对应关系？如果有，其对应关系是啥？</li>
<li>如果希望虚拟地址与物理地址相等，则需要如何修改lab2，完成此事？ 鼓励通过编程来具体完成这个问题</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">有 比如 PG_reserved 这个表示的这个页是否被内核保留 与 页表项的 PTE_U 这个参数有关系</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">kern/mm/memlayout.h</span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> KERNBASE 0x0 改成0x0 完事</span></span><br><span class="line">修改虚拟地址基址 减去一个 <span class="number">0xC0000000</span> 就等于物理地址了</span><br></pre></td></tr></table></figure>

<p>Challenge 以后看看有没有时间做吧 这个 Lab 2 还是在机房里面阅读的…</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>内存分配</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 3 含 Challenge</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-3/</url>
    <content><![CDATA[<h3 id="Lab-3-练习补充"><a href="#Lab-3-练习补充" class="headerlink" title="Lab 3 练习补充"></a>Lab 3 练习补充</h3><p>在练习开始之前 先讲讲 两个数据结构</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">mm_struct</span> &#123;</span> <span class="comment">// 描述一个进程的虚拟地址空间 每个进程的 pcb 中 会有一个指针指向本结构体</span></span><br><span class="line">    <span class="keyword">list_entry_t</span> mmap_list;        <span class="comment">// 链接同一页目录表的虚拟内存空间 的 双向链表的 头节点 </span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">vma_struct</span> *<span class="title">mmap_cache</span>;</span> <span class="comment">// 当前正在使用的虚拟内存空间</span></span><br><span class="line">    <span class="keyword">pde_t</span> *pgdir;                  <span class="comment">// mm_struct 所维护的页表地址(拿来找 PTE)</span></span><br><span class="line">    <span class="keyword">int</span> map_count;                 <span class="comment">// 虚拟内存块的数目</span></span><br><span class="line">    <span class="keyword">void</span> *sm_priv;                 <span class="comment">// 记录访问情况链表头地址(用于置换算法)</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">vma_struct</span> &#123;</span> <span class="comment">// 虚拟内存空间</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">mm_struct</span> *<span class="title">vm_mm</span>;</span> <span class="comment">// 虚拟内存空间属于的进程</span></span><br><span class="line">    <span class="keyword">uintptr_t</span> vm_start; <span class="comment">// 连续地址的虚拟内存空间的起始位置和结束位置</span></span><br><span class="line">    <span class="keyword">uintptr_t</span> vm_end;</span><br><span class="line">    <span class="keyword">uint32_t</span> vm_flags; <span class="comment">// 虚拟内存空间的属性 (读/写/执行)</span></span><br><span class="line">    <span class="keyword">list_entry_t</span> list_link; <span class="comment">// 双向链表 从小到大将虚拟内存空间链接起来</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<a id="more"></a>
<p>总而言之就是 mm_struct 描述了整个进程的虚拟地址空间 而 vma_struct 描述了 进程中的一小部分虚拟内存空间<br><img src="/images/mm_vma.png" alt="mm_vma"></p>
<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>本实验依赖实验1/2。请把你做的实验1/2的代码填入本实验中代码中有“LAB1”,“LAB2”的注释相应部分。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">就下面三个 复制过去就好</span><br><span class="line">pmm.c default_pmm.c trap.c</span><br></pre></td></tr></table></figure>

<h3 id="练习1：给未被映射的地址映射上物理页"><a href="#练习1：给未被映射的地址映射上物理页" class="headerlink" title="练习1：给未被映射的地址映射上物理页"></a>练习1：给未被映射的地址映射上物理页</h3><p>完成do_pgfault（mm/vmm.c）函数，给未被映射的地址映射上物理页。设置访问权限 的时候需要参考页面所在 VMA 的权限，同时需要注意映射物理页时需要操作内存控制 结构所指定的页表，而不是内核的页表。注意：在LAB3 EXERCISE 1处填写代码。执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make qemu</span><br></pre></td></tr></table></figure>
<p>后，如果通过check_pgfault函数的测试后，会有“check_pgfault() succeeded!”的输出，表示练习1基本正确。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">do_pgfault()</span><br><span class="line"> ptep = get_pte(mm-&gt;pgdir, addr, <span class="number">1</span>); <span class="comment">// 根据引发缺页异常的地址 去找到 地址所对应的 PTE 如果找不到 则创建一页表</span></span><br><span class="line">    <span class="keyword">if</span> (*ptep == <span class="number">0</span>) &#123; <span class="comment">// PTE 所指向的 物理页表地址 若不存在 则分配一物理页并将逻辑地址和物理地址作映射 (就是让 PTE 指向 物理页帧)</span></span><br><span class="line">        <span class="keyword">if</span> (pgdir_alloc_page(mm-&gt;pgdir, addr, perm) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> failed;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">// 如果 PTE 存在 说明此时 P 位为 0 该页被换出到外存中 需要将其换入内存</span></span><br><span class="line">        <span class="keyword">if</span>(swap_init_ok) &#123; <span class="comment">// 是否可以换入页面</span></span><br><span class="line">            struct Page *page = <span class="literal">NULL</span>;</span><br><span class="line">            ret = swap_in(mm, addr, &amp;page); <span class="comment">// 根据 PTE 找到 换出那页所在的硬盘地址 并将其从外存中换入</span></span><br><span class="line">            <span class="keyword">if</span> (ret != <span class="number">0</span>) &#123;</span><br><span class="line">                cprintf(<span class="string">"swap_in in do_pgfault failed\n"</span>);</span><br><span class="line">                <span class="keyword">goto</span> failed;</span><br><span class="line">            &#125;</span><br><span class="line">            page_insert(mm-&gt;pgdir, page, addr, perm); <span class="comment">// 建立虚拟地址和物理地址之间的对应关系(更新 PTE 因为 已经被换入到内存中了)</span></span><br><span class="line">            swap_map_swappable(mm, addr, page, <span class="number">0</span>); <span class="comment">// 使这一页可以置换</span></span><br><span class="line">            page-&gt;pra_vaddr = addr; <span class="comment">// 设置 这一页的虚拟地址</span></span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>
<ul>
<li>请描述页目录项（Page Directory Entry）和页表项（Page Table Entry）中组成部分对ucore实现页替换算法的潜在用处。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">其实很想说这道题之前不是问过了吗?...</span><br></pre></td></tr></table></figure>

<ul>
<li>AVL CPU 不理会这个属性 可以不管 (有可能在32位系统使用大过 4G内存的时候 用到这几位) </li>
<li>G Global 全局位 表示是否将虚拟地址与物理地址的转换结果缓存到 TLB 中</li>
<li>D Dirty 脏页位 当 CPU 对这个页进行写操作时 会置 1</li>
<li>PAT Page Attribute Table 页属性表位 置 0</li>
<li>A Accessed 访问位 若为 1 则 说明 CPU 访问过了 CPU 会定时清 0 记录被置 1 的频率 当内存不足时 会将 使用频率较低的页面换出到外存 同时将 P位 置 0 下次访问 该页时 会引起 Pagefault 异常 中断处理程序再将此页换上</li>
<li>PCD Page-level Cache Disable 页级高速缓存位 置 0 即可 读的时候 高速缓存是否有效 若有效则直接从高速缓存中读出 若无效的话 则必须实实在在的从 I/O 端口去读数据</li>
<li>PWT Page-level Write-Through 页级通写位 控制是先写到高速缓存里再慢慢回写到内存里 还是 直接慢慢写到内存里</li>
<li>US User/Superviosr 普通用户/超级用户位</li>
<li>RW Read/Write 读写位</li>
<li>P Present 存在位 (虚拟页式存储的关键位 若为 0 则发起缺页异常)</li>
</ul>
<p><img src="/images/pagetableentries.png" alt="pagetable_entries"></p>
<ul>
<li>如果ucore的缺页服务例程在执行过程中访问内存，出现了页访问异常，请问硬件要做哪些事情？</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">页访问异常 会将产生页访问异常的线性地址存入 cr2 寄存器中 并且给出 错误码 error_code 说明是页访问异常的具体原因</span><br><span class="line">uCore OS 会将其 存入 struct trapframe 中 tf_err 等到中断服务例程 调用页访问异常处理函数(do_pgfault()) 时</span><br><span class="line">再判断 具体原因 </span><br><span class="line">若不在某个VMA的地址范围内 或 不满足正确的读写权限 则是非法访问</span><br><span class="line">若在此范围 且 权限也正确 则 认为是 合法访问 只是没有建立虚实对应关系 应分配一页 并修改页表 完成 虚拟地址到 物理地址的映射 刷新 TLB 最后再 调用 iret 重新执行引发页访问异常的 那条指令</span><br><span class="line">若是在外存中 则将其换入 内存 刷新 TLB 然后退出中断服务例程 重新执行引发页访问异常的 那条指令</span><br></pre></td></tr></table></figure>

<h3 id="练习2：补充完成基于FIFO的页面替换算法"><a href="#练习2：补充完成基于FIFO的页面替换算法" class="headerlink" title="练习2：补充完成基于FIFO的页面替换算法"></a>练习2：补充完成基于FIFO的页面替换算法</h3><p>完成vmm.c中的do_pgfault函数，并且在实现FIFO算法的swap_fifo.c中完成map_swappable和swap_out_victim函数。通过对swap的测试。注意：在LAB3 EXERCISE 2处填写代码。执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">make qemu</span><br></pre></td></tr></table></figure>
<p>后，如果通过check_swap函数的测试后，会有“check_swap() succeeded!”的输出，表示练习2基本正确。<br>请在实验报告中简要说明你的设计实现过程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">此时完成的是 FIFO 置换算法 因此 每次换出的都应该是 最先进来的 页</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> _fifo_map_swappable(struct mm_struct *mm, <span class="keyword">uintptr_t</span> addr, struct Page *page, <span class="keyword">int</span> swap_in) &#123;</span><br><span class="line">    <span class="keyword">list_entry_t</span> *head=(<span class="keyword">list_entry_t</span>*) mm-&gt;sm_priv;</span><br><span class="line">    <span class="keyword">list_entry_t</span> *entry=&amp;(page-&gt;pra_page_link);</span><br><span class="line"> </span><br><span class="line">    assert(entry != <span class="literal">NULL</span> &amp;&amp; head != <span class="literal">NULL</span>);</span><br><span class="line"></span><br><span class="line">    list_add(head, entry); <span class="comment">// 就是将这一页加入到链表头中(最近访问过的放前面) 使其可以被置换算法使用到</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> _fifo_swap_out_victim(struct mm_struct *mm, struct Page ** ptr_page, <span class="keyword">int</span> in_tick) &#123;</span><br><span class="line">    <span class="keyword">list_entry_t</span> *head=(<span class="keyword">list_entry_t</span>*) mm-&gt;sm_priv;</span><br><span class="line">    assert(head != <span class="literal">NULL</span>);</span><br><span class="line">    assert(in_tick==<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">list_entry_t</span> *le = head-&gt;prev; <span class="comment">// 换出最先进来的页 (因为每次访问一个页 都是插入到头节点的后面 因此 头节点的前面就是最先访问的页)</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span>* <span class="title">page</span> = <span class="title">le2page</span>(<span class="title">le</span>, <span class="title">pra_page_link</span>);</span> <span class="comment">// 和之前一样 通过 le 这个链表节点的地址 减去 pra_page_link 在 Page 结构体中的 Offset 得到 Page 的地址</span></span><br><span class="line">    list_del(le); <span class="comment">// 删掉这个节点</span></span><br><span class="line">    *ptr_page = page; <span class="comment">// 将这一页地址存到 ptr_page 中 给 调用本函数的函数使用</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>请在实验报告中回答如下问题：</p>
<ul>
<li>如果要在ucore上实现”extended clock页替换算法”请给你的设计方案，现有的swap_manager框架是否足以支持在ucore中实现此算法？如果是，请给你的设计方案。如果不是，请给出你的新的扩展和基此扩展的设计方案。并需要回答如下问题<ul>
<li>需要被换出的页的特征是什么？</li>
<li>在ucore中如何判断具有这样特征的页？</li>
<li>何时进行换入和换出操作？</li>
</ul>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当然能够支持</span><br><span class="line">首选 页表项的 Dirty Bit 为 0 的页 且 Access Bit 为 0 的页 其次是 访问了但没修改的页 最次是 访问了修改了的页</span><br><span class="line">!(*ptep &amp; PTE_A) &amp;&amp; !(*ptep &amp; PTE_D)  没被访问过 也没被修改过</span><br><span class="line">(*ptep &amp; PTE_A) &amp;&amp; !(*ptep &amp; PTE_D) 被访问过 但没被修改过</span><br><span class="line">!(*ptep &amp; PTE_A) &amp;&amp; (*ptep &amp; PTE_D) 没被访问过 但被修改过</span><br><span class="line">换入是在缺页异常的时候 换出是在物理页帧满的时候</span><br></pre></td></tr></table></figure>

<p>至此 这两道题就完成了 比之前的 Lab 要简单多了 这次只花了 一天半时间 下面是 Challenge 花了我一个小时(跑出去饭堂吃饭 哈哈哈哈哈 0.0)</p>
<h3 id="扩展练习-Challenge-1：实现识别dirty-bit的-extended-clock页替换算法"><a href="#扩展练习-Challenge-1：实现识别dirty-bit的-extended-clock页替换算法" class="headerlink" title="扩展练习 Challenge 1：实现识别dirty bit的 extended clock页替换算法"></a>扩展练习 Challenge 1：实现识别dirty bit的 extended clock页替换算法</h3><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">swap_fifo.c</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">swap_manager</span> <span class="title">swap_manager_fifo</span> = &#123;</span></span><br><span class="line">     .name            = <span class="string">"fifo swap manager"</span>,</span><br><span class="line">     .init            = &amp;_fifo_init,</span><br><span class="line">     .init_mm         = &amp;_fifo_init_mm,</span><br><span class="line">     .tick_event      = &amp;_fifo_tick_event,</span><br><span class="line">     .map_swappable   = &amp;_fifo_map_swappable,</span><br><span class="line">     .set_unswappable = &amp;_fifo_set_unswappable,</span><br><span class="line">    <span class="comment">//  .swap_out_victim = &amp;_fifo_swap_out_victim,</span></span><br><span class="line">     .swap_out_victim = &amp;_extended_clock_swap_out_victim, <span class="comment">// 将 选择换出的页的函数改掉</span></span><br><span class="line">     .check_swap      = &amp;_fifo_check_swap,</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> _extended_clock_swap_out_victim(struct mm_struct *mm, struct Page ** ptr_page, <span class="keyword">int</span> in_tick) &#123;</span><br><span class="line">    <span class="keyword">list_entry_t</span> *head = (<span class="keyword">list_entry_t</span>*)mm-&gt;sm_priv;</span><br><span class="line">    assert(head != <span class="literal">NULL</span>);</span><br><span class="line">    assert(in_tick == <span class="number">0</span>);</span><br><span class="line">    <span class="keyword">list_entry_t</span> *le = head-&gt;prev;</span><br><span class="line">    assert(head != le);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i; <span class="comment">// 循环三次 寻找合适的置换页</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">2</span>; i++) &#123;</span><br><span class="line">        <span class="comment">/* 第一次循环 寻找 没被访问过的 且 没被修改过的 同时将被访问过的页的 访问位 清 0</span></span><br><span class="line"><span class="comment">            第二次循环 依然是寻找 没被访问过的 且 没被修改过的 因为到了此次循环 访问位都被清 0 了 不存在被访问过的</span></span><br><span class="line"><span class="comment">            只需要找没被修改过的即可 同时将被修改过的页 修改位 清 0</span></span><br><span class="line"><span class="comment">            第三次循环 还是找 没被访问过 且 没被修改过的 此时 第一次循环 已经将所有访问位 清 0 了</span></span><br><span class="line"><span class="comment">             第二次循环 也已经将所有修改位清 0 了 故 在第三次循环 一定有 没被访问过 也没被修改过的 页</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">        <span class="keyword">while</span> (le != head) &#123;</span><br><span class="line">            <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">page</span> = <span class="title">le2page</span>(<span class="title">le</span>, <span class="title">pra_page_link</span>);</span>            </span><br><span class="line">            <span class="keyword">pte_t</span> *ptep = get_pte(mm-&gt;pgdir, page-&gt;pra_vaddr, <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (!(*ptep &amp; PTE_A) &amp;&amp; !(*ptep &amp; PTE_D)) &#123; <span class="comment">// 没被访问过 也没被修改过 </span></span><br><span class="line">                list_del(le);</span><br><span class="line">                *ptr_page = page;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (i == <span class="number">0</span>) &#123;</span><br><span class="line">                *ptep &amp;= <span class="number">0xFFFFFFDF</span>;</span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (i == <span class="number">1</span>) &#123;</span><br><span class="line">                *ptep &amp;= <span class="number">0xFFFFFFBF</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            le = le-&gt;prev;</span><br><span class="line">        &#125;</span><br><span class="line">        le = le-&gt;prev;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后放上 运行结果<br><img src="/images/lab3_finish.png" alt="lab3_finish"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>内存分配</tag>
        <tag>虚拟存储</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 4</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-4/</url>
    <content><![CDATA[<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>本实验依赖实验1/2/3。请把你做的实验1/2/3的代码填入本实验中代码中有“LAB1”,“LAB2”,“LAB3”的注释相应部分。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vmm.c trap.c default_pmm.c pmm.c swap_fifo.c 这几个补上去就完事了</span><br></pre></td></tr></table></figure>

<h3 id="练习1：分配并初始化一个进程控制块"><a href="#练习1：分配并初始化一个进程控制块" class="headerlink" title="练习1：分配并初始化一个进程控制块"></a>练习1：分配并初始化一个进程控制块</h3><p>alloc_proc函数（位于kern/process/proc.c中）负责分配并返回一个新的struct proc_struct结构，用于存储新建立的内核线程的管理信息。ucore需要对这个结构进行最基本的初始化，你需要完成这个初始化过程。</p>
<blockquote>
<p>【提示】在alloc_proc函数的实现中，需要初始化的proc_struct结构中的成员变量至少包括：state/pid/runs/kstack/need_resched/parent/mm/context/tf/cr3/flags/name。</p>
</blockquote>
<a id="more"></a>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">很简单 按照进程控制块的内容填写就行了</span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct *<span class="title">alloc_proc</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proc_struct</span> *<span class="title">proc</span> = <span class="title">kmalloc</span>(<span class="title">sizeof</span>(<span class="title">struct</span> <span class="title">proc_struct</span>));</span></span><br><span class="line">    <span class="keyword">if</span> (proc != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        proc-&gt;state = PROC_UNINIT; <span class="comment">// 进程状态</span></span><br><span class="line">        proc-&gt;pid = <span class="number">-1</span>; <span class="comment">// 进程ID</span></span><br><span class="line">        proc-&gt;runs = <span class="number">0</span>; <span class="comment">// 进程时间片</span></span><br><span class="line">        proc-&gt;kstack = <span class="number">0</span>; <span class="comment">// 进程所使用的内存栈地址</span></span><br><span class="line">        proc-&gt;need_resched = <span class="literal">NULL</span>; <span class="comment">// 进程是否能被调度</span></span><br><span class="line">        proc-&gt;parent = <span class="literal">NULL</span>; <span class="comment">// 父进程</span></span><br><span class="line">        proc-&gt;mm = <span class="literal">NULL</span>; <span class="comment">// 进程所用的虚拟内存</span></span><br><span class="line">        <span class="built_in">memset</span>(&amp;(proc-&gt;context), <span class="number">0</span>, <span class="keyword">sizeof</span>(struct context)); <span class="comment">// 进程的上下文</span></span><br><span class="line">        proc-&gt;tf = <span class="literal">NULL</span>; <span class="comment">// 中断帧指针</span></span><br><span class="line">        proc-&gt;cr3 = boot_cr3; <span class="comment">// 页目录表地址 设为 内核页目录表基址</span></span><br><span class="line">        proc-&gt;flags = <span class="number">0</span>; <span class="comment">// 标志位</span></span><br><span class="line">        <span class="built_in">memset</span>(&amp;(proc-&gt;name), <span class="number">0</span>, PROC_NAME_LEN); <span class="comment">// 进程名</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> proc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>请在实验报告中简要说明你的设计实现过程。请回答如下问题：</p>
<ul>
<li>请说明proc_struct中struct context context和struct trapframe *tf成员变量含义和在本实验中的作用是啥？（提示通过看代码和编程调试可以判断出来）</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">context 中 保存着各种寄存器的内容 这是为了保存进程上下文 为进程调度做准备</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">context</span> &#123;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> eip;</span><br><span class="line">    <span class="keyword">uint32_t</span> esp;</span><br><span class="line">    <span class="keyword">uint32_t</span> ebx;</span><br><span class="line">    <span class="keyword">uint32_t</span> ecx;</span><br><span class="line">    <span class="keyword">uint32_t</span> edx;</span><br><span class="line">    <span class="keyword">uint32_t</span> esi;</span><br><span class="line">    <span class="keyword">uint32_t</span> edi;</span><br><span class="line">    <span class="keyword">uint32_t</span> ebp;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">trapframe 保存着 用于特权级转换的 栈 esp 寄存器</span><br><span class="line"></span><br><span class="line">当进程发生特权级转换的时候 中断帧记录了进入中断时任务的上下文 当退出中断时 恢复环境</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">pushregs</span> &#123;</span></span><br><span class="line">        <span class="keyword">uint32_t</span> reg_edi;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_esi;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_ebp;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_oesp;          <span class="comment">/* Useless */</span></span><br><span class="line">        <span class="keyword">uint32_t</span> reg_ebx;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_edx;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_ecx;</span><br><span class="line">        <span class="keyword">uint32_t</span> reg_eax;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_gs;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding0;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_fs;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding1;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_es;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding2;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_ds;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding3;</span><br><span class="line">    <span class="keyword">uint32_t</span> tf_trapno;</span><br><span class="line">    <span class="comment">/* below here defined by x86 hardware */</span></span><br><span class="line">    <span class="keyword">uint32_t</span> tf_err;</span><br><span class="line">    <span class="keyword">uintptr_t</span> tf_eip;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_cs;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding4;</span><br><span class="line">    <span class="keyword">uint32_t</span> tf_eflags;</span><br><span class="line">    <span class="comment">/* below here only when crossing rings, such as from user to kernel */</span></span><br><span class="line">    <span class="keyword">uintptr_t</span> tf_esp;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_ss;</span><br><span class="line">    <span class="keyword">uint16_t</span> tf_padding5;</span><br><span class="line">&#125; __attribute__((packed));</span><br></pre></td></tr></table></figure>

<p><img src="/images/pcb.png" alt="pcb"></p>
<h3 id="练习2：为新创建的内核线程分配资源"><a href="#练习2：为新创建的内核线程分配资源" class="headerlink" title="练习2：为新创建的内核线程分配资源"></a>练习2：为新创建的内核线程分配资源</h3><p>创建一个内核线程需要分配和设置好很多资源。kernel_thread函数通过调用do_fork函数完成具体内核线程的创建工作。do_kernel函数会调用alloc_proc函数来分配并初始化一个进程控制块，但alloc_proc只是找到了一小块内存用以记录进程的必要信息，并没有实际分配这些资源。ucore一般通过do_fork实际创建新的内核线程。do_fork的作用是，创建当前内核线程的一个副本，它们的执行上下文、代码、数据都一样，但是存储位置不同。在这个过程中，需要给新内核线程分配资源，并且复制原进程的状态。你需要完成在kern/process/proc.c中的do_fork函数中的处理过程。它的大致执行步骤包括：</p>
<ul>
<li>调用alloc_proc，首先获得一块用户信息块。</li>
<li>为进程分配一个内核栈。</li>
<li>复制原进程的内存管理信息到新进程（但内核线程不必做此事）</li>
<li>复制原进程上下文到新进程</li>
<li>将新进程添加到进程列表</li>
<li>唤醒新进程</li>
<li>返回新进程号</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">跟着注释做就是了</span><br><span class="line">先是创建一个进程控制块 然后设置进程控制块</span><br><span class="line">要特别注意的是 设置 进程/线程 的 PID 调用的 get_pid() 方法 和 添加进程/线程到链表中时 要将中断暂时关闭</span><br><span class="line">避免执行的中途被再次中断</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_fork</span><span class="params">(<span class="keyword">uint32_t</span> clone_flags, <span class="keyword">uintptr_t</span> <span class="built_in">stack</span>, struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ((proc = alloc_proc()) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> fork_out;</span><br><span class="line">    &#125;</span><br><span class="line">    proc-&gt;parent = current;</span><br><span class="line">    <span class="keyword">if</span> (setup_kstack(proc) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_fork_cleanup_proc;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (copy_mm(clone_flags, proc) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_fork_cleanup_kstack;</span><br><span class="line">    &#125;</span><br><span class="line">    copy_thread(proc, <span class="built_in">stack</span>, tf);</span><br><span class="line">    <span class="keyword">bool</span> intr_flag;</span><br><span class="line">    local_intr_save(intr_flag);</span><br><span class="line">    &#123;</span><br><span class="line">        proc-&gt;pid = get_pid();</span><br><span class="line">        hash_proc(proc);</span><br><span class="line">        nr_process++;</span><br><span class="line">        list_add(&amp;proc_list, &amp;(proc-&gt;list_link));</span><br><span class="line">    &#125;</span><br><span class="line">    local_intr_restore(intr_flag);</span><br><span class="line"></span><br><span class="line">    wakeup_proc(proc);</span><br><span class="line">    ret = proc-&gt;pid;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请在实验报告中简要说明你的设计实现过程。请回答如下问题：</p>
<ul>
<li>请说明ucore是否做到给每个新fork的线程一个唯一的id？请说明你的分析和理由。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">可以保证 每个fork的线程 唯一ID</span><br><span class="line">调用的 get_pid() 函数 每次都从 进程控制块链表中 找到合适的ID</span><br></pre></td></tr></table></figure>

<h3 id="练习3：阅读代码，理解-proc-run-函数和它调用的函数如何完成进程切换的"><a href="#练习3：阅读代码，理解-proc-run-函数和它调用的函数如何完成进程切换的" class="headerlink" title="练习3：阅读代码，理解 proc_run 函数和它调用的函数如何完成进程切换的"></a>练习3：阅读代码，理解 proc_run 函数和它调用的函数如何完成进程切换的</h3><p>请在实验报告中简要说明你对proc_run函数的分析。并回答如下问题：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">当前进程/线程 切换到 proc 这个进程/线程</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">proc_run</span><span class="params">(struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (proc != current) &#123;</span><br><span class="line">        <span class="keyword">bool</span> intr_flag;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">proc_struct</span> *<span class="title">prev</span> = <span class="title">current</span>, *<span class="title">next</span> = <span class="title">proc</span>;</span></span><br><span class="line">        local_intr_save(intr_flag); <span class="comment">// 关闭中断</span></span><br><span class="line">        &#123;</span><br><span class="line">            current = proc; <span class="comment">// 将当前进程换为 要切换到的进程</span></span><br><span class="line">            <span class="comment">// 设置任务状态段tss中的特权级0下的 esp0 指针为 next 内核线程 的内核栈的栈顶</span></span><br><span class="line">            load_esp0(next-&gt;kstack + KSTACKSIZE);</span><br><span class="line">            lcr3(next-&gt;cr3); <span class="comment">// 重新加载 cr3 寄存器(页目录表基址) 进行进程间的页表切换</span></span><br><span class="line">            switch_to(&amp;(prev-&gt;context), &amp;(next-&gt;context)); <span class="comment">// 调用 switch_to 进行上下文的保存与切换</span></span><br><span class="line">        &#125;</span><br><span class="line">        local_intr_restore(intr_flag);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>在本实验的执行过程中，创建且运行了几个内核线程？</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">两个内核线程 一个为 idle_proc 为 第 0 个内核线程 完成内核中的初始化 然后调度执行其他进程或线程</span><br><span class="line">另一个为 init_proc 本次实验的内核线程 只用来打印字符串</span><br></pre></td></tr></table></figure>

<ul>
<li>语句local_intr_save(intr_flag);….local_intr_restore(intr_flag);在这里有何作用?请说明理由</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">关闭中断 避免进程切换的中途 再被中断(其他进程再进行调度)</span><br></pre></td></tr></table></figure>

<p>最后还是附上执行结果 可以去🍚了 好冷 这周怕是 2018年 广东最冷的一周了<br><img src="/images/lab4_finish.png" alt="lab4_finish"></p>
<h4 id="讲讲-switch-to-切换进程"><a href="#讲讲-switch-to-切换进程" class="headerlink" title="讲讲 switch_to 切换进程"></a>讲讲 switch_to 切换进程</h4><p>吃完饭回来了 顺便讲讲这个 switch_to 的汇编</p>
<figure class="highlight x86asm"><table><tr><td class="code"><pre><span class="line">struct context &#123;</span><br><span class="line">    uint32_t <span class="built_in">eip</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">ebx</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">ecx</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">edx</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">esi</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">edi</span><span class="comment">;</span></span><br><span class="line">    uint32_t <span class="built_in">ebp</span><span class="comment">;</span></span><br><span class="line">&#125;<span class="comment">;</span></span><br><span class="line">switch_to 的作用是 保存当前进程的上下文 并且 恢复被调度上处理机的进程的上下文</span><br><span class="line"><span class="meta">.text</span></span><br><span class="line"><span class="meta">.globl</span> switch_to</span><br><span class="line"><span class="symbol">switch_to:</span>                      # switch_to(from, to)</span><br><span class="line"></span><br><span class="line">    调用 switch_to 后栈的情况  |     To    <span class="built_in">esp</span> + <span class="number">8</span> |</span><br><span class="line">                             |   From   <span class="built_in">esp</span> + <span class="number">4</span>  |</span><br><span class="line">                             |  <span class="keyword">Ret</span> Addr &lt;- <span class="built_in">esp</span>  |</span><br><span class="line">    movl <span class="number">4</span>(%esp), %eax       此处要取的是 From 因此为 <span class="built_in">esp</span> + <span class="number">4</span></span><br><span class="line">    popl <span class="number">0</span>(%eax)  <span class="keyword">pop</span> 掉返回地址 存到 From 的 <span class="built_in">eip</span> 中 From 的 context <span class="built_in">eip</span>为栈底 故而 context 的 <span class="built_in">eip</span> 当返回地址</span><br><span class="line">    movl %esp, <span class="number">4</span>(%eax)         这之后都只是将寄存器的内容 移到 context 相应的地址而已</span><br><span class="line">    movl %ebx, <span class="number">8</span>(%eax)</span><br><span class="line">    movl %ecx, <span class="number">12</span>(%eax)</span><br><span class="line">    movl %edx, <span class="number">16</span>(%eax)</span><br><span class="line">    movl %esi, <span class="number">20</span>(%eax)</span><br><span class="line">    movl %edi, <span class="number">24</span>(%eax)</span><br><span class="line">    movl %ebp, <span class="number">28</span>(%eax)</span><br><span class="line"></span><br><span class="line">    此时的 栈的情况                                     |    To  <span class="built_in">esp</span> + <span class="number">4</span>   |</span><br><span class="line">    因为在上面已经将 返回地址 <span class="keyword">pop</span>掉了 因此这里没有返回地址了  |   From &lt;- <span class="built_in">esp</span>    |</span><br><span class="line">    movl <span class="number">4</span>(%esp), %eax          此处要取 To 的地址 因为 返回地址之前已经被 <span class="keyword">pop</span>掉了 所以此时为 <span class="built_in">esp</span> + <span class="number">4</span></span><br><span class="line">  </span><br><span class="line">    movl <span class="number">28</span>(%eax), %ebp         这之后也没啥好说的 都是将 context 移到寄存器 恢复环境</span><br><span class="line">    movl <span class="number">24</span>(%eax), %edi</span><br><span class="line">    movl <span class="number">20</span>(%eax), %esi</span><br><span class="line">    movl <span class="number">16</span>(%eax), %edx</span><br><span class="line">    movl <span class="number">12</span>(%eax), %ecx</span><br><span class="line">    movl <span class="number">8</span>(%eax), %ebx</span><br><span class="line">    movl <span class="number">4</span>(%eax), %esp</span><br><span class="line"></span><br><span class="line">    pushl <span class="number">0</span>(%eax)      <span class="built_in">eip</span> 作返回地址 因为原先的 返回地址已经被<span class="keyword">pop</span>掉了</span><br><span class="line"></span><br><span class="line">    <span class="keyword">ret</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>进程</tag>
        <tag>线程</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 6</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-6/</url>
    <content><![CDATA[<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>请把你做的实验2/3/4/5的代码填入本实验中代码中有“LAB1”/“LAB2”/“LAB3”/“LAB4”“LAB5”的注释相应部分。并确保编译通过。注意：为了能够正确执行lab6的测试应用程序，可能需对已完成的实验1/2/3/4/5的代码进行进一步改进。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">复制以下文件 其中 proc.c 和 trap.c 需要进行修正</span><br><span class="line">vmm.c trap.c default_pmm.c pmm.c proc.c swap_fifo.c</span><br><span class="line">proc.c:</span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct *<span class="title">alloc_proc</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 添加如下几行 初始化进程所属就绪队列 初始化就绪队列指针 初始化时间片</span></span><br><span class="line">    proc-&gt;rq = <span class="literal">NULL</span>;</span><br><span class="line">    list_init(&amp;(proc-&gt;run_link));</span><br><span class="line">    proc-&gt;time_slice = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line">trap.c:</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">trap_dispatch</span><span class="params">(struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 在时钟中断下 添加以下几行 并去掉之前设置 进程需要调度标记</span></span><br><span class="line">    <span class="comment">// 这个标记现在已经被 调度程序所使用了 不再需要自己控制</span></span><br><span class="line">    ++ticks;</span><br><span class="line">    sched_class_proc_tick(current);</span><br><span class="line">    <span class="comment">// current-&gt;need_resched = 1;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="练习1-使用-Round-Robin-调度算法"><a href="#练习1-使用-Round-Robin-调度算法" class="headerlink" title="练习1: 使用 Round Robin 调度算法"></a>练习1: 使用 Round Robin 调度算法</h3><p>完成练习0后，建议大家比较一下（可用kdiff3等文件比较软件）个人完成的lab5和练习0完成后的刚修改的lab6之间的区别，分析了解lab6采用RR调度算法后的执行过程。执行make grade，大部分测试用例应该通过。但执行priority.c应该过不去。</p>
<p>请在实验报告中完成：</p>
<ul>
<li>请理解并分析sched_calss中各个函数指针的用法，并接合Round Robin 调度算法描述ucore的调度执行过程</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 初始化算法所需要的数据结构</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">RR_init</span><span class="params">(struct run_queue *rq)</span> </span>&#123;</span><br><span class="line">    list_init(&amp;(rq-&gt;run_list));</span><br><span class="line">    rq-&gt;proc_num = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 进程入队 将进程加入就绪队列(不同的就绪队列的时间片不同 也就是说有不同优先级的就绪队列)</span></span><br><span class="line"><span class="comment"> 在 RR调度中 </span></span><br><span class="line"><span class="comment"> 当进程时间片为0 或 应某种情况被阻塞 则将其加入到就绪队列 并将其 时间片进行重置 */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">RR_enqueue</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    assert(list_empty(&amp;(proc-&gt;run_link)));</span><br><span class="line">    list_add_before(&amp;(rq-&gt;run_list), &amp;(proc-&gt;run_link));</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;time_slice == <span class="number">0</span> || proc-&gt;time_slice &gt; rq-&gt;max_time_slice) &#123;</span><br><span class="line">        proc-&gt;time_slice = rq-&gt;max_time_slice;</span><br><span class="line">    &#125;</span><br><span class="line">    proc-&gt;rq = rq;</span><br><span class="line">    rq-&gt;proc_num ++;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 进程出队 将进程从就绪队列中删去</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">RR_dequeue</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    assert(!list_empty(&amp;(proc-&gt;run_link)) &amp;&amp; proc-&gt;rq == rq);</span><br><span class="line">    list_del_init(&amp;(proc-&gt;run_link));</span><br><span class="line">    rq-&gt;proc_num --;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 挑选出下一个进程 占用处理机去运行</span></span><br><span class="line"><span class="comment">   在 RR调度中 直接按照就绪队列的顺序轮询</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct * <span class="title">RR_pick_next</span><span class="params">(struct run_queue *rq)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">list_entry_t</span> *le = list_next(&amp;(rq-&gt;run_list));</span><br><span class="line">    <span class="keyword">if</span> (le != &amp;(rq-&gt;run_list)) &#123;</span><br><span class="line">        <span class="keyword">return</span> le2proc(le, run_link);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 时钟中断时 调用此函数 在 RR调度中 每次调用都会减少当前进程时间片</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">RR_proc_tick</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;time_slice &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        proc-&gt;time_slice --;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;time_slice == <span class="number">0</span>) &#123;</span><br><span class="line">        proc-&gt;need_resched = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Round Robin 调度执行过程</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">设置当前进程剩余时间片</span><br><span class="line">每次时钟中断 都将当前进程剩余时间片 -1</span><br><span class="line">直到 剩余时间片 为 0 当前进程的 need_resched 被置1 意为 需要被调度</span><br><span class="line">中断服务例程 一发现当前进程需要被调度 就 调用 schedule() 将它调度</span><br><span class="line">会将当前进程 放入就绪队列中 并将其时间片设为当前队列的最大时间片</span><br><span class="line">接着调用 pick_next() 选择下一个需要换上处理机的进程 若选择成功 就让其 出就绪队列</span><br><span class="line">若查找不到 则 内核线程 idle() 顶上 该进程会死循环 查找可被调度的进程</span><br><span class="line">最后调用 proc_run() 进行进程切换</span><br></pre></td></tr></table></figure>

<ul>
<li>请在实验报告中简要说明如何设计实现”多级反馈队列调度算法”，给出概要设计，鼓励给出详细设计</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Multilevel Feedback queues(MLFQ) 的思想</span><br><span class="line">1. 时间片大小随优先级级别增加而增加</span><br><span class="line">2. 进程在当前时间片没有完成 则降到下一优先级</span><br><span class="line"></span><br><span class="line">init</span><br><span class="line">初始化算法维护的数据结构</span><br><span class="line"></span><br><span class="line">enqueue</span><br><span class="line">若该进程的时间片为0 则不改变其优先级 并将其放入相应优先级的对垒</span><br><span class="line">若该进程时间片不为0 说明 它不需要那么多的时间片 将其优先级降低一级 并将其放入相应优先级的队列中</span><br><span class="line">最后设置它的时间片为其队列的最大时间片</span><br><span class="line"></span><br><span class="line">dequeue</span><br><span class="line">从相应优先级的队列中删去该进程</span><br><span class="line"></span><br><span class="line">pick_next</span><br><span class="line">使用某种调度算法 选择下一个要执行的进程的优先级 并从相应优先级的就绪队列中选出下一个换上处理机的进程</span><br><span class="line"></span><br><span class="line">proc_tick</span><br><span class="line">时钟中断所使用 每次时钟中断 减少当前进程的时间片 若为0 则 将进程标记为需要调度</span><br></pre></td></tr></table></figure>

<h3 id="练习2-实现-Stride-Scheduling-调度算法"><a href="#练习2-实现-Stride-Scheduling-调度算法" class="headerlink" title="练习2: 实现 Stride Scheduling 调度算法"></a>练习2: 实现 Stride Scheduling 调度算法</h3><p>首先需要换掉RR调度器的实现，即用default_sched_stride_c覆盖default_sched.c。然后根据此文件和后续文档对Stride度器的相关描述，完成Stride调度算法的实现。</p>
<p>请在实验报告中简要说明你的设计实现过程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">proc.c:</span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct *<span class="title">alloc_proc</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    proc-&gt;rq = <span class="literal">NULL</span>;</span><br><span class="line">    list_init(&amp;(proc-&gt;run_link));</span><br><span class="line">    proc-&gt;time_slice = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 在 实验0 的基础上 再加下面三行 这是初始化 Stride 调度算法 所使用的</span></span><br><span class="line">    <span class="comment">// 斜堆实现的 Priority Queue</span></span><br><span class="line">    proc-&gt;lab6_run_pool.parent = proc-&gt;lab6_run_pool.left = proc-&gt;lab6_run_pool.right = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="comment">// 优先级 (和步进成反比)</span></span><br><span class="line">    proc-&gt;lab6_priority = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 步进值</span></span><br><span class="line">    proc-&gt;lab6_stride = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里只实现了用 Priority Queue 作为 stride 调度算法所使用的数据结构<br>用 list 也是可以的 只不过查找 删除的时候 会随着进程数目的增加而呈现出线性关系</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 就是 32位系统下 int 的最大值 2147483647</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BIG_STRIDE (((uint32_t)-1) / 2)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">stride_init</span><span class="params">(struct run_queue *rq)</span> </span>&#123;</span><br><span class="line">    list_init(&amp;rq-&gt;run_list);</span><br><span class="line">    rq-&gt;lab6_run_pool = <span class="literal">NULL</span>;</span><br><span class="line">    rq-&gt;proc_num = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">stride_enqueue</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    rq-&gt;lab6_run_pool = skew_heap_insert(rq-&gt;lab6_run_pool, &amp;(proc-&gt;lab6_run_pool), proc_stride_comp_f);</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;lab6_priority == <span class="number">0</span>) &#123;</span><br><span class="line">        proc-&gt;lab6_priority = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;time_slice == <span class="number">0</span> || proc-&gt;time_slice &gt; rq-&gt;max_time_slice) &#123;</span><br><span class="line">       proc-&gt;time_slice = rq-&gt;max_time_slice;</span><br><span class="line">    &#125;</span><br><span class="line">    proc-&gt;rq = rq;</span><br><span class="line">    ++rq-&gt;proc_num;</span><br><span class="line">&#125;   </span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">stride_dequeue</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    rq-&gt;lab6_run_pool = skew_heap_remove(rq-&gt;lab6_run_pool, &amp;(proc-&gt;lab6_run_pool), proc_stride_comp_f);</span><br><span class="line">    --rq-&gt;proc_num;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct * <span class="title">stride_pick_next</span><span class="params">(struct run_queue *rq)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (rq-&gt;lab6_run_pool == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 斜堆的顶就是 stride 值最小的进程</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proc_struct</span> *<span class="title">p</span> = <span class="title">le2proc</span>(<span class="title">rq</span>-&gt;<span class="title">lab6_run_pool</span>, <span class="title">lab6_run_pool</span>);</span></span><br><span class="line">    <span class="comment">// 提前增加 stride 的值 因为在之后调度别的进程之前 一定会执行这么多</span></span><br><span class="line">    p-&gt;lab6_stride += BIG_STRIDE / p-&gt;lab6_priority;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">stride_proc_tick</span><span class="params">(struct run_queue *rq, struct proc_struct *proc)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (proc-&gt;time_slice == <span class="number">0</span>) &#123;</span><br><span class="line">        proc-&gt;need_resched = <span class="number">1</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        --proc-&gt;time_slice;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="BIG-STRIDE-的值是怎么来的"><a href="#BIG-STRIDE-的值是怎么来的" class="headerlink" title="BIG_STRIDE 的值是怎么来的?"></a>BIG_STRIDE 的值是怎么来的?</h4><p>Stride 调度算法 的思路是 每次找 stride步进值最小的进程</p>
<p>每个进程 每次执行完以后 都要在 stride步进 += pass步长<br>其中 步长 是和 优先级成反比的 因此 步长可以反映出进程的优先级</p>
<p>但是随着每次调度 步长不断增加 有可能会有溢出的风险</p>
<p>因此 需要设置一个步长的最大值 使得他们哪怕溢出 还是能够进行比较</p>
<p>在 uCore 中 BIG_STRIDE 的值是采用 无符号32位整数表示 而 stride 也是无符号32位整数<br>也就是说 最大值只能为 <code>(2^32 - 1)</code></p>
<p>如果一个 进程的 stride 已经为 <code>(2^32 -1)</code> 时 那么再加上 pass步长 一定会溢出 然后又从0开始算<br>这样 整个调度算法的比较 就没有意义了</p>
<p>这说明 我们必须得约定一个 最大的步长 使得两个进程的步进值哪怕其中一个溢出 或者都溢出 还能够进行比较</p>
<p>首先 因为 步长 和 优先级成反比 可以得到下面一条<br><code>pass = BIG_STRIDE / priority &lt;= BIG_STRIDE</code></p>
<p>进而得到<br><code>pass_max &lt;= BIG_STRIDE</code></p>
<p>最大步长 - 最小步长 一定小于等于步长<br><code>max_stride - min_stride &lt;= pass_max</code></p>
<p>所以得出<br><code>max_stride - min_stride &lt;= BIG_STRIDE</code></p>
<p>前面说了 uCore 中 BIG_STRIDE 用的 无符号32位整数 最大值只能为 <code>(2^32 - 1)</code><br>而 又因为是无符号的 因此 最小 只能为 0 而且我们需要把32位无符号整数进行比较 需要保证任意两个进程stride的差值在32位有符号数能够表示的范围之内 故 BIG_STRIDE 为 <code>(2^32 - 1) / 2</code></p>
<p>最后还是实验结果</p>
<p><img src="/images/lab6_finish.png" alt="lab6_finish"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>调度</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 5</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-5/</url>
    <content><![CDATA[<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>请把你做的实验1/2/3/4的代码填入本实验中代码中有“LAB1”,“LAB2”,“LAB3”,”LAB4”的注释相应部分。注意：为了能够正确执行lab5的测试应用程序，可能需对已完成的实验1/2/3/4的代码进行进一步改进。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">这几个文件补上 说真的最烦这个步骤 但是这次 不只是直接复制 还要补充</span><br><span class="line">vmm.c trap.c default_pmm.c pmm.c proc.c</span><br></pre></td></tr></table></figure>


<p>以下为补充的代码块</p>
<a id="more"></a>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">proc.c: </span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct * <span class="title">alloc_proc</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    proc-&gt;state = PROC_UNINIT;</span><br><span class="line">    proc-&gt;pid = <span class="number">-1</span>;</span><br><span class="line">    proc-&gt;runs = <span class="number">0</span>;</span><br><span class="line">    proc-&gt;kstack = <span class="number">0</span>;</span><br><span class="line">    proc-&gt;need_resched = <span class="literal">NULL</span>;</span><br><span class="line">    proc-&gt;parent = <span class="literal">NULL</span>;</span><br><span class="line">    proc-&gt;mm = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="built_in">memset</span>(&amp;(proc-&gt;context), <span class="number">0</span>, <span class="keyword">sizeof</span>(struct context));</span><br><span class="line">    proc-&gt;tf = <span class="literal">NULL</span>;</span><br><span class="line">    proc-&gt;cr3 = boot_cr3;</span><br><span class="line">    proc-&gt;flags = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">memset</span>(&amp;(proc-&gt;name), <span class="number">0</span>, PROC_NAME_LEN);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 新添加: 初始化进程等待状态 初始化进程相关指针</span></span><br><span class="line">    proc-&gt;wait_state = <span class="number">0</span>; </span><br><span class="line">    proc-&gt;cptr = proc-&gt;yptr = proc-&gt; optr = <span class="literal">NULL</span>;</span><br><span class="line">    |   cptr: proc is parent          |</span><br><span class="line">    |   yptr: proc is younger sibling |</span><br><span class="line">    |   optr: proc is older sibling   |</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_fork</span><span class="params">(<span class="keyword">uint32_t</span> clone_flags, <span class="keyword">uintptr_t</span> <span class="built_in">stack</span>, struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ((proc = alloc_proc()) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> fork_out;</span><br><span class="line">    &#125;</span><br><span class="line">    proc-&gt;parent = current;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 添加这行 确保 当前进程正在等待    </span></span><br><span class="line">    assert(current-&gt;wait_state == <span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (setup_kstack(proc) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_fork_cleanup_proc;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (copy_mm(clone_flags, proc) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_fork_cleanup_kstack;</span><br><span class="line">    &#125;</span><br><span class="line">    copy_thread(proc, <span class="built_in">stack</span>, tf);</span><br><span class="line">    <span class="keyword">bool</span> intr_flag;</span><br><span class="line">    local_intr_save(intr_flag);</span><br><span class="line">    &#123;</span><br><span class="line">        proc-&gt;pid = get_pid();</span><br><span class="line">        hash_proc(proc);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 删除此行 nr_process++ 和 加入链表那行 添加下面那行;</span></span><br><span class="line">        <span class="comment">// 将原来的简单 计数 改成设置进程的相关链接</span></span><br><span class="line">        set_links(proc);</span><br><span class="line">    &#125;</span><br><span class="line">    local_intr_restore(intr_flag);</span><br><span class="line"></span><br><span class="line">    wakeup_proc(proc);</span><br><span class="line">    ret = proc-&gt;pid;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">trap.c:</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">trap_dispatch</span><span class="params">(struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 时间片用完 设置进程 为 需要被调度</span></span><br><span class="line">    <span class="keyword">if</span> (++ticks % TICK_NUM == <span class="number">0</span>) &#123;</span><br><span class="line">        assert(current != <span class="literal">NULL</span>);</span><br><span class="line">        current-&gt;need_resched = <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">idt_init</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="keyword">sizeof</span>(idt) / <span class="keyword">sizeof</span>(struct gatedesc); i++) &#123;</span><br><span class="line">        SETGATE(idt[i], <span class="number">0</span>, GD_KTEXT, __vectors[i], DPL_KERNEL);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 添加下面这行</span></span><br><span class="line">    <span class="comment">// 设置给用户态用的中断门 让用户态能够进行系统调用</span></span><br><span class="line">    SETGATE(idt[T_SYSCALL], <span class="number">1</span>, GD_KTEXT, __vectors[T_SYSCALL], DPL_USER);</span><br><span class="line"></span><br><span class="line">    lidt(&amp;idt_pd);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="练习1-加载应用程序并执行"><a href="#练习1-加载应用程序并执行" class="headerlink" title="练习1: 加载应用程序并执行"></a>练习1: 加载应用程序并执行</h3><p>do_execv函数调用load_icode（位于kern/process/proc.c中）来加载并解析一个处于内存中的ELF执行文件格式的应用程序，建立相应的用户内存空间来放置应用程序的代码段、数据段等，且要设置好proc_struct结构中的成员变量trapframe中的内容，确保在执行此进程后，能够从应用程序设定的起始执行地址开始执行。需设置正确的trapframe内容。</p>
<p>请在实验报告中简要说明你的设计实现过程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">以下为要填写的内容 首先清空进程原先的中断帧 然后再将 中断帧中的 代码段 和 数据段</span><br><span class="line">修改为 用户态的段选择子 栈指针设置为 用户栈顶 eip 设置为 用户程序的入口地址</span><br><span class="line">最后 确保在用户进程中能够响应中断</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">load_icode</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">char</span> *binary, <span class="keyword">size_t</span> <span class="built_in">size</span>)</span> </span>&#123;</span><br><span class="line">    tf-&gt;tf_cs = USER_CS;</span><br><span class="line">    tf-&gt;tf_ds = tf-&gt;tf_es = tf-&gt;tf_ss = USER_DS;</span><br><span class="line">    tf-&gt;tf_esp = USTACKTOP;</span><br><span class="line">    tf-&gt;tf_eip = elf-&gt;e_entry;</span><br><span class="line">    tf-&gt;tf_eflags = FL_IF;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请在实验报告中描述当创建一个用户态进程并加载了应用程序后，CPU是如何让这个应用程序最终在用户态执行起来的。即这个用户态进程被ucore选择占用CPU执行（RUNNING态）到具体执行应用程序第一条指令的整个经过。</p>
<p>在此之前先理一下 用户态进程是怎么来的</p>
<ol>
<li>创建了一个 硬构造了 第0个 内核线程 idleproc</li>
<li>idleproc 通过 <code>kernel_thread</code> 创建了 第1个内核线程 initproc 在 lab 4 中只是打印字符串</li>
<li>initproc 通过 <code>kernel_execve</code> 将 hello应用程序执行码覆盖到了 initproc 用户虚拟内存空间 来创建 用户态进程</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">创建一个用户态进程并加载了应用程序之后 调度器 schedule 调用 proc_run</span><br><span class="line">设置 指针 current 为当前执行PCB 并加载 该进程的 内核栈和页目录表</span><br><span class="line">调用 switch_to 因为 当前进程的 context 其中的 eip 被设置为 forkret(copy_thread 拷贝父进程的中断帧时设置的) 因此 switch_to ret 后</span><br><span class="line">会跳转到 forkret 处 forkret 又会 将栈 设置为 当前进程的trapframe 然后跳到 __trapret</span><br><span class="line">此时 __trapret 会根据当前进程的trapframe 恢复上下文 最后 退出中断 iret 从系统调用的函数调用路径 返回</span><br><span class="line">切换到用户进程 hello 第一句语句 _start 处 开始执行</span><br></pre></td></tr></table></figure>

<h3 id="练习2-父进程复制自己的内存空间给子进程"><a href="#练习2-父进程复制自己的内存空间给子进程" class="headerlink" title="练习2: 父进程复制自己的内存空间给子进程"></a>练习2: 父进程复制自己的内存空间给子进程</h3><p>创建子进程的函数do_fork在执行中将拷贝当前进程（即父进程）的用户内存地址空间中的合法内容到新进程中（子进程），完成内存资源的复制。具体是通过copy_range函数（位于kern/mm/pmm.c中）实现的，请补充copy_range的实现，确保能够正确执行。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">这个函数是用来 拷贝父进程的用户内存地址空间到子进程中 share 此处没用到 不管它</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">copy_range</span><span class="params">(<span class="keyword">pde_t</span> *to, <span class="keyword">pde_t</span> *from, <span class="keyword">uintptr_t</span> start, <span class="keyword">uintptr_t</span> <span class="built_in">end</span>, <span class="keyword">bool</span> share)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 找到父进程的 页虚拟内存地址 和 子进程的 页虚拟内存地址 将 父进程的页 拷贝到 子进程的页</span></span><br><span class="line">    <span class="keyword">void</span>* src_kvaddr = page2kva(page);</span><br><span class="line">    <span class="keyword">void</span>* dst_kvaddr = page2kva(npage);</span><br><span class="line">    <span class="built_in">memcpy</span>(dst_kvaddr, src_kvaddr, PGSIZE);</span><br><span class="line">    ret = page_insert(to, npage, start, perm);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="练习3-阅读分析源代码，理解进程执行-fork-exec-wait-exit-的实现，以及系统调用的实现"><a href="#练习3-阅读分析源代码，理解进程执行-fork-exec-wait-exit-的实现，以及系统调用的实现" class="headerlink" title="练习3: 阅读分析源代码，理解进程执行 fork/exec/wait/exit 的实现，以及系统调用的实现"></a>练习3: 阅读分析源代码，理解进程执行 fork/exec/wait/exit 的实现，以及系统调用的实现</h3><p>请在实验报告中简要说明你对 fork/exec/wait/exit函数的分析。并回答如下问题：</p>
<ul>
<li>请分析fork/exec/wait/exit在实现中是如何影响进程的执行状态的？</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">fork 创建新的 PCB 进程状态为 UNINIT</span><br><span class="line">exec 将当前进程的内存布局清除 再调用 load_icode 读出 ELF映像中的内存布局 并填写 进程状态不改变</span><br><span class="line">wait 当前进程若无子进程 则返回错误 若有子进程 则判定 是否为 ZOMBIE 子进程 有则释放子进程的资源 并返回子进程的返回状态码</span><br><span class="line">若无 ZOMBIE 状态子进程 则进入 SLEEPING 状态 等子进程唤醒</span><br><span class="line">exit 清除当前进程几乎所有资源(PCB和内核栈不清除) 将所有子进程(如果有的话)设置为 init 进程(内核) 将当前进程状态设置为 ZOMBIE</span><br><span class="line">若有父进程在等待当前进程exit 则 唤醒父进程</span><br></pre></td></tr></table></figure>

<ul>
<li>请给出ucore中一个用户态进程的执行状态生命周期图（包执行状态，执行状态之间的变换关系，以及产生变换的事件或函数调用）</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">                                             RUNNING----------------+</span><br><span class="line">                                               A |                  |</span><br><span class="line">                                               | |                  |</span><br><span class="line">                                            proc_run()            exit()  </span><br><span class="line">                                               | |                  |</span><br><span class="line">                                               | V                  V</span><br><span class="line">--alloc_page()--&gt; UNINIT --wakeup_proc()--&gt; RUNNABLE --exit()--&gt; ZOMBIE</span><br><span class="line">                                               A                    A</span><br><span class="line">                                               |                    |</span><br><span class="line">                                           子进程exit()            exit()</span><br><span class="line">                                               |                    |</span><br><span class="line">                                               |                    |</span><br><span class="line">                                            SLEEPING----------------+</span><br></pre></td></tr></table></figure>

<p>最后附上运行结果 做这个 lab 的时候 给绕的很晕 要特别注意 各个函数 分别修改了 中断帧 和 上下文的 eip esp 的哪些内容<br>比如 <code>kernel_thread</code> 将 <code>tf-&gt;eip</code> 设置为 <code>kernel_thread_entry</code> 当此进程被调度上去的时候<br>就会跳到 <code>kernel_thread_entry</code> 去执行 <code>fn</code> 这个函数<br><code>copy_thread</code> 将 <code>tf-&gt;eip</code> 设置为 <code>forkret</code> 而 <code>forkret</code> 会执行这条指令 <code>movl 4(%esp), %esp</code> 将 <code>context-&gt;esp</code> 当做 栈顶<br>而 <code>context-&gt;esp</code> 又在 copy_thread 中 被设置为 中断帧的栈顶</p>
<p><img src="/images/lab5_finish.png" alt="lab5_finish"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>进程</tag>
        <tag>线程</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 7</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-7/</url>
    <content><![CDATA[<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>请把你做的实验代码填入本实验中代码中有“LAB1”/“LAB2”/“LAB3”/“LAB4”/“LAB5”/“LAB6”的注释相应部分。并确保编译通过。注意：为了能够正确执行lab7的测试应用程序，可能需对已完成的实验1/2/3/4/5/6的代码进行进一步改进。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">这次又是要修改 trap.c</span><br><span class="line">vmm.c trap.c default_pmm.c pmm.c proc.c swap_fifo.c</span><br><span class="line">trap.c:</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">trap_dispatch</span><span class="params">(struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    ++ticks;</span><br><span class="line">    <span class="comment">/* 注销掉下面这一句 因为这一句被包含在了 run_timer_list()</span></span><br><span class="line"><span class="comment">        run_timer_list() 在之前的基础上 加入了对 timer 的支持 */</span></span><br><span class="line">    <span class="comment">// sched_class_proc_tick(current);</span></span><br><span class="line">    run_timer_list();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="练习1-理解内核级信号量的实现和基于内核级信号量的哲学家就餐问题"><a href="#练习1-理解内核级信号量的实现和基于内核级信号量的哲学家就餐问题" class="headerlink" title="练习1: 理解内核级信号量的实现和基于内核级信号量的哲学家就餐问题"></a>练习1: 理解内核级信号量的实现和基于内核级信号量的哲学家就餐问题</h3><p>请在实验报告中给出内核级信号量的设计描述，并说其大致执行流流程。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 先是定义了一个信号量的数据结构</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> value; <span class="comment">// 计数值 用于 PV操作</span></span><br><span class="line">    <span class="keyword">wait_queue_t</span> wait_queue; <span class="comment">// 进程等待队列</span></span><br><span class="line">&#125; <span class="keyword">semaphore_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 用于等待队列 存放了当前等待的线程PCB 和 唤醒原因 和 等待队列 和 用于还原结构体的等待队列标志</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proc_struct</span> *<span class="title">proc</span>;</span></span><br><span class="line">    <span class="keyword">uint32_t</span> wakeup_flags;</span><br><span class="line">    <span class="keyword">wait_queue_t</span> *wait_queue;</span><br><span class="line">    <span class="keyword">list_entry_t</span> wait_link;</span><br><span class="line">&#125; <span class="keyword">wait_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化信号量中的计数值和等待队列</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sem_init</span><span class="params">(<span class="keyword">semaphore_t</span> *sem, <span class="keyword">int</span> value)</span> </span>&#123;</span><br><span class="line">    sem-&gt;value = value;</span><br><span class="line">    wait_queue_init(&amp;(sem-&gt;wait_queue));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    P操作 要关闭中断并保存 eflag 寄存器的值 避免共享变量被多个线程同时修改</span></span><br><span class="line"><span class="comment">    判断 计数值是否大于 0 若大于 0 说明此时没有其他线程访问临界区 则直接将计数值 减 1 并 返回</span></span><br><span class="line"><span class="comment">    若 计数值小于 0 则 已经有其他线程访问临界区了 就将当前线程放入等待队列中 并调用调度函数</span></span><br><span class="line"><span class="comment">    等到进程被唤醒 再将当前进程从等待队列中 取出并删去 最后判断等待的线程是因为什么原因被唤醒</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">static</span> __noinline <span class="keyword">uint32_t</span> __down(<span class="keyword">semaphore_t</span> *sem, <span class="keyword">uint32_t</span> wait_state) &#123;</span><br><span class="line">    <span class="keyword">bool</span> intr_flag;</span><br><span class="line">    local_intr_save(intr_flag);</span><br><span class="line">    <span class="keyword">if</span> (sem-&gt;value &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        sem-&gt;value --;</span><br><span class="line">        local_intr_restore(intr_flag);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">wait_t</span> __wait, *wait = &amp;__wait;</span><br><span class="line">    wait_current_set(&amp;(sem-&gt;wait_queue), wait, wait_state);</span><br><span class="line">    local_intr_restore(intr_flag);</span><br><span class="line"></span><br><span class="line">    schedule();</span><br><span class="line"></span><br><span class="line">    local_intr_save(intr_flag);</span><br><span class="line">    wait_current_del(&amp;(sem-&gt;wait_queue), wait);</span><br><span class="line">    local_intr_restore(intr_flag);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (wait-&gt;wakeup_flags != wait_state) &#123;</span><br><span class="line">        <span class="keyword">return</span> wait-&gt;wakeup_flags;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    V操作 也要关闭中断 并保存 eflag 寄存器的值 防止共享变量同时被多个线程访问或修改</span></span><br><span class="line"><span class="comment">    先判断等待队列是否为空 若为空 则将计数值 加 1 并返回</span></span><br><span class="line"><span class="comment">    若不为空 则说明还有线程在等待 此时取出等待队列的第一个线程 并将其 唤醒 唤醒的过程中 </span></span><br><span class="line"><span class="comment">    将其从等待队列中删除</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="keyword">static</span> __noinline <span class="keyword">void</span> __up(<span class="keyword">semaphore_t</span> *sem, <span class="keyword">uint32_t</span> wait_state) &#123;</span><br><span class="line">    <span class="keyword">bool</span> intr_flag;</span><br><span class="line">    local_intr_save(intr_flag);</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">wait_t</span> *wait;</span><br><span class="line">        <span class="keyword">if</span> ((wait = wait_queue_first(&amp;(sem-&gt;wait_queue))) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">            sem-&gt;value ++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            assert(wait-&gt;proc-&gt;wait_state == wait_state);</span><br><span class="line">            wakeup_wait(&amp;(sem-&gt;wait_queue), wait, wait_state, <span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    local_intr_restore(intr_flag);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>请在实验报告中给出给用户态进程/线程提供信号量机制的设计方案，并比较说明给内核级提供信号量机制的异同。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">可以将内核级信号量 包装成系统调用 供用户进程使用 但不能直接使用信号量结构体的指针作为参数</span><br><span class="line">不能持有或访问内核的指针 可以在每个进程的PCB里维护一个信号量指针数组 供用户态进程及其进程下的多线程使用</span><br><span class="line">不同的地方在于 需要通过系统调用进入 内核态 进行操作</span><br></pre></td></tr></table></figure>

<h3 id="练习2-完成内核级条件变量和基于内核级条件变量的哲学家就餐问题"><a href="#练习2-完成内核级条件变量和基于内核级条件变量的哲学家就餐问题" class="headerlink" title="练习2: 完成内核级条件变量和基于内核级条件变量的哲学家就餐问题"></a>练习2: 完成内核级条件变量和基于内核级条件变量的哲学家就餐问题</h3><p>首先掌握管程机制，然后基于信号量实现完成条件变量实现，然后用管程机制实现哲学家就餐问题的解决方案（基于条件变量）。</p>
<p>请在实验报告中给出内核级条件变量的设计描述，并说其大致执行流流程。</p>
<p>首先可以确定 这里实现的是 Hoare管程 因为 等待条件变量的进程的优先级更高</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 管程数据结构</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">monitor</span>&#123;</span></span><br><span class="line">    <span class="keyword">semaphore_t</span> mutex; <span class="comment">// 二值信号量 用来互斥访问管程</span></span><br><span class="line">    <span class="keyword">semaphore_t</span> next; <span class="comment">// 用于 条件同步 用于发出signal操作的进程等条件为真之前进入睡眠</span></span><br><span class="line">    <span class="keyword">int</span> next_count; <span class="comment">// 记录睡在 signal 操作的进程数</span></span><br><span class="line">    <span class="keyword">condvar_t</span> *cv; <span class="comment">// 条件变量</span></span><br><span class="line">&#125; <span class="keyword">monitor_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 条件变量数据结构</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">condvar</span>&#123;</span></span><br><span class="line">    <span class="keyword">semaphore_t</span> sem; <span class="comment">// 用于条件同步 用于发出wait操作的进程等待条件为真之前进入睡眠</span></span><br><span class="line">    <span class="keyword">int</span> count; <span class="comment">// 记录睡在 wait 操作的进程数(等待条件变量成真)</span></span><br><span class="line">    <span class="keyword">monitor_t</span> * owner; <span class="comment">// 所属管程</span></span><br><span class="line">&#125; <span class="keyword">condvar_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 初始化管程</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">monitor_init</span> <span class="params">(<span class="keyword">monitor_t</span> * mtp, <span class="keyword">size_t</span> num_cv)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    assert(num_cv&gt;<span class="number">0</span>);</span><br><span class="line">    mtp-&gt;next_count = <span class="number">0</span>; <span class="comment">// 睡在signal进程数 初始化为0</span></span><br><span class="line">    mtp-&gt;cv = <span class="literal">NULL</span>;</span><br><span class="line">    sem_init(&amp;(mtp-&gt;mutex), <span class="number">1</span>); <span class="comment">// 二值信号量 保护管程 使进程访问管程操作为互斥的</span></span><br><span class="line">    sem_init(&amp;(mtp-&gt;next), <span class="number">0</span>); <span class="comment">// 条件同步信号量</span></span><br><span class="line">    mtp-&gt;cv =(<span class="keyword">condvar_t</span> *) kmalloc(<span class="keyword">sizeof</span>(<span class="keyword">condvar_t</span>)*num_cv); <span class="comment">// 获取一块内核空间 放置条件变量</span></span><br><span class="line">    assert(mtp-&gt;cv!=<span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">for</span>(i=<span class="number">0</span>; i&lt;num_cv; i++)&#123;</span><br><span class="line">        mtp-&gt;cv[i].count=<span class="number">0</span>;</span><br><span class="line">        sem_init(&amp;(mtp-&gt;cv[i].sem),<span class="number">0</span>);</span><br><span class="line">        mtp-&gt;cv[i].owner=mtp;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 管程wait操作</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">先将 因为条件不成立而睡眠的进程计数加1</span></span><br><span class="line"><span class="comment">分支1. 当 管程的 next_count 大于 0 说明 有进程睡在了 signal 操作上 我们将其唤醒</span></span><br><span class="line"><span class="comment">分支2. 当 管程的 next_count 小于 0 说明 当前没有进程睡在 signal操作数 只需要释放互斥体</span></span><br><span class="line"><span class="comment">然后 再将 自身阻塞 等待 条件变量的条件为真 被唤醒后 将条件不成立而睡眠的进程计数减1 因为现在成立了</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">cond_wait</span> <span class="params">(<span class="keyword">condvar_t</span> *cvp)</span> </span>&#123;</span><br><span class="line">    cprintf(<span class="string">"cond_wait begin:  cvp %x, cvp-&gt;count %d, cvp-&gt;owner-&gt;next_count %d\n"</span>, cvp, cvp-&gt;count, cvp-&gt;owner-&gt;next_count);</span><br><span class="line"></span><br><span class="line">    cvp-&gt;count++;</span><br><span class="line">    <span class="keyword">if</span> (cvp-&gt;owner-&gt;next_count &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        up(&amp;(cvp-&gt;owner-&gt;next));</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        up(&amp;(cvp-&gt;owner-&gt;mutex));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    down(&amp;(cvp-&gt;sem)); <span class="comment">// 阻塞自己 等待条件成真</span></span><br><span class="line">    cvp-&gt;count--;</span><br><span class="line"></span><br><span class="line">    cprintf(<span class="string">"cond_wait end:  cvp %x, cvp-&gt;count %d, cvp-&gt;owner-&gt;next_count %d\n"</span>, cvp, cvp-&gt;count, cvp-&gt;owner-&gt;next_count);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 管程signal操作</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">分支1. 因为条件不成立而睡眠的进程计数小于等于0 时 说明 没有进程需要唤醒 则直接返回</span></span><br><span class="line"><span class="comment">分支2. 因为条件不成立而睡眠的进程计数大于0 说明有进程需要唤醒 就将其唤醒</span></span><br><span class="line"><span class="comment">同时设置 条件变量所属管程的 next_count 加1 以用来告诉 wait操作 有进程睡在了 signal操作上</span></span><br><span class="line"><span class="comment">然后自己将自己阻塞 等待条件同步 被唤醒 被唤醒后 睡在 signal 操作上的进程应该减少 故 next_count 应减 1</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">cond_signal</span> <span class="params">(<span class="keyword">condvar_t</span> *cvp)</span> </span>&#123;</span><br><span class="line">    cprintf(<span class="string">"cond_signal begin: cvp %x, cvp-&gt;count %d, cvp-&gt;owner-&gt;next_count %d\n"</span>, cvp, cvp-&gt;count, cvp-&gt;owner-&gt;next_count);  </span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (cvp-&gt;count &gt; <span class="number">0</span>) &#123; <span class="comment">// 若存在因为当前条件变量而等待的进程的话</span></span><br><span class="line">        up(&amp;(cvp-&gt;sem));</span><br><span class="line">        cvp-&gt;owner-&gt;next_count++; <span class="comment">// 所属管程的 next 计数 加 1 表示当前进程会被等待者堵塞</span></span><br><span class="line">        down(&amp;(cvp-&gt;owner-&gt;next)); <span class="comment">// 阻塞自己 等待条件同步</span></span><br><span class="line">        cvp-&gt;owner-&gt;next_count--;</span><br><span class="line">    &#125;</span><br><span class="line">    cprintf(<span class="string">"cond_signal end: cvp %x, cvp-&gt;count %d, cvp-&gt;owner-&gt;next_count %d\n"</span>, cvp, cvp-&gt;count, cvp-&gt;owner-&gt;next_count);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/lab7_monitor.png" alt="lab7_monitor"></p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">check_sync.c:</span><br><span class="line"><span class="comment">// 测试编号为i的哲学家是否能获得刀叉 如果能获得 则将状态改为正在吃 并且 尝试唤醒 因为wait操作睡眠的进程</span></span><br><span class="line"><span class="comment">// cond_signal 还会阻塞自己 等被唤醒的进程唤醒自己</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">phi_test_condvar</span> <span class="params">(i)</span> </span>&#123; </span><br><span class="line">    <span class="keyword">if</span>(state_condvar[i]==HUNGRY&amp;&amp;state_condvar[LEFT]!=EATING</span><br><span class="line">            &amp;&amp;state_condvar[RIGHT]!=EATING) &#123;</span><br><span class="line">        cprintf(<span class="string">"phi_test_condvar: state_condvar[%d] will eating\n"</span>,i);</span><br><span class="line">        state_condvar[i] = EATING ;</span><br><span class="line">        cprintf(<span class="string">"phi_test_condvar: signal self_cv[%d] \n"</span>,i);</span><br><span class="line">        cond_signal(&amp;mtp-&gt;cv[i]) ;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 拿刀叉</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">phi_take_forks_condvar</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    down(&amp;(mtp-&gt;mutex));    <span class="comment">// P操作进入临界区</span></span><br><span class="line">    state_condvar[i] = HUNGRY; <span class="comment">// 饥饿状态 准备进食</span></span><br><span class="line">    phi_test_condvar(i); <span class="comment">// 测试当前是否能获得刀叉 </span></span><br><span class="line">    <span class="keyword">while</span> (state_condvar[i] != EATING) &#123;</span><br><span class="line">        cond_wait(&amp;mtp-&gt;cv[i]); <span class="comment">// 若不能拿 则阻塞自己 等其它进程唤醒</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span>(mtp-&gt;next_count&gt;<span class="number">0</span>)</span><br><span class="line">        up(&amp;(mtp-&gt;next));</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        up(&amp;(mtp-&gt;mutex));</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 放刀叉</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">phi_put_forks_condvar</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">    down(&amp;(mtp-&gt;mutex)); <span class="comment">// P操作进入临界区</span></span><br><span class="line">    state_condvar[i] = THINKING; <span class="comment">// 思考状态</span></span><br><span class="line">    phi_test_condvar(LEFT); <span class="comment">// 试试左右两边能否获得刀叉</span></span><br><span class="line">    phi_test_condvar(RIGHT);</span><br><span class="line">    <span class="keyword">if</span>(mtp-&gt;next_count&gt;<span class="number">0</span>) <span class="comment">// 有哲学家睡在 signal操作 则将其唤醒</span></span><br><span class="line">        up(&amp;(mtp-&gt;next));</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        up(&amp;(mtp-&gt;mutex)); <span class="comment">// 离开临界区</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">哲学家-&gt;试试拿刀叉-&gt;能拿-&gt;signal 唤醒被wait阻塞的进程-&gt;阻塞自己</span><br><span class="line">                  |             |                  A</span><br><span class="line">                  |             V                  |</span><br><span class="line">                  -&gt;不能拿-&gt;wait阻塞自己             |</span><br><span class="line">                                                   |</span><br><span class="line">哲学家-&gt;放刀叉-&gt;让左右两边试试拿刀叉-&gt;有哲学家睡在signal 唤醒他</span><br></pre></td></tr></table></figure>

<p>请在实验报告中给出给用户态进程/线程提供条件变量机制的设计方案，并比较说明给内核级提供条件变量机制的异同。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">依然是通过提供系统调用</span><br></pre></td></tr></table></figure>


<p>请在实验报告中回答：能否不用基于信号量机制来完成条件变量？如果不能，请给出理由，如果能，请给出设计说明和具体实现。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">可以 可以用 自旋锁</span><br></pre></td></tr></table></figure>

<p>实验完成结果<br><img src="/images/lab7_finish.png" alt="lab7_finish"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>信号量</tag>
        <tag>同步互斥</tag>
        <tag>管程</tag>
        <tag>哲学家就餐</tag>
        <tag>并发</tag>
        <tag>临界区</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统 uCore Lab 8</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F-uCore-Lab-8/</url>
    <content><![CDATA[<h3 id="练习0：填写已有实验"><a href="#练习0：填写已有实验" class="headerlink" title="练习0：填写已有实验"></a>练习0：填写已有实验</h3><p>请把你做的实验代码填入本实验中代码中有“LAB1”/“LAB2”/“LAB3”/“LAB4”/“LAB5”/“LAB6” /“LAB7”的注释相应部分。并确保编译通过。注意：为了能够正确执行lab8的测试应用程序，可能需对已完成的实验1/2/3/4/5/6/7的代码进行进一步改进。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">vmm.c default_pmm.c pmm.c proc.c swap_fifo.c trap.c check_sync.c</span><br><span class="line">proc.c: </span><br><span class="line"><span class="function"><span class="keyword">static</span> struct proc_struct *<span class="title">alloc_proc</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 初始化 PCB 下的 fs(进程相关的文件信息)</span></span><br><span class="line">    proc-&gt;filesp = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">do_fork</span><span class="params">(<span class="keyword">uint32_t</span> clone_flags, <span class="keyword">uintptr_t</span> <span class="built_in">stack</span>, struct trapframe *tf)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 使用 copy_files()函数复制父进程的fs到子进程中</span></span><br><span class="line">    <span class="keyword">if</span> (copy_files(clone_flags, proc) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_fork_cleanup_kstack;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="练习1-完成读文件操作的实现"><a href="#练习1-完成读文件操作的实现" class="headerlink" title="练习1: 完成读文件操作的实现"></a>练习1: 完成读文件操作的实现</h3><p>首先了解打开文件的处理流程，然后参考本实验后续的文件读写操作的过程分析，编写在sfs_inode.c中sfs_io_nolock读文件中数据的实现代码。</p>
<p>这里唯一没想到的就是 alen 因为 后面 *alenp 要返回真实的长度 我实现的里面没注意到这一点</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">sfs_io_nolock</span><span class="params">(struct sfs_fs *sfs, struct sfs_inode *<span class="built_in">sin</span>, <span class="keyword">void</span> *buf, <span class="keyword">off_t</span> offset, <span class="keyword">size_t</span> *alenp, <span class="keyword">bool</span> <span class="built_in">write</span>)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 先判断第一块的情况 如果没对齐 就从偏移的地方读取</span></span><br><span class="line">    <span class="keyword">if</span> ((blkoff = offset % SFS_BLKSIZE) != <span class="number">0</span>)  &#123;</span><br><span class="line">        <span class="comment">// 判断 endpos 和 offset 是否在同一块中</span></span><br><span class="line">        <span class="comment">// 若为同一块 则 size 为 endpos - offset</span></span><br><span class="line">        <span class="comment">// 若不为同一块 则 size 为 SFS_BLKSIZE - blkoff(偏移) 为 第一块要读的大小</span></span><br><span class="line">        <span class="built_in">size</span> = (nblks != <span class="number">0</span>) ? (SFS_BLKSIZE - blkoff) : (endpos - offset);</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_bmap_load_nolock(sfs, <span class="built_in">sin</span>, blkno, &amp;ino)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_buf_op(sfs, buf, <span class="built_in">size</span>, ino, blkoff)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        alen += <span class="built_in">size</span>;</span><br><span class="line">        <span class="keyword">if</span> (nblks == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        buf += <span class="built_in">size</span>, blkno++; nblks--;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 中间对齐的情况</span></span><br><span class="line">    <span class="built_in">size</span> = SFS_BLKSIZE;</span><br><span class="line">    <span class="keyword">while</span> (nblks != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_bmap_load_nolock(sfs, <span class="built_in">sin</span>, blkno, &amp;ino)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_block_op(sfs, buf, ino, <span class="number">1</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        alen += <span class="built_in">size</span>, buf += <span class="built_in">size</span>, blkno++, nblks--;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 末尾最后一块没对齐的情况</span></span><br><span class="line">    <span class="keyword">if</span> ((<span class="built_in">size</span> = endpos % SFS_BLKSIZE) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_bmap_load_nolock(sfs, <span class="built_in">sin</span>, blkno, &amp;ino)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> ((ret = sfs_buf_op(sfs, buf, <span class="built_in">size</span>, ino, <span class="number">0</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> out;</span><br><span class="line">        &#125;</span><br><span class="line">        alen += <span class="built_in">size</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="练习2-完成基于文件系统的执行程序机制的实现"><a href="#练习2-完成基于文件系统的执行程序机制的实现" class="headerlink" title="练习2: 完成基于文件系统的执行程序机制的实现"></a>练习2: 完成基于文件系统的执行程序机制的实现</h3><p>改写proc.c中的load_icode函数和其他相关函数，实现基于文件系统的执行程序机制。执行：make qemu。如果能看看到sh用户程序的执行界面，则基本成功了。如果在sh用户界面上可以执行”ls”,”hello”等其他放置在sfs文件系统中的其他执行程序，则可以认为本实验基本成功。</p>
<p>可以在 Lab 7 的基础上进行修改 读elf文件变成了从磁盘上读 而不是直接在内存中读<br>此外 参数在栈中的布局</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">| High Address |</span><br><span class="line">----------------</span><br><span class="line">|   Argument   |</span><br><span class="line">|      n       |</span><br><span class="line">----------------</span><br><span class="line">|     ...      |</span><br><span class="line">----------------</span><br><span class="line">|   Argument   |</span><br><span class="line">|      1       |</span><br><span class="line">----------------</span><br><span class="line">|    padding   |</span><br><span class="line">----------------</span><br><span class="line">|   null ptr   |</span><br><span class="line">----------------</span><br><span class="line">|  Ptr Arg n   |</span><br><span class="line">----------------</span><br><span class="line">|     ...      |</span><br><span class="line">----------------</span><br><span class="line">|  Ptr  Arg 1  |</span><br><span class="line">----------------</span><br><span class="line">|  Arg  Count  | &lt;-- user esp</span><br><span class="line">----------------</span><br><span class="line">| Low  Address |</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">int</span> <span class="title">load_icode</span><span class="params">(<span class="keyword">int</span> fd, <span class="keyword">int</span> argc, <span class="keyword">char</span> **kargv)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (current-&gt;mm != <span class="literal">NULL</span>) &#123;</span><br><span class="line">        panic(<span class="string">"load_icode: current-&gt;mm must be empty.\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> ret = -E_NO_MEM;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">mm_struct</span> *<span class="title">mm</span>;</span></span><br><span class="line">    <span class="keyword">if</span> ((mm = mm_create()) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_mm;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (setup_pgdir(mm) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_pgdir_cleanup_mm;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">Page</span> *<span class="title">page</span>;</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">elfhdr</span> __<span class="title">elf</span>, *<span class="title">elf</span> = &amp;__<span class="title">elf</span>;</span></span><br><span class="line">    <span class="keyword">if</span> ((ret = load_icode_read(fd, elf, <span class="keyword">sizeof</span>(struct elfhdr), <span class="number">0</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_elf_cleanup_pgdir;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (elf-&gt;e_magic != ELF_MAGIC) &#123;</span><br><span class="line">        ret = -E_INVAL_ELF;</span><br><span class="line">        <span class="keyword">goto</span> bad_elf_cleanup_pgdir;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">proghdr</span> __<span class="title">ph</span>, *<span class="title">ph</span> = &amp;__<span class="title">ph</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">uint32_t</span> i;</span><br><span class="line">    <span class="keyword">uint32_t</span> vm_flags, perm;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; elf-&gt;e_phnum; ++i) &#123;</span><br><span class="line">        <span class="keyword">if</span> ((ret = load_icode_read(fd, ph, <span class="keyword">sizeof</span>(struct proghdr), elf-&gt;e_phoff + <span class="keyword">sizeof</span>(struct proghdr) * i)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> bad_elf_cleanup_pgdir;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_type != ELF_PT_LOAD) &#123;</span><br><span class="line">            <span class="keyword">continue</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_filesz &gt; ph-&gt;p_memsz) &#123;</span><br><span class="line">            ret = -E_INVAL_ELF;</span><br><span class="line">            <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_filesz == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">continue</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        vm_flags = <span class="number">0</span>, perm = PTE_U;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_flags &amp; ELF_PF_X) vm_flags |= VM_EXEC;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_flags &amp; ELF_PF_W) vm_flags |= VM_WRITE;</span><br><span class="line">        <span class="keyword">if</span> (ph-&gt;p_flags &amp; ELF_PF_R) vm_flags |= VM_READ;</span><br><span class="line">        <span class="keyword">if</span> (vm_flags &amp; VM_WRITE) perm |= PTE_W;</span><br><span class="line">        <span class="keyword">if</span> ((ret = mm_map(mm, ph-&gt;p_va, ph-&gt;p_memsz, vm_flags, <span class="literal">NULL</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">off_t</span> offset = ph-&gt;p_offset;</span><br><span class="line">        <span class="keyword">size_t</span> off, <span class="built_in">size</span>;</span><br><span class="line">        <span class="keyword">uintptr_t</span> start = ph-&gt;p_va, <span class="built_in">end</span>, la = ROUNDDOWN(start, PGSIZE);</span><br><span class="line"></span><br><span class="line">        ret = -E_NO_MEM;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">end</span> = ph-&gt;p_va + ph-&gt;p_filesz;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (start &lt; <span class="built_in">end</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((page = pgdir_alloc_page(mm-&gt;pgdir, la, perm)) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">            &#125;</span><br><span class="line">            off = start - la, <span class="built_in">size</span> = PGSIZE - off, la += PGSIZE;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">end</span> &lt; la) &#123;</span><br><span class="line">                <span class="built_in">size</span> -= la - <span class="built_in">end</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> ((ret = load_icode_read(fd, page2kva(page) + off, <span class="built_in">size</span>, offset)) != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">            &#125;</span><br><span class="line">            start += <span class="built_in">size</span>, offset += <span class="built_in">size</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">end</span> = ph-&gt;p_va + ph-&gt;p_memsz;</span><br><span class="line">        <span class="keyword">if</span> (start &lt; la) &#123;</span><br><span class="line">            <span class="keyword">if</span> (start == <span class="built_in">end</span>) &#123;</span><br><span class="line">                <span class="keyword">continue</span> ;</span><br><span class="line">            &#125;</span><br><span class="line">            off = start + PGSIZE - la, <span class="built_in">size</span> = PGSIZE - off;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">end</span> &lt; la) &#123;</span><br><span class="line">                <span class="built_in">size</span> -= la - <span class="built_in">end</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">memset</span>(page2kva(page) + off, <span class="number">0</span>, <span class="built_in">size</span>);</span><br><span class="line">            start += <span class="built_in">size</span>;</span><br><span class="line">            assert((<span class="built_in">end</span> &lt; la &amp;&amp; start == <span class="built_in">end</span>) || (<span class="built_in">end</span> &gt;= la &amp;&amp; start == la));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (start &lt; <span class="built_in">end</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((page = pgdir_alloc_page(mm-&gt;pgdir, la, perm)) == <span class="literal">NULL</span>) &#123;</span><br><span class="line">                <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">            &#125;</span><br><span class="line">            off = start - la, <span class="built_in">size</span> = PGSIZE - off, la += PGSIZE;</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">end</span> &lt; la) &#123;</span><br><span class="line">                <span class="built_in">size</span> -= la - <span class="built_in">end</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="built_in">memset</span>(page2kva(page) + off, <span class="number">0</span>, <span class="built_in">size</span>);</span><br><span class="line">            start += <span class="built_in">size</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    </span><br><span class="line">    &#125;</span><br><span class="line">    vm_flags = VM_READ | VM_WRITE | VM_STACK;</span><br><span class="line">    <span class="keyword">if</span> ((ret = mm_map(mm, USTACKTOP - USTACKSIZE, USTACKSIZE, vm_flags, <span class="literal">NULL</span>)) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">goto</span> bad_cleanup_mmap;</span><br><span class="line">    &#125;</span><br><span class="line">    assert(pgdir_alloc_page(mm-&gt;pgdir, USTACKTOP-PGSIZE , PTE_USER) != <span class="literal">NULL</span>);</span><br><span class="line">    assert(pgdir_alloc_page(mm-&gt;pgdir, USTACKTOP<span class="number">-2</span>*PGSIZE , PTE_USER) != <span class="literal">NULL</span>);</span><br><span class="line">    assert(pgdir_alloc_page(mm-&gt;pgdir, USTACKTOP<span class="number">-3</span>*PGSIZE , PTE_USER) != <span class="literal">NULL</span>);</span><br><span class="line">    assert(pgdir_alloc_page(mm-&gt;pgdir, USTACKTOP<span class="number">-4</span>*PGSIZE , PTE_USER) != <span class="literal">NULL</span>);</span><br><span class="line">    mm_count_inc(mm);</span><br><span class="line">    current-&gt;mm = mm;</span><br><span class="line">    current-&gt;cr3 = PADDR(mm-&gt;pgdir);</span><br><span class="line">    lcr3(PADDR(mm-&gt;pgdir));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 先算出所有参数加起来的长度</span></span><br><span class="line">    <span class="keyword">uint32_t</span> total_len = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; argc; ++i) &#123;</span><br><span class="line">        total_len += strnlen(kargv[i], EXEC_MAX_ARG_LEN) + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 用户栈顶 减去所有参数加起来的长度 再 4字节对齐 找到 真正存放字符串参数的栈的位置</span></span><br><span class="line">    <span class="keyword">char</span> *arg_str = (USTACKTOP - total_len) &amp; <span class="number">0xfffffffc</span>;</span><br><span class="line">    <span class="comment">// 放字符串参数的栈的位置的下面 是存放指向字符串参数的指针</span></span><br><span class="line">    <span class="keyword">int32_t</span> *arg_ptr = (<span class="keyword">int32_t</span> *)arg_str - argc;</span><br><span class="line">    <span class="comment">// 指向字符串参数的指针下面 是参数的个数</span></span><br><span class="line">    <span class="keyword">int32_t</span> *stacktop = arg_ptr - <span class="number">1</span>;</span><br><span class="line">    *stacktop = argc;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; argc; ++i) &#123;</span><br><span class="line">        <span class="keyword">uint32_t</span> arg_len = strnlen(kargv[i], EXEC_MAX_ARG_LEN);</span><br><span class="line">        <span class="built_in">strncpy</span>(arg_str, kargv[i], arg_len);</span><br><span class="line">        *arg_ptr = arg_str;</span><br><span class="line">        arg_str += arg_len + <span class="number">1</span>;</span><br><span class="line">        ++arg_ptr;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">trapframe</span> *<span class="title">tf</span> = <span class="title">current</span>-&gt;<span class="title">tf</span>;</span></span><br><span class="line">    <span class="built_in">memset</span>(tf, <span class="number">0</span>, <span class="keyword">sizeof</span>(struct trapframe));</span><br><span class="line"></span><br><span class="line">    tf-&gt;tf_cs = USER_CS;</span><br><span class="line">    tf-&gt;tf_ds = tf-&gt;tf_es = tf-&gt;tf_ss = USER_DS;</span><br><span class="line">    tf-&gt;tf_esp = stacktop;</span><br><span class="line">    tf-&gt;tf_eip = elf-&gt;e_entry;</span><br><span class="line">    tf-&gt;tf_eflags |= FL_IF;</span><br><span class="line">    ret = <span class="number">0</span>;</span><br><span class="line">out:</span><br><span class="line">    <span class="keyword">return</span> ret;</span><br><span class="line">bad_cleanup_mmap:</span><br><span class="line">    exit_mmap(mm);</span><br><span class="line">bad_elf_cleanup_pgdir:</span><br><span class="line">    put_pgdir(mm);</span><br><span class="line">bad_pgdir_cleanup_mm:</span><br><span class="line">    mm_destroy(mm);</span><br><span class="line">bad_mm:</span><br><span class="line">    <span class="keyword">goto</span> out;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="祝贺我通过自己的努力，完成了uCore-OS-lab1-lab8！"><a href="#祝贺我通过自己的努力，完成了uCore-OS-lab1-lab8！" class="headerlink" title="祝贺我通过自己的努力，完成了uCore OS lab1 - lab8！"></a>祝贺我通过自己的努力，完成了uCore OS lab1 - lab8！</h1><p>三个月从0到1 断断续续的做 机房做 宿舍做 回家做 终于完成了! 继续努力!</p>
<p>整个实验的<a href="https://github.com/Yu2erer/ucore_os_lab" target="_blank" rel="noopener">代码</a></p>
<p><img src="/images/Lab8_finish.png" alt="Lab8_finish"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>uCore</tag>
        <tag>Lab</tag>
        <tag>文件系统</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 I/O子系统</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-I_O%E5%AD%90%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="三种常见设备接口类型"><a href="#三种常见设备接口类型" class="headerlink" title="三种常见设备接口类型"></a>三种常见设备接口类型</h2><ul>
<li>字符设备<ul>
<li>键盘/鼠标 串口</li>
</ul>
</li>
<li>块设备<ul>
<li>磁盘 磁带 光驱</li>
</ul>
</li>
<li>网络设备<ul>
<li>以太网 无线 蓝牙</li>
</ul>
</li>
</ul>
<a id="more"></a>

<h3 id="三种设备访问特征"><a href="#三种设备访问特征" class="headerlink" title="三种设备访问特征"></a>三种设备访问特征</h3><ul>
<li>字符设备<ul>
<li>以字节为单位顺序访问</li>
<li>get() put() 通常使用文件访问接口和语义来访问</li>
</ul>
</li>
<li>块设备<ul>
<li>均匀的数据块访问</li>
<li>原始 I/O 或 文件系统接口</li>
<li>内存映射文件访问</li>
</ul>
</li>
<li>网络设备<ul>
<li>格式化报文交换</li>
<li>send/receive 网络报文</li>
<li>通过网络接口支持多种网络协议</li>
</ul>
</li>
</ul>
<h2 id="同步与异步I-O"><a href="#同步与异步I-O" class="headerlink" title="同步与异步I/O"></a>同步与异步I/O</h2><ol>
<li>用户发起 I/O请求 </li>
<li>请求会发送到内核中的设备驱动 </li>
<li>设备驱动将其转换为对硬件的控制 </li>
<li>硬件控制完成之后 会产生中断 由内核的中断处理例程进行响应</li>
<li>送回设备驱动 回到用户态</li>
</ol>
<ul>
<li>阻塞I/O Wait<ul>
<li>读数据时 进程进入等待状态 直到完成数据读出</li>
<li>写数据时 进程进入等待状态 直到设备完成数据写入处理</li>
</ul>
</li>
<li>非阻塞I/O Don’t Wait(可能会失败 或者少写)<ul>
<li>立即从read或write系统调用返回 返回值为成功传输的字节数</li>
<li>read或write的传输字节数可能为0</li>
</ul>
</li>
<li>异步I/O Tell Me Later<ul>
<li>读数据时 使用指针标记好用户缓冲区 立即返回 稍后内核将填充缓冲区并通知用户</li>
<li>写数据时 使用指针标记好用户缓冲区 立即返回 稍后内核将处理数据并通知用户</li>
</ul>
</li>
</ul>
<p><img src="/images/Asynchronous_I_O.png" alt="Asynchronous_I_O"></p>
<h2 id="I-O结构"><a href="#I-O结构" class="headerlink" title="I/O结构"></a>I/O结构</h2><ul>
<li>北桥(高速设备)<ul>
<li>内存</li>
<li>AGP/PCI-Express</li>
<li>Built-in Display</li>
</ul>
</li>
<li>南桥(I/O设备)<ul>
<li>ATA/IDE</li>
<li>PCI总线</li>
<li>USB/Firewire总线</li>
<li>Interrupt控制器</li>
</ul>
</li>
</ul>
<h3 id="CPU与设备的连接"><a href="#CPU与设备的连接" class="headerlink" title="CPU与设备的连接"></a>CPU与设备的连接</h3><p><img src="/images/Device_Connects_CPU.png" alt="Device_Connects_CPU"></p>
<ul>
<li>设备控制器<ul>
<li>CPU和I/O设备间的接口</li>
<li>向CPU提供特殊指令和寄存器</li>
</ul>
</li>
<li>I/O地址<ul>
<li>CPU用来控制I/O硬件</li>
<li>通过映射到内存地址或I/O指令对端口的操作进行访问</li>
</ul>
</li>
<li>CPU与设备的通信方式<ul>
<li>轮询(CPU直接访问I/O端口或者是映射到的内存地址 不用中断控制器)</li>
<li>设备中断</li>
<li>DMA(将数据直接放到内存)</li>
</ul>
</li>
</ul>
<h3 id="I-O指令和内存映射I-O"><a href="#I-O指令和内存映射I-O" class="headerlink" title="I/O指令和内存映射I/O"></a>I/O指令和内存映射I/O</h3><ul>
<li>I/O指令<ul>
<li>通过I/O端口号访问设备寄存器</li>
<li>特殊的CPU指令 out in</li>
</ul>
</li>
<li>内存映射I/O<ul>
<li>设备的寄存器/存储被映射到内存物理地址空间中</li>
<li>通过内存 load/store指令完成 I/O操作</li>
<li>MMU设置映射 硬件跳线或程序在启动时设置地址</li>
</ul>
</li>
</ul>
<h3 id="内核I-O结构"><a href="#内核I-O结构" class="headerlink" title="内核I/O结构"></a>内核I/O结构</h3><p><img src="/images/Kernel_I_O_Subsystem.png" alt="Kernel_I_O_Subsystem"></p>
<h3 id="I-O请求生命周期"><a href="#I-O请求生命周期" class="headerlink" title="I/O请求生命周期"></a>I/O请求生命周期</h3><p><img src="/images/Life_cycle_of_I_O_Request.png" alt="Life_cycle_of_I_O_Request"></p>
<h3 id="CPU与设备控制器的数据传输"><a href="#CPU与设备控制器的数据传输" class="headerlink" title="CPU与设备控制器的数据传输"></a>CPU与设备控制器的数据传输</h3><ul>
<li>程序控制I/O(PIO Programmed I/O)<ul>
<li>通过CPU的 in/out 或者 load/store 传输所有数据</li>
<li>硬件简单 编程容易</li>
<li>消耗的CPU时间和数据量成正比</li>
<li>适用于简单的 小型的设备I/O</li>
</ul>
</li>
<li>直接内存访问(DMA)<ul>
<li>设备控制器可直接访问系统总线</li>
<li>控制器直接与内存互相传输数据</li>
<li>设备传输数据不影响CPU</li>
<li>需要CPU参与设置</li>
<li>适用于高吞吐量I/O</li>
</ul>
</li>
</ul>
<h4 id="通过直接I-O寻址读取磁盘数据的步骤"><a href="#通过直接I-O寻址读取磁盘数据的步骤" class="headerlink" title="通过直接I/O寻址读取磁盘数据的步骤"></a>通过直接I/O寻址读取磁盘数据的步骤</h4><ol>
<li>设备驱动收到读取磁盘数据到内存地址X</li>
<li>设备驱动控制磁盘控制器从磁盘读取数据</li>
<li>磁盘控制器初始化DMA传送</li>
<li>磁盘控制器传送数据到DMA控制器</li>
<li>DMA控制器传送C字节数据到内存地址X</li>
<li>DMA控制器完成数据传输后 产生中断请求 通知CPU传送完成</li>
</ol>
<p><img src="/images/I_O_Addressing.png" alt="I_O_Addressing"></p>
<h4 id="I-O设备通知操作系统的机制"><a href="#I-O设备通知操作系统的机制" class="headerlink" title="I/O设备通知操作系统的机制"></a>I/O设备通知操作系统的机制</h4><p>操作系统需要了解设备状态</p>
<ul>
<li>I/O操作完成时间</li>
<li>I/O操作遇到错误</li>
</ul>
<p>两种方式通知操作系统</p>
<ul>
<li>轮询(我感觉都不像是通知了 明明是CPU自己去查)</li>
<li>设备中断</li>
</ul>
<p>一些设备可能结合了轮询和设备中断</p>
<ul>
<li>高带宽网络设备<ul>
<li>第一个传入数据包到达前采用中断</li>
<li>轮询 后面的数据包直到硬件缓存为空</li>
</ul>
</li>
</ul>
<h5 id="轮询"><a href="#轮询" class="headerlink" title="轮询"></a>轮询</h5><p>I/O设备在特定的 状态寄存器中放置状态和错误信息 操作系统 定期检测 状态寄存器</p>
<ul>
<li>简单</li>
<li>I/O操作频繁或不可预测时 开销大(总去查)和延时长(很长时间没去查)</li>
</ul>
<h5 id="设备中断"><a href="#设备中断" class="headerlink" title="设备中断"></a>设备中断</h5><p>设备中断处理例程</p>
<ol>
<li>CPU在 I/O 之前设置任务参数</li>
<li>CPU发出 I/O请求后 继续执行其他任务</li>
<li>I/O设备处理 I/O请求</li>
<li>I/O设备处理完成时 触发CPU中断请求</li>
<li>CPU接受中断 分发到相应中断处理例程</li>
</ol>
<p><img src="/images/Device_Interrupts.png" alt="Device_Interrupts"></p>
<ul>
<li>处理不可预测时间效果好(CPU会在每两条指令执行期间去检查是否有中断请求)</li>
<li>开销相对较高(CPU中断频率太高)</li>
</ul>
<h2 id="磁盘工作机制和性能参数"><a href="#磁盘工作机制和性能参数" class="headerlink" title="磁盘工作机制和性能参数"></a>磁盘工作机制和性能参数</h2><p>读取或写入时 磁头必须被定位在期望的磁道 并从所期望的柱面和扇区的开始</p>
<ul>
<li>寻道时间<ul>
<li>定位到期望的磁道所花费时间</li>
</ul>
</li>
<li>旋转延时<ul>
<li>从0扇区开始到达目的地花费的时间</li>
</ul>
</li>
</ul>
<p>平均旋转延迟时间 = 磁盘旋转一周的时间的一半</p>
<h3 id="磁盘I-O传输时间"><a href="#磁盘I-O传输时间" class="headerlink" title="磁盘I/O传输时间"></a>磁盘I/O传输时间</h3><p>Ta = Ts + 1/2r + b/rN</p>
<ul>
<li>Ts 寻道时间(时间最长 最需要优化)</li>
<li>1/2r 旋转延时的时间为磁盘旋转一周的时间的一半</li>
<li>b/rN <ul>
<li>b 传输的比特数</li>
<li>N 磁道上的比特数</li>
<li>r 磁盘转数</li>
</ul>
</li>
</ul>
<p><img src="/images/Disk_I_O_Transfer_times.png" alt="Disk_I_O_Transfer_times"></p>
<h3 id="磁盘调度算法"><a href="#磁盘调度算法" class="headerlink" title="磁盘调度算法"></a>磁盘调度算法</h3><p>通过优化磁盘访问请求顺序来提高磁盘访问性能</p>
<ul>
<li>寻道时间是磁道访问最耗时的部分</li>
<li>同时会有多个在同一磁盘上的I/O请求</li>
<li>随机处理磁盘访问请求的性能很差</li>
</ul>
<h4 id="先进先出算法-FIFO"><a href="#先进先出算法-FIFO" class="headerlink" title="先进先出算法(FIFO)"></a>先进先出算法(FIFO)</h4><ul>
<li>按顺序处理请求</li>
<li>公平对待所有进程</li>
<li>在有很多进程的情况下 接近随机调度的性能</li>
</ul>
<p>磁盘访问序列 = 98,183,37,122,14,124,65,67<br>初始磁头位置 53</p>
<p><img src="/images/FIFO_Disk_Scheduling_Algorithm.png" alt="FIFO_Disk_Scheduling_Algorithm"></p>
<h4 id="最短服务时间优先-SSTF"><a href="#最短服务时间优先-SSTF" class="headerlink" title="最短服务时间优先(SSTF)"></a>最短服务时间优先(SSTF)</h4><ul>
<li>选择从磁臂当前位置需要移动最少的I/O请求</li>
<li>总是选择最短寻道时间</li>
</ul>
<p>磁盘访问序列 = 98,183,37,122,14,124,65,67<br>初始磁头位置 53</p>
<p><img src="/images/SSTF_Disk_Scheduling_Algorithm.png" alt="SSTF_Disk_Scheduling_Algorithm"></p>
<h4 id="扫描算法-SCAN"><a href="#扫描算法-SCAN" class="headerlink" title="扫描算法(SCAN)"></a>扫描算法(SCAN)</h4><p>磁臂在一个方向上移动 访问所有未完成的请求 直到磁臂到达该方向上最后的磁道 也称为电梯算法(elevator algorithm)</p>
<ul>
<li>中间磁道的访问性能较好 两头的比较差 C-SCAN算法改进了这个缺点</li>
</ul>
<p>磁盘访问序列 = 98,183,37,122,14,124,65,67<br>初始磁头位置 53</p>
<p><img src="/images/SCAN_Disk_Scheduling_Algorithm.png" alt="SCAN_Disk_Scheduling_Algorithm"></p>
<h4 id="循环扫描算法-C-SCAN"><a href="#循环扫描算法-C-SCAN" class="headerlink" title="循环扫描算法(C-SCAN)"></a>循环扫描算法(C-SCAN)</h4><p>限制了仅在一个方向上扫描 当最后一个磁道也被访问过了以后 磁币返回到磁盘的另外一段再次进行</p>
<ul>
<li>就算对头没有I/O请求也要走到头 浪费了 C-LOOK算法改进了这个缺点</li>
</ul>
<h4 id="C-LOOK算法"><a href="#C-LOOK算法" class="headerlink" title="C-LOOK算法"></a>C-LOOK算法</h4><p>磁臂先到达该方向上最后一个请求处 然后立即反转 而不是先到最后点路径上的所有请求</p>
<h4 id="N步扫描-N-Step-SCAN-算法"><a href="#N步扫描-N-Step-SCAN-算法" class="headerlink" title="N步扫描(N-Step-SCAN)算法"></a>N步扫描(N-Step-SCAN)算法</h4><p>用于解决磁头粘着问题</p>
<ul>
<li><p>磁头粘着(Arm Stickiness)现象</p>
<ul>
<li>SSTF SCAN CSCAN等算法中 可能出现磁头停留在某处不动的情况</li>
<li>进程反复请求对某一磁道的I/O操作</li>
</ul>
</li>
<li><p>将磁盘请求队列分成长度为N的子队列</p>
</li>
<li><p>按FIFO算法依次处理所有子队列</p>
</li>
<li><p>扫描算法处理每个队列</p>
</li>
</ul>
<h4 id="双队列扫描算法-FSCAN"><a href="#双队列扫描算法-FSCAN" class="headerlink" title="双队列扫描算法(FSCAN)"></a>双队列扫描算法(FSCAN)</h4><p>FSCAN算法是N步扫描算法的简化 只将磁盘请求队列分成两个子队列 可以减少平均等待时间</p>
<ul>
<li>把磁盘I/O请求分成两个队列</li>
<li>交替使用扫描算法处理一个队列</li>
<li>新生成的磁盘I/O请求放入另一队列中 所有的新请求都将被推迟到下一次扫描时处理</li>
</ul>
<h3 id="磁盘缓存"><a href="#磁盘缓存" class="headerlink" title="磁盘缓存"></a>磁盘缓存</h3><p>磁盘缓存是磁盘扇区在内存中的缓存区</p>
<ul>
<li>磁盘缓存的调度算法很类似虚拟存储调度算法</li>
<li>磁盘的访问频率远低于虚拟存储中的内存访问频率</li>
<li>通常磁盘缓存调度算法会比虚拟存储复杂</li>
</ul>
<h4 id="单缓存与双缓存"><a href="#单缓存与双缓存" class="headerlink" title="单缓存与双缓存"></a>单缓存与双缓存</h4><ul>
<li>单缓存(Single Buffer Cache) <ul>
<li>读和写不能同时进行 速度受限</li>
</ul>
</li>
</ul>
<p><img src="/images/Single_Buffer_Cache.png" alt="Single_Buffer_Cache"></p>
<ul>
<li>双缓存(Double Buffer Cache)<ul>
<li>读和写可同时进行</li>
</ul>
</li>
</ul>
<p><img src="/images/Double_Buffer_Cache.png" alt="Double_Buffer_Cache"></p>
<h4 id="访问频率置换算法-Frequency-based-Replacement"><a href="#访问频率置换算法-Frequency-based-Replacement" class="headerlink" title="访问频率置换算法(Frequency-based Replacement)"></a>访问频率置换算法(Frequency-based Replacement)</h4><ul>
<li>解决的问题<ul>
<li>在一段密集磁盘访问后 LFU算法的引用计数变化无法反映当前的引用情况</li>
</ul>
</li>
<li>算法思路<ul>
<li>考虑磁盘访问的密集特征 对密集引用不计数</li>
<li>在短周期中使用LRU算法 而在长周期中使用LFU算法</li>
</ul>
</li>
</ul>
<p>把LRU算法中的特殊栈分成三部分 并在每个缓存块增加一个引用计数</p>
<p><img src="/images/Frequency_based_Replacement.png" alt="Frequency_based_Replacement"></p>
<ul>
<li>栈中缓存块被访问时移到栈顶 如果该块在新区域 引用计数不变 否则 引用计数加1<ul>
<li>在新区域中引用计数不变的目的是避免密集访问对引用计数不利影响</li>
<li>在中间区域和旧区域中引用计数加1是为了使用LFU算法</li>
</ul>
</li>
<li>未缓存数据块读入后放在栈顶，引用计数为1</li>
<li>中间区域的定义是为了避免新读入的缓存块在第一次出新区域时马上被置换 有一个过渡期</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>磁盘调度算法</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 信号量</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E4%BF%A1%E5%8F%B7%E9%87%8F/</url>
    <content><![CDATA[<h2 id="信号量-Semaphore"><a href="#信号量-Semaphore" class="headerlink" title="信号量(Semaphore)"></a>信号量(Semaphore)</h2><p>操作系统提供的一种协调共享资源访问的方法 由 Dijkstra 提出</p>
<ul>
<li>软件同步是平等线程间的一种同步协调机制</li>
<li>OS是管理者 地位高于进程</li>
<li>用信号量表示系统资源数量</li>
<li>早期操作系统主要同步机制(现在很少用)</li>
<li>信号量是一种抽象数据类型<ul>
<li>由一个整型变量(sem)和两个原子操作组成</li>
<li>P() sem - 1 若 sem &lt; 0 进入等待 否则继续</li>
<li>V() sem + 1 若 sem &lt;= 0 唤醒一个等待进程</li>
</ul>
</li>
</ul>
<a id="more"></a>

<p><img src="/images/Synchronization_method_semaphore.png" alt="Synchronization_method_semaphore"></p>
<h3 id="信号量的特性"><a href="#信号量的特性" class="headerlink" title="信号量的特性"></a>信号量的特性</h3><p>信号量是 被保护 的整数变量</p>
<ul>
<li>初始化完成后 只能通过 P() 和 V() 操作修改</li>
<li>由操作系统保证 PV操作是原子操作</li>
<li>P() 可能阻塞(没有资源 处于等待状态)</li>
<li>V() 不会阻塞(只会释放资源 唤醒等待状态的进程)</li>
</ul>
<p>通常假定信号量是 公平的</p>
<ul>
<li>线程不会被无限期阻塞在 P() 操作</li>
<li>假定信号量等待按先进先出排队</li>
</ul>
<h3 id="信号量的实现"><a href="#信号量的实现" class="headerlink" title="信号量的实现"></a>信号量的实现</h3><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Semaphore</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> sem;</span><br><span class="line">    WaitQueue q;</span><br><span class="line">&#125;</span><br><span class="line">Semaphore::P() &#123;</span><br><span class="line">   sem--;</span><br><span class="line">   <span class="keyword">if</span> (sem &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        Add <span class="keyword">this</span> thread t to q;</span><br><span class="line">        block(p);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">Semaphore::V() &#123;</span><br><span class="line">    sem++; </span><br><span class="line">    <span class="keyword">if</span> (sem &lt;= <span class="number">0</span>) &#123; <span class="comment">// +1 后 还小于等于 0 说明前面有进程还在等待</span></span><br><span class="line">        Remove a thread t from q;</span><br><span class="line">        wakeup(t);        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="信号量分类"><a href="#信号量分类" class="headerlink" title="信号量分类"></a>信号量分类</h3><ul>
<li>二进制信号量 资源数目为 0 或 1</li>
<li>资源信号量 可为任何非负值</li>
<li>两者等价 基于一个可以实现另一个</li>
</ul>
<h3 id="信号量的使用"><a href="#信号量的使用" class="headerlink" title="信号量的使用"></a>信号量的使用</h3><ul>
<li>互斥访问<ul>
<li>临界区的互斥访问控制</li>
</ul>
</li>
<li>条件同步<ul>
<li>线程间等事件等待</li>
</ul>
</li>
</ul>
<h4 id="信号量实现临界区互斥访问"><a href="#信号量实现临界区互斥访问" class="headerlink" title="信号量实现临界区互斥访问"></a>信号量实现临界区互斥访问</h4><p>每类资源设置一个信号量 初值为 1</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">mutex = <span class="keyword">new</span> Semaphore(<span class="number">1</span>);</span><br><span class="line">------------------------</span><br><span class="line">mutex-&gt;P();</span><br><span class="line"><span class="comment">/* 信号量 - 1 = 0 可以进入</span></span><br><span class="line"><span class="comment">    若第二个进程也想进入</span></span><br><span class="line"><span class="comment">    信号量 - 1 = -1 在这里进入等待状态 */</span></span><br><span class="line">Critical Section;</span><br><span class="line">mutex-&gt;V();</span><br><span class="line"><span class="comment">/* 信号量 + 1 = 0 说明有第二个线程还在等待状态</span></span><br><span class="line"><span class="comment">    唤醒 第二个线程 */</span></span><br></pre></td></tr></table></figure>

<ul>
<li>必须成对使用 P() 和 V() 操作<ul>
<li>P() 保证互斥访问临界资源</li>
<li>V() 在使用后释放临界资源</li>
<li>PV操作不能次序错误 重复 遗漏</li>
</ul>
</li>
</ul>
<h4 id="信号量实现条件同步"><a href="#信号量实现条件同步" class="headerlink" title="信号量实现条件同步"></a>信号量实现条件同步</h4><p>条件同步设置一个信号量 初值为 0(事件出现 + 1)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">condition = <span class="keyword">new</span> Semaphore(<span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>线程A</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">...M...</span><br><span class="line"></span><br><span class="line">condition-&gt;P(); </span><br><span class="line">&#x2F;*</span><br><span class="line">    若此时线程B还未执行完X模块</span><br><span class="line">    那么此时信号量 就会由 0 - 1 &#x3D; -1</span><br><span class="line">    进入等待状态 等候 线程B执行完X模块 执行V()操作</span><br><span class="line">    将其唤醒</span><br><span class="line"></span><br><span class="line">    若此时线程B 已经执行完X模块</span><br><span class="line">    那么此时信号量 就会由 0 + 1 &#x3D; 1</span><br><span class="line">    则线程A直接继续往下执行</span><br><span class="line">*&#x2F;</span><br><span class="line"></span><br><span class="line">...需要线程B执行完 X模块...</span><br></pre></td></tr></table></figure>

<p>线程B</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">...X...</span><br><span class="line"></span><br><span class="line">condition-&gt;V();</span><br><span class="line"></span><br><span class="line">...Y...</span><br></pre></td></tr></table></figure>

<h4 id="生产者消费者问题"><a href="#生产者消费者问题" class="headerlink" title="生产者消费者问题"></a>生产者消费者问题</h4><p>有界缓冲区的生产者消费者问题</p>
<ul>
<li>一个或多个生产者在生成数据后放在一个缓冲区里</li>
<li>单个消费者从缓冲区取出数据处理</li>
<li>任何时刻 只能有一个 生产者或消费者可访问缓冲区</li>
</ul>
<p><img src="/images/Producer-consumer_problem.png" alt="Producer-consumer_problem"></p>
<h5 id="信号量解决生产者消费者问题"><a href="#信号量解决生产者消费者问题" class="headerlink" title="信号量解决生产者消费者问题"></a>信号量解决生产者消费者问题</h5><ul>
<li>任何时刻只能有一个线程操作缓冲区 (互斥访问)</li>
<li>缓冲区空时 消费者必须等待生产者 (条件同步)</li>
<li>缓冲区满时 生产者必须等待消费者 (条件同步)</li>
</ul>
<p>用信号量描述每个约束</p>
<ul>
<li>二进制信号量(mutex) 描述 互斥关系</li>
<li>资源信号量(fullBuffers) 描述 缓冲区有数据</li>
<li>资源信号量(emptyBuffers) 描述 缓冲区有位置</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Class BoundedBuffer &#123;</span><br><span class="line">    mutex = <span class="keyword">new</span> Semaphore(<span class="number">1</span>);</span><br><span class="line">    fullBuffers = <span class="keyword">new</span> Semaphore(<span class="number">0</span>);</span><br><span class="line">    emptyBuffers = <span class="keyword">new</span> Semaphore(n);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 生产者</span></span><br><span class="line">BoundedBuffer::Deposit(c) &#123;</span><br><span class="line">    emptyBuffers-&gt;P(); </span><br><span class="line"><span class="comment">// 检查是否有空缓存区 emptyBuffers 信号量 - 1</span></span><br><span class="line"></span><br><span class="line">    mutex-&gt;P(); <span class="comment">// 互斥访问临界区</span></span><br><span class="line">    Add c to the <span class="built_in">buffer</span>;</span><br><span class="line">    mutex-&gt;V();</span><br><span class="line"></span><br><span class="line">    fullBuffers-&gt;V();</span><br><span class="line"><span class="comment">// 写入了一个数据到缓冲区 fullBuffers + 1</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 消费者</span></span><br><span class="line">BoundedBuffer::Remove(c) &#123;</span><br><span class="line">    fullBuffers-&gt;P(); </span><br><span class="line"><span class="comment">// 检查缓冲区是否有数据 fullBuffers + 1</span></span><br><span class="line"></span><br><span class="line">    mutex-&gt;P(); <span class="comment">// 互斥访问临界区</span></span><br><span class="line">    Remove c from <span class="built_in">buffer</span>;</span><br><span class="line">    mutex-&gt;V();</span><br><span class="line"></span><br><span class="line">    emptyBuffers-&gt;V(); </span><br><span class="line"><span class="comment">// 取出缓冲区的内容 emptyBuffers 信号量 + 1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="使用信号量的困难"><a href="#使用信号量的困难" class="headerlink" title="使用信号量的困难"></a>使用信号量的困难</h4><ul>
<li>读/开发代码比较困难<ul>
<li>需要能运用信号量</li>
</ul>
</li>
<li>容易出错<ul>
<li>使用的信号量被另一个线程占用</li>
<li>忘记释放信号量</li>
</ul>
</li>
<li>不能处理死锁</li>
</ul>
<h2 id="管程-Monitor"><a href="#管程-Monitor" class="headerlink" title="管程(Monitor)"></a>管程(Monitor)</h2><p>用于多线程互斥访问 共享资源 的程序结构</p>
<ul>
<li>采用面向对象方法 简化了线程间的同步控制</li>
<li>任一时刻最多只有一个线程执行管程代码</li>
<li>正在管程中的线程 可临时放弃 管程的互斥访问 等待事件出现时恢复</li>
</ul>
<p><img src="/images/Synchronization_method_monitor.png" alt="Synchronization_method_monitor"></p>
<h3 id="管程的使用"><a href="#管程的使用" class="headerlink" title="管程的使用"></a>管程的使用</h3><ul>
<li>在对象/模块中 收集相关共享数据</li>
<li>定义访问共享数据的方法</li>
</ul>
<h3 id="管程的组成"><a href="#管程的组成" class="headerlink" title="管程的组成"></a>管程的组成</h3><ul>
<li>一个锁<ul>
<li>控制管程代码的互斥访问</li>
</ul>
</li>
<li>0或者多个条件变量(0个条件变量 则和 信号量差不多)<ul>
<li>管理共享数据的并发访问</li>
</ul>
</li>
</ul>
<p><img src="/images/Monitor.png" alt="Monitor"></p>
<h4 id="条件变量-Condition-Variable"><a href="#条件变量-Condition-Variable" class="headerlink" title="条件变量(Condition Variable)"></a>条件变量(Condition Variable)</h4><p>管程内的等待机制</p>
<ul>
<li>进入管程的线程因资源被占用而进入等待状态</li>
<li>每个条件变量表示一种等待原因 对应一个等待队列</li>
<li>Wait()操作<ul>
<li>将自己阻塞在等待队列中</li>
<li>唤醒一个等待者或释放管程的互斥访问</li>
</ul>
</li>
<li>Signal()操作<ul>
<li>将等待队列中的一个线程唤醒</li>
<li>如果等待队列为空 则等同于空操作</li>
</ul>
</li>
</ul>
<h5 id="条件变量实现"><a href="#条件变量实现" class="headerlink" title="条件变量实现"></a>条件变量实现</h5><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Class Condition &#123;</span><br><span class="line">    <span class="keyword">int</span> numWaiting = <span class="number">0</span>; <span class="comment">// 等待线程数</span></span><br><span class="line">    WaitQueue q;</span><br><span class="line">&#125;</span><br><span class="line">Condition::Wait(lock)&#123;</span><br><span class="line">    numWaiting++; <span class="comment">// 有线程处于等待状态</span></span><br><span class="line">    Add <span class="keyword">this</span> thread t  to q;</span><br><span class="line">    <span class="built_in">release</span>(lock); <span class="comment">// 释放管程的访问权</span></span><br><span class="line">    schedule(); <span class="comment">//need mutex</span></span><br><span class="line">    require(lock); <span class="comment">// 请求管程访问权</span></span><br><span class="line">&#125;</span><br><span class="line">Condition::Signal()&#123;</span><br><span class="line">    <span class="keyword">if</span> (numWaiting &gt; <span class="number">0</span>) &#123; </span><br><span class="line">    <span class="comment">// 如果等待队列为空 则为空操作</span></span><br><span class="line">        Remove a thread t from q;</span><br><span class="line">        wakeup(t); <span class="comment">//need mutex</span></span><br><span class="line">        numWaiting--;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="管程解决生产者消费者问题"><a href="#管程解决生产者消费者问题" class="headerlink" title="管程解决生产者消费者问题"></a>管程解决生产者消费者问题</h4><figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BoundedBuffer</span> &#123;</span></span><br><span class="line">    Lock lock;</span><br><span class="line">    <span class="comment">// 管程入口的锁</span></span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">    <span class="comment">// 缓冲区里的个数</span></span><br><span class="line">    Condition notFull, notEmpty;</span><br><span class="line">    <span class="comment">// 条件变量</span></span><br><span class="line">&#125;</span><br><span class="line">BoundedBuffer::Deposit(c) &#123;</span><br><span class="line">    lock-&gt;Acquire();</span><br><span class="line">    <span class="comment">// 管程进入申请</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    用信号量解决的时候 不能把检查条件放在申请互斥操作的后面 是因为会产生死锁 因为都申请了互斥操作了 别的线程不可读不可写 当前线程只能一直在那等</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">    而用管程不同 它可以放弃管程的互斥访问权限 调度到其他线程去</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">    <span class="keyword">while</span> (count == n)</span><br><span class="line">        notFull.Wait(&amp;lock);</span><br><span class="line">    <span class="comment">// 判断缓冲区是否已经满了 满了就等待条件满足 再继续执行</span></span><br><span class="line"></span><br><span class="line">    Add c to the <span class="built_in">buffer</span>;</span><br><span class="line">    count++;</span><br><span class="line"></span><br><span class="line">    notEmpty.Signal();</span><br><span class="line"></span><br><span class="line">    lock-&gt;Release();</span><br><span class="line">    <span class="comment">// 管程释放</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">BoundedBuffer::Remove(c) &#123;</span><br><span class="line">    lock-&gt;Acquire();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (count == <span class="number">0</span>)    </span><br><span class="line">        notEmpty.Wait(&amp;lock);</span><br><span class="line"></span><br><span class="line">    Remove c from <span class="built_in">buffer</span>;</span><br><span class="line">    count--;</span><br><span class="line"></span><br><span class="line">    notFull.Signal();</span><br><span class="line">    <span class="comment">// 将等待线程中的其中一个唤醒</span></span><br><span class="line"></span><br><span class="line">    lock-&gt;Release();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="管程条件变量的释放处理方式"><a href="#管程条件变量的释放处理方式" class="headerlink" title="管程条件变量的释放处理方式"></a>管程条件变量的释放处理方式</h3><ul>
<li>Hansen管程(当前正在执行的线程更优先)<ul>
<li>用于真实OS或Java中</li>
</ul>
</li>
<li>Hoare管程<ul>
<li>多见于教材</li>
</ul>
</li>
</ul>
<h4 id="Hansen管程"><a href="#Hansen管程" class="headerlink" title="Hansen管程"></a>Hansen管程</h4><p>当前正在执行的线程优先级更高 因此线程连续执行 所以其效率更高</p>
<ul>
<li>条件变量释放仅是一个提示 需要重新检查条件 用 while</li>
</ul>
<p>因为在另一个线程中 它释放了条件变量 但是它还是在继续往下执行 有可能在这段期间 条件变量发生变化</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Hansen_style::Deposit()&#123;</span><br><span class="line">  lock-&gt;acquire();</span><br><span class="line">  <span class="keyword">while</span> (count == n) &#123; <span class="comment">// 这里是 while</span></span><br><span class="line">       notFull.wait(&amp;lock); </span><br><span class="line">  &#125;</span><br><span class="line">  Add thing;</span><br><span class="line">  count++;</span><br><span class="line">  notEmpty.signal();</span><br><span class="line">  lock-&gt;<span class="built_in">release</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/Hansen_monitor.png" alt="Hansen_monitor"></p>
<h4 id="Hoare管程"><a href="#Hoare管程" class="headerlink" title="Hoare管程"></a>Hoare管程</h4><p>等待条件变量的优先级更高</p>
<ul>
<li>条件变量释放同时表示放弃管程访问</li>
<li>释放后条件变量的状态可用</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Hoare_style::Deposit()&#123;</span><br><span class="line">  lock-&gt;acquire();</span><br><span class="line">  <span class="keyword">if</span> (count == n) &#123; <span class="comment">// 这里是 if</span></span><br><span class="line">       notFull.wait(&amp;lock); </span><br><span class="line">  &#125;</span><br><span class="line">  Add thing;</span><br><span class="line">  count++;</span><br><span class="line">  notEmpty.signal();</span><br><span class="line">  lock-&gt;<span class="built_in">release</span>();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="/images/Hoare_monitor.png" alt="Hoare_monitor"></p>
<h4 id="哲学家就餐问题"><a href="#哲学家就餐问题" class="headerlink" title="哲学家就餐问题"></a>哲学家就餐问题</h4><p>5个哲学家围绕一张圆桌坐 </p>
<ul>
<li>桌子放5支叉子</li>
<li>每两个哲学家之间放一支</li>
<li>哲学家动作包含思考和就餐<ul>
<li>进餐时需同时拿到左右两边两把叉子</li>
<li>思考时将两支叉子放回原处</li>
</ul>
</li>
<li>要求不出现有人永远拿不到叉子</li>
</ul>
<p><img src="/images/Dining_philosophers_problem.png" alt="Dining_philosophers_problem"></p>
<h5 id="信号量解决哲学家就餐问题"><a href="#信号量解决哲学家就餐问题" class="headerlink" title="信号量解决哲学家就餐问题"></a>信号量解决哲学家就餐问题</h5><p>方案1</p>
<ul>
<li>会发生死锁! </li>
<li>当五个哲学家同时拿起左边的叉子 所有人都在等待别人放下右边的叉子</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 5 <span class="comment">// 哲学家个数</span></span></span><br><span class="line">semaphore fork[<span class="number">5</span>]; <span class="comment">// 5支叉子的信号量初值为1</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">philosopher</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123; </span><br><span class="line">    <span class="comment">// 哲学家编号：0 － 4</span></span><br><span class="line">    <span class="keyword">while</span> (ture) &#123;</span><br><span class="line">        think();</span><br><span class="line">        P(fork[i]); <span class="comment">// 拿左边的叉子</span></span><br><span class="line">        P(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 拿右边的叉子</span></span><br><span class="line">        eat();</span><br><span class="line">        V(fork[i]); <span class="comment">// 放下左边的叉子</span></span><br><span class="line">        V(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 放下右边的叉子</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>方案2</p>
<ul>
<li>互斥访问正确</li>
<li>每次只能有一个哲学家进餐 性能差</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 5 <span class="comment">// 哲学家个数</span></span></span><br><span class="line">semaphore fork[<span class="number">5</span>]; <span class="comment">// 信号量初值为1</span></span><br><span class="line">semaphore mutex; <span class="comment">// 互斥信号量，初值1</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">philosopher</span><span class="params">(<span class="keyword">int</span> i)</span></span></span><br><span class="line"><span class="function"><span class="comment">// 哲学家编号：0 － 4</span></span></span><br><span class="line"><span class="function">    <span class="title">while</span><span class="params">(TRUE)</span> </span>&#123;</span><br><span class="line">        think();</span><br><span class="line">        P(mutex); <span class="comment">// 进入临界区	 只有一个哲学家能就餐</span></span><br><span class="line"></span><br><span class="line">        P(fork[i]); <span class="comment">// 去拿左边的叉子</span></span><br><span class="line">        P(fork[(i + <span class="number">1</span>) % N]);<span class="comment">// 去拿右边的叉子</span></span><br><span class="line">        eat();	</span><br><span class="line">        V(fork[i]); <span class="comment">// 放下左边的叉子</span></span><br><span class="line">        V(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 放下右边的叉子</span></span><br><span class="line"></span><br><span class="line">        V(mutex); <span class="comment">// 退出临界区	</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>方案3</p>
<ul>
<li>根据编号不同 采取不同动作 避免都拿到一部分资源构造环路的情况</li>
<li>没有死锁 并允许多人同时就餐</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 5 <span class="comment">// 哲学家个数</span></span></span><br><span class="line">semaphore fork[<span class="number">5</span>]; <span class="comment">// 信号量初值为1</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">philosopher</span><span class="params">(<span class="keyword">int</span> i)</span> <span class="comment">// 哲学家编号：0 － 4</span></span></span><br><span class="line"><span class="function">    <span class="title">while</span><span class="params">(TRUE)</span> </span>&#123;</span><br><span class="line">        think();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (i % <span class="number">2</span> == <span class="number">0</span>) &#123; </span><br><span class="line">            <span class="comment">// 偶数 先拿左 后拿右 奇数 先拿右 后拿左</span></span><br><span class="line">            P(fork[i]); <span class="comment">// 去拿左边的叉子</span></span><br><span class="line">            P(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 去拿右边的叉子</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            P(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 去拿右边的叉子</span></span><br><span class="line">            P(fork[i]); <span class="comment">// 去拿左边的叉子 </span></span><br><span class="line">        &#125;</span><br><span class="line">        eat();</span><br><span class="line"></span><br><span class="line">        V(fork[i]); <span class="comment">// 放下左边的叉子</span></span><br><span class="line">        V(fork[(i + <span class="number">1</span>) % N]); <span class="comment">// 放下右边的叉子</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="读者写者问题"><a href="#读者写者问题" class="headerlink" title="读者写者问题"></a>读者写者问题</h4><p>共享资源的两类使用者</p>
<ul>
<li>读者 只读取数据 不修改</li>
<li>写者 读取和修改数据</li>
<li>读读允许<ul>
<li>同一时刻允许有多个读者同时读</li>
</ul>
</li>
<li>读写互斥<ul>
<li>没有写者时 读者才能读</li>
<li>没有读者时 写者才能写</li>
</ul>
</li>
<li>写写互斥<ul>
<li>没有其他写者时 写者才能写</li>
</ul>
</li>
</ul>
<h5 id="信号量解决读者写者问题"><a href="#信号量解决读者写者问题" class="headerlink" title="信号量解决读者写者问题"></a>信号量解决读者写者问题</h5><p>此方案 读者优先</p>
<ul>
<li>信号量WriteMutex<ul>
<li>控制读写操作互斥</li>
<li>初始化为 1</li>
</ul>
</li>
<li>读者计数Rcount<ul>
<li>正在进行读操作的读者数目</li>
<li>初始化为 0</li>
</ul>
</li>
<li>信号量CountMutex(对 Rcount 进行保护)<ul>
<li>控制对读者计数的互斥修改</li>
<li>初始化为 1</li>
</ul>
</li>
</ul>
<p>Write线程</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">P(WriteMutex); <span class="comment">// 读写互斥</span></span><br><span class="line">    <span class="built_in">write</span>();</span><br><span class="line">V(WriteMutex);</span><br></pre></td></tr></table></figure>

<p>Reader线程</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">P(CountMutex); <span class="comment">// 保护 Rcount</span></span><br><span class="line"><span class="keyword">if</span> (Rcount == <span class="number">0</span>)</span><br><span class="line">    P(WriteMutex); </span><br><span class="line">    <span class="comment">// 只在第一个读者 开启读写互斥 </span></span><br><span class="line">++Rcount; <span class="comment">// 加一读者</span></span><br><span class="line">V(CountMutex);</span><br><span class="line"><span class="built_in">read</span>();</span><br><span class="line">P(CountMutex);</span><br><span class="line">--Rcount;</span><br><span class="line"><span class="keyword">if</span> (Rcount == <span class="number">0</span>)</span><br><span class="line">    V(WriteMutex); </span><br><span class="line">    <span class="comment">// 没有读者了 让写者有机会访问临界区去写</span></span><br><span class="line">V(CountMutex);</span><br></pre></td></tr></table></figure>

<h5 id="读者写者问题的优先策略"><a href="#读者写者问题的优先策略" class="headerlink" title="读者写者问题的优先策略"></a>读者写者问题的优先策略</h5><ul>
<li>读者优先策略<ul>
<li>只要有读者正在读状态 后来的读者都能直接进入</li>
<li>若读者持续不断进入 则写者就处于饥饿</li>
</ul>
</li>
<li>写者优先策略<ul>
<li>只要有写者就绪 写者应尽快执行写操作</li>
<li>若写者持续不断就绪 则读者就处于饥饿</li>
</ul>
</li>
</ul>
<h5 id="管程解决读者写者问题"><a href="#管程解决读者写者问题" class="headerlink" title="管程解决读者写者问题"></a>管程解决读者写者问题</h5><p>管程的状态变量</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">AR = <span class="number">0</span>; <span class="comment">// 当前读的读者</span></span><br><span class="line">AW = <span class="number">0</span>; <span class="comment">// 当前写的写者</span></span><br><span class="line">WR = <span class="number">0</span>; <span class="comment">// 等待读的读者</span></span><br><span class="line">WW = <span class="number">0</span>; <span class="comment">// 等待写的写者</span></span><br><span class="line">Lock lock;</span><br><span class="line">Condition okToRead;</span><br><span class="line">Condition okToWrite;</span><br></pre></td></tr></table></figure>

<p>读者</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Database::Read() &#123;</span><br><span class="line">    <span class="comment">// Wait until no writers;</span></span><br><span class="line"></span><br><span class="line">    StartRead(); </span><br><span class="line">    <span class="built_in">read</span> database;</span><br><span class="line">    <span class="comment">// check out – wake up waiting writers; </span></span><br><span class="line">    DoneRead();</span><br><span class="line">&#125;</span><br><span class="line">Database::StartRead() &#123;</span><br><span class="line">    lock.Acquire();</span><br><span class="line">    <span class="keyword">while</span> ((AW+WW) &gt; <span class="number">0</span>) &#123; </span><br><span class="line">    <span class="comment">// 只要有写者就等 说明写者优先</span></span><br><span class="line">        WR++;</span><br><span class="line">        okToRead.wait(&amp;lock);</span><br><span class="line">        WR--;</span><br><span class="line">    &#125;</span><br><span class="line">    AR++;</span><br><span class="line">    lock.Release();</span><br><span class="line">&#125;</span><br><span class="line">Database::DoneRead() &#123;</span><br><span class="line">    lock.Acquire();</span><br><span class="line">    AR--;</span><br><span class="line">    <span class="keyword">if</span> (AR == <span class="number">0</span> &amp;&amp; WW &gt; <span class="number">0</span>) &#123; </span><br><span class="line">    <span class="comment">// 当前没有读者且有等待写的写者 则唤醒 写者</span></span><br><span class="line">        okToWrite.signal();</span><br><span class="line">    &#125;</span><br><span class="line">    lock.Release();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写者</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">Database::Write() &#123;</span><br><span class="line">    <span class="comment">// Wait until no readers/writers;</span></span><br><span class="line"></span><br><span class="line">    StartWrite(); </span><br><span class="line">    <span class="built_in">write</span> database;</span><br><span class="line">    <span class="comment">// check out-wake up waiting readers/writers; </span></span><br><span class="line">    DoneWrite(); </span><br><span class="line">&#125;</span><br><span class="line">Database::StartWrite() &#123;</span><br><span class="line">    lock.Acquire();</span><br><span class="line">    <span class="keyword">while</span> ((AW+AR) &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 只要有正在写的写者或者正在读的读者 就等(不等等待读的读者 说明写者优先)</span></span><br><span class="line">        WW++;</span><br><span class="line">        okToWrite.wait(&amp;lock);</span><br><span class="line">        WW--;</span><br><span class="line">    &#125;</span><br><span class="line">    AW++;</span><br><span class="line">    lock.Release();</span><br><span class="line">&#125;</span><br><span class="line">Database::DoneWrite() &#123;</span><br><span class="line">    lock.Acquire();</span><br><span class="line">    AW--;</span><br><span class="line">    <span class="keyword">if</span> (WW &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 优先唤醒等待写的写者</span></span><br><span class="line">        okToWrite.signal();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span> (WR &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="comment">// 如果没有等待写的写者 才唤醒等待读的读者</span></span><br><span class="line">        okToRead.broadcast();</span><br><span class="line">    &#125;</span><br><span class="line">    lock.Release();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>信号量</tag>
        <tag>同步互斥</tag>
        <tag>管程</tag>
        <tag>哲学家就餐</tag>
        <tag>并发</tag>
        <tag>临界区</tag>
        <tag>生产者消费者</tag>
        <tag>读者写者</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 同步互斥</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E5%90%8C%E6%AD%A5%E4%BA%92%E6%96%A5/</url>
    <content><![CDATA[<h3 id="并发进程的正确性"><a href="#并发进程的正确性" class="headerlink" title="并发进程的正确性"></a>并发进程的正确性</h3><p>执行过程是不确定性和不可重现的 程序错误可能是间歇性发生的</p>
<ul>
<li>独立进程<ul>
<li>不和其他进程共享资源或状态</li>
<li>确定性 输入状态决定结果</li>
<li>可重现 能够重现起始条件</li>
<li>调度顺序不重要</li>
</ul>
</li>
<li>并发进程<ul>
<li>在多个进程间有资源共享(都要CPU 内存)</li>
<li>不确定性</li>
<li>不可重现</li>
</ul>
</li>
</ul>
<a id="more"></a>

<h3 id="进程并发执行的好处"><a href="#进程并发执行的好处" class="headerlink" title="进程并发执行的好处"></a>进程并发执行的好处</h3><p>进程需要与计算机中其他进程或设备进行协作</p>
<ol>
<li>共享资源</li>
<li>加速<ul>
<li>I/O操作和CPU计算可以并行</li>
<li>程序可划分成多个模块放在多个处理机上并行执行</li>
</ul>
</li>
<li>模块化<ul>
<li>将大程序分解成小程序(gcc会调用 cpp cc1 cc2 as ld)</li>
<li>使系统易于复用和扩展</li>
</ul>
</li>
</ol>
<h3 id="原子操作-Atomic-Operation"><a href="#原子操作-Atomic-Operation" class="headerlink" title="原子操作(Atomic Operation)"></a>原子操作(Atomic Operation)</h3><p>一次不存在任何中断或失败的操作 要么执行成功 要么没有执行</p>
<p>操作系统需要利用同步机制 即允许并发执行 以便进行资源共享 和提高速度 同时保证一些操作是原子操作</p>
<h4 id="进程的交互关系"><a href="#进程的交互关系" class="headerlink" title="进程的交互关系"></a>进程的交互关系</h4><ul>
<li>互斥(Mutual Exclusion)<ul>
<li>一个进程占用资源 其他进程不能使用</li>
</ul>
</li>
<li>死锁(Deadlock)<ul>
<li>多个进程各占用部分资源 形成循环等待</li>
</ul>
</li>
<li>饥饿(Starvation)<ul>
<li>其他进程可能轮流占用资源 一个进程一直得不到资源</li>
</ul>
</li>
</ul>
<h2 id="临界区-Critical-Section"><a href="#临界区-Critical-Section" class="headerlink" title="临界区(Critical Section)"></a>临界区(Critical Section)</h2><p>进程访问临界资源的一段需要互斥执行的代码 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">entry section</span><br><span class="line">    critical section</span><br><span class="line">exit section</span><br><span class="line">    remainder section</span><br></pre></td></tr></table></figure>

<ul>
<li>进入区(entry section)<ul>
<li>检查 可否 进入临界区的一段代码</li>
<li>若可进入 设置相应 正在访问临界区 标志</li>
</ul>
</li>
<li>退出区(exit section)<ul>
<li>清除 正在访问临界区 标志</li>
</ul>
</li>
<li>剩余区(remainder section)<ul>
<li>代码中的其余部分</li>
</ul>
</li>
</ul>
<h3 id="临界区访问规则"><a href="#临界区访问规则" class="headerlink" title="临界区访问规则"></a>临界区访问规则</h3><ul>
<li>空闲则入<ul>
<li>没有进程在临界区时 任何进程可进入</li>
</ul>
</li>
<li>忙则等待<ul>
<li>有进程在临界区时 其他进程不能进入临界区</li>
</ul>
</li>
<li>有限等待<ul>
<li>等待进入临界区的进程不能无限期等待</li>
</ul>
</li>
<li>让权等待(可选)<ul>
<li>不能进入临界区的进程 应释放CPU</li>
</ul>
</li>
</ul>
<h3 id="临界区的实现方法"><a href="#临界区的实现方法" class="headerlink" title="临界区的实现方法"></a>临界区的实现方法</h3><ul>
<li>禁用中断</li>
<li>软件方法(共享变量)</li>
<li>更高级的抽象方法(操作系统)</li>
</ul>
<p>不同临界区实现机制衡量标准</p>
<ul>
<li>性能和并发级别</li>
</ul>
<h4 id="禁用中断"><a href="#禁用中断" class="headerlink" title="禁用中断"></a>禁用中断</h4><p>没有中断 没有上下文切换 因此没有并发</p>
<ul>
<li>硬件将中断处理延迟到中断被启用之后</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">local_irq_save(unsigned long flags);</span><br><span class="line">critical section</span><br><span class="line">local_irq_restore(unsigned long flags);</span><br></pre></td></tr></table></figure>

<ul>
<li>进入临界区<ul>
<li>禁止所有中断 并保存标志</li>
</ul>
</li>
<li>退出临界区<ul>
<li>使能所有中断 并恢复标志</li>
</ul>
</li>
</ul>
<h5 id="禁用中断缺点"><a href="#禁用中断缺点" class="headerlink" title="禁用中断缺点"></a>禁用中断缺点</h5><ul>
<li>禁用中断后 进程无法被停止 <ul>
<li>整个系统为此停下来</li>
<li>可能导致其他进程处于饥饿状态</li>
</ul>
</li>
<li>临界区可能很长<ul>
<li>导致无法确定响应中断所需的时间</li>
</ul>
</li>
</ul>
<h4 id="基于软件同步方法"><a href="#基于软件同步方法" class="headerlink" title="基于软件同步方法"></a>基于软件同步方法</h4><p>线程通过共享变量来同步它们的行为</p>
<h5 id="Peterson算法"><a href="#Peterson算法" class="headerlink" title="Peterson算法"></a>Peterson算法</h5><p>共享变量</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> turn; <span class="comment">// 表示该谁进入临界区</span></span><br><span class="line"><span class="keyword">boolean</span> flag[]; <span class="comment">// 表示进程申请进入临界区</span></span><br></pre></td></tr></table></figure>

<p>进程i 进入区代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">flag[i] = <span class="keyword">true</span>;</span><br><span class="line">turn = j;</span><br><span class="line"><span class="keyword">while</span> (flag[j] &amp;&amp; turn == j)</span><br></pre></td></tr></table></figure>

<p>退出区代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">flag[i] = <span class="keyword">false</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当进程 j 没有申请进入临界区 则 flag[j] &#x3D; false;  turn &#x3D; j;</span><br><span class="line"></span><br><span class="line">那么进程 i 可以直接进入临界区</span><br><span class="line"> </span><br><span class="line">当进程 j 也申请进入临界区 则 flag[j] &#x3D; true; turn 有 两种情况 当 进程 i 先写 turn &#x3D; i; 进程 j 后写 turn &#x3D; j; 时 turn &#x3D; j;</span><br><span class="line"> </span><br><span class="line">那么进程 i 进不去临界区 进程 j 可以进入临界区</span><br><span class="line"></span><br><span class="line">也就是说 先写turn这个标志的 能进入临界区 后写的 要等</span><br></pre></td></tr></table></figure>

<p>Peterson算法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">    flag[i] = <span class="keyword">true</span>;</span><br><span class="line">    turn = j;</span><br><span class="line">    <span class="keyword">while</span> ( flag[j] &amp;&amp; turn == j);</span><br><span class="line">    </span><br><span class="line">            CRITICAL SECTION</span><br><span class="line"></span><br><span class="line">    flag[i] = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">            REMAINDER SECTION</span><br><span class="line"></span><br><span class="line">   &#125; <span class="keyword">while</span> (<span class="keyword">true</span>);</span><br></pre></td></tr></table></figure>

<h5 id="Dekkers算法"><a href="#Dekkers算法" class="headerlink" title="Dekkers算法"></a>Dekkers算法</h5><p>对比 Peterson算法 优点是可以很方便扩展到多个线程</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">进程 i 申请进入临界区 flag[i] &#x3D; true;</span><br><span class="line">当进程 j 也申请进入临界区 则 flag[j] &#x3D; true;</span><br><span class="line">这时再判断 turn 这个共享变量 允许哪个进程进入临界区</span><br><span class="line">若不允许 则将进程 flag[i] &#x3D; false; 并一直等待 直到允许</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">do</span> &#123;</span><br><span class="line">    flag[i] = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">while</span> flag[j] == <span class="keyword">true</span> &#123; </span><br><span class="line">        <span class="keyword">if</span> turn != i &#123;</span><br><span class="line">            flag[i] = <span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">while</span> turn != i &#123; &#125; </span><br><span class="line">            flag[i] = <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">            CRITICAL SECTION</span><br><span class="line"></span><br><span class="line">    turn = j;</span><br><span class="line">    flag[i] = <span class="keyword">false</span>;</span><br><span class="line">        EMAINDER SECTION</span><br><span class="line">   &#125; <span class="keyword">while</span> (<span class="keyword">true</span>);</span><br></pre></td></tr></table></figure>

<h5 id="N线程的软件方法-Eisenberg-和-McGuire"><a href="#N线程的软件方法-Eisenberg-和-McGuire" class="headerlink" title="N线程的软件方法(Eisenberg 和 McGuire)"></a>N线程的软件方法(Eisenberg 和 McGuire)</h5><p>将线程排成环 若要进入临界区 则将 线程的 flag 标志 填写好 再去看 turn 标志</p>
<ul>
<li>线程Ti要等待从 turn 到 i-1的线程 都退出临界区后访问临界区</li>
<li>线程Ti退出时 将turn改成下一个请求线程</li>
</ul>
<p><img src="/images/Eisenberg_McGuire.png" alt="Eisenberg_McGuire"></p>
<h5 id="基于软件同步方法的缺点"><a href="#基于软件同步方法的缺点" class="headerlink" title="基于软件同步方法的缺点"></a>基于软件同步方法的缺点</h5><ul>
<li>复杂<ul>
<li>需要两个进程间共享变量</li>
</ul>
</li>
<li>需要忙等待<ul>
<li>浪费CPU时间 (需要频繁查询共享变量的状态)</li>
</ul>
</li>
</ul>
<h4 id="更高级的抽象方法"><a href="#更高级的抽象方法" class="headerlink" title="更高级的抽象方法"></a>更高级的抽象方法</h4><p>硬件提供一些同步原语</p>
<ul>
<li>中断禁用</li>
<li>原子操作指令(从硬件上保证其原子性)</li>
</ul>
<h5 id="锁-Lock"><a href="#锁-Lock" class="headerlink" title="锁(Lock)"></a>锁(Lock)</h5><p>一个抽象的数据结构 基于原子操作指令</p>
<ul>
<li>一个二进制变量(锁定/解锁)</li>
<li>Lock::Acquire()<ul>
<li>锁被释放前一直等待 然后得到锁</li>
</ul>
</li>
<li>Lock::Release()<ul>
<li>释放锁 唤醒任何等待的进程</li>
</ul>
</li>
</ul>
<h3 id="原子操作指令"><a href="#原子操作指令" class="headerlink" title="原子操作指令"></a>原子操作指令</h3><p>现代CPU体系结构提供的一些特殊原子操作指令</p>
<h4 id="测试和置位指令-TS-Test-and-Set"><a href="#测试和置位指令-TS-Test-and-Set" class="headerlink" title="测试和置位指令(TS Test-and-Set)"></a>测试和置位指令(TS Test-and-Set)</h4><ul>
<li>从内存单元中读取值</li>
<li>测试该值是否为1(真或假)</li>
<li>内存单元值设置为1</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">TestAndSet</span><span class="params">(<span class="keyword">boolean</span> *target)</span>‏ </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> rv = *target;</span><br><span class="line">    *target = <span class="keyword">true</span>;</span><br><span class="line">    <span class="keyword">return</span> rv:</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="交换指令-Exchange"><a href="#交换指令-Exchange" class="headerlink" title="交换指令(Exchange)"></a>交换指令(Exchange)</h4><p>交换内存中的两个值</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Exchange</span> <span class="params">(<span class="keyword">boolean</span> *a, <span class="keyword">boolean</span> *b)</span>‏ </span>&#123;</span><br><span class="line">    <span class="keyword">boolean</span> temp = *a;</span><br><span class="line">    *a = *b;</span><br><span class="line">    *b = temp:</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="TS指令实现自旋锁-Spinlock"><a href="#TS指令实现自旋锁-Spinlock" class="headerlink" title="TS指令实现自旋锁(Spinlock)"></a>TS指令实现自旋锁(Spinlock)</h5><p>线程在等待的时候消耗CPU时间</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lock</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> value = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* 通过TS指令 读出 value的值 然后写入1</span></span><br><span class="line"><span class="comment">    若value为0 说明锁被释放 TS指令读出0 并写入 1 进入临界区</span></span><br><span class="line"><span class="comment">    若value为1 说明锁为忙状态 TS指令读出1 并写入 1 状态并没有改变 但是陷入循环 一直等待</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">Lock::Acquire() &#123;</span><br><span class="line">   <span class="keyword">while</span> (test-<span class="keyword">and</span>-<span class="built_in">set</span>(value))</span><br><span class="line">      ; <span class="comment">//spin</span></span><br><span class="line">&#125;</span><br><span class="line">Lock::Release() &#123;</span><br><span class="line">    value = <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="TS指令实现无忙等待锁"><a href="#TS指令实现无忙等待锁" class="headerlink" title="TS指令实现无忙等待锁"></a>TS指令实现无忙等待锁</h5><p>在锁的数据结构中加入等待队列</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Lock</span> &#123;</span></span><br><span class="line">   <span class="keyword">int</span> value = <span class="number">0</span>;</span><br><span class="line">   WaitQueue q;</span><br><span class="line">&#125;</span><br><span class="line">Lock::Acquire() &#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">        若锁处于忙状态则将当前进程加入等待队列并执行调度程序</span></span><br><span class="line"><span class="comment">        使得其他进程可以占用CPU继续执行</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="keyword">while</span> (test-<span class="keyword">and</span>-<span class="built_in">set</span>(value)) &#123;</span><br><span class="line">      add <span class="keyword">this</span> TCB to wait <span class="built_in">queue</span> q;</span><br><span class="line">      schedule();</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br><span class="line">Lock::Release() &#123;</span><br><span class="line">   value = <span class="number">0</span>;</span><br><span class="line">   <span class="built_in">remove</span> one thread t from q;</span><br><span class="line">   <span class="comment">/*</span></span><br><span class="line"><span class="comment">    将等待进程放到就绪队列里去 相当于唤醒等待进程</span></span><br><span class="line"><span class="comment">   */</span></span><br><span class="line">   wakeup(t);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="原子操作指令锁特征"><a href="#原子操作指令锁特征" class="headerlink" title="原子操作指令锁特征"></a>原子操作指令锁特征</h4><ul>
<li>优点<ul>
<li>适用于单处理机或者共享内存的多处理机中任意数量的进程同步(禁用中断只适用于单处理机 多处理机的情况下 禁止单个处理机的中断 其他处理机仍然能够响应中断)</li>
<li>简单且容易证明</li>
<li>支持多临界区</li>
</ul>
</li>
<li>缺点<ul>
<li>忙等待锁会消耗处理机时间</li>
<li>可能导致饥饿 进程离开临界区时有多个等待进程的情况</li>
</ul>
</li>
<li>死锁<ul>
<li>拥有临界区的低优先级进程 但请求访问临界区的高优先级进程获得处理机并等待临界区</li>
</ul>
</li>
</ul>
<h3 id="同步方法总结"><a href="#同步方法总结" class="headerlink" title="同步方法总结"></a>同步方法总结</h3><p>锁是一种高级的同步抽象方法</p>
<ul>
<li>互斥可以使用锁来实现</li>
<li>需要硬件支持</li>
</ul>
<h4 id="常用三种同步实现方法总结"><a href="#常用三种同步实现方法总结" class="headerlink" title="常用三种同步实现方法总结"></a>常用三种同步实现方法总结</h4><ul>
<li>禁用中断(仅限于单处理机)</li>
<li>软件方法(共享变量 复杂)</li>
<li>原子操作指令(单处理机或多处理机均可)</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>并发</tag>
        <tag>临界区</tag>
        <tag>原子操作</tag>
        <tag>锁</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 处理机调度</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E5%A4%84%E7%90%86%E6%9C%BA%E8%B0%83%E5%BA%A6/</url>
    <content><![CDATA[<h2 id="CPU资源的时分复用"><a href="#CPU资源的时分复用" class="headerlink" title="CPU资源的时分复用"></a>CPU资源的时分复用</h2><p>进程切换 CPU资源的当前占用者的切换</p>
<ul>
<li>保存当前进程在 PCB中的执行上下文(CPU状态)</li>
<li>恢复下一个进程的执行上下文</li>
</ul>
<h3 id="处理机调度"><a href="#处理机调度" class="headerlink" title="处理机调度"></a>处理机调度</h3><ul>
<li>从就绪队列中挑选出下一个占用CPU运行的进程</li>
<li>从多个可用CPU中挑选就绪进程可使用的CPU资源</li>
</ul>
<a id="more"></a>

<h4 id="调度程序"><a href="#调度程序" class="headerlink" title="调度程序"></a>调度程序</h4><p>挑选就绪进程的内核函数 (若为多CPU 则还有挑选处理机的功能)</p>
<h5 id="调度时机"><a href="#调度时机" class="headerlink" title="调度时机"></a>调度时机</h5><p>内核运行调度程序的条件</p>
<ul>
<li>进程从 运行状态 切换到 等待状态 (等待某个事件)</li>
<li>进程结束</li>
</ul>
<h5 id="非抢占系统"><a href="#非抢占系统" class="headerlink" title="非抢占系统"></a>非抢占系统</h5><p>当前进程主动放弃CPU</p>
<h5 id="可抢占系统"><a href="#可抢占系统" class="headerlink" title="可抢占系统"></a>可抢占系统</h5><ul>
<li>中断请求被服务例程响应完成时<ul>
<li>进程从 运行状态 切换到 就绪状态</li>
</ul>
</li>
<li>当前进程被抢占<ul>
<li>进程时间片用完</li>
<li>某进程从 等待状态 切换到 就绪状态 (它的优先级更高 则抢占)</li>
</ul>
</li>
</ul>
<p><img src="/images/process_control_with_process_state.png" alt="process_control_with_process_state"></p>
<h3 id="处理机资源使用模式"><a href="#处理机资源使用模式" class="headerlink" title="处理机资源使用模式"></a>处理机资源使用模式</h3><p>进程在 CPU计算 和 I/O操作 间交替</p>
<ul>
<li>在时间片机制下 进程可能在结束 当前CPU计算前 被迫放弃 CPU</li>
<li>应选择一个合适的 时间尺度 来作为 时间片的基本单位</li>
</ul>
<p><img src="/images/cpu_io_mode.png" alt="cpu_io_mode"></p>
<h4 id="比较调度算法的准则"><a href="#比较调度算法的准则" class="headerlink" title="比较调度算法的准则"></a>比较调度算法的准则</h4><ul>
<li>CPU利用率<ul>
<li>CPU 处于忙状态的 时间百分比</li>
</ul>
</li>
<li>吞吐量<ul>
<li>单位时间内完成的 进程数量</li>
</ul>
</li>
<li>周转时间<ul>
<li>进程从 初始化 到 结束(包括等待)的总时间</li>
</ul>
</li>
<li>等待时间<ul>
<li>进程在 就绪队列中 的总时间</li>
</ul>
</li>
<li>响应时间<ul>
<li>从提交请求到产生响应所花费的总时间</li>
</ul>
</li>
</ul>
<h4 id="处理机调度策略的目标"><a href="#处理机调度策略的目标" class="headerlink" title="处理机调度策略的目标"></a>处理机调度策略的目标</h4><ul>
<li>响应时间</li>
<li>吞吐量</li>
<li>公平性</li>
</ul>
<h5 id="处理机调度策略响应时间目标"><a href="#处理机调度策略响应时间目标" class="headerlink" title="处理机调度策略响应时间目标"></a>处理机调度策略响应时间目标</h5><p>响应时间是操作系统的计算延迟</p>
<ul>
<li>减少响应时间<ul>
<li>及时处理用户的输入请求 尽快将输出反馈给用户</li>
</ul>
</li>
<li>减少平均响应时间的波动<ul>
<li>在交互系统中 可预测性比高差异低平均更重要</li>
</ul>
</li>
</ul>
<h5 id="处理机调度策略吞吐量目标"><a href="#处理机调度策略吞吐量目标" class="headerlink" title="处理机调度策略吞吐量目标"></a>处理机调度策略吞吐量目标</h5><p>吞吐量是操作系统的计算带宽 (单位时间内 尽可能多的完成进程数量)</p>
<ul>
<li>增加吞吐量<ul>
<li>减少开销(操作系统开销 上下文切换)</li>
<li>提高系统资源的利用率(CPU I/O设备)</li>
</ul>
</li>
<li>减少等待时间<ul>
<li>减少每个进程的等待时间</li>
</ul>
</li>
</ul>
<h5 id="处理机调度策略公平性目标"><a href="#处理机调度策略公平性目标" class="headerlink" title="处理机调度策略公平性目标"></a>处理机调度策略公平性目标</h5><p>保证每个进程的等待时间相同 (公平通常会增加平均响应时间)</p>
<h2 id="调度算法"><a href="#调度算法" class="headerlink" title="调度算法"></a>调度算法</h2><ul>
<li>先来先服务算法<ul>
<li>FCFS: First Come, First Served</li>
</ul>
</li>
<li>短进程优先算法<ul>
<li>SPN: Shortest Process Next</li>
<li>SJF: Shortest Job First(短作业优先算法)</li>
<li>SRT: Shortest Remaining Time(短剩余时间优先算法)</li>
</ul>
</li>
<li>最高响应比优先算法 (按照进程在就绪队列里的等待时间)<ul>
<li>HRRN: Highest Response Ratio Next</li>
</ul>
</li>
<li>时间片轮转算法 (各个进程轮流占用一个相等的时间片)<ul>
<li>RR: Round Robin</li>
</ul>
</li>
<li>多级反馈队列算法 (将就绪队列分成不同子序列 使用不同的算法)<ul>
<li>MLFQ: Multilevel Feedback Queues</li>
</ul>
</li>
<li>公平共享调度算法 (按照进程占用的资源来调度)<ul>
<li>FSS: Fair Share Scheduling</li>
</ul>
</li>
</ul>
<h3 id="先来先服务算法-FCFS-First-Come-First-Served"><a href="#先来先服务算法-FCFS-First-Come-First-Served" class="headerlink" title="先来先服务算法(FCFS First Come, First Served)"></a>先来先服务算法(FCFS First Come, First Served)</h3><p>依据进程进入就绪队列的先后顺序排列</p>
<ul>
<li>进程进入等待或结束状态时 就绪队列中下一个进程占用CPU</li>
</ul>
<h4 id="先来先服务算法的周转时间-TAT-Turnaround-time"><a href="#先来先服务算法的周转时间-TAT-Turnaround-time" class="headerlink" title="先来先服务算法的周转时间(TAT Turnaround time)"></a>先来先服务算法的周转时间(TAT Turnaround time)</h4><p>周转时间 = 等待时间 + CPU使用时间</p>
<p>3个进程 计算时间分别为 12 3 3</p>
<p><img src="/images/FCFS_TAT.png" alt="FCFS_TAT"></p>
<h4 id="先来先服务算法的特征"><a href="#先来先服务算法的特征" class="headerlink" title="先来先服务算法的特征"></a>先来先服务算法的特征</h4><ul>
<li>优点<ul>
<li>简单</li>
</ul>
</li>
<li>缺点<ul>
<li>平均等待时间波动较大 (短进程排在长进程后面)</li>
<li>I/O资源 和 CPU资源 利用率较低 (I/O 可以和 CPU并行)</li>
</ul>
</li>
</ul>
<h3 id="短进程优先算法-SPN-Shortest-Process-Next"><a href="#短进程优先算法-SPN-Shortest-Process-Next" class="headerlink" title="短进程优先算法(SPN Shortest Process Next)"></a>短进程优先算法(SPN Shortest Process Next)</h3><p>选择就绪队列中 预期的执行时间最短 进程占用CPU进入运行状态</p>
<p><img src="/images/SPN.png" alt="SPN"></p>
<p>当有一个新的进程进入就绪队列 且 它的预期执行时间比当前正在执行的进程所剩的执行时间还短<br>允许其优先 则为 SRT: Shortest Remaining Time (短剩余时间优先)</p>
<h4 id="短进程优先算法的周转时间"><a href="#短进程优先算法的周转时间" class="headerlink" title="短进程优先算法的周转时间"></a>短进程优先算法的周转时间</h4><p>具有最优平均周转时间</p>
<p><img src="/images/SPN_TAT.png" alt="SPN_TAT"></p>
<h4 id="短进程优先算法的特征"><a href="#短进程优先算法的特征" class="headerlink" title="短进程优先算法的特征"></a>短进程优先算法的特征</h4><ul>
<li>可能导致饥饿<ul>
<li>连续的短进程流会使长进程无法获得CPU资源</li>
</ul>
</li>
<li>如何 预测进程的执行时间<ul>
<li>询问用户 用户欺骗就结束相应进程(骗CPU自己执行时间很短 以最快占用CPU资源)</li>
<li>用过去预测未来</li>
</ul>
</li>
</ul>
<h4 id="短进程优先算法的执行时间预估"><a href="#短进程优先算法的执行时间预估" class="headerlink" title="短进程优先算法的执行时间预估"></a>短进程优先算法的执行时间预估</h4><p>用历史的执行时间来预估未来的执行时间</p>
<p>每次都带上一个衰减系数 之前的执行时间权重是最小的</p>
<p><img src="/images/SPN_Execution_time_estimation.png" alt="SPN_Execution_time_estimation"></p>
<h3 id="最高响应比优先算法-HRRN-Highest-Response-Ratio-Next"><a href="#最高响应比优先算法-HRRN-Highest-Response-Ratio-Next" class="headerlink" title="最高响应比优先算法(HRRN Highest Response Ratio Next)"></a>最高响应比优先算法(HRRN Highest Response Ratio Next)</h3><p>选择就绪队列中响应比R值最高的进程 </p>
<ul>
<li>基于短进程优先算法的改进</li>
<li>不可抢占</li>
<li>关注进程等待时间</li>
<li>防止无限期推迟</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">R &#x3D; (w + s) &#x2F; s</span><br><span class="line">w: 等待时间(waiting time)</span><br><span class="line">s: 执行时间(service time)</span><br></pre></td></tr></table></figure>

<p>等的时间越长 它的优先级越高 在一定程度上避免 短进程优先算法导致的饥饿现象</p>
<h3 id="时间片轮转算法-RR-Round-Robin"><a href="#时间片轮转算法-RR-Round-Robin" class="headerlink" title="时间片轮转算法(RR Round Robin)"></a>时间片轮转算法(RR Round Robin)</h3><ul>
<li>将时间片q 作为 分配处理机资源的基本时间单位</li>
<li>时间片结束时 按 FCFS(先来先服务)算法 切换到下一个就绪进程</li>
<li>每个进程分到了 1 / n(进程总数) 的时间</li>
</ul>
<p><img src="/images/RR.png" alt="RR"></p>
<h4 id="时间片轮转算法平均等待时间"><a href="#时间片轮转算法平均等待时间" class="headerlink" title="时间片轮转算法平均等待时间"></a>时间片轮转算法平均等待时间</h4><p>4个进程的执行时间</p>
<ul>
<li>P1 53</li>
<li>P2 8</li>
<li>P3 68</li>
<li>P4 24</li>
</ul>
<p>等待时间 = 周转时间 - CPU执行时间</p>
<p><img src="/images/RR_AWT.png" alt="RR_AWT"></p>
<h4 id="时间片轮转算法特征"><a href="#时间片轮转算法特征" class="headerlink" title="时间片轮转算法特征"></a>时间片轮转算法特征</h4><p>有额外的上下文切换开销 (正常情况下是在 进程进入等待状态或结束状态时 才切换进程)</p>
<ul>
<li>当时间片太大时<ul>
<li>等待时间过长</li>
<li>极限情况退化成 FCFS(先来先服务)</li>
</ul>
</li>
<li>当时间片太小时<ul>
<li>反应迅速 但产生大量的上下文切换</li>
<li>大量上下文切换开销影响系统的吞吐量(单位时间内 运行的总进程数)</li>
</ul>
</li>
<li>时间片长度选择目标<ul>
<li>通常为 10ms左右 为一个时间片</li>
<li>维持上下文开销处于 1% 以内</li>
</ul>
</li>
</ul>
<h3 id="多级队列调度算法-MQ-Multilevel-Queues"><a href="#多级队列调度算法-MQ-Multilevel-Queues" class="headerlink" title="多级队列调度算法(MQ Multilevel Queues)"></a>多级队列调度算法(MQ Multilevel Queues)</h3><ul>
<li>就绪队列被划分为多个独立的子序列 (分前台后台)</li>
<li>每个队列拥有自己的调度策略</li>
<li>队列间调度<ul>
<li>固定优先级<ul>
<li>先处理前台 后处理后台</li>
<li>可能导致饥饿</li>
</ul>
</li>
<li>时间片轮转<ul>
<li>每个队列都得到一个确定的能够调度其进程的CPU总时间</li>
<li>80% CPU时间用于前台 20% CPU时间用于后台</li>
</ul>
</li>
</ul>
</li>
</ul>
<h4 id="多级反馈队列算法-MLFQ-Multilevel-Feedback-Queues"><a href="#多级反馈队列算法-MLFQ-Multilevel-Feedback-Queues" class="headerlink" title="多级反馈队列算法(MLFQ Multilevel Feedback Queues)"></a>多级反馈队列算法(MLFQ Multilevel Feedback Queues)</h4><p>进程可在不同队列间移动的多级队列算法</p>
<ul>
<li>时间片大小随优先级级别增加而增加</li>
<li>进程在当前时间片没有完成 则降到下一优先级</li>
</ul>
<p><img src="/images/MLFQ.png" alt="MLFQ"></p>
<h4 id="多级反馈队列算法特征"><a href="#多级反馈队列算法特征" class="headerlink" title="多级反馈队列算法特征"></a>多级反馈队列算法特征</h4><ul>
<li>CPU密集型进程的优先级会下降的很快</li>
<li>I/O密集型进程保留在高优先级</li>
</ul>
<h3 id="公平共享调度算法-FSS-Fair-Share-Scheduling"><a href="#公平共享调度算法-FSS-Fair-Share-Scheduling" class="headerlink" title="公平共享调度算法(FSS Fair Share Scheduling)"></a>公平共享调度算法(FSS Fair Share Scheduling)</h3><p>FSS 控制用户对系统资源的访问</p>
<ul>
<li>一些用户组比其他用户组更重要</li>
<li>保证不重要的组无法垄断资源</li>
</ul>
<p>未使用的资源按比例分配 没有达到资源使用率目标的组获得更高优先级</p>
<p><img src="/images/FSS.png" alt="FSS"></p>
<h3 id="传统调度算法总结"><a href="#传统调度算法总结" class="headerlink" title="传统调度算法总结"></a>传统调度算法总结</h3><ul>
<li>先来先服务算法(FCFS)<ul>
<li>不公平 平均等待时间变动大</li>
</ul>
</li>
<li>短进程优先算法(SPN)<ul>
<li>不公平 平均周转时间最小</li>
<li>需要精确的预测执行时间</li>
<li>可能导致饥饿</li>
</ul>
</li>
<li>最高响应比优先算法(HRRN)<ul>
<li>基于 SPN调度(关注等待时间)</li>
<li>不可抢占</li>
</ul>
</li>
<li>时间片轮转算法(RR)<ul>
<li>公平 平均等待时间较差 交互比较好</li>
</ul>
</li>
<li>多级反馈队列算法(MLFQ)<ul>
<li>多种算法的集合(在实际系统中所使用)</li>
</ul>
</li>
<li>公平共享调度(FSS)<ul>
<li>公平</li>
</ul>
</li>
</ul>
<h2 id="实时操作系统"><a href="#实时操作系统" class="headerlink" title="实时操作系统"></a>实时操作系统</h2><p>正确性依赖于 其时间 和功能两方面的操作系统</p>
<ul>
<li>实时操作系统的性能指标<ul>
<li>时间约束的及时性(deadlines)</li>
<li>速度和平均性能相对不重要</li>
</ul>
</li>
<li>实时操作系统的特性<ul>
<li>时间约束的可预测性</li>
</ul>
</li>
</ul>
<h3 id="实时操作系统分类"><a href="#实时操作系统分类" class="headerlink" title="实时操作系统分类"></a>实时操作系统分类</h3><ul>
<li>强实时操作系统<ul>
<li>要求在指定时间内必须完成重要的任务</li>
</ul>
</li>
<li>弱实时操作系统<ul>
<li>重要进程有高优先级 但并非必须完成</li>
</ul>
</li>
</ul>
<h3 id="实时任务"><a href="#实时任务" class="headerlink" title="实时任务"></a>实时任务</h3><p>任务(工作单元)</p>
<ul>
<li>完成任务所需要的资源</li>
<li>定时参数</li>
</ul>
<p><img src="/images/real_time_task.png" alt="real_time_task"></p>
<h3 id="周期实时任务"><a href="#周期实时任务" class="headerlink" title="周期实时任务"></a>周期实时任务</h3><p>一系列相似的任务</p>
<ul>
<li>任务有规律地重复</li>
<li>周期p = 任务请求时间间隔 (0 &lt; p)</li>
<li>执行时间e = 最大执行时间(0 &lt; e &lt; p)</li>
<li>使用率U = e / p</li>
</ul>
<p><img src="/images/real_time_periodic_task.png" alt="real_time_periodic_task"></p>
<h4 id="硬时限和软时限"><a href="#硬时限和软时限" class="headerlink" title="硬时限和软时限"></a>硬时限和软时限</h4><ul>
<li>硬时限(Hard deadline)<ul>
<li>错过任务时 会导致灾难性或非常严重的后果</li>
<li>必须验证 在最坏的情况下满足时限</li>
</ul>
</li>
<li>软时限(Soft deadline)<ul>
<li>通常能满足任务时限 若不满足 则降低要求</li>
<li>尽力满足任务时限</li>
</ul>
</li>
</ul>
<h4 id="可调度性"><a href="#可调度性" class="headerlink" title="可调度性"></a>可调度性</h4><p>可调度表示一个实时操作系统能够满足任务时限要求</p>
<ul>
<li>需要确定实时任务的执行顺序</li>
<li>静态优先级调度 (提前将执行顺序排出)<ul>
<li>速率单调调度算法(RM: Rate Monotonic)</li>
</ul>
</li>
<li>动态优先级调度 (执行过程中 再给出执行顺序)<ul>
<li>最早截止时间优先算法(EDF: Earliest Deadline First)</li>
</ul>
</li>
</ul>
<h5 id="速率单调调度算法-RM-Rate-Monotonic"><a href="#速率单调调度算法-RM-Rate-Monotonic" class="headerlink" title="速率单调调度算法(RM Rate Monotonic)"></a>速率单调调度算法(RM Rate Monotonic)</h5><p>通过周期安排优先级 (频率越高 周期越短 优先级越高)</p>
<h5 id="最早截止时间优先算法-EDF-Earliest-Deadline-First"><a href="#最早截止时间优先算法-EDF-Earliest-Deadline-First" class="headerlink" title="最早截止时间优先算法(EDF Earliest Deadline First)"></a>最早截止时间优先算法(EDF Earliest Deadline First)</h5><p>截止时间越早 优先级越高</p>
<h2 id="多处理器调度"><a href="#多处理器调度" class="headerlink" title="多处理器调度"></a>多处理器调度</h2><ul>
<li>多个处理机组成一个多处理机系统</li>
<li>处理机间可负载共享</li>
</ul>
<h3 id="对称多处理器-SMP-Symmetric-multiprocessing-调度"><a href="#对称多处理器-SMP-Symmetric-multiprocessing-调度" class="headerlink" title="对称多处理器(SMP Symmetric multiprocessing)调度"></a>对称多处理器(SMP Symmetric multiprocessing)调度</h3><ul>
<li>截止时间越早 优先级越高 每个处理器运行自己的调度程序</li>
<li>调度程序对共享资源的访问需要进行同步</li>
</ul>
<p><img src="/images/multiprocessor.png" alt="multiprocessor"></p>
<h4 id="对称多处理器进程分配"><a href="#对称多处理器进程分配" class="headerlink" title="对称多处理器进程分配"></a>对称多处理器进程分配</h4><ul>
<li>静态进程分配<ul>
<li>进程从开始到结束都被分配到一个固定的处理机上执行</li>
<li>每个处理机有自己的就绪队列</li>
<li>调度开销小</li>
<li>各处理机可能忙闲不均</li>
</ul>
</li>
<li>动态进程分配<ul>
<li>进程在执行中可分配到任意空闲处理机上执行</li>
<li>所有处理机共享一个就绪队列</li>
<li>调度开销大</li>
<li>各处理机的负载是均衡的</li>
</ul>
</li>
</ul>
<h3 id="优先级反置-Priority-Inversion"><a href="#优先级反置-Priority-Inversion" class="headerlink" title="优先级反置(Priority Inversion)"></a>优先级反置(Priority Inversion)</h3><p>操作系统中出现高优先级进程长时间等待低优先级进程所占用资源的现象</p>
<ul>
<li>只会在 基于优先级的可抢占调度算法中出现</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">T1 运行要占用资源 L1 </span><br><span class="line">T2 运行也要占用资源 L1</span><br><span class="line">但 此时 T1 已经占用了资源 L1</span><br><span class="line">所以 T2 只能等 T1 用完资源 L1 再进入执行状态</span><br><span class="line">此时 出现一个 进程T3 它的优先级 比 T1 大 比 T2 小</span><br><span class="line">因此 它会抢先 T1 进入执行状态</span><br><span class="line">那么 T2 为了资源L1 必须等 T3执行完以后 T1再执行完以后</span><br><span class="line">T2 才能进入执行状态</span><br></pre></td></tr></table></figure>

<p><img src="/images/priority_inversion.png" alt="priority_inversion"></p>
<h4 id="如何解决优先级反置"><a href="#如何解决优先级反置" class="headerlink" title="如何解决优先级反置?"></a>如何解决优先级反置?</h4><ul>
<li>优先级继承(Priority Inheritance)</li>
<li>优先级天花板协议(Priority Ceiling Protocol)</li>
</ul>
<h5 id="优先级继承-Priority-Inheritance"><a href="#优先级继承-Priority-Inheritance" class="headerlink" title="优先级继承(Priority Inheritance)"></a>优先级继承(Priority Inheritance)</h5><p>占用资源的低优先级进程继承申请资源的高优先级进程的优先级 (只有在低优先级进程被阻塞时 才会提高占用资源进程的优先级)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">T3 进入执行状态 且在 t2 时刻占用资源s</span><br><span class="line">此时 T3 处于阻塞状态 T1 优先级 大于 T3 优先级</span><br><span class="line">T1抢先 T3 并进入执行状态 且 请求资源s</span><br><span class="line">因为 T1 申请资源s 导致 T3的优先级提高 T3 因此继续执行</span><br><span class="line">直到释放资源s后 T1 获取资源 并继续执行</span><br></pre></td></tr></table></figure>

<p><img src="/images/priority_inheritance.png" alt="priority_inheritance"></p>
<h5 id="优先级天花板协议-Priority-Ceiling-Protocol"><a href="#优先级天花板协议-Priority-Ceiling-Protocol" class="headerlink" title="优先级天花板协议(Priority Ceiling Protocol)"></a>优先级天花板协议(Priority Ceiling Protocol)</h5><p>占用资源的进程的优先级和所有可能申请该资源的进程的最高优先级相同</p>
<ul>
<li>不管是否发生等待 都提升占用资源进程的优先级</li>
<li>优先级高于系统中所有被锁定的资源的优先级上限 任务执行临界区时就不会被阻塞</li>
</ul>
<p>临界区 = 一个访问共用资源的程序片段</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>调度</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 文件系统</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E6%96%87%E4%BB%B6%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h2 id="文件系统和文件"><a href="#文件系统和文件" class="headerlink" title="文件系统和文件"></a>文件系统和文件</h2><ul>
<li>文件系统是操作系统中管理持久性数据的子系统 提供数据存储和访问功能</li>
<li>文件是具有符号名 由字节序列构成的数据项集合<ul>
<li>文件是文件系统的基本数据单位</li>
<li>文件名是文件的标识符号</li>
</ul>
</li>
</ul>
<a id="more"></a>

<h3 id="文件系统的功能"><a href="#文件系统的功能" class="headerlink" title="文件系统的功能"></a>文件系统的功能</h3><ul>
<li>分配文件磁盘空间<ul>
<li>管理文件块(位置和顺序)</li>
<li>管理空闲空间(位置)</li>
<li>分配算法(策略)</li>
</ul>
</li>
<li>管理文件集合<ul>
<li>定位 文件及其内容</li>
<li>命名 通过名字找到文件</li>
<li>文件系统结构 文件组织方式</li>
</ul>
</li>
<li>数据可靠和安全<ul>
<li>安全 多层次保护数据安全</li>
<li>可靠 持久保存文件 避免系统崩溃 攻击</li>
</ul>
</li>
</ul>
<h4 id="文件属性"><a href="#文件属性" class="headerlink" title="文件属性"></a>文件属性</h4><p>名称 类型 位置 大小 保护 创建者 创建时间 最近修改时间</p>
<h4 id="文件头"><a href="#文件头" class="headerlink" title="文件头"></a>文件头</h4><p>文件系统元数据中的文件信息</p>
<ul>
<li>文件属性</li>
<li>文件存储位置和顺序</li>
</ul>
<h3 id="文件描述符"><a href="#文件描述符" class="headerlink" title="文件描述符"></a>文件描述符</h3><p>操作系统在打开文件表中维护的打开文件状态和信息</p>
<ul>
<li>文件指针<ul>
<li>最近一次读写位置</li>
<li>每个进程分别维护自己的打开文件指针</li>
</ul>
</li>
<li>文件打开计数<ul>
<li>当前打开文件的次数</li>
<li>最后一个进程关闭文件时 将其从打开文件表中移除</li>
</ul>
</li>
<li>文件的磁盘位置<ul>
<li>缓存数据访问信息</li>
</ul>
</li>
<li>访问权限<ul>
<li>每个进程的文件访问模式信息</li>
</ul>
</li>
</ul>
<h3 id="文件的用户视图和系统视图"><a href="#文件的用户视图和系统视图" class="headerlink" title="文件的用户视图和系统视图"></a>文件的用户视图和系统视图</h3><ul>
<li>文件的用户视图<ul>
<li>持久的数据结构</li>
</ul>
</li>
<li>系统访问接口<ul>
<li>字节序列的集合(Unix)</li>
<li>系统不关心存储在磁盘上的数据结构</li>
</ul>
</li>
<li>操作系统的文件视图<ul>
<li>数据块的集合</li>
<li>数据块是逻辑存储单元 而扇区是物理存储单元</li>
<li>块大小和扇区大小通常是不同的 通常是几个扇区构成一个数据块</li>
</ul>
</li>
</ul>
<h4 id="用户视图到系统视图的转换"><a href="#用户视图到系统视图的转换" class="headerlink" title="用户视图到系统视图的转换"></a>用户视图到系统视图的转换</h4><p>文件系统中的基本操作单位是数据块 <code>getc()和putc()</code>即使每次只访问1字节的数据 也需要缓存目标数据4096字节</p>
<ul>
<li>进程读文件<ul>
<li>获取字节所在的数据块(数据块是逻辑存储单位)</li>
<li>返回数据块内对应部分</li>
</ul>
</li>
<li>进程写文件<ul>
<li>获取数据块</li>
<li>修改数据块中对应部分</li>
<li>写回数据块</li>
</ul>
</li>
</ul>
<h4 id="访问模式"><a href="#访问模式" class="headerlink" title="访问模式"></a>访问模式</h4><p>操作系统需要了解进程如何访问文件</p>
<ul>
<li>顺序访问<ul>
<li>按字节依次读取</li>
<li>大多数文件访问都是顺序访问</li>
</ul>
</li>
<li>随机访问<ul>
<li>从中间读写</li>
<li>不常用 但很重要</li>
<li>虚拟内存中把内存页存储在文件</li>
</ul>
</li>
<li>索引访问<ul>
<li>依据数据特征索引</li>
<li>通常操作系统不完整提供索引访问</li>
<li>数据库是建立在索引内容的磁盘访问之上</li>
</ul>
</li>
</ul>
<h4 id="文件内部结构"><a href="#文件内部结构" class="headerlink" title="文件内部结构"></a>文件内部结构</h4><ul>
<li>无结构<ul>
<li>单词 字节序列</li>
</ul>
</li>
<li>简单记录结构<ul>
<li>分列</li>
<li>固定长度</li>
<li>可变长度</li>
</ul>
</li>
<li>复杂结构<ul>
<li>格式化的文档(PDF Word)</li>
<li>可执行文件</li>
</ul>
</li>
</ul>
<h4 id="文件共享和访问控制"><a href="#文件共享和访问控制" class="headerlink" title="文件共享和访问控制"></a>文件共享和访问控制</h4><p>多用户系统中的文件共享是很有必要的</p>
<ul>
<li>访问控制<ul>
<li>每个用户能够获得哪些文件的哪些访问权限</li>
<li>访问模式 读 写 执行 删除 列表</li>
</ul>
</li>
<li>文件访问控制列表(ACL)<ul>
<li>&lt;文件实体, 权限&gt;</li>
</ul>
</li>
<li>Unix模式<ul>
<li>&lt;用户|组|所有人, 读|写|可执行&gt;</li>
<li>用户标识ID<ul>
<li>识别用户 表明每个用户所允许的权限及保护模式</li>
</ul>
</li>
<li>组标识ID<ul>
<li>允许用户组成组 并指定了组访问权限</li>
</ul>
</li>
</ul>
</li>
</ul>
<h5 id="语义一致性"><a href="#语义一致性" class="headerlink" title="语义一致性"></a>语义一致性</h5><p>规定多进程如何同时访问共享文件</p>
<ul>
<li>与同步算法相似</li>
<li>因磁盘I/O和网络延迟而设计简单</li>
<li>Unix文件系统(UFS)语义(相当于把一致性问题丢回给用户自己处理)<ul>
<li>对打开文件的写入内容立即对其他打开同一文件的其他用户可见</li>
<li>共享文件指针允许多用户同时读取和写入文件</li>
</ul>
</li>
<li>会话语义<ul>
<li>写入内容只有当文件关闭时可见(一次就要写完整 效率低)</li>
</ul>
</li>
<li>读写锁<ul>
<li>一些操作系统和文件系统提供该功能(又是将一致性问题抛给用户)</li>
</ul>
</li>
</ul>
<h4 id="分层文件系统"><a href="#分层文件系统" class="headerlink" title="分层文件系统"></a>分层文件系统</h4><p>文件以目录的方式组织起来</p>
<ul>
<li>目录是一类特殊文件<ul>
<li>目录的内容是文件索引表&lt;文件名, 指向文件的指针&gt;</li>
</ul>
</li>
<li>目录和文件的树型结构<ul>
<li>早期的文件系统是扁平的(只有一层目录)</li>
</ul>
</li>
</ul>
<p><img src="/images/Hierarchical_File_System.png" alt="Hierarchical_File_System"></p>
<h5 id="目录操作"><a href="#目录操作" class="headerlink" title="目录操作"></a>目录操作</h5><p>操作系统应该只允许内核修改目录 以确保映射的完整性 应用程序通过系统调用访问目录</p>
<ul>
<li>搜索文件</li>
<li>创建文件</li>
<li>删除文件</li>
<li>列目录</li>
<li>重命名文件</li>
<li>遍历路径</li>
</ul>
<h5 id="目录实现"><a href="#目录实现" class="headerlink" title="目录实现"></a>目录实现</h5><ul>
<li>文件名的线性列表 包含了指向数据块的指针<ul>
<li>编程简单</li>
<li>执行搜索耗时</li>
</ul>
</li>
<li>哈希表<ul>
<li>减少目录搜索时间</li>
<li>冲突 两个文件名的哈希值相同</li>
<li>固定大小</li>
</ul>
</li>
</ul>
<h5 id="文件别名"><a href="#文件别名" class="headerlink" title="文件别名"></a>文件别名</h5><p>两个或多个文件名关联同一个文件</p>
<p><img src="/images/Alias.png" alt="Alias"></p>
<ul>
<li>硬链接<ul>
<li>多个文件项指向一个文件(删除操作只在最后一个指向这个文件时 才真正删除)</li>
</ul>
</li>
<li>软链接<ul>
<li>通过快捷方式指向其他文件</li>
<li>通过存储真实文件的逻辑名称来实现</li>
</ul>
</li>
</ul>
<h5 id="文件目录中的循环"><a href="#文件目录中的循环" class="headerlink" title="文件目录中的循环"></a>文件目录中的循环</h5><p>父目录指向子目录 子目录又指回父目录 无限循环</p>
<p><img src="/images/Directory_Loop.png" alt="Directory_Loop"></p>
<ul>
<li>解决循环的办法<ul>
<li>只允许到文件的链接 不允许在子目录里的链接</li>
<li>增加链接时 用循环检测算法确定是否合理(银行家算法 开销大)</li>
</ul>
</li>
<li>实际上是限制路径可遍历文件目录的长度</li>
</ul>
<h5 id="名字解析-路径遍历"><a href="#名字解析-路径遍历" class="headerlink" title="名字解析(路径遍历)"></a>名字解析(路径遍历)</h5><p>名字解析是指将逻辑名字转换成物理资源(文件)</p>
<ul>
<li>遍历文件名录直到找到目标文件</li>
</ul>
<p>当解析 <code>&quot;/bin/ls&quot;</code>时</p>
<ul>
<li>读取根目录的文件头(在磁盘固定位置)</li>
<li>读取根目录的数据块 搜索 <code>bin</code> 项</li>
<li>读取 <code>bin</code> 的文件头</li>
<li>读取 <code>bin</code> 的数据块 搜索 <code>ls</code> 项</li>
<li>读取 <code>ls</code> 的文件头</li>
</ul>
<p>当前工作目录(PWD)</p>
<ul>
<li>每个进程都会指向一个文件目录用于解析文件名(可以提高效率)</li>
<li>允许用户指定相对路径来代替绝对路径 如 <code>PWD=&quot;/bin”</code> 能够解析 <code>“ls”</code></li>
</ul>
<h4 id="文件系统挂载"><a href="#文件系统挂载" class="headerlink" title="文件系统挂载"></a>文件系统挂载</h4><p>文件系统需要先挂载才能被访问</p>
<p><img src="/images/Mounting_File_Systems.png" alt="Mounting_File_Systems"></p>
<h4 id="文件系统种类"><a href="#文件系统种类" class="headerlink" title="文件系统种类"></a>文件系统种类</h4><ul>
<li>磁盘文件系统<ul>
<li>文件存储在数据存储设备上 如磁盘</li>
<li>FAT NTFS ext2/3 ISO9660</li>
</ul>
</li>
<li>数据库文件系统<ul>
<li>文件特征是可被寻址辨识的</li>
<li>WinFS</li>
</ul>
</li>
<li>日志文件系统<ul>
<li>记录文件系统的修改/事件</li>
<li>避免操作执行到一半没执行下去的数据丢失</li>
</ul>
</li>
<li>网络/分布式文件系统<ul>
<li>NFS SMB AFS GFS</li>
</ul>
</li>
<li>特殊/虚拟文件系统<ul>
<li>管道</li>
</ul>
</li>
</ul>
<h5 id="网络-分布式文件系统"><a href="#网络-分布式文件系统" class="headerlink" title="网络/分布式文件系统"></a>网络/分布式文件系统</h5><ul>
<li>文件可以通过网络被共享<ul>
<li>文件处于远程服务器</li>
<li>客户端远程挂载服务器文件系统</li>
<li>标准系统文件访问被转换成远程访问</li>
<li>标准文件共享协议<ul>
<li>NFS for Unix</li>
<li>CIFS for Windows</li>
</ul>
</li>
</ul>
</li>
<li>分布式文件系统的挑战<ul>
<li>客户端和客户端上的用户辨别起来很复杂</li>
<li>NFS 不安全</li>
<li>一致性问题(更难把握)</li>
<li>错误处理(更难)</li>
</ul>
</li>
</ul>
<h3 id="文件系统实现"><a href="#文件系统实现" class="headerlink" title="文件系统实现"></a>文件系统实现</h3><p>分层结构</p>
<ul>
<li>虚拟(逻辑)文件系统(VFS Virtual File System)</li>
<li>特定文件系统模块</li>
</ul>
<p><img src="/images/File_system_implementation.png" alt="File_system_implementation"></p>
<h4 id="虚拟文件系统-VFS"><a href="#虚拟文件系统-VFS" class="headerlink" title="虚拟文件系统(VFS)"></a>虚拟文件系统(VFS)</h4><ul>
<li>目的<ul>
<li>对所有不同文件系统的抽象</li>
</ul>
</li>
<li>功能<ul>
<li>提供相同的文件和文件系统接口</li>
<li>管理所有文件和文件系统关联的数据结构</li>
<li>高效查询例程 遍历文件系统</li>
<li>与特定文件系统模块的交互</li>
</ul>
</li>
</ul>
<h4 id="文件系统基本数据结构"><a href="#文件系统基本数据结构" class="headerlink" title="文件系统基本数据结构"></a>文件系统基本数据结构</h4><ul>
<li>文件卷控制块(Unix superblock)<ul>
<li>每个文件系统一个</li>
<li>文件系统详细信息</li>
<li>块 块大小 空余块 计数/指针</li>
</ul>
</li>
<li>文件控制块(Unix vnode or inode)<ul>
<li>每个文件一个</li>
<li>文件详细信息</li>
<li>访问权限 拥有者 大小 数据块位置</li>
</ul>
</li>
<li>目录项(Linux dentry)<ul>
<li>每个目录项一个(子目录或文件)</li>
<li>将目录项数据结构及树型布局编码成树型数据结构</li>
<li>每个目录项对应的文件控制块 父目录 子目录 在哪</li>
</ul>
</li>
</ul>
<h5 id="文件系统的组织视图"><a href="#文件系统的组织视图" class="headerlink" title="文件系统的组织视图"></a>文件系统的组织视图</h5><p><img src="/images/File_system_organization.png" alt="File_system_organization"></p>
<ol>
<li>vol文件卷控制块 指向每一个目录项 每一个目录项组织成树状结构</li>
<li>每一个目录项指向每一个文件</li>
<li>每一个文件的文件控制块知道文件里头的数据块</li>
</ol>
<h4 id="文件系统的存储结构"><a href="#文件系统的存储结构" class="headerlink" title="文件系统的存储结构"></a>文件系统的存储结构</h4><ul>
<li>文件系统数据结构<ul>
<li>卷控制块(superblock 每个文件系统一个)</li>
<li>文件控制块(inode 每个文件一个)</li>
<li>目录节点(dentry 每个目录项一个)</li>
</ul>
</li>
<li>以上都持久存储在外存中<ul>
<li>存储设备的数据块中</li>
</ul>
</li>
<li>当需要时加载进内存<ul>
<li>卷控制模块 当文件系统挂载时进入内存</li>
<li>文件控制块 当文件被访问时候进入内存</li>
<li>目录节点 在遍历一个文件路径时进入内存</li>
</ul>
</li>
</ul>
<h5 id="文件系统的存储视图"><a href="#文件系统的存储视图" class="headerlink" title="文件系统的存储视图"></a>文件系统的存储视图</h5><p><img src="/images/File_system_storage.png" alt="File_system_storage"></p>
<h3 id="多种磁盘缓存位置"><a href="#多种磁盘缓存位置" class="headerlink" title="多种磁盘缓存位置"></a>多种磁盘缓存位置</h3><p><img src="/images/Disk_cache.png" alt="Disk_cache"></p>
<ul>
<li>磁盘控制器 扇区缓存</li>
<li>内存 数据块缓存 打开文件表 内存虚拟盘(用内存来虚拟一个磁盘)</li>
</ul>
<h4 id="两种数据块缓存方式"><a href="#两种数据块缓存方式" class="headerlink" title="两种数据块缓存方式"></a>两种数据块缓存方式</h4><ul>
<li>数据块缓存</li>
<li>页缓存 统一缓存数据块和内存页</li>
</ul>
<h5 id="数据块缓存"><a href="#数据块缓存" class="headerlink" title="数据块缓存"></a>数据块缓存</h5><ul>
<li>数据块按需进入内存<ul>
<li>提供read()操作</li>
<li>预读 预先读取后面的数据块</li>
</ul>
</li>
<li>数据块使用后被缓存<ul>
<li>假设数据将会再次用到</li>
<li>写操作可能被缓存和延迟写入</li>
</ul>
</li>
</ul>
<p><img src="/images/Data_Block_cache.png" alt="Data_Block_cache"></p>
<h5 id="页缓存"><a href="#页缓存" class="headerlink" title="页缓存"></a>页缓存</h5><ul>
<li>虚拟页式存储<ul>
<li>在虚拟地址空间中虚拟页面可映射到本地外存文件中</li>
</ul>
</li>
<li>文件数据块的页缓存<ul>
<li>在虚拟内存中文件数据块被映射成页</li>
<li>文件的读/写操作被转换成对内存的访问</li>
<li>可能导致缺页或被设置为脏页</li>
<li>会带来问题 页面置换算法需要协调虚拟存储和页缓存间的页面数</li>
</ul>
</li>
</ul>
<p><img src="/images/Page_cache_0.png" alt="Page_cache"></p>
<p><img src="/images/Page_cache_1.png" alt="Page_cache"></p>
<h5 id="文件系统中打开文件的数据结构"><a href="#文件系统中打开文件的数据结构" class="headerlink" title="文件系统中打开文件的数据结构"></a>文件系统中打开文件的数据结构</h5><ul>
<li>文件描述符<ul>
<li>每个被打开的文件都有一个文件描述符</li>
<li>文件状态信息<ul>
<li>目录项 当前文件指针 文件操作设置</li>
</ul>
</li>
</ul>
</li>
<li>打开文件表<ul>
<li>每个进程都有一个打开文件表</li>
<li>一个系统级的打开文件表</li>
<li>有文件被打开时 文件卷就不能被卸载</li>
</ul>
</li>
</ul>
<p><img src="/images/Open_file_table.png" alt="Open_file_table"></p>
<h5 id="打开文件锁"><a href="#打开文件锁" class="headerlink" title="打开文件锁"></a>打开文件锁</h5><p>一些文件系统提供文件锁 用于协调多进程的文件访问</p>
<ul>
<li>强制<ul>
<li>根据锁保持情况和访问需求确定是否拒绝访问</li>
</ul>
</li>
<li>劝告<ul>
<li>进程可以查找锁的状态来决定怎么做</li>
</ul>
</li>
</ul>
<h3 id="文件大小"><a href="#文件大小" class="headerlink" title="文件大小"></a>文件大小</h3><ul>
<li>大多数文件都很小<ul>
<li>需要对小文件提供很好的支持</li>
<li>数据块空间不能太大</li>
</ul>
</li>
<li>一些文件非常大<ul>
<li>必须支持大文件(64位文件偏移 考虑到文件很大 数据块大小要考虑)</li>
<li>大文件访问需要搞笑</li>
</ul>
</li>
</ul>
<h3 id="文件分配"><a href="#文件分配" class="headerlink" title="文件分配"></a>文件分配</h3><ul>
<li>分配方式<ul>
<li>连续分配</li>
<li>链式分配</li>
<li>索引分配</li>
</ul>
</li>
<li>指标<ul>
<li>存储效率 外部碎片(连续分配)</li>
<li>读写性能 访问速度</li>
</ul>
</li>
</ul>
<h4 id="连续分配"><a href="#连续分配" class="headerlink" title="连续分配"></a>连续分配</h4><p>文件头指定起始块和长度</p>
<p><img src="/images/Continuous_Allocation.png" alt="Continuous_Allocation"></p>
<ul>
<li>分配策略<ul>
<li>最先匹配 最佳匹配</li>
</ul>
</li>
<li>优点<ul>
<li>文件读取表现好</li>
<li>高效的顺序和随机访问</li>
</ul>
</li>
<li>缺点<ul>
<li>外部碎片</li>
<li>文件增长问题(预留几块)</li>
</ul>
</li>
</ul>
<h4 id="链式分配"><a href="#链式分配" class="headerlink" title="链式分配"></a>链式分配</h4><p>文件以数据块链表方式存储 文件头包含了第一块和最后一块的指针</p>
<p><img src="/images/Linked_Allocation.png" alt="Linked_Allocation"></p>
<ul>
<li>优点<ul>
<li>创建 增大 缩小很容易</li>
<li>没有外部碎片</li>
</ul>
</li>
<li>缺点<ul>
<li>无法实现真正的随机访问</li>
<li>可靠性差 破坏一个链 后面的数据块就丢了</li>
</ul>
</li>
</ul>
<h4 id="索引分配"><a href="#索引分配" class="headerlink" title="索引分配"></a>索引分配</h4><p>为每个文件创建一个索引数据块(存于文件头中) 指向文件数据块的指针列表</p>
<p><img src="/images/Indexed_Allocation.png" alt="Indexed_Allocation"></p>
<ul>
<li>优点<ul>
<li>创建 增大 缩小很容易</li>
<li>没有外部碎片</li>
<li>支持直接访问</li>
</ul>
</li>
<li>缺点<ul>
<li>当文件很小时 存储索引的开销(浪费多一块数据块)</li>
<li>当文件很大 索引数据块存不下索引号 只能再加索引数据块</li>
</ul>
</li>
</ul>
<h5 id="大文件的索引分配"><a href="#大文件的索引分配" class="headerlink" title="大文件的索引分配"></a>大文件的索引分配</h5><p>链式索引块(IB + IB + …)</p>
<p><img src="/images/Link_Index_Allocation.png" alt="Link_Index_Allocation"></p>
<p>多级索引块(IB * IB * …)</p>
<p><img src="/images/Multilevel_Index_Allocation.png" alt="Multilevel_Index_Allocation"></p>
<h4 id="UFS多级索引分配-UFS-Unix-File-System"><a href="#UFS多级索引分配-UFS-Unix-File-System" class="headerlink" title="UFS多级索引分配(UFS Unix File System)"></a>UFS多级索引分配(UFS Unix File System)</h4><p>将各种分配方法集合到一起</p>
<p><img src="/images/Unix_file_system.png" alt="Unix_file_system"></p>
<ul>
<li>文件头包含13个指针<ul>
<li>10个指针指向数据块</li>
<li>第1个指针指向索引块</li>
<li>第12个指针指向二级索引快</li>
<li>第13个指针指向三级索引快</li>
</ul>
</li>
<li>效果<ul>
<li>提高了文件大小限制阈值</li>
<li>动态分配数据块 文件扩展很容易</li>
<li>小文件开销小</li>
<li>只为大文件分配间接索引块 大文件在访问数据块时需要大量查询</li>
</ul>
</li>
</ul>
<h3 id="空闲空间管理"><a href="#空闲空间管理" class="headerlink" title="空闲空间管理"></a>空闲空间管理</h3><p>跟踪记录文件卷中未分配的数据块</p>
<h4 id="位图-组织空闲空间"><a href="#位图-组织空闲空间" class="headerlink" title="位图 组织空闲空间"></a>位图 组织空闲空间</h4><p>用位图代表空闲数据块列表</p>
<ul>
<li>Di = 0 表明数据块i为空闲 否则 表示已分配</li>
<li>使用简单 但是可能会是一个很大的向量表<ul>
<li>160GB磁盘-&gt;40M数据块(假设4kb为一块)-&gt;5MB位图</li>
</ul>
</li>
<li>假定空闲空间在磁盘中均匀分布 则找到 空闲块之前要扫描 n/r<ul>
<li>n = 磁盘上数据块的总数</li>
<li>r = 空闲块的数目(空闲块占的越多 越容易找到)</li>
</ul>
</li>
</ul>
<h3 id="磁盘分区"><a href="#磁盘分区" class="headerlink" title="磁盘分区"></a>磁盘分区</h3><p>通常磁盘通过分区来最大限度减少寻道时间</p>
<ul>
<li>分区是一组柱面的集合</li>
<li>每个分区都可视为逻辑上独立的磁盘</li>
</ul>
<p><img src="/images/Disk_partitioning.png" alt="Disk_partitioning"></p>
<h3 id="多磁盘管理"><a href="#多磁盘管理" class="headerlink" title="多磁盘管理"></a>多磁盘管理</h3><p>使用多磁盘可改善</p>
<ul>
<li>吞吐量(通过并行)</li>
<li>可靠性和可用性(通过冗余)</li>
</ul>
<h3 id="冗余磁盘阵列-RAID-Redundant-Array-of-Inexpensive-Disks"><a href="#冗余磁盘阵列-RAID-Redundant-Array-of-Inexpensive-Disks" class="headerlink" title="冗余磁盘阵列(RAID Redundant Array of Inexpensive Disks)"></a>冗余磁盘阵列(RAID Redundant Array of Inexpensive Disks)</h3><ul>
<li>多种磁盘管理技术</li>
<li>RAID-0 RAID-1 RAID-4 RAID-5</li>
</ul>
<h4 id="冗余磁盘阵列的实现"><a href="#冗余磁盘阵列的实现" class="headerlink" title="冗余磁盘阵列的实现"></a>冗余磁盘阵列的实现</h4><ul>
<li>软件<ul>
<li>操作系统内核的文件卷管理</li>
</ul>
</li>
<li>硬件<ul>
<li>RAID硬件控制器(I/O)</li>
</ul>
</li>
</ul>
<h4 id="RAID-0-磁盘条带化"><a href="#RAID-0-磁盘条带化" class="headerlink" title="RAID-0(磁盘条带化)"></a>RAID-0(磁盘条带化)</h4><p>把数据块分成多个子块 存储在独立的磁盘上(就是分开存) 基于数据块</p>
<ul>
<li>通过独立磁盘上并行数据块访问提供更大的磁盘带宽</li>
</ul>
<p><img src="/images/RAID_0.png" alt="RAID_0"></p>
<h4 id="RAID-1-磁盘镜像"><a href="#RAID-1-磁盘镜像" class="headerlink" title="RAID-1(磁盘镜像)"></a>RAID-1(磁盘镜像)</h4><p>向两个磁盘写入 从任何一个读取(就是存多份)</p>
<ul>
<li>可靠性成倍增长</li>
<li>读取性能线性增加</li>
</ul>
<p><img src="/images/RAID_1.png" alt="RAID_1"></p>
<h4 id="RAID-4-带校验的磁盘条带化"><a href="#RAID-4-带校验的磁盘条带化" class="headerlink" title="RAID-4(带校验的磁盘条带化)"></a>RAID-4(带校验的磁盘条带化)</h4><p>数据块级的磁盘条带化加专用奇偶校验磁盘(就是分开存 校验磁盘单独存) 基于数据块</p>
<ul>
<li>允许从任意一个故障磁盘中恢复</li>
</ul>
<p><img src="/images/RAID_4.png" alt="RAID_4"></p>
<h4 id="RAID-5-带分布式校验的磁盘条带化"><a href="#RAID-5-带分布式校验的磁盘条带化" class="headerlink" title="RAID-5(带分布式校验的磁盘条带化)"></a>RAID-5(带分布式校验的磁盘条带化)</h4><p>把校验和数据块分摊到各个磁盘上 减少对校验和的磁盘的读写压力(就是分开存 同时校验磁盘也分开存) 基于数据块<br>只有 RAID-3 基于位</p>
<p><img src="/images/RAID_5.png" alt="RAID_5"></p>
<h5 id="可纠正多个磁盘错误的冗余磁盘阵列"><a href="#可纠正多个磁盘错误的冗余磁盘阵列" class="headerlink" title="可纠正多个磁盘错误的冗余磁盘阵列"></a>可纠正多个磁盘错误的冗余磁盘阵列</h5><ul>
<li>RAID-5 每组条带块有一个奇偶校验快<ul>
<li>允许一个磁盘错误</li>
</ul>
</li>
<li>RAID-6 每组条带块有两个冗余块<ul>
<li>允许两个磁盘错误</li>
</ul>
</li>
</ul>
<h5 id="RAID嵌套"><a href="#RAID嵌套" class="headerlink" title="RAID嵌套"></a>RAID嵌套</h5><ul>
<li>RAID 0 + 1(条带化提高性能 再做一个磁盘镜像 可靠性提高)</li>
</ul>
<p><img src="/images/RAID_0+1.png" alt="RAID_0+1"></p>
<ul>
<li>RAID 1 + 0(先做磁盘镜像 再做条带化)</li>
</ul>
<p><img src="/images/RAID_1+0.png" alt="RAID_1+0"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>文件系统</tag>
        <tag>冗余磁盘阵列</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 死锁和进程通信</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E6%AD%BB%E9%94%81%E5%92%8C%E8%BF%9B%E7%A8%8B%E9%80%9A%E4%BF%A1/</url>
    <content><![CDATA[<h2 id="死锁"><a href="#死锁" class="headerlink" title="死锁"></a>死锁</h2><p>由于竞争资源或者通信关系 两个或更多线程在执行中出现 用于相互等待只能由其他进程引发的事件</p>
<h3 id="资源分类"><a href="#资源分类" class="headerlink" title="资源分类"></a>资源分类</h3><ul>
<li>可重用资源(Resuable Resource)<ul>
<li>资源不能被删除且在任何时刻只能有一个进程使用</li>
<li>进程释放资源后 其他进程可重用</li>
<li>处理器 I/O通道 存储器 设备</li>
<li>文件(进程访问过程中不能被删除) 数据库 信号量</li>
<li>可能出现死锁(每个进程占用一部分资源并请求其它资源)</li>
</ul>
</li>
<li>消耗资源(Consumable Resource)<ul>
<li>资源创建和消耗</li>
<li>在I/O缓冲区的中断 信号 消息</li>
<li>可能出现死锁(进程间相互等待接受对方的消息)</li>
</ul>
</li>
</ul>
<a id="more"></a>

<h3 id="资源分配图"><a href="#资源分配图" class="headerlink" title="资源分配图"></a>资源分配图</h3><p>资源和进程间的分配和占用关系有向图</p>
<p><img src="/images/Resource_allocation_mapping.png" alt="Resource_allocation_mapping"></p>
<h3 id="出现死锁的必要条件"><a href="#出现死锁的必要条件" class="headerlink" title="出现死锁的必要条件"></a>出现死锁的必要条件</h3><ul>
<li>互斥<ul>
<li>任何时刻只能有一个进程使用一个资源实例</li>
</ul>
</li>
<li>持有并等待<ul>
<li>进程保持至少一个资源 并正在等待获取其他进程持有的资源</li>
</ul>
</li>
<li>非抢占<ul>
<li>资源只能在进程使用后自愿释放</li>
</ul>
</li>
<li>循环等待</li>
</ul>
<p><img src="/images/Resource_allocation_mapping_Deadlock.png" alt="Resource_allocation_mapping_Deadlock"></p>
<h3 id="死锁处理方法"><a href="#死锁处理方法" class="headerlink" title="死锁处理方法"></a>死锁处理方法</h3><p>通常操作系统忽略死锁 由应用进程处理死锁</p>
<ul>
<li>死锁预防(Deadlock Prevention)<ul>
<li>确保系统永远不会进入死锁状态(资源利用率低)</li>
</ul>
</li>
<li>死锁避免(Deadlock Avoidance)<ul>
<li>在使用前进行判断 只允许不会出现死锁的进程请求资源</li>
</ul>
</li>
<li>死锁检测和恢复(Deadlock Detection &amp; Recovery)<ul>
<li>在检测到系统进入死锁状态后 进行恢复</li>
</ul>
</li>
</ul>
<h4 id="死锁预防-Deadlock-Prevention"><a href="#死锁预防-Deadlock-Prevention" class="headerlink" title="死锁预防(Deadlock Prevention)"></a>死锁预防(Deadlock Prevention)</h4><p>限制并发进程对资源的请求 使系统在任何时刻都不满足死锁的必要条件</p>
<ul>
<li>互斥<ul>
<li>把互斥的共享资源封装成可同时访问</li>
</ul>
</li>
<li>持有并等待<ul>
<li>进程请求资源时 要求它不持有任何其他资源</li>
<li>仅允许进程在开始执行时 一次请求所有需要的资源</li>
<li>资源利用率低</li>
</ul>
</li>
<li>非抢占<ul>
<li>如进程请求不能立即分配的资源 则释放已占有资源</li>
<li>只在能够同时获得所有需要资源时 才执行分配操作</li>
</ul>
</li>
<li>循环等待<ul>
<li>对资源排序 要求进程按顺序请求资源</li>
<li>资源利用率低</li>
</ul>
</li>
</ul>
<h4 id="死锁避免-Deadlock-Avoidance"><a href="#死锁避免-Deadlock-Avoidance" class="headerlink" title="死锁避免(Deadlock Avoidance)"></a>死锁避免(Deadlock Avoidance)</h4><p>利用额外的先验信息 在分配资源时判断是否会出现死锁 只在不会死锁时分配资源</p>
<ul>
<li>要求进程声明需要资源的最大数目</li>
<li>限定 提供 与 分配的资源数量 确保满足进程的最大需求</li>
<li>动态检查资源分配状态 确保不会出现环形等待</li>
</ul>
<h5 id="系统资源分配的安全状态"><a href="#系统资源分配的安全状态" class="headerlink" title="系统资源分配的安全状态"></a>系统资源分配的安全状态</h5><p>当进程请求资源时 系统判定分配后是否处于安全状态</p>
<ul>
<li>系统处于安全状态<ul>
<li>针对所有已占有进程存在安全序列</li>
</ul>
</li>
<li>序列&lt;P1, P2, …, PN&gt; 是安全的<ul>
<li>Pi要求的资源 &lt;= 当前可用资源 + 所有Pj持有资源 其中 j &lt; i</li>
<li>如Pi的资源请求不能立即分配 则Pi等待所有Pj(j &lt; i) 完成</li>
<li>Pi完成后 Pi + 1 可得到所需资源 执行并释放所分配的资源</li>
<li>最终整个序列的所有Pi都能获得所需资源</li>
</ul>
</li>
<li>系统处于安全状态 一定没有死锁</li>
<li>系统处于不安全状态 可能出现死锁<ul>
<li>避免死锁就是确保系统不会进入不安全状态</li>
</ul>
</li>
</ul>
<h4 id="银行家算法-Banker’s-algorithm"><a href="#银行家算法-Banker’s-algorithm" class="headerlink" title="银行家算法(Banker’s algorithm)"></a>银行家算法(Banker’s algorithm)</h4><p>是一个避免死锁产生的算法</p>
<ul>
<li>申请资源的线程在第一次申请资源的时候 声明所需最大资源数 在满足所有资源要求并完成后 及时归还操作系统</li>
<li>在申请资源的线程所需资源数量时 不超过操作系统拥有的最大值时 操作系统尽量满足申请资源的线程的需求</li>
</ul>
<h5 id="银行家算法数据结构"><a href="#银行家算法数据结构" class="headerlink" title="银行家算法数据结构"></a>银行家算法数据结构</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">n &#x3D; 线程数量</span><br><span class="line">m &#x3D; 资源类型数量</span><br></pre></td></tr></table></figure>
<ul>
<li>Max(总需求量) = n x m 矩阵</li>
<li>Available(剩余空闲量) 长度为 m 的向量</li>
<li>Allocation(已分配量) = n x m 矩阵</li>
<li>Need(未来需要量) = n x m 矩阵</li>
</ul>
<p><code>Need[i, j] = Max[i, j] - Allocation[i, j]</code></p>
<h5 id="银行家算法安全状态判断"><a href="#银行家算法安全状态判断" class="headerlink" title="银行家算法安全状态判断"></a>银行家算法安全状态判断</h5><ul>
<li>Work 和 Finish 分别是长度为 m 和 n 的向量初始化<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Work &#x3D; Available &#x2F;&#x2F; 当前资源剩余空闲量</span><br><span class="line">Finish[i] &#x3D; false for i : 1, 2, ..., n &#x2F;&#x2F; 线程i有没有完成</span><br></pre></td></tr></table></figure>
</li>
</ul>
<ol>
<li>寻找线程Ti:<ul>
<li>Finish[i] = false</li>
<li>Need[i] &lt;= Work</li>
<li>去 步骤3</li>
</ul>
</li>
<li>找到线程Ti<ul>
<li>Work = Work + Allocation[i]</li>
<li>Finish[i] = true</li>
<li>回到 步骤1</li>
</ul>
</li>
<li>检查所有线程是否满足 Finish[i] == true<ul>
<li>若等 则系统处于安全状态</li>
</ul>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">init: Requesti 线程Ti的资源请求向量</span><br><span class="line">      Requesti[j] 线程Ti请求资源Rj的实例</span><br><span class="line">do-while:</span><br><span class="line">1.如果 Requesti ≤ Need[i], 转到步骤2。否则, 拒绝资源申请, 因为线程已经超过了其最大要求</span><br><span class="line">2.如果 Requesti ≤ Available, 转到步骤3。否则, Ti 必须等待, 因为资源不可用</span><br><span class="line">3.通过安全状态判断来确定是否分配资源给Ti: 生成一个需要判断状态是否安全的资源分配环境</span><br><span class="line">Available &#x3D; Available - Requesti;</span><br><span class="line">Allocation[i] &#x3D; Allocation[i] + Requesti;</span><br><span class="line">Need[i]&#x3D; Need[i] – Requesti;</span><br><span class="line">若安全 则分配资源给Ti</span><br><span class="line">若不安全 则拒绝Ti的资源请求</span><br></pre></td></tr></table></figure>

<h5 id="银行家算法示例"><a href="#银行家算法示例" class="headerlink" title="银行家算法示例"></a>银行家算法示例</h5><p>满足安全序列 <code>T2-&gt;T1-&gt;T3-&gt;T4</code><br><img src="/images/Banker_algorithm_0.png" alt="Banker_algorithm_0"><br><img src="/images/Banker_algorithm_1.png" alt="Banker_algorithm_1"><br><img src="/images/Banker_algorithm_2.png" alt="Banker_algorithm_2"><br><img src="/images/Banker_algorithm_3.png" alt="Banker_algorithm_3"></p>
<h4 id="死锁检测-Deadlock-Detection"><a href="#死锁检测-Deadlock-Detection" class="headerlink" title="死锁检测(Deadlock Detection)"></a>死锁检测(Deadlock Detection)</h4><ul>
<li>允许系统进入死锁状态</li>
<li>系统维护资源分配图</li>
<li>定期调用死锁检测算法来搜索图中是否存在死锁</li>
<li>出现死锁时 用死锁恢复机制进行恢复</li>
</ul>
<h5 id="死锁检测算法数据结构"><a href="#死锁检测算法数据结构" class="headerlink" title="死锁检测算法数据结构"></a>死锁检测算法数据结构</h5><p>和银行家算法相比 没有最大资源请求量的判断</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Available 长度为 m 的向量 每种类型可用资源的数量</span><br><span class="line">Allocation 一个 n x m 的矩阵 当前分配给各个进程每种类型资源的数量</span><br></pre></td></tr></table></figure>

<p>算法需要O(n^2 x m) 操作检测是否系统处于死锁状态 开销大 因此实际操作系统不管死锁</p>
<ol>
<li>初始化 Work 和 Finish:<ul>
<li>Work = Available // work为当前资源剩余量</li>
<li>Allocation[i] &gt; 0时 Finish[i] = false 否则为 true // 线程是否完成</li>
</ul>
</li>
<li>寻找线程Ti满足:<ul>
<li>Finish[i] = false // 线程没有结束 且 此线程需要的资源量小于剩余资源量</li>
<li>Requesti &lt;= Work</li>
<li>若没有找到 则跳到步骤4</li>
</ul>
</li>
<li>将找到的线程拥有的资源释放回当前空闲资源<ul>
<li>Work = Work + Allocation[i]</li>
<li>Finish[i] = true</li>
<li>跳到步骤2</li>
</ul>
</li>
<li>检查所有线程的 Finish 若有一个为 false 则系统处于死锁状态</li>
</ol>
<h5 id="死锁检测算法的使用"><a href="#死锁检测算法的使用" class="headerlink" title="死锁检测算法的使用"></a>死锁检测算法的使用</h5><ul>
<li>死锁检测的时间和周期选择依据<ul>
<li>死锁多久可能会发生</li>
<li>多少进程需要被回滚</li>
</ul>
</li>
<li>资源图可能有多个循环<ul>
<li>难于分辨造成死锁的关键进程</li>
</ul>
</li>
</ul>
<h4 id="死锁恢复-Deadlock-Recovery"><a href="#死锁恢复-Deadlock-Recovery" class="headerlink" title="死锁恢复(Deadlock Recovery)"></a>死锁恢复(Deadlock Recovery)</h4><p>选择哪个进程去终止?</p>
<ul>
<li>终止所有死锁的进程</li>
<li>一次只终止一个进程直到死锁消除</li>
<li>终止进程的顺序应该是<ul>
<li>进程的优先级(选最低的)</li>
<li>进程已运行时间以及还需运行时间(运行时间越长越考虑留下 因为已经利用资源算了很长时间了)</li>
<li>进程已占用资源</li>
<li>进程完成需要的资源</li>
<li>终止进程数目(越少越好)</li>
<li>进程是交互还是批处理(让交互的继续执行)</li>
</ul>
</li>
</ul>
<p>怎么样终止进程? 资源抢占</p>
<ul>
<li>选择被抢占进程(成本最小的)</li>
<li>进程回退 返回到一些安全状态 重启进程到安全状态</li>
<li>可能出现饥饿 同一进程可能一直被选作抢占者</li>
</ul>
<h2 id="进程通信-IPC-Inter-Process-Communication"><a href="#进程通信-IPC-Inter-Process-Communication" class="headerlink" title="进程通信(IPC Inter-Process Communication)"></a>进程通信(IPC Inter-Process Communication)</h2><p>进程通信是进程进行通信和同步的机制</p>
<ul>
<li>IPC提供2个基本操作<ul>
<li>发送操作 send(message)</li>
<li>接受操作 receive(message)</li>
</ul>
</li>
<li>进程通信流程<ul>
<li>在通信进程间建立通信链路</li>
<li>通过 send/receive 交换信息</li>
</ul>
</li>
<li>进程链路特征<ul>
<li>物理(共享内存 硬件总线)</li>
<li>逻辑(逻辑属性)</li>
</ul>
</li>
</ul>
<h3 id="进程通信实现与划分"><a href="#进程通信实现与划分" class="headerlink" title="进程通信实现与划分"></a>进程通信实现与划分</h3><ul>
<li>进程通信实现<ul>
<li>间接通信</li>
<li>直接通信</li>
</ul>
</li>
<li>进程通信可划分为<ul>
<li>阻塞与非阻塞通信</li>
</ul>
</li>
</ul>
<h4 id="间接通信-通过系统维护的消息队列"><a href="#间接通信-通过系统维护的消息队列" class="headerlink" title="间接通信(通过系统维护的消息队列)"></a>间接通信(通过系统维护的消息队列)</h4><p>生命周期可以不同(两个进程不需要同时存在)</p>
<ul>
<li>每个消息队列都有一个唯一的标识</li>
<li>只有共享了相同消息队列的进程 才能够通信</li>
<li>通信链路属性<ul>
<li>只有共享了相同消息队列的进程 才建立连接</li>
<li>连接可以为单向也能为双向</li>
<li>消息队列可以与多个进程相关联</li>
</ul>
</li>
<li>间接通信流程<ol>
<li>创建一个新的消息队列</li>
<li>通过消息队列发送和接受消息(只关心消息队列是谁)</li>
<li>销毁消息队列</li>
</ol>
</li>
</ul>
<p><img src="/images/Indirect_communication.png" alt="Indirect_communication"></p>
<h4 id="直接通信-两个进程必须同时存在才能进行通讯"><a href="#直接通信-两个进程必须同时存在才能进行通讯" class="headerlink" title="直接通信 两个进程必须同时存在才能进行通讯"></a>直接通信 两个进程必须同时存在才能进行通讯</h4><ul>
<li>进程必须正确命名对方</li>
<li>通信链路的属性<ul>
<li>自动建立链路</li>
<li>一条链路恰好对应一对通信进程</li>
<li>每对进程之间只有一个链路存在</li>
<li>链路可以为单向 但通常为双向</li>
</ul>
</li>
</ul>
<p><img src="/images/Direct_communication.png" alt="Direct_communication"></p>
<h4 id="阻塞与非阻塞通信"><a href="#阻塞与非阻塞通信" class="headerlink" title="阻塞与非阻塞通信"></a>阻塞与非阻塞通信</h4><p>进程通信可划分为阻塞(同步)通信与非阻塞(异步)通信</p>
<h5 id="阻塞通信"><a href="#阻塞通信" class="headerlink" title="阻塞通信"></a>阻塞通信</h5><ul>
<li>阻塞发送<ul>
<li>发送者在发送消息后进入等待 直到接受者成功收到</li>
</ul>
</li>
<li>阻塞接受<ul>
<li>接受者在请求接受消息后进入等待 直到成功收到消息</li>
</ul>
</li>
</ul>
<h5 id="非阻塞通信"><a href="#非阻塞通信" class="headerlink" title="非阻塞通信"></a>非阻塞通信</h5><ul>
<li>非阻塞发送<ul>
<li>发送者在消息发送后 可立即进行其他操作</li>
<li>没有消息发送时 接受者在请求接受消息后 接受不到任何消息(可以做别的事)</li>
</ul>
</li>
</ul>
<h3 id="通信链路缓冲"><a href="#通信链路缓冲" class="headerlink" title="通信链路缓冲"></a>通信链路缓冲</h3><p>进程发送的消息在链路上可能有三种缓冲方式</p>
<ul>
<li>0 容量<ul>
<li>发送方必须等待接收方(必须有接收方)</li>
</ul>
</li>
<li>有限容量<ul>
<li>通信链路缓冲队列满时 发送方必须等待</li>
</ul>
</li>
<li>无限容量<ul>
<li>发送方不需要等待</li>
</ul>
</li>
</ul>
<h3 id="信号-Signal"><a href="#信号-Signal" class="headerlink" title="信号(Signal)"></a>信号(Signal)</h3><p>进程间的软件中断通知和处理机制(SIGKILL SIGSTOP SIGCONT)</p>
<ul>
<li>信号的接受处理<ul>
<li>捕获(Catch) 执行进程指定的信号处理函数被调用</li>
<li>忽略(Ignore) 执行操作系统指定的缺省处理(进程终止 进程挂起)</li>
<li>屏蔽(Mask) 禁止进程接受和处理信号(可能是暂时的 当处理同样类型的信号)</li>
</ul>
</li>
<li>不足<ul>
<li>传送的信息量小 只有一个信号类型</li>
</ul>
</li>
</ul>
<h4 id="信号的实现"><a href="#信号的实现" class="headerlink" title="信号的实现"></a>信号的实现</h4><p><img src="/images/Signal_implementation.png" alt="Signal_implementation"></p>
<h4 id="信号的使用示例"><a href="#信号的使用示例" class="headerlink" title="信号的使用示例"></a>信号的使用示例</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;signal.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">sigproc</span><span class="params">()</span> </span>&#123; 		</span><br><span class="line">signal(SIGINT, sigproc);   <span class="comment">/* NOTE some versions of UNIX will reset </span></span><br><span class="line"><span class="comment">				            * signal to default after each call. So for </span></span><br><span class="line"><span class="comment">				            * portability reset signal each time */</span></span><br><span class="line">	<span class="built_in">printf</span>(“you have pressed ctrl-c - disabled \n”);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">quitproc</span><span class="params">()</span> </span>&#123; 		 </span><br><span class="line">	<span class="built_in">printf</span>(“ctrl-\\ pressed to quit\n”);   <span class="comment">/* this is “ctrl” &amp; “\” */</span></span><br><span class="line">	<span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123; </span><br><span class="line">    signal(SIGINT, sigproc); <span class="comment">// signal 是注册信号的系统调用 </span></span><br><span class="line">    signal(SIGQUIT, quitproc);</span><br><span class="line">	<span class="built_in">printf</span>(“ctrl-c disabled use ctrl-\\ to quit\n”);</span><br><span class="line">	<span class="keyword">for</span>(;;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="管道-Pipe"><a href="#管道-Pipe" class="headerlink" title="管道(Pipe)"></a>管道(Pipe)</h3><p>进程间基于内存文件的通信机制 进程不知道也不关心另一端</p>
<ul>
<li>子进程从父进程继承文件描述符</li>
<li>缺省文件描述符 0 stdin, 1 stdout, 2 stderr</li>
</ul>
<h4 id="管道相关系统调用"><a href="#管道相关系统调用" class="headerlink" title="管道相关系统调用"></a>管道相关系统调用</h4><ul>
<li>读管道 read() scanf() 是基于它实现的</li>
<li>写管道 write() printf() 是基于它实现的</li>
<li>创建管道 pipe(rgfd)<ul>
<li>rgfd是2个文件描述符组成的数组</li>
<li>rgfd[0] 是读文件描述符</li>
<li>rgfd[1] 是写文件描述符</li>
</ul>
</li>
</ul>
<h4 id="管道示例"><a href="#管道示例" class="headerlink" title="管道示例"></a>管道示例</h4><p><img src="/images/Pipe.png" alt="Pipe"></p>
<ol>
<li>创建管道</li>
<li>为ls创建一个进程 设置 stdout 为管道写端</li>
<li>为more创建一个进程 设置 stdin 为管道读端</li>
</ol>
<h3 id="消息队列"><a href="#消息队列" class="headerlink" title="消息队列"></a>消息队列</h3><p>消息队列是由操作系统维护的以字节序列为基本单位的间接通信机制</p>
<ul>
<li>每个消息(Message)是一个字节序列</li>
<li>相同标识的消息组成按先进先出顺序组成一个消息队列(Message Queues)</li>
</ul>
<p><img src="/images/Message_Queues.png" alt="Message_Queues"></p>
<h4 id="消息队列的系统调用"><a href="#消息队列的系统调用" class="headerlink" title="消息队列的系统调用"></a>消息队列的系统调用</h4><ul>
<li>msgget()<ul>
<li>获取消息队列标识</li>
</ul>
</li>
<li>msgsnd()<ul>
<li>发送消息</li>
</ul>
</li>
<li>msgrcv()<ul>
<li>接收消息</li>
</ul>
</li>
<li>msgctl()<ul>
<li>消息队列控制</li>
<li>因为消息队列独立于创建它的进程 需要有系统调用完成消息队列的创建和销毁</li>
</ul>
</li>
</ul>
<h3 id="共享内存"><a href="#共享内存" class="headerlink" title="共享内存"></a>共享内存</h3><p>共享内存是把同一个物理内存区域同时映射到多个进程的内存地址空间的通信机制</p>
<ul>
<li>进程<ul>
<li>每个进程都有私有内存地址空间</li>
<li>每个进程的内存地址空间需明确设置共享内存段</li>
</ul>
</li>
<li>线程<ul>
<li>同一进程中的线程总是共享相同的内存地址空间</li>
</ul>
</li>
<li>优点<ul>
<li>快速 方便地共享数据</li>
</ul>
</li>
<li>不足<ul>
<li>必须用额外的同步机制来协调数据访问</li>
</ul>
</li>
</ul>
<h4 id="共享内存的实现"><a href="#共享内存的实现" class="headerlink" title="共享内存的实现"></a>共享内存的实现</h4><p>通过页表项映射到同一物理页帧</p>
<p><img src="/images/Share_memory_implement.png" alt="Share_memory_implement"></p>
<ul>
<li>速度最快<ul>
<li>没有系统调用干预 没有数据复制</li>
</ul>
</li>
<li>不提供同步</li>
</ul>
<h4 id="共享内存的系统调用"><a href="#共享内存的系统调用" class="headerlink" title="共享内存的系统调用"></a>共享内存的系统调用</h4><p>为了保证数据的完整性 需要信号量等机制协调共享内存的访问冲突</p>
<ul>
<li>shmget()<ul>
<li>创建共享段</li>
</ul>
</li>
<li>shmat()<ul>
<li>把共享段映射到进程地址空间</li>
</ul>
</li>
<li>shmdt()<ul>
<li>取消共享段到进程地址空间的映射</li>
</ul>
</li>
<li>shmctl()<ul>
<li>共享段的控制</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>死锁</tag>
        <tag>进程通信</tag>
        <tag>管道</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 物理内存管理 连续内存分配</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E8%BF%9E%E7%BB%AD%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/</url>
    <content><![CDATA[<h3 id="计算机体系结构"><a href="#计算机体系结构" class="headerlink" title="计算机体系结构"></a>计算机体系结构</h3><p>计算机体系结构 由 CPU 内存 I/O设备 组成</p>
<h4 id="CPU-组成结构"><a href="#CPU-组成结构" class="headerlink" title="CPU 组成结构"></a>CPU 组成结构</h4><ul>
<li>寄存器 容量小</li>
<li>ALU 控制逻辑</li>
<li>高速缓存 L1 L2</li>
<li>MMU 内存管理单元</li>
</ul>
<a id="more"></a>
<h4 id="内存层次"><a href="#内存层次" class="headerlink" title="内存层次"></a>内存层次</h4><ul>
<li>CPU<ul>
<li>L1缓存</li>
<li>L2缓存 (以上由 MMU 内存管理单元完成)</li>
</ul>
</li>
<li>高速缓存不命中 (以下由操作系统控制)<ul>
<li>内存 (内存中若找不到 有可能是缺页 存到外存里了 要将它从外存中换上内存)</li>
</ul>
</li>
</ul>
<h4 id="内存特点"><a href="#内存特点" class="headerlink" title="内存特点"></a>内存特点</h4><p>内存 最小访问单位 字节 是 8 bit<br>32位总线 一次读写 4字节 有地址对齐的问题</p>
<h3 id="操作系统内存管理"><a href="#操作系统内存管理" class="headerlink" title="操作系统内存管理"></a>操作系统内存管理</h3><p>操作系统希望 各个进程有各自的内存空间 同时 它们地址应该是可以重叠的<br>因此有了操作系统内存管理的目标</p>
<ul>
<li>抽象: 逻辑地址空间</li>
<li>保护: 独立地址空间</li>
<li>共享: 共享地址空间 (内核内存地址)</li>
<li>虚拟化: 更大的地址空间</li>
</ul>
<h4 id="操作系统中采用的内存管理方式"><a href="#操作系统中采用的内存管理方式" class="headerlink" title="操作系统中采用的内存管理方式"></a>操作系统中采用的内存管理方式</h4><ul>
<li>重定位(relocation) 修改段寄存器地址</li>
<li>分段(segmentation) 希望内存不连续 程序逻辑结构不需要连成一片 分为堆栈 代码段 数据段 三段 但是 一段里的内存还是要连续的 所以有了 下面的分页</li>
<li>分页(paging) 最小的单位 一页 4Kb 如果用一个字节的话 开销粒度太细 管理难度高</li>
<li>虚拟存储 (virtual memory) 大多数 系统 如 Linux 采用按需页式虚拟存储</li>
</ul>
<h4 id="内存管理实现上高度依赖硬件"><a href="#内存管理实现上高度依赖硬件" class="headerlink" title="内存管理实现上高度依赖硬件"></a>内存管理实现上高度依赖硬件</h4><ul>
<li>与计算机存储架构紧耦合</li>
<li>MMU 内存管理单元 处理 CPU 存储访问的硬件</li>
</ul>
<h3 id="地址空间和地址生成"><a href="#地址空间和地址生成" class="headerlink" title="地址空间和地址生成"></a>地址空间和地址生成</h3><ul>
<li>物理地址空间 硬件支持的地址空间 地址唯一</li>
<li>线性地址空间 CPU看到的地址</li>
<li>逻辑地址空间 CPU运行的进程看到的地址</li>
</ul>
<h4 id="地址生成过程"><a href="#地址生成过程" class="headerlink" title="地址生成过程"></a>地址生成过程</h4><p>CPU层次中所做: </p>
<ul>
<li>ALU: 需要逻辑地址的内存内容</li>
<li>MMU 进行逻辑地址和物理地址的转换</li>
<li>CPU 控制逻辑 给总线发送物理地址请求</li>
<li>内存 发送物理地址的内容给CPU 或者 接受CPU数据到物理地址</li>
</ul>
<p>操作系统层次中所做:</p>
<ul>
<li>建立逻辑地址 LA 和 物理地址PA 的映射 这是页表所做的</li>
<li>地址检查<br>每次访问 会检查 段偏移寄存器和段基址寄存器 它们的界限为操作系统设置的初始 base 和 最大 limit 的逻辑地址空间</li>
</ul>
<p>从程序编写层次:</p>
<ul>
<li>高级语言程序 写出函数</li>
<li>编译生成汇编源代码 此时仍然是符号来指代函数</li>
<li>汇编成二进制代码 用具体地址来代替符号</li>
<li>链接 加入函数库</li>
<li>重定位 加载程序时 视程序实际位置改变符号地址</li>
</ul>
<h3 id="连续内存分配"><a href="#连续内存分配" class="headerlink" title="连续内存分配"></a>连续内存分配</h3><p>给进程分配一块不小于指定大小的连续内存空间</p>
<h4 id="内存碎片"><a href="#内存碎片" class="headerlink" title="内存碎片"></a>内存碎片</h4><p>空闲内存 不能被利用</p>
<ul>
<li>外部碎片<br>分配单元之间未被使用内存</li>
<li>内部碎片<br>分配单元内部未被使用的内存 由于取整导致</li>
</ul>
<h4 id="动态分区分配"><a href="#动态分区分配" class="headerlink" title="动态分区分配"></a>动态分区分配</h4><p>当程序被加载完毕时 分配一个进程指定大小可变的分区(块 内存块)<br>分区的地址是连续的</p>
<h4 id="动态分区分配策略"><a href="#动态分区分配策略" class="headerlink" title="动态分区分配策略"></a>动态分区分配策略</h4><h5 id="最先匹配-First-Fit-Allocation-找第一个可用空间比-n-大的空闲块"><a href="#最先匹配-First-Fit-Allocation-找第一个可用空间比-n-大的空闲块" class="headerlink" title="最先匹配(First Fit Allocation) 找第一个可用空间比 n 大的空闲块"></a>最先匹配(First Fit Allocation) 找第一个可用空间比 n 大的空闲块</h5><ul>
<li>空闲分区列表按地址顺序排序</li>
<li>分配过程中 搜索第一个合适的分区</li>
<li>释放分区时 检查是否可与临近空闲分区合并</li>
</ul>
<h5 id="最先匹配优缺点"><a href="#最先匹配优缺点" class="headerlink" title="最先匹配优缺点"></a>最先匹配优缺点</h5><ul>
<li>优点: 简单 在高地址空间有大块的空闲分区</li>
<li>缺点: 产生外部碎片 分配大块分区 会比较慢</li>
</ul>
<h5 id="最佳匹配-Best-Fit-allocation-所有都找一遍-找不小于需要的内存的最小空闲分区"><a href="#最佳匹配-Best-Fit-allocation-所有都找一遍-找不小于需要的内存的最小空闲分区" class="headerlink" title="最佳匹配(Best Fit allocation) 所有都找一遍 找不小于需要的内存的最小空闲分区"></a>最佳匹配(Best Fit allocation) 所有都找一遍 找不小于需要的内存的最小空闲分区</h5><ul>
<li>空闲分区按照大小排序</li>
<li>分配时 查找一个最合适的分区</li>
<li>释放时 查找并且合并临近的空闲分区 因为是空闲分区列表按照大小排序 因此 合并的时候 先要查找 才能进行合并 花的时间会较多</li>
</ul>
<h5 id="最佳匹配优缺点"><a href="#最佳匹配优缺点" class="headerlink" title="最佳匹配优缺点"></a>最佳匹配优缺点</h5><ul>
<li>优点: 大部分分配的尺寸较小时 效果好 可避免大的空闲分区被拆分 可减少外部碎片的大小</li>
<li>缺点: 外部碎片 释放分区较慢 容易产生很多无用小碎片</li>
</ul>
<h5 id="最差匹配-Worst-Fit-Allocation"><a href="#最差匹配-Worst-Fit-Allocation" class="headerlink" title="最差匹配(Worst Fit Allocation)"></a>最差匹配(Worst Fit Allocation)</h5><ul>
<li>使用尺寸不小于所需内存的最大空闲分区</li>
<li>空闲分区列表由大到小排序</li>
<li>分配时 选最大的分区</li>
<li>释放时 检查临近的空闲分区合并 和 最佳匹配相同 因为是按大小排序的 需要查找一遍 按照地址合并 花的时间较多</li>
</ul>
<h5 id="最差匹配优缺点"><a href="#最差匹配优缺点" class="headerlink" title="最差匹配优缺点"></a>最差匹配优缺点</h5><ul>
<li>优点: 中等大小的分配较多时 效果最好 避免出现太多小碎片</li>
<li>缺点: 释放分区较慢 外部碎片 容易破坏大的空闲分区 后续难以分配大的分区</li>
</ul>
<h4 id="以上三种分配策略-总结"><a href="#以上三种分配策略-总结" class="headerlink" title="以上三种分配策略 总结"></a>以上三种分配策略 总结</h4><p>都会产生外部碎片 都不产生 内部碎片</p>
<h3 id="碎片整理"><a href="#碎片整理" class="headerlink" title="碎片整理"></a>碎片整理</h3><p>通过调整进程占用的分区位置 来减少或避免分区碎片</p>
<h4 id="为什么需要碎片整理"><a href="#为什么需要碎片整理" class="headerlink" title="为什么需要碎片整理?"></a>为什么需要碎片整理?</h4><p>可用内存空间已经被分配完了 只剩下一些内存碎片 此时又需要分配内存空间 就需要进行碎片整理 来获取更大的可用内存空间</p>
<h4 id="碎片整理方法"><a href="#碎片整理方法" class="headerlink" title="碎片整理方法"></a>碎片整理方法</h4><ul>
<li>碎片紧凑 (compaction)<br>通过移动分配给进程的内存分区 以合并外部碎片</li>
<li>分区对换 (Swapping in/out)<br>通过抢占并回收处于等待状态进程的分区 存到外存里去 以增大可用内存空间</li>
</ul>
<p>它们都只产生内部碎片 不产生外部碎片</p>
<h4 id="碎片紧凑条件"><a href="#碎片紧凑条件" class="headerlink" title="碎片紧凑条件"></a>碎片紧凑条件</h4><p>所有的应用程序可动态重定位</p>
<h4 id="碎片紧凑需要解决的问题"><a href="#碎片紧凑需要解决的问题" class="headerlink" title="碎片紧凑需要解决的问题"></a>碎片紧凑需要解决的问题</h4><ul>
<li>什么时候移动? 当然是进程处等待状态下</li>
<li>开销</li>
</ul>
<h4 id="分区对换"><a href="#分区对换" class="headerlink" title="分区对换"></a>分区对换</h4><p>在 Linux 或者 Unix 有个分区 叫对换区 是充分利用内存的做法<br>当只有一个进程能运行的时候<br>当前进程主动让出处理机使用权限<br>把它对换的外存当中 再把 外存搬回去<br>在早期内存很紧张的情况下 Unix 使用这种方式 实现进程的交替进行 开销超大 因为 内存和外存的速度差的很远</p>
<h3 id="伙伴系统-Buddy-System"><a href="#伙伴系统-Buddy-System" class="headerlink" title="伙伴系统 (Buddy System)"></a>伙伴系统 (Buddy System)</h3><p>是连续内存分配的实例<br>约定整个可分配的分区大小为 2 的 n 次方</p>
<h4 id="伙伴系统实现"><a href="#伙伴系统实现" class="headerlink" title="伙伴系统实现"></a>伙伴系统实现</h4><p>空闲块按大小和起始地址组织成二维数组<br>初始状态 只有 一个 2 的 n 次方的空闲块</p>
<h4 id="伙伴系统分配过程"><a href="#伙伴系统分配过程" class="headerlink" title="伙伴系统分配过程"></a>伙伴系统分配过程</h4><p>从小到大在空闲块数组中找最小的可用空闲块<br>如果空闲块过大 就 二等分 直到得到合适的可用空闲块</p>
<h4 id="伙伴系统释放过程"><a href="#伙伴系统释放过程" class="headerlink" title="伙伴系统释放过程"></a>伙伴系统释放过程</h4><p>把释放的块放入空闲块数组<br>合并满足合并条件的空闲块</p>
<h4 id="伙伴系统合并条件-2-的-n-次方"><a href="#伙伴系统合并条件-2-的-n-次方" class="headerlink" title="伙伴系统合并条件 2 的 n 次方"></a>伙伴系统合并条件 2 的 n 次方</h4><p>大小相同 地址相邻<br>起始地址较低的块的起始地址必须是 2 的 i + 1 的倍数</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>内存分配</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 物理内存管理 非连续内存分配</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E7%89%A9%E7%90%86%E5%86%85%E5%AD%98%E7%AE%A1%E7%90%86-%E9%9D%9E%E8%BF%9E%E7%BB%AD%E5%86%85%E5%AD%98%E5%88%86%E9%85%8D/</url>
    <content><![CDATA[<h3 id="为什么需要非连续内存分配"><a href="#为什么需要非连续内存分配" class="headerlink" title="为什么需要非连续内存分配"></a>为什么需要非连续内存分配</h3><p>连续内存分配有缺点</p>
<ul>
<li>分配给程序的物理内存必须连续</li>
<li>存在外碎片 和 内碎片</li>
<li>内存分配的动态修改困难 (难以动态的增加内存和减少内存)</li>
<li>内存利用率低</li>
</ul>
<p>之前连续物理内存地址分配 给进程分配内存 必须给其分配连续的物理内存区域 若内存中没有满足需要的内存大小的连续内存区域 分配内存就会失败</p>
<h3 id="非连续内存分配目标"><a href="#非连续内存分配目标" class="headerlink" title="非连续内存分配目标"></a>非连续内存分配目标</h3><p>提高内存利用效率和管理灵活性</p>
<ul>
<li>允许程序使用非连续物理内存空间</li>
<li>允许共享代码与数据 (两个进程共用一个函数库的时候)</li>
<li>支持动态加载和动态链接</li>
</ul>
<a id="more"></a>
<h3 id="非连续内存分配需要解决的问题"><a href="#非连续内存分配需要解决的问题" class="headerlink" title="非连续内存分配需要解决的问题"></a>非连续内存分配需要解决的问题</h3><ul>
<li>如何实现虚拟地址 -&gt; 物理地址的转换 软件实现(开销大 灵活) 硬件实现(开销小 够用)</li>
<li>如何选择非连续分配中的内存块大小 段式存储(segmention 以段为单位 分配的时候这一个段在物理内存中必须是连续的) 页式存储(paging 以页为单位 页与页之间是不连续的)</li>
</ul>
<h3 id="段式存储管理"><a href="#段式存储管理" class="headerlink" title="段式存储管理"></a>段式存储管理</h3><p>进程的段地址空间由若干个段组成<br>例如: 主代码段 公用库代码段 堆栈段 堆数据 数据段 符号表<br>段的逻辑地址空间是连续的 通过段式存储管理 各个段的实际物理内存可以是不连续的 分离的 以此实现非连续内存分配</p>
<h4 id="段访问机制"><a href="#段访问机制" class="headerlink" title="段访问机制"></a>段访问机制</h4><h5 id="段的概念"><a href="#段的概念" class="headerlink" title="段的概念"></a>段的概念</h5><ul>
<li>段 表示访问方式和存储数据 等属性 相同的一段地址空间</li>
<li>对应一个连续内存块</li>
<li>若干个段组成连续的逻辑地址空间</li>
</ul>
<p>段基址先去查进程的段表里面 找到段描述符 找到段的起始地址和其长度 MMU(存储管理单元) 取出长度和偏移作比较 若非法 则内存异常 若合法 MMU 再利用其段基址和段偏移相加(不开分页的情况) 找到实际要访问的物理内存</p>
<p><img src="/images/segmention.png"></p>
<h3 id="页式存储管理"><a href="#页式存储管理" class="headerlink" title="页式存储管理"></a>页式存储管理</h3><h4 id="页式存储管理基本单位"><a href="#页式存储管理基本单位" class="headerlink" title="页式存储管理基本单位"></a>页式存储管理基本单位</h4><p>物理页面:<br>页帧 (Page Frame)</p>
<ul>
<li>把物理地址空间划分为大小相同的基本分配单位</li>
<li>大小 2的 n 次方 32位系统里 选择 4096 4k 作为页帧的大小 之所以要 选择 2的整数次幂 是因为在地址转换比较方便 在计算机中二进制移位 = 乘法的快速实现的方式</li>
</ul>
<p>逻辑页面:<br>页 (Page)</p>
<ul>
<li>把逻辑地址空间划分为大小相同的基本分配单位</li>
<li>帧和页的大小必须是相同的</li>
</ul>
<h4 id="页式存储管理机制的性能问题"><a href="#页式存储管理机制的性能问题" class="headerlink" title="页式存储管理机制的性能问题"></a>页式存储管理机制的性能问题</h4><ul>
<li>访问一个内存单元需要2次内存访问 (内存访问性能问题)<ul>
<li>第一次访问获取页表项</li>
<li>第二次访问获取数据</li>
</ul>
</li>
<li>页表大小可能会很大</li>
</ul>
<p>假设 32K的物理内容 1K 占一项目 32项 且 1项为 4个字节 共128个字节<br>64位机器 64位地址总线 如果每页 1024个字节 那么一个页表的大小为 2^64的内存 / 2^10 一页的大小 = 2^54个页面 假设每个页表项为 8 个字节 那么 就是 2^57</p>
<h4 id="如何解决页式存储管理带来的性能问题"><a href="#如何解决页式存储管理带来的性能问题" class="headerlink" title="如何解决页式存储管理带来的性能问题"></a>如何解决页式存储管理带来的性能问题</h4><ul>
<li>缓存 (Cachine) 缓存下页表项 下次访问有极大的可能性直接访问到物理内存 减少访问次数 就是下面的 TLB 快表</li>
<li>间接访问 (Indirection) 对长的页表 对它切段 就是下面的 多级页表</li>
</ul>
<h4 id="页到页帧"><a href="#页到页帧" class="headerlink" title="页到页帧"></a>页到页帧</h4><ul>
<li>逻辑地址到物理地址的转换</li>
</ul>
<p>页帧计算物理地址<br><img src="/images/pageframe.png"><br>页内偏移 == 帧内偏移<br>页号大小(连续的) != 帧号大小<br>不是所有页都有对应的帧<br>页号计算逻辑地址<br><img src="/images/page.png"></p>
<ul>
<li>页表结构<br>每个进程都有一个页表</li>
<li>每个页面对应一个页表项</li>
<li>随进程运行状态而动态改变 (页表项里面有属性 叫页表项标志)</li>
<li>页表基址寄存器 PTBR(Page Table Base Register)</li>
</ul>
<p><img src="/images/logaddr2phyaddr0.png"><br>乘上 2的n次方 就相当于 将帧号左移n位</p>
<p><img src="/images/logaddr2phyaddr1.png"></p>
<ul>
<li>MMU/TLB</li>
</ul>
<p>MMU 存储管理单元 就是上面图中所写的 段部件<br>TLB (Translation Look-aside Buffer) 快表<br>快表 实际上就是把近期访问过的页表项缓存到 CPU 里</p>
<ul>
<li>TLB 使用 关联存储(associative memory) 实现 具备快速访问性能</li>
<li>如果 TLB 命中 物理页号能很快取出</li>
<li>如果 TLB 不命中 则将对应的表项被更新到 TLB 中</li>
</ul>
<p>快表虽然那么好 但是快表有容量限制和性能限制</p>
<h3 id="多级页表"><a href="#多级页表" class="headerlink" title="多级页表"></a>多级页表</h3><p>通过间接引用将页号分为 k 级 有效减少每级页表的长度</p>
<p>二级页表 逻辑地址转换物理地址 实例<br><img src="/images/coarsepagetable.png"></p>
<h3 id="反置页表"><a href="#反置页表" class="headerlink" title="反置页表"></a>反置页表</h3><p>对于大地址空间 (64-Bits)系统 多级页表变得繁琐</p>
<h4 id="反置页表思路"><a href="#反置页表思路" class="headerlink" title="反置页表思路"></a>反置页表思路</h4><p>不让页表与逻辑地址空间的大小对应<br>让页表与物理地址空间的大小对应</p>
<h4 id="反置页表优缺点"><a href="#反置页表优缺点" class="headerlink" title="反置页表优缺点"></a>反置页表优缺点</h4><ul>
<li>优点<ul>
<li>页表大小相对于物理内存较小</li>
<li>页表大小与逻辑地址空间无关</li>
</ul>
</li>
<li>缺点<ul>
<li>页表信息对调后 需要根据帧号来找页号</li>
<li>在页寄存器里找逻辑地址的页号比较困难</li>
</ul>
</li>
</ul>
<h4 id="页寄存器"><a href="#页寄存器" class="headerlink" title="页寄存器"></a>页寄存器</h4><p>包含:</p>
<ul>
<li>使用位（Residence Bit）：此帧是否被进程占用</li>
<li>占用页号（Occupier）：对应的页号p</li>
<li>保护位（Protection Bits）：约定这一页的访问方式，可读，可写……</li>
</ul>
<h5 id="页寄存器中的地址转换"><a href="#页寄存器中的地址转换" class="headerlink" title="页寄存器中的地址转换"></a>页寄存器中的地址转换</h5><p>CPU 生成的逻辑地址找到物理地址的方式 是通过 Hash 映射 来减少搜索的范围<br>反置页表的 地址转换的特别之处在于 它将进程的PID 也考虑进来</p>
<h5 id="页寄存器示例"><a href="#页寄存器示例" class="headerlink" title="页寄存器示例"></a>页寄存器示例</h5><ul>
<li>物理内存大小：40964096=4K4KB=16MB</li>
<li>页面大小：4096bytes=4KB</li>
<li>页帧数：4096=4K</li>
<li>页寄存器使用的空间：8*4096=32Kbytes（假定每个页寄存器占8字节）</li>
<li>页寄存器带来的额外开销：32K/16M=0.2%（大约）</li>
<li>虚拟内存的大小：任意</li>
</ul>
<h5 id="快表缓存页表项后的页寄存器搜索步骤"><a href="#快表缓存页表项后的页寄存器搜索步骤" class="headerlink" title="快表缓存页表项后的页寄存器搜索步骤"></a>快表缓存页表项后的页寄存器搜索步骤</h5><ol>
<li>对逻辑地址进行 Hash 对换</li>
<li>在快表中查找对应页表项</li>
<li>有冲突时 遍历冲突项链表</li>
<li>若查找失败 产生异常</li>
</ol>
<h3 id="段页式存储管理"><a href="#段页式存储管理" class="headerlink" title="段页式存储管理"></a>段页式存储管理</h3><p>段式存储在内存保护方面有优势(因为同一个段的访问方式和存储的数据都是相同或者相类似的)</p>
<p>页式存储在内存利用和优化转移到后备存储方式有优势 (分了很多标准大小的块)</p>
<p>段页式存储管理的示例图在上面</p>
<h4 id="段页式存储管理内存共享"><a href="#段页式存储管理内存共享" class="headerlink" title="段页式存储管理内存共享"></a>段页式存储管理内存共享</h4><p>通过指向相同的页表基址 实现进程间的段共享</p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>内存分配</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 系统启动、中断、调用</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E7%B3%BB%E7%BB%9F%E5%90%AF%E5%8A%A8%E3%80%81%E4%B8%AD%E6%96%AD%E3%80%81%E8%B0%83%E7%94%A8/</url>
    <content><![CDATA[<h3 id="系统启动"><a href="#系统启动" class="headerlink" title="系统启动"></a>系统启动</h3><p>CPU加电之后 初始化寄存器 从 CS:IP = 0xF000:FFF0 执行第一条指令 系统处于实模式 故 PC = 16 * CS + IP 此时物理地址为 0xFFFF0 20位地址总线 可用内存为 1MB 此时距离 1MB 只剩下 16个字节<br><img src="/images/memorylayout.png"></p>
<h4 id="16个字节够用吗"><a href="#16个字节够用吗" class="headerlink" title="16个字节够用吗?"></a>16个字节够用吗?</h4><p>0xffff0 物理地址处为跳转指令 会长跳转到 BIOS 代码真正开始的地方</p>
<h4 id="BIOS-初始化"><a href="#BIOS-初始化" class="headerlink" title="BIOS 初始化"></a>BIOS 初始化</h4><ul>
<li>基本输入输出</li>
<li>系统设置信息</li>
<li>开机后自检<ol>
<li>硬件自检POST<ul>
<li>检测系统中内存或显卡等关键部位的存在和工作状态</li>
<li>查找并执行显卡等接口的初始化程序</li>
</ul>
</li>
<li>系统初始化<ul>
<li>检测配置即插即用设备</li>
<li>更新 ESCD 扩展系统配置数据</li>
</ul>
</li>
</ol>
</li>
<li>系统自启动等</li>
</ul>
<a id="more"></a>
<p>初始化完成之后 会去读磁盘上的加载程序 读取后会将其加载到 0x7c00</p>
<h5 id="多操作系统怎么办"><a href="#多操作系统怎么办" class="headerlink" title="多操作系统怎么办?"></a>多操作系统怎么办?</h5><p>过去只有一个分区 可以直接去读 加载程序 但是现在普遍都有多分区 因此要加一个主引导记录 先去读0 盘 0 道 1扇区(CHS方法)主引导扇区(512字节 最后两字节必须为 0x55 0xaa)里的 MBR 主引导记录 再去找活动分区 最后才去读加载程序将其加载到 0x7c00<br>过程为 MBR-&gt;主分区 OBR 系统引导记录 / 子扩展分区 OBR</p>
<h5 id="为什么是-0x7c00"><a href="#为什么是-0x7c00" class="headerlink" title="为什么是 0x7c00?"></a>为什么是 0x7c00?</h5><p>在 PC 5150 的 BIOS 开发中 运行的操作系统 为 32KB 的 要满足 MBR 不会过早的被覆盖 那就只能够将其加载到 32KB的末尾 同时 MBR 本身也是程序 有可能会使用到栈 故需要为栈分配一些空间 估计总共 1KB 内存就足够了 因此 将其放置在 32KB的末尾前1KB处 即 0x8000 - 0x400 = 0x7c00</p>
<h5 id="系统启动规范"><a href="#系统启动规范" class="headerlink" title="系统启动规范"></a>系统启动规范</h5><p>BIOS-MBR 主引导记录最多支持4个分区 一个分区占用 16字节 四个分区占用 64字节<br>BIOS-GPT 全局唯一标识分区表 不受4个分区的限制<br>PXE 网络启动标准 通过服务器下载内核镜像来加载<br>UEFI 统一可扩展固件接口 目标是在所有平台上一致的操作系统启动服务 会对引导记录进行可信性检查 只有通过可信性检查的才能运行</p>
<h4 id="加载程序"><a href="#加载程序" class="headerlink" title="加载程序"></a>加载程序</h4><p>当 BIOS 将加载程序从磁盘的主引导扇区加载到 0x7c00 之后 就跳转到 CS:IP 0x0000:7c00 处执行加载程序的指令 加载程序 又将操作系统的代码和数据从硬盘中加载到内存 最后跳转到 操作系统的起始地址</p>
<h5 id="为什么BIOS不直接读操作系统内核镜像"><a href="#为什么BIOS不直接读操作系统内核镜像" class="headerlink" title="为什么BIOS不直接读操作系统内核镜像?"></a>为什么BIOS不直接读操作系统内核镜像?</h5><p>因为磁盘上有文件系统 机器出厂的时候 不能限制为某一种的文件系统 而 BIOS 不可能认识所有文件系统 因此有一个基本的约定 BIOS 不需要认识文件系统 直接从最开始的扇区 读取加载程序 再用加载程序来识别文件系统 再来读取操作系统的内核镜像 再加载到内存中 此外 BIOS 在设计的时候 就只能读取一个扇区 而一个 OS 可能不止是一个扇区 会增加BIOS 工作的难度</p>
<h4 id="BIOS-系统调用"><a href="#BIOS-系统调用" class="headerlink" title="BIOS 系统调用"></a>BIOS 系统调用</h4><p>BIOS 以中断调用的方式 提供了基本的 I/O操作 仅能用于实模式 因为此时为实模式 存储中断程序数组的为中断向量表</p>
<ul>
<li>int 0x10: 字符显示</li>
<li>int 0x13：磁盘扇区读写</li>
<li>int 0x15: 检测内存大小 0xe820 0xe801(最大识别4GB内存) 0x88(最大识别64MB内存)</li>
<li>int 0x16：键盘输入</li>
</ul>
<h4 id="BootLoader-到-OS"><a href="#BootLoader-到-OS" class="headerlink" title="BootLoader 到 OS"></a>BootLoader 到 OS</h4><ul>
<li>使能保护模式<ol>
<li>开启 A20 地址线 若被禁止 CPU 将会采用 8086/8088 的地址回绕</li>
<li>加载 GDT 全局描述符表</li>
<li>CR0 控制寄存器的 PE 位 置 1</li>
</ol>
</li>
<li>从硬盘读取 kernel in ELF格式的 kernel 读到内存中指定的位置</li>
<li>跳到 OS 的入口处</li>
</ul>
<h5 id="段机制-和-页机制"><a href="#段机制-和-页机制" class="headerlink" title="段机制 和 页机制"></a>段机制 和 页机制</h5><p>段选择子 Segment Register 本质上就是个索引 指向描述符表的其中一项 TI 表示是在 GDT 还是 LDT<br>RPL 表示 请求特权级<br><img src="/images/segmentregister.png"><br>段描述符 Segment descriptro 描述了起始地址 和 大小 通过段描述符 来找到代码段的起始地址和大小<br><img src="/images/segmentdescriptor.png"><br>在 Bootloader 中使用 lgdt 机器指令将 段描述符表 GDT Global Descriptor Table 全局描述符表 / LDT 本地描述符表 加载 到 GDTR 寄存器 / LDTR<br>由于后面还有个页机制 在段机制下 就将映射关系弄的简单一些 采用了 平坦模型 整个 4G 内存都是一个段<br>在页机制下 将起到分段的作用</p>
<h3 id="系统中断、异常、调用"><a href="#系统中断、异常、调用" class="headerlink" title="系统中断、异常、调用"></a>系统中断、异常、调用</h3><h4 id="什么是中断"><a href="#什么是中断" class="headerlink" title="什么是中断?"></a>什么是中断?</h4><p>CPU 暂停正在执行的程序转而去执行处理该事件的程序</p>
<h4 id="为什么需要系统中断、异常、调用-中断不会使效率变低吗"><a href="#为什么需要系统中断、异常、调用-中断不会使效率变低吗" class="headerlink" title="为什么需要系统中断、异常、调用?中断不会使效率变低吗?"></a>为什么需要系统中断、异常、调用?中断不会使效率变低吗?</h4><ul>
<li>计算机运行时 内核是被信任的第三方</li>
<li>只有内核才能够执行特权指令</li>
<li>方便应用程序调用</li>
</ul>
<p>中断会不会使效率变低是要取决于看中断的角度 中断虽然打断了当前操作的执行 但是却正是因为中断使得系统能够并发运行<br>系统本质上是一个死循环 但是死循环做不了什么大事 而系统运行的目的是为了等候某些事情的发生 系统是被动的 因此也可以说 操作系统是中断所驱动的</p>
<h4 id="中断的分类"><a href="#中断的分类" class="headerlink" title="中断的分类"></a>中断的分类</h4><ol>
<li>外部中断 也称之为硬件中断<ul>
<li>可屏蔽中断 使用 INTR 信号线通知CPU</li>
<li>不可屏蔽中断 使用 NMI 信号线通知CPU<br><img src="/images/interrupttype.png"></li>
</ul>
</li>
<li>内部中断 又称软中断和异常<ul>
<li>int 8位立即数</li>
<li>int3 调试断点指令</li>
<li>into 中断溢出指令</li>
<li>bound 数组索引越界</li>
<li>ud2 未定义指令</li>
</ul>
</li>
</ol>
<p>除了 int 8位立即数以外 其它都可以称作是异常</p>
<h4 id="中断、异常和系统调用区别"><a href="#中断、异常和系统调用区别" class="headerlink" title="中断、异常和系统调用区别"></a>中断、异常和系统调用区别</h4><ul>
<li>系统调用(system call)：应用程序主动向操作系统发出的服务请求</li>
<li>异常(exception): 非法指令或其他原因导致当前指令执行失败后的处理请求</li>
<li>中断(handware interrupt)：来自硬件设备的处理请求</li>
</ul>
<p>每个中断或异常与一个中断服务例程(Interrupt Service Routine ISR) 相关联<br>Linux 系统调用并没有使用调用门 而是直接使用int 0x80中断 来完成系统调用</p>
<h4 id="系统调用的过程"><a href="#系统调用的过程" class="headerlink" title="系统调用的过程"></a>系统调用的过程</h4><p>首先在Linux系统调用中 采用的是 int 0x80中断 来完成 因此在实现系统调用之前 要实现中断<br>在之前实模式下面 BIOS的中断是使用 中断向量表(Interrupt Vector Table IVT)来存储中断处理程序入口<br>而此时我们处于保护模式下 BIOS的中断已经不可用了 好在保护模式下也有一个用于存储中断处理程序入口的表<br>中断描述符表(Interrupt Descriptor Table IDT)里面存储了中断描述符又称门<br>门有四种 任务门 中断门 调用门 陷阱门 除了调用门其它门都能存储在IDT中<br>IDT 的起始地址和大小保存在中断描述符表寄存器 IDTR 通过机器指令 lidt 保存</p>
<h6 id="中断门、陷阱门描述符格式"><a href="#中断门、陷阱门描述符格式" class="headerlink" title="中断门、陷阱门描述符格式"></a>中断门、陷阱门描述符格式</h6><p><img src="/images/interruptgate.png"><br><img src="/images/trapgate.png"><br>在实际实现中 进行分段操作的 不是在段机制 而是在页机制中完成的<br>因此 此处的段选择子是整个内核代码段 而目标代码段的偏移量 是整个 4G 的空间</p>
<h4 id="系统调用的实现"><a href="#系统调用的实现" class="headerlink" title="系统调用的实现"></a>系统调用的实现</h4><p>由于系统调用在 Linux 是利用中断门的方式来实现的 每个系统调用对应一个中断向量号 其实就是一个中断描述符表的索引 在 IDT 中安装 0x80号中断对应的描述符 在该描述符表里注册系统调用的中断处理例程 建立系统调用子功能表 利用 eax寄存器 获取子功能号<br><img src="/images/syscall.png"></p>
<h4 id="系统调用的特别之处"><a href="#系统调用的特别之处" class="headerlink" title="系统调用的特别之处"></a>系统调用的特别之处</h4><ul>
<li>它会进行堆栈的切换 </li>
<li>特权级的转换 </li>
</ul>
<p>Linux中只用到了 0级特权级 和 3级特权级 因此 只在0级栈与3级栈中切换<br>系统调用的传递参数的方式可以使用栈或者寄存器 其中用寄存器来传递参数则比较简单<br>使用栈传递的话 是用户进程先将参数压入3级栈 然后内核将其读出来再压入0级栈即可</p>
<h4 id="堆栈是怎么切换的"><a href="#堆栈是怎么切换的" class="headerlink" title="堆栈是怎么切换的?"></a>堆栈是怎么切换的?</h4><p>CPU 会从 TSS 任务状态段 (Task Status Segment)获取ss0和esp0<br>定位内核态堆栈后 会将用户态堆栈的 ss3和esp3压入内核态堆栈<br><img src="/images/interruptstack.png"><br>当发生中断且特权级有变化的时候 处理器会将用户栈 SS 和 EIP 压入内核栈中 以备返回时重新加载到栈段寄存器 SS 和栈指针 ESP 后在新栈压入 EFLAGS 寄存器 由于要切换到目标代码段 段间转移，要将 CS 和 EIP 保存到当前栈中备份 以便中断程序执行结束后能恢复到被中断的进程 有的异常会有错误码<br><img src="/images/errorcode.png"><br>EXT EXTernal event 外部事件 IDT 表示是否指向中断描述符表 TI 表示是指向 GDT 还是 LDT</p>
<h4 id="特权级是怎么转换的"><a href="#特权级是怎么转换的" class="headerlink" title="特权级是怎么转换的?"></a>特权级是怎么转换的?</h4><p>3级特权级 进入 0级特权级 只需要进入中断即可<br>那么 操作系统一开始运行的时候处于 0级特权级 是怎么进入 3级特权级的?<br>通过 欺骗CPU 就是让CPU以为目前就处于中断处理程序中 然后通过 iret 退出中断 进入到 3级特权级</p>
<h4 id="系统调用的开销"><a href="#系统调用的开销" class="headerlink" title="系统调用的开销"></a>系统调用的开销</h4><ul>
<li>保护程序上下文</li>
<li>第一次调用要建立一个堆栈</li>
<li>验证参数 内核代码对用户进程不信任</li>
<li>内核态映射到用户态的地址空间 不同进程的切换 需要更新 cr3寄存器 存放页目录表物理地址</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 虚拟存储概念</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E8%99%9A%E6%8B%9F%E5%AD%98%E5%82%A8%E6%A6%82%E5%BF%B5/</url>
    <content><![CDATA[<h3 id="为什么需要虚拟存储"><a href="#为什么需要虚拟存储" class="headerlink" title="为什么需要虚拟存储?"></a>为什么需要虚拟存储?</h3><p>计算机系统常出现内存空间不够用</p>
<h3 id="如何解决内存空间不够用"><a href="#如何解决内存空间不够用" class="headerlink" title="如何解决内存空间不够用?"></a>如何解决内存空间不够用?</h3><ul>
<li>覆盖(Overlay)</li>
</ul>
<p>应用程序 手动 把需要的指令和数据保存在内存中 (将代码分成若干模块 彼此无调用关系的 将其附庸到一块区域 需要用户自己来确定何时将其模块加入程序 开发难度高)</p>
<ul>
<li>交换(对换 Swapping)</li>
</ul>
<p>操作系统 自动 把暂时不能执行的程序保存到外存中 (因为对换的是一个进程的整个地址空间 所以开销会比较大)</p>
<ul>
<li>虚拟存储</li>
</ul>
<p>在有限容量的内存中 以页为单位自动装入更多更大的程序</p>
<a id="more"></a>
<h3 id="覆盖和交换技术"><a href="#覆盖和交换技术" class="headerlink" title="覆盖和交换技术"></a>覆盖和交换技术</h3><h4 id="覆盖技术目标"><a href="#覆盖技术目标" class="headerlink" title="覆盖技术目标"></a>覆盖技术目标</h4><p>在较小的可用内存中运行较大的程序 (一个程序的运行内存空间都不够)</p>
<h4 id="覆盖技术实现方法"><a href="#覆盖技术实现方法" class="headerlink" title="覆盖技术实现方法"></a>覆盖技术实现方法</h4><p>依据程序的逻辑结构 将程序模块划分为若干功能相对独立的模块<br>将不会同时执行的模块共享同一块内存区域</p>
<ul>
<li>必要部分(常用功能)的代码和数据常驻内存</li>
<li>可选部分(非常用功能)放在其他程序模块中 只在需要的时候装入内存</li>
<li>不存在调用关系的模块可相互覆盖 共用同一块内存区域</li>
</ul>
<p><img src="/images/overlay.png" alt="overlay"></p>
<h5 id="覆盖技术的不足"><a href="#覆盖技术的不足" class="headerlink" title="覆盖技术的不足"></a>覆盖技术的不足</h5><ul>
<li><p>增加编程难度</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">需要程序员 划分功能模块 并确定模块间的覆盖关系</span><br></pre></td></tr></table></figure></li>
<li><p>增加执行时间</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">从外存 装入覆盖 模块 时间换空间</span><br></pre></td></tr></table></figure>
<h4 id="交换技术目标"><a href="#交换技术目标" class="headerlink" title="交换技术目标"></a>交换技术目标</h4><p>增加正在运行或需要运行的程序的内存 (一个程序的运行内存空间足够了 但是想要运行更多的进程)</p>
<h4 id="交换技术实现方法"><a href="#交换技术实现方法" class="headerlink" title="交换技术实现方法"></a>交换技术实现方法</h4></li>
<li><p>将暂时不能运行的程序放到外存</p>
</li>
<li><p>换入换出基本单位 整个进程的地址空间</p>
</li>
<li><p>换出(Swap out) 把一个进程的整个地址空间保存到外存</p>
</li>
<li><p>换入(Swap in) 把外存中的某进程的地址空间读入到内存</p>
</li>
</ul>
<p><img src="/images/swapping.png" alt="swapping"></p>
<h4 id="交换技术有什么问题"><a href="#交换技术有什么问题" class="headerlink" title="交换技术有什么问题?"></a>交换技术有什么问题?</h4><ul>
<li><p>交换时机</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当内存不够 或 有不够的可能时换出</span><br></pre></td></tr></table></figure></li>
<li><p>交换区大小</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">存放所有用户进程的所有内存映像的拷贝</span><br></pre></td></tr></table></figure></li>
<li><p>程序换入时的重定位 换出时再换入时要放在原处吗?</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">采用动态地址映射的方法</span><br></pre></td></tr></table></figure>
<h4 id="覆盖与交换的比较"><a href="#覆盖与交换的比较" class="headerlink" title="覆盖与交换的比较"></a>覆盖与交换的比较</h4></li>
<li><p>覆盖</p>
<ul>
<li>只能发生在没有调用关系的模块间</li>
<li>程序员必须知道这种逻辑覆盖结构</li>
<li>发生在运行程序的内部模块间</li>
</ul>
</li>
<li><p>交换</p>
<ul>
<li>以进程为单位</li>
<li>不需要模块间的逻辑覆盖结构</li>
<li>发生在内存进程间</li>
</ul>
</li>
</ul>
<p>虚拟存储 使用的就是 交换的思路 只不过它是以页来做单位 且 是操作系统来完成的交换</p>
<h3 id="局部性原理-Principle-of-locality"><a href="#局部性原理-Principle-of-locality" class="headerlink" title="局部性原理 (Principle of locality)"></a>局部性原理 (Principle of locality)</h3><p>程序在执行过程中的一个较短时期 所执行的指令地址和指令的操作数地址 分别局限于一定区域</p>
<ul>
<li>时间局部性<ul>
<li>一条指令的一次执行和下一次执行 一个数据的一次访问和下次访问 都集中在一个较短时期</li>
</ul>
</li>
<li>空间局部性<ul>
<li>当前指令和邻近的几条指令 当前访问的数据和邻近的几个数据 都集中在一个较小的区域</li>
</ul>
</li>
<li>分支局部性<ul>
<li>一条跳转指令的两次执行 很可能跳转到相同的内存位置</li>
</ul>
</li>
<li>局部性原理意义<ul>
<li>从理论上来说 虚拟存储是能够实现的 并且可以取得满意的成果</li>
</ul>
</li>
</ul>
<h4 id="不同程序编写方法的局部性特征"><a href="#不同程序编写方法的局部性特征" class="headerlink" title="不同程序编写方法的局部性特征"></a>不同程序编写方法的局部性特征</h4><p>例子：页面大小为4K，分配给每个进程的物理页面数为1。在一个进程中，定义了如下的二维数组int A[1024][1024]，该数组按行存放在内存，每一行放在一个页面中</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">程序编写方法<span class="number">1</span>：</span><br><span class="line"><span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; <span class="number">1024</span>; j++)</span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">1024</span>; i++)</span><br><span class="line">    A[i][j] = <span class="number">0</span>; <span class="comment">// 按行 清 0</span></span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">程序编写方法<span class="number">2</span>：</span><br><span class="line"><span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; <span class="number">1024</span>; i++)</span><br><span class="line"><span class="keyword">for</span> (j = <span class="number">0</span>; j &lt; <span class="number">1024</span>; j++)</span><br><span class="line">    A[i][j] = <span class="number">0</span>; <span class="comment">// 按列 清 0</span></span><br></pre></td></tr></table></figure>

<p><img src="/images/principleoflocality.png" alt="principle_of_locality"></p>
<p>第一种编程方法 按行 清 0 按照题目所说 分配给每个进程的物理页面数为 1 就会产生 1024 x 1024 次缺页异常 因为 不断的在换页</p>
<p>第二种编程方法 按列 清 0 从 0 ~ 1023 都是在同一页 只有换行的时候 才会换页 故会 产生 1024 次 缺页异常</p>
<p>如果物理内存小 这两种编程方法的性能差距是巨大的</p>
<h3 id="虚拟存储"><a href="#虚拟存储" class="headerlink" title="虚拟存储"></a>虚拟存储</h3><p>将不常用的部分内存暂存到外存</p>
<h4 id="虚拟存储的目标"><a href="#虚拟存储的目标" class="headerlink" title="虚拟存储的目标"></a>虚拟存储的目标</h4><ul>
<li>只把部分程序放到内存中 从而运行比物理内存大的程序</li>
<li>由操作系统自动完成</li>
<li>在内存与外存之间只交换进程的部分内容</li>
</ul>
<h4 id="虚拟存储思路"><a href="#虚拟存储思路" class="headerlink" title="虚拟存储思路"></a>虚拟存储思路</h4><ul>
<li>加载程序时 只将当前执行所需要的部分页面或段装入内存</li>
<li>指令执行时中需要的指令或数据不在内存中时 处理器通知操作系统将相应的页面或段调入内存</li>
<li>操作系统将内存中暂时不用的页面或段保存到外存</li>
</ul>
<h4 id="虚拟存储实现方式"><a href="#虚拟存储实现方式" class="headerlink" title="虚拟存储实现方式"></a>虚拟存储实现方式</h4><ul>
<li>虚拟段式存储</li>
<li>虚拟页式存储</li>
</ul>
<h4 id="虚拟存储基本特征"><a href="#虚拟存储基本特征" class="headerlink" title="虚拟存储基本特征"></a>虚拟存储基本特征</h4><ul>
<li>不连续性<ul>
<li>物理内存分配非连续</li>
<li>虚拟地址空间使用非连续</li>
</ul>
</li>
<li>大用户空间<ul>
<li>提供给用户的虚拟内存可大于实际的物理内存</li>
</ul>
</li>
<li>部分交换<ul>
<li>虚拟存储只对部分虚拟地址空间的换入和换出</li>
</ul>
</li>
</ul>
<h4 id="虚拟存储的支持技术"><a href="#虚拟存储的支持技术" class="headerlink" title="虚拟存储的支持技术"></a>虚拟存储的支持技术</h4><ul>
<li>硬件<ul>
<li>页式或短时存储中的地址转换机制 (硬件要知道它在内存里还是在外存里)</li>
</ul>
</li>
<li>操作系统<ul>
<li>管理内存和外存间页面或段的换入换出 (操作系统要知道它要换的页面或段是哪些)</li>
</ul>
</li>
</ul>
<h4 id="虚拟页式存储"><a href="#虚拟页式存储" class="headerlink" title="虚拟页式存储"></a>虚拟页式存储</h4><p>在页式存储管理的基础上 增加请求调页和页面置换</p>
<ul>
<li>思路<ul>
<li>当用户进程要加载到内存运行时 只装入部分页面 就启动程序运行</li>
<li>进程在运行中发现有需要的代码或数据不在内存中 则向系统发起缺页异常请求 然后就进入了 中断服务例程</li>
<li>操作系统在处理缺页异常时 将外存的页面调入内存 使进程能够继续运行</li>
</ul>
</li>
</ul>
<h4 id="虚拟页式存储地址转换"><a href="#虚拟页式存储地址转换" class="headerlink" title="虚拟页式存储地址转换"></a>虚拟页式存储地址转换</h4><p>其实和之前 非虚拟页式存储地址转换 是一样的 只不过页表项里会有一个 P 位 表示这个页是否存在 若不存在 就会发起缺页异常中断</p>
<p><img src="/images/logaddr2phyaddr0.png" alt="logaddr2phyaddr0"></p>
<p><img src="/images/X86pagetable.png" alt="x86pagetable"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">地址转换依然是 逻辑地址的 前 10 位 加上 cr3 寄存器里面的 页目录表基址 找到 PDE </span><br><span class="line">读出 PDE 找到 页表项基址 加上 逻辑地址的中间 10位 找到 PTE</span><br><span class="line">再将 从 PTE 读出的 物理页面帧号 + 逻辑地址的 低 12 位 找到 物理地址</span><br></pre></td></tr></table></figure>

<p>页表项 结构和之前说的一样<br><img src="/images/pagetableentries.png" alt="pagetable_entries"></p>
<ul>
<li>AVL CPU 不理会这个属性 可以不管 (有可能在32位系统使用大过 4G内存的时候 用到这几位) </li>
<li>G Global 全局位 表示是否将虚拟地址与物理地址的转换结果缓存到 TLB 中</li>
<li>D Dirty 脏页位 当 CPU 对这个页进行写操作时 会置 1</li>
<li>PAT Page Attribute Table 页属性表位 置 0</li>
<li>A Accessed 访问位 若为 1 则 说明 CPU 访问过了 CPU 会定时清 0 记录被置 1 的频率 当内存不足时 会将 使用频率较低的页面换出到外存 同时将 P位 置 0 下次访问 该页时 会引起 Pagefault 异常 中断处理程序再将此页换上</li>
<li>PCD Page-level Cache Disable 页级高速缓存位 置 0 即可 读的时候 高速缓存是否有效 若有效则直接从高速缓存中读出 若无效的话 则必须实实在在的从 I/O 端口去读数据</li>
<li>PWT Page-level Write-Through 页级通写位 控制是先写到高速缓存里再慢慢回写到内存里 还是 直接慢慢写到内存里</li>
<li>US User/Superviosr 普通用户/超级用户位</li>
<li>RW Read/Write 读写位</li>
<li>P Present 存在位 (虚拟页式存储的关键位 若为 0 则发起缺页异常)</li>
</ul>
<h4 id="缺页异常的处理例程"><a href="#缺页异常的处理例程" class="headerlink" title="缺页异常的处理例程"></a>缺页异常的处理例程</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">CPU 执行 Load M</span><br><span class="line">这条指令会去找 M 对应的 页表项 若 这个页表项有效(P 位 为 1) 则直接访问</span><br><span class="line">若无效 就会产生缺页异常 导致操作系统的缺页异常的服务例程会执行</span><br><span class="line">缺页异常服务例程 会去找外存中的那个页面 若找到 就将其读入内存</span><br><span class="line">读入内存之前 还要先判断物理内存中是否有空闲页 若找到空闲页 就将这页读进来 并修改页表项 最后重新执行 一开始的那条指令</span><br><span class="line">若找不到空闲页 依据页面置换算法 找一页不常用的页 若这一页被修改过 则将其写到外存 </span><br><span class="line">若未修改过 则将其丢弃 且 修改 这一页的 P 位 为 0 使其失效</span><br><span class="line">将需要访问的页装入被替换掉的页 且 修改 页表项的 P 位 为 1</span><br></pre></td></tr></table></figure>
<p><img src="/images/pagefault.png" alt="pagefault"></p>
<h4 id="虚拟页式存储中的外存管理"><a href="#虚拟页式存储中的外存管理" class="headerlink" title="虚拟页式存储中的外存管理"></a>虚拟页式存储中的外存管理</h4><p>在何处保存未被映射的页?</p>
<ul>
<li>应能方便地找到在外存中的页面内容 (直接做一个分区做对换区 Linux Unix)</li>
<li>交换空间 (磁盘或者文件) <ul>
<li>采用特殊格式存储未被映射的页面 (用一个文件来存)</li>
</ul>
</li>
</ul>
<p>虚拟页式存储中的外存怎么选择?</p>
<ul>
<li>代码段 它就是可执行的二进制文件 没必要复制</li>
<li>代码加载的共享库程序段 也是不会变的 没必要复制</li>
<li>其他段 可以放到交换空间里去</li>
</ul>
<h4 id="虚拟页式存储管理的性能"><a href="#虚拟页式存储管理的性能" class="headerlink" title="虚拟页式存储管理的性能"></a>虚拟页式存储管理的性能</h4><p>有效存储访问时间 (Effective memory access time EAT)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">EAT &#x3D; 访存时间 * (1 - p) + 缺页异常处理时间 * 缺页率p * (1 + 页修改概率q) (若不开启虚拟页式存储 则 EAT &#x3D; 访存时间)</span><br><span class="line">加多一个 页修改概率q 是因为被替换的页被修改过的话 还要写到外存里保存起来</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>虚拟存储</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 虚拟存储页面置换算法</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E8%99%9A%E6%8B%9F%E5%AD%98%E5%82%A8%E9%A1%B5%E9%9D%A2%E7%BD%AE%E6%8D%A2%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h3 id="页面置换算法概念"><a href="#页面置换算法概念" class="headerlink" title="页面置换算法概念"></a>页面置换算法概念</h3><ul>
<li>功能<ul>
<li>当出现缺页异常时 需调入新页面且内存已满 置换算法选择被置换的物理页面</li>
</ul>
</li>
<li>设计目标<ul>
<li>尽可能地减少页面调入调出次数</li>
<li>把未来不再访问 或 短期内不访问的页面调出</li>
</ul>
</li>
<li>页面锁定 (Frame locking)<ul>
<li>描述必须常驻内存的逻辑页面</li>
<li>操作系统的关键代码</li>
<li>要求响应速度的代码和数据</li>
<li>页表中的锁定标志位 (Lock bit)</li>
</ul>
</li>
</ul>
<a id="more"></a>
<h3 id="页面置换算法评价方法"><a href="#页面置换算法评价方法" class="headerlink" title="页面置换算法评价方法"></a>页面置换算法评价方法</h3><ul>
<li>记录进程访问内存的页面轨迹<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">虚拟地址访问用(页号, 位移)表示</span><br><span class="line">(3,0),  (1,9),  (4,1),  (2,1),  (5,3),  (2,0),  (1,9),  (2,4),  (3,1),  (4,8)</span><br><span class="line">对应的页面轨迹</span><br><span class="line">3, 1, 4, 2, 5, 2, 1, 2, 3, 4</span><br><span class="line">替换如 c, a, d, b, e, b, a, b, c, d</span><br></pre></td></tr></table></figure></li>
<li>评价标准<ul>
<li>模拟页面置换算法的行为 记录各个置换算法产生缺页的次数 越少越好</li>
</ul>
</li>
</ul>
<h3 id="局部页面置换算法"><a href="#局部页面置换算法" class="headerlink" title="局部页面置换算法"></a>局部页面置换算法</h3><p>置换页面的选择范围仅限于当前进程占用的物理页面内 (在当前进程中的物理页面来决定换哪个页)</p>
<ul>
<li>最优算法 (预测未来来决定 太理想)</li>
<li>先进先出算法 (先进先出并不一定反映了实际的访问特征 性能较差)</li>
<li>最近最久未使用算法 (统计过去 假设过去的特征是对未来的预测)<ul>
<li>时钟算法</li>
<li>最不常用算法</li>
</ul>
</li>
</ul>
<h4 id="最优页面置换算法-OPT-optional"><a href="#最优页面置换算法-OPT-optional" class="headerlink" title="最优页面置换算法 (OPT optional)"></a>最优页面置换算法 (OPT optional)</h4><p>置换在未来最长时间不访问页面</p>
<h5 id="最优页面置换算法实现"><a href="#最优页面置换算法实现" class="headerlink" title="最优页面置换算法实现"></a>最优页面置换算法实现</h5><ul>
<li>缺页时 计算内存中的每个逻辑页面的下一次访问时间</li>
<li>选择 未来最长时间 不访问 的页面</li>
</ul>
<h5 id="最优页面置换算法特征"><a href="#最优页面置换算法特征" class="headerlink" title="最优页面置换算法特征"></a>最优页面置换算法特征</h5><ul>
<li>缺页次数最少</li>
<li>实际系统中无法实现</li>
<li>无法预知每个页面在下次访问前的等待时间</li>
<li>但是能作为置换算法性能评测依据</li>
</ul>
<h5 id="最优页面置换算法示例"><a href="#最优页面置换算法示例" class="headerlink" title="最优页面置换算法示例"></a>最优页面置换算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次 第二次 第三次 第四次都是正常访问的</span><br><span class="line">访问第五次的时候 要访问 e 不在物理内存 产生缺页</span><br><span class="line">缺页异常中断服务例程 引用最优页面置换算法 算出未来最长时间不访问的页面 为 d 它 在 10 的时候访问</span><br><span class="line">将 d页 替换为 e页</span><br><span class="line">一共发生了 两次 缺页异常</span><br></pre></td></tr></table></figure>
<p><img src="/images/OPT_page_replacement_algorithm.png" alt="OPT_page_replacement_algorithm"></p>
<h4 id="先进先出置换算法-FIFO-First-In-First-Out"><a href="#先进先出置换算法-FIFO-First-In-First-Out" class="headerlink" title="先进先出置换算法 (FIFO First-In First-Out)"></a>先进先出置换算法 (FIFO First-In First-Out)</h4><p>选择在内存中驻留时间 最长的 页面进行置换</p>
<h5 id="先进先出置换算法实现"><a href="#先进先出置换算法实现" class="headerlink" title="先进先出置换算法实现"></a>先进先出置换算法实现</h5><ul>
<li>维护一个记录所有位于内存中的逻辑页面的链表</li>
<li>链表元素按照驻留时间排序 链首最长 链尾最短</li>
<li>出现缺页时 选择链首页面进行置换 新页面加到链尾</li>
</ul>
<h5 id="先进先出置换算法特征"><a href="#先进先出置换算法特征" class="headerlink" title="先进先出置换算法特征"></a>先进先出置换算法特征</h5><ul>
<li>实现简单</li>
<li>性能较差 调出的页面很可能是经常访问的</li>
<li>进程分配物理页面数增加时 缺页不一定减少(Belady 现象)</li>
<li>有缺陷 不单独使用 揉到其它算法里一起使用</li>
</ul>
<h5 id="先进先出置换算法示例"><a href="#先进先出置换算法示例" class="headerlink" title="先进先出置换算法示例"></a>先进先出置换算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次 第二次 第三次 第四次 都是正常访问</span><br><span class="line">第五次访问 e 出现缺页异常 进入缺页异常中断服务例程 引用先进先出置换算法</span><br><span class="line">因为 物理页帧 a 是最早进来的 按照先进先出的规则 a 换为 e</span><br><span class="line">整个过程 共发生了 五次 缺页异常</span><br></pre></td></tr></table></figure>

<p><img src="/images/FIFO_page_replacement_algorithm.png" alt="FIFO_page_replacement_algorithm"></p>
<h4 id="最近最久未使用页面置换算法-LRU-Least-Recently-Used"><a href="#最近最久未使用页面置换算法-LRU-Least-Recently-Used" class="headerlink" title="最近最久未使用页面置换算法 (LRU Least Recently Used)"></a>最近最久未使用页面置换算法 (LRU Least Recently Used)</h4><p>属于 最优置换算法 和 先进先出置换算法 的折中</p>
<ul>
<li>选择最长时间没有被引用的页面进行置换</li>
<li>某些页面长时间未被访问 则它们在将来还可能会长时间不会访问</li>
</ul>
<h5 id="最近最久未使用算法实现"><a href="#最近最久未使用算法实现" class="headerlink" title="最近最久未使用算法实现"></a>最近最久未使用算法实现</h5><ul>
<li>缺页时 计算内存中每个逻辑页面的上一次访问时间</li>
<li>选择上一次使用到当前时间 最长的 页面置换出去</li>
</ul>
<h5 id="最近最久未使用算法特征"><a href="#最近最久未使用算法特征" class="headerlink" title="最近最久未使用算法特征"></a>最近最久未使用算法特征</h5><p>最优置换算法的一种近似</p>
<h5 id="最近最久未使用算法示例"><a href="#最近最久未使用算法示例" class="headerlink" title="最近最久未使用算法示例"></a>最近最久未使用算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次 第二次 第三次 第四次 都不产生缺页异常</span><br><span class="line">第五次访问 e 产生缺页异常 </span><br><span class="line">过去的时间 c 是最长时间没有被访问的 因此 将 c 换为 e</span><br><span class="line">总共出现 三次 缺页异常</span><br></pre></td></tr></table></figure>
<p><img src="/images/LRU_page_replacement_algorithm.png" alt="LRU_page_replacement_algorithm"></p>
<h5 id="最近最久未使用算法可能实现"><a href="#最近最久未使用算法可能实现" class="headerlink" title="最近最久未使用算法可能实现"></a>最近最久未使用算法可能实现</h5><p>每次访问开销都很大</p>
<ul>
<li>页面链表<ul>
<li>系统维护一个按最近一次访问时间排序的链表<ul>
<li>链表首节点是最近刚刚访问过的页面</li>
<li>链表尾节点是最久未使用的页面</li>
</ul>
</li>
<li>访问内存时 找到相应页面 并将其移到链首</li>
<li>缺页时 置换链表尾节点的页面</li>
</ul>
</li>
<li>活动页面栈<ul>
<li>访问时 将页号压入栈顶 并将栈内相同的页号删去</li>
<li>缺页时 置换栈底页面</li>
</ul>
</li>
</ul>
<h5 id="最近最久未使用算法栈实现"><a href="#最近最久未使用算法栈实现" class="headerlink" title="最近最久未使用算法栈实现"></a>最近最久未使用算法栈实现</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一 ~ 第四次 相安无事 但要将依次访问过的页 压入栈顶</span><br><span class="line">第五次 访问 e 缺页异常 将栈底 的 c 换为 e</span><br><span class="line">第六次 访问 b 栈里已经有 b 了 将 栈中的 b 删去 再压入栈顶</span><br><span class="line">共 发生 三次 缺页异常</span><br></pre></td></tr></table></figure>

<p><img src="/images/LRU_stack_page_replacement_algorithm.png" alt="LRU_stack_page_replacement_algorithm"></p>
<h4 id="时钟页面置换算法-Clock"><a href="#时钟页面置换算法-Clock" class="headerlink" title="时钟页面置换算法 (Clock)"></a>时钟页面置换算法 (Clock)</h4><p>仅对页面的访问情况进行大致统计 (统计过去一段时间 这个页面是否被访问过 若访问过则留下 没访问过则按照现有顺序做置换)</p>
<ul>
<li>数据结构<ul>
<li>在页表项加入 访问位 Access 位 描述页面在过去一段时间内访问情况</li>
<li>各页面组织成环形链表</li>
<li>指针指向最先调入的页面 后绕着环形链表走 看起来很像 时钟</li>
</ul>
</li>
<li>算法<ul>
<li>访问页面时 在页表项 记录页面访问情况</li>
<li>缺页时 从指针处开始顺序查找未被访问过的页表进行置换</li>
</ul>
</li>
</ul>
<h5 id="时钟置换算法特征"><a href="#时钟置换算法特征" class="headerlink" title="时钟置换算法特征"></a>时钟置换算法特征</h5><p>时钟算法是 LRU(最久未使用算法) 和 FIFO(先进先出算法) 做折中<br>因为它既不像 LRU 考虑的那么详细 又不像 FIFO 只在意一段时间内若不访问 就做置换 考虑的那么粗</p>
<h5 id="时钟置换算法实现"><a href="#时钟置换算法实现" class="headerlink" title="时钟置换算法实现"></a>时钟置换算法实现</h5><ul>
<li>页面装入内存时 访问位 置 0</li>
<li>访问页面 (读/写) 时 访问位 置 1</li>
<li>缺页时 从指针当前位置顺序检查环形链表<ul>
<li>访问位为 0 则置换此页</li>
<li>访问位为 1 则访问位置 0 指针继续移动到下一个页面 直到找到可置换页面</li>
</ul>
</li>
</ul>
<h5 id="时钟置换算法示例"><a href="#时钟置换算法示例" class="headerlink" title="时钟置换算法示例"></a>时钟置换算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一 ~ 第四次访问 的页面都已在环形链表中 故而 只将访问位 置 1</span><br><span class="line">第五次访问 e 缺页异常 指针扫描环形链表 访问位 为 1 的 则置 0 </span><br><span class="line">不断地循环扫描 直至 扫出可置换页面 这里 会循环了一遍以后 将 页面 a 换为 e</span><br><span class="line">一共发生了 四次 缺页异常</span><br></pre></td></tr></table></figure>

<p><img src="/images/Clock_page_replacement_algorithm.png" alt="Clock_page_replacement_algorithm"></p>
<h5 id="改进时钟置换算法"><a href="#改进时钟置换算法" class="headerlink" title="改进时钟置换算法"></a>改进时钟置换算法</h5><ul>
<li>时钟置换算法缺点</li>
</ul>
<p>之前的时钟置换算法如果要置换的页是被修改过的 那么就会先要将修改过的页 写到外存 然后<br>才将要换入的页读入内存 消耗时间过长</p>
<ul>
<li>改进</li>
</ul>
<p>减少修改页的缺页处理开销 (如果要置换的页被修改了 则不置换此页 同时操作系统定期将修改过的页写到外存)</p>
<ul>
<li>算法<ul>
<li>在页面中加入修改位 并在访问时 进行相应修改</li>
<li>缺页时 修改页面标志位 以跳过有修改的页面</li>
</ul>
</li>
</ul>
<h6 id="改进时钟置换算法实例"><a href="#改进时钟置换算法实例" class="headerlink" title="改进时钟置换算法实例"></a>改进时钟置换算法实例</h6><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次 只访问 c 访问位 置 1</span><br><span class="line">第二次 写 a 访问位 修改位 都置 1</span><br><span class="line">第三次 访问 d 访问位 置 1</span><br><span class="line">第四次 写 b 访问位 修改位 都 置 1</span><br><span class="line">第五次 访问 e 缺页异常 循环一遍 访问位 为 1 改成 0 访问位 为 0 若 修改位 为 1 改成 0 </span><br><span class="line">最后只会将 c 换 为 e 因为循环下来 a 和 b 页都是被修改过的 跳过 只有 c 访问位和修改位都为 0</span><br><span class="line">共发生 三次 缺页异常</span><br></pre></td></tr></table></figure>
<p><img src="/images/Improved_Clock_page_replacement_algorithm.png" alt="Improved_Clock_page_replacement_algorithm"></p>
<h4 id="最不常用算法-LFU-Least-Frequently-Used"><a href="#最不常用算法-LFU-Least-Frequently-Used" class="headerlink" title="最不常用算法 (LFU Least Frequently Used)"></a>最不常用算法 (LFU Least Frequently Used)</h4><p>缺页时 置换访问次数最少的页</p>
<h5 id="最不常用算法实现"><a href="#最不常用算法实现" class="headerlink" title="最不常用算法实现"></a>最不常用算法实现</h5><ul>
<li>每个页面设置一个访问计数</li>
<li>访问页数 访问计数 + 1</li>
<li>缺页时 置换 计数最小的页面</li>
</ul>
<h5 id="最不常用算法特征"><a href="#最不常用算法特征" class="headerlink" title="最不常用算法特征"></a>最不常用算法特征</h5><ul>
<li>算法开销大</li>
<li>开始时频繁使用 以后不使用的页面难换出 (因为页面在里面待的越久 访问计数越大)<ul>
<li>解决方法 较大的计数定期右移</li>
</ul>
</li>
</ul>
<h5 id="LRU-和-LFU-的区别"><a href="#LRU-和-LFU-的区别" class="headerlink" title="LRU 和 LFU 的区别"></a>LRU 和 LFU 的区别</h5><ul>
<li>LRU 关注多久未使用 时间越短越好 (维护栈)</li>
<li>LFU 关注次数 待的越久越好 (稍微简单些)</li>
</ul>
<h5 id="最不常用算法示例"><a href="#最不常用算法示例" class="headerlink" title="最不常用算法示例"></a>最不常用算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">每次访问 修改 页面的计数 前 四次访问 都只是 累加</span><br><span class="line">到第五次访问 e 缺页异常 寻找当前计数最小的页 为 a 并将其置换</span><br><span class="line">共产生 五次 缺页异常</span><br></pre></td></tr></table></figure>

<p>LFU 不能够比较快的访问 但是 它可以用在 读取硬盘这种存储访问 时间长一些也没关系</p>
<p><img src="/images/LFU_page_replacement_algorithm.png" alt="LFU_page_replacement_algorithm.png"></p>
<h4 id="Belady-现象"><a href="#Belady-现象" class="headerlink" title="Belady 现象"></a>Belady 现象</h4><p>采用 FIFO 等算法时 可能出现分配的物理页面数增加 缺页次数反而升高的异常现象</p>
<h5 id="产生-Belady-现象的原因"><a href="#产生-Belady-现象的原因" class="headerlink" title="产生 Belady 现象的原因"></a>产生 Belady 现象的原因</h5><ul>
<li>FIFO 算法的置换特征 与 进程访问内存的动态特征矛盾</li>
<li>被它置换出去的页面 并不一定是 进程 近期不会访问的页面</li>
</ul>
<p>总而言之就是预测(近期不访问的页面)不合理</p>
<h5 id="FIFO-的-Belady-现象"><a href="#FIFO-的-Belady-现象" class="headerlink" title="FIFO 的 Belady 现象"></a>FIFO 的 Belady 现象</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">当物理页面数 为 3 时 使用 FIFO 页面置换算法 共产生 9 次 缺页次数</span><br><span class="line">当物理页面数 为 4 时 使用 FIFO 页面置换算法 反而产生了 10 次 缺页次数</span><br></pre></td></tr></table></figure>
<p><img src="/images/FIFO_Belady_3.png" alt="FIFO_Belady_3"></p>
<p><img src="/images/FIFO_Belady_4.png" alt="FIFO_Belady_4"></p>
<h5 id="LRU-没有-Belady-现象"><a href="#LRU-没有-Belady-现象" class="headerlink" title="LRU 没有 Belady 现象"></a>LRU 没有 Belady 现象</h5><p><img src="/images/LRU_No_Belady.png" alt="LRU_No_Belady"></p>
<h5 id="LRU-FIFO-和-Clock-比较"><a href="#LRU-FIFO-和-Clock-比较" class="headerlink" title="LRU FIFO 和 Clock 比较"></a>LRU FIFO 和 Clock 比较</h5><ul>
<li>LRU 和 FIFO 本质都是 先进先出算法<ul>
<li>LRU 根据页面最近访问时间排序</li>
<li>LRU 需要动态地调整顺序 (算法性能好 系统开销大)</li>
<li>FIFO 根据页面进入内存的实际排序</li>
<li>FIFO 的页面进入时间是固定不变的 (系统开销较小 但会发生 Belady 现象)</li>
</ul>
</li>
</ul>
<p>当页面进入内存后 没有被访问了 最近访问时间和进入内存的时间相同时 LRU 可退化成 FIFO</p>
<ul>
<li>Clock 算法是 它们的折中<ul>
<li>页面访问时 不动态调整页面在链表中的顺序 只是做一个标记</li>
<li>缺页时 再将它移动到链表末尾 (跳过访问位 为 1 的页面)</li>
</ul>
</li>
</ul>
<p>对于未被访问的页面 LRU FIFO Clock 性能一样 因为都退化到 FIFO 了<br>对于被访问过的页面 Clock 算法 不能记录准确的访问时间 而 LRU 可以</p>
<h3 id="全局页面置换算法"><a href="#全局页面置换算法" class="headerlink" title="全局页面置换算法"></a>全局页面置换算法</h3><p>置换页面的选择范围是所有(所有进程)可换出的物理页面 (考虑的角度是从各个进程所需的内存不同)</p>
<ul>
<li>工作集算法</li>
<li>缺页率算法</li>
</ul>
<p>给进程分配可变数目的物理页面</p>
<h4 id="局部页面置换算法的不足"><a href="#局部页面置换算法的不足" class="headerlink" title="局部页面置换算法的不足"></a>局部页面置换算法的不足</h4><p>局部页面置换算法没有考虑进程访存的差异 (进程在不同阶段内存需求是不同的)</p>
<p>(使用局部页面置换算法 无论怎么置换 物理页面始终是那么多 缺页次数不能减少 如果给它加一些物理页面 性能会提高很多)</p>
<h4 id="全局页面置换算法需要解决的问题"><a href="#全局页面置换算法需要解决的问题" class="headerlink" title="全局页面置换算法需要解决的问题"></a>全局页面置换算法需要解决的问题</h4><ul>
<li>进程在不同阶段的内存需求是变化的</li>
<li>分配给进程的内存也需要在不同阶段有所变化</li>
<li>全局置换算法需要确定分配给进程的物理页面数</li>
</ul>
<h4 id="CPU利用率和并发进程数的关系"><a href="#CPU利用率和并发进程数的关系" class="headerlink" title="CPU利用率和并发进程数的关系"></a>CPU利用率和并发进程数的关系</h4><p><img src="/images/CPU_process.png" alt="CPU_process"></p>
<ul>
<li>CPU利用率 与 并发进程数存在相互促进和制约的关系<ul>
<li>进程数少时 提高并发进程数 可提高 CPU利用率</li>
<li>并发进程导致内存访问增加</li>
<li>并发进程的内存访问会降低访存局部性特征 (两个进程所做的事情不相干 局部性原理不适用)</li>
<li>局部性特征下降 缺页率上升和 CPU利用率下降</li>
</ul>
</li>
</ul>
<h5 id="工作集"><a href="#工作集" class="headerlink" title="工作集"></a>工作集</h5><p>一个进程当前正在使用的逻辑页面的集合 可表示为二元函数 W(t, Δ)</p>
<ul>
<li>t 是当前的执行时刻</li>
<li>Δ 为工作集窗口(Working-set window) 一个定长的页面访问时间窗口</li>
<li>W(t, Δ) 为在当前时刻t前的 Δ 时间窗口中所有访问页面所组成的集合</li>
<li>| W(t, Δ) | 为工作集大小 页面数量</li>
</ul>
<h6 id="工作集的变化"><a href="#工作集的变化" class="headerlink" title="工作集的变化"></a>工作集的变化</h6><p><img src="/images/Working_set.png" alt="Working_set"></p>
<ul>
<li>进程开始执行后 随着访问新页面逐步建立较稳定的工作集</li>
<li>当内存访问的局部性区域的位置大致稳定时(只访问那几个页面 没有大的改变时) 工作集大小也大致稳定</li>
<li>局部性区域的位置改变时(进程前一项事情做完 去做下一项事情时) 工作集快速扩张和快速收缩过渡到下一个稳定值</li>
</ul>
<h5 id="常驻集"><a href="#常驻集" class="headerlink" title="常驻集"></a>常驻集</h5><p>在当前时刻 进程实际驻留在内存当中的页面集合</p>
<ul>
<li>工作集和常驻集的关系<ul>
<li>工作集是进程运行过程中固有的性质 (进程在一段时间访问的页面集合)</li>
<li>常驻集 取决于 系统分配给进程的物理页面数 和 页面置换算法 (实际在内存中的页)</li>
</ul>
</li>
<li>缺页率和常驻集的关系<ul>
<li>常驻集 包含 工作集时 缺页较少 (进程访问的页都在内存里)</li>
<li>工作集发送剧烈过渡时 缺页较多</li>
<li>进程常驻集大小达到一定数目后 缺页率不会明显下降 (内存够进程功能使用了 再去加内存 反而效率下降)</li>
</ul>
</li>
</ul>
<h4 id="工作集置换算法"><a href="#工作集置换算法" class="headerlink" title="工作集置换算法"></a>工作集置换算法</h4><p>换出不在工作集中的页面 (并不一定是在缺页时才做 因此开销大)</p>
<h5 id="工作集置换算法实现"><a href="#工作集置换算法实现" class="headerlink" title="工作集置换算法实现"></a>工作集置换算法实现</h5><p>窗口大小 τ 当前时刻前τ个内存访问的页引用是工作集</p>
<ul>
<li>访存链表 维护窗口内的访存页面链表</li>
<li>访存时 换出不在工作集的页面 (开销大)</li>
<li>缺页时 换入页面</li>
</ul>
<h5 id="工作集置换算法示例"><a href="#工作集置换算法示例" class="headerlink" title="工作集置换算法示例"></a>工作集置换算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次访问 缺页 装入 c 此时 窗口大小 τ 为 4 不需要换出页</span><br><span class="line">第二次访问 不缺页 但是 此时在工作集的 页为 5 而窗口大小 τ 为 4 需要将最早的页 e 放到外存</span><br><span class="line">以此类推</span><br><span class="line">共 发送 五次缺页</span><br></pre></td></tr></table></figure>

<p><img src="/images/Working_set_page_replacement_algorithm.png" alt="Working_set_page_replacement_algorithm"></p>
<h4 id="缺页率算法-PFF-Page-Fault-Frequency"><a href="#缺页率算法-PFF-Page-Fault-Frequency" class="headerlink" title="缺页率算法 (PFF Page Fault Frequency)"></a>缺页率算法 (PFF Page Fault Frequency)</h4><p>缺页率 (Page fault rate) = 缺页次数 / 内存访问次数(不好用) 更多时候使用 缺页平均时间间隔的倒数</p>
<ul>
<li>影响缺页率因素<ul>
<li>页面置换算法 (只有这个能自己控制)</li>
<li>分配给进程的物理页面数</li>
<li>页面大小</li>
<li>程序的编写方法 (局部性特征)</li>
</ul>
</li>
</ul>
<h5 id="缺页率算法实现"><a href="#缺页率算法实现" class="headerlink" title="缺页率算法实现"></a>缺页率算法实现</h5><p>通过调节常驻集的大小 使每个进程的缺页率保持在一个合理的范围</p>
<ul>
<li>若进程缺页率过高 则增加常驻集以分配更多物理页面数</li>
<li>若进程缺页率过低 减少常驻集 将一些页面置换到外存</li>
</ul>
<p><img src="/images/PFF_process.png" alt="PFF_process"></p>
<ul>
<li>访存时 设置引用位标记</li>
<li>缺页时 计算从上次缺页时间 t_last 到现在 t_current 的时间间隔<ul>
<li>如果 t_current - t_last &gt; T 缺页率低 则置换 所有在[t_last, t_current]中没有被引用的页 使其用在更有意义的地方(CPU并发率提高)</li>
<li>如果 t_current - t_last &lt;= T 缺页率高 则增加缺失页到常驻集中</li>
</ul>
</li>
</ul>
<h5 id="缺页率算法示例"><a href="#缺页率算法示例" class="headerlink" title="缺页率算法示例"></a>缺页率算法示例</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">第一次访问 出现缺页 直接加(因为没有之前状态可供参考) 第二次 第三次 不缺页</span><br><span class="line">第四次 访问 d 出现缺页 时间间隔 为 3 &gt; 窗口大小 2 说明缺页率低 将这段时间没有访问过的页 置换到外存 只将 c 和 d 留下</span><br><span class="line">第五次访问 不缺页 第六次访问 e 缺页异常 时间间隔 为 2 &lt;&#x3D; 窗口大小 2 直接将 e 加入内存</span><br><span class="line">第七次 第八次 都不缺页 第九次访问 a 缺页异常 时间间隔 为 3 &gt; 窗口大小 2 只留下 c e a 三个页</span><br><span class="line">共 发送 五次 缺页异常</span><br></pre></td></tr></table></figure>
<p><img src="/images/PFF_page_replacement_algorithm.png" alt="PFF_page_replacement_algorithm"><br>跟 工作集算法对比 工作集算法每一次访问都在考虑置换哪一个页面到外存 缺页率算法只有在时间间隔够大的时候 才置换到外存 将置换这一过程放到了 缺页中断来完成<br>和 局部置换算法一样 开销降低</p>
<h3 id="抖动和负载控制"><a href="#抖动和负载控制" class="headerlink" title="抖动和负载控制"></a>抖动和负载控制</h3><p>抖动产生是因为 随着驻留内存的进程数目增加 分配给每个进程的物理页面数不断减小 缺页率不断上升<br>因此操作系统需在 并发水平 和 缺页率之间达到一个平衡 需要选择一个适当的进程数目 和 进程所需要的物理页面数</p>
<ul>
<li>抖动问题(Thrashing)<ul>
<li>进程物理页面太少 不能包含工作集</li>
<li>造成大量缺页 频繁置换</li>
<li>进程运行速度变慢</li>
</ul>
</li>
</ul>
<h4 id="负载控制"><a href="#负载控制" class="headerlink" title="负载控制"></a>负载控制</h4><p>通过调节并发进程数 (MPL Multiprogramming level) 来进行系统负载控制</p>
<ul>
<li>找到 平均缺页间隔时间 (MTBF Mean time between page faults) = 缺页异常处理时间 (PFST Page fault service time) 的范围</li>
</ul>
<p><img src="/images/thrashing.png" alt="thrashing"></p>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>虚拟存储</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 进程和线程</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E8%BF%9B%E7%A8%8B%E5%92%8C%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<h2 id="进程"><a href="#进程" class="headerlink" title="进程"></a>进程</h2><p>指一个具有一定独立功能的程序在一个数据集合上的一次动态执行的过程</p>
<h4 id="进程的组成"><a href="#进程的组成" class="headerlink" title="进程的组成"></a>进程的组成</h4><p>进程包含了正在运行的程序所有状态信息</p>
<ul>
<li>代码</li>
<li>数据</li>
<li>状态寄存器<ul>
<li>CPU 状态 CR0</li>
<li>指令指针 EIP</li>
</ul>
</li>
<li>通用寄存器</li>
<li>进程占用的系统资源<ul>
<li>打开文件</li>
<li>已分配内存</li>
</ul>
</li>
</ul>
<a id="more"></a>

<h4 id="进程的特点"><a href="#进程的特点" class="headerlink" title="进程的特点"></a>进程的特点</h4><ul>
<li>动态性<ul>
<li>可动态创建 结束进程</li>
</ul>
</li>
<li>并发性<ul>
<li>进程可以被独立调度并占用CPU运行</li>
</ul>
</li>
<li>独立性<ul>
<li>不同进程的工作互不影响</li>
</ul>
</li>
<li>制约性<ul>
<li>因访问共享数据 资源 进程间的同步而产生制约</li>
</ul>
</li>
</ul>
<h4 id="进程与程序的联系"><a href="#进程与程序的联系" class="headerlink" title="进程与程序的联系"></a>进程与程序的联系</h4><p>进程是操作系统处于执行状态下的程序的抽象 同一个程序的多次执行<br>过程对应为不同的进程</p>
<ul>
<li>程序 = 文件(静态可执行文件)</li>
<li>进程 = 执行中的程序 = 程序 + 执行状态</li>
</ul>
<h5 id="进程执行需要的资源"><a href="#进程执行需要的资源" class="headerlink" title="进程执行需要的资源"></a>进程执行需要的资源</h5><ul>
<li>CPU 执行指令</li>
<li>内存 保存代码和数据</li>
</ul>
<h4 id="进程与程序的区别"><a href="#进程与程序的区别" class="headerlink" title="进程与程序的区别"></a>进程与程序的区别</h4><ul>
<li>进程是动态的 程序是静态的<ul>
<li>程序是有序代码的集合</li>
<li>进程是程序的执行 进程有内核态和用户态</li>
</ul>
</li>
<li>进程是暂时的(是一个状态变化的过程) 程序是永久的(保存到文件系统时)</li>
<li>进程与程序的组成不同<ul>
<li>进程的组成包括程序 数据 进程控制块</li>
</ul>
</li>
</ul>
<h3 id="进程控制块-PCB-Process-Control-Block"><a href="#进程控制块-PCB-Process-Control-Block" class="headerlink" title="进程控制块 (PCB Process Control Block)"></a>进程控制块 (PCB Process Control Block)</h3><p>操作系统管理控制进程运行所用的信息集合 (操作系统用 PCB 来描述进程的基本情况以及运行变化的过程)</p>
<ul>
<li>PCB 是进程存在的唯一标识<ul>
<li>每个进程都在操作系统中有一个对应的 PCB</li>
</ul>
</li>
</ul>
<h4 id="进程控制块存些什么"><a href="#进程控制块存些什么" class="headerlink" title="进程控制块存些什么?"></a>进程控制块存些什么?</h4><ul>
<li>进程标识信息 (PID)</li>
<li>处理机现场保存 (调度之前需要 保存上下文 保存其他寄存器 以便下次调度回来的时候使用)</li>
<li>进程控制信息 (调度优先级)<ul>
<li>调度和状态信息 (调度进程和处理机使用情况)</li>
<li>进程间通信信息</li>
<li>存储管理信息 (指向进程映像存储空间数据结构 占用的存储空间用完以后还给操作系统)</li>
<li>进程所用资源 (进程使用的系统资源 打开文件)</li>
<li>有关数据结构连接信息 (与PCB相关的进程队列 不同的状态处于不同的进程队列)</li>
</ul>
</li>
</ul>
<p><img src="/images/PCB_Context.png" alt="PCB_Context"></p>
<h4 id="进程控制块的组织-数据结构实现"><a href="#进程控制块的组织-数据结构实现" class="headerlink" title="进程控制块的组织 (数据结构实现)"></a>进程控制块的组织 (数据结构实现)</h4><h5 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h5><p>同一状态的进程其 PCB 成一链表 多个状态对应多个不同的链表</p>
<ul>
<li>各状态的进程形成不同的链表 就绪链表 阻塞链表</li>
</ul>
<h5 id="索引表"><a href="#索引表" class="headerlink" title="索引表"></a>索引表</h5><p>同一状态的进程 归入一个 索引表(由索引指向PCB) 多个状态对应多个不同的索引表</p>
<ul>
<li>各状态的进程形成不同的索引表 就绪索引表 阻塞索引表</li>
</ul>
<p><img src="/images/PCB_Data_Structure.png" alt="PCB_Data_Structure"></p>
<h4 id="进程状态"><a href="#进程状态" class="headerlink" title="进程状态"></a>进程状态</h4><ul>
<li>进程创建 操作系统分配给进程所需要的资源 构建 PCB<ul>
<li>系统初始化时</li>
<li>用户请求创建一个新进程</li>
<li>正在运行的进程执行了创建进程的系统调用</li>
</ul>
</li>
<li>进程执行<ul>
<li>进程创建完后 会放入就绪队列 等待 CPU调度</li>
<li>内核选择一个就绪的进程 让它占用处理机并执行</li>
</ul>
</li>
<li>进程等待(阻塞) 进程等待是进程本身发起的 不是外部让它去等待的<ul>
<li>请求并等待的系统服务 无法马上完成</li>
<li>启动某个操作 无法马上完成</li>
<li>需要的数据没有到达</li>
</ul>
</li>
<li>进程抢占<ul>
<li>高优先级进程就绪</li>
<li>进程执行时间片用完</li>
</ul>
</li>
<li>进程唤醒 进程唤醒是由别的进程或操作系统唤醒 不是自己唤醒自己<ul>
<li>被阻塞的进程需要的资源可被满足</li>
<li>被阻塞的进程等待的事件到达</li>
</ul>
</li>
<li>进程结束 将进程所占用的资源还给操作系统<ul>
<li>正常退出</li>
<li>错误退出</li>
<li>致命错误(强制性的)</li>
<li>被其他进程所杀(强制性的)</li>
</ul>
</li>
</ul>
<h5 id="三状态进程模型"><a href="#三状态进程模型" class="headerlink" title="三状态进程模型"></a>三状态进程模型</h5><ul>
<li>运行状态(Running)<ul>
<li>进程正在处理机上执行</li>
</ul>
</li>
<li>就绪状态(Ready)<ul>
<li>进程获得了除处理机之外的所需资源 得到处理机后可运行</li>
</ul>
</li>
<li>等待状态(又称阻塞状态 Blocked)<ul>
<li>进程正在等待某个事件 而暂停的状态</li>
</ul>
</li>
<li>创建状态(New)<ul>
<li>进程正在被创建 还没到就绪状态前的状态</li>
</ul>
</li>
<li>结束状态(Exit)<ul>
<li>进程正在从系统中消失时的状态 系统正在回收它所占用的资源</li>
</ul>
</li>
</ul>
<p><img src="/images/process_state.png" alt="process_state"></p>
<h5 id="进程切换"><a href="#进程切换" class="headerlink" title="进程切换"></a>进程切换</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">进程 1 执行 sleep() 进入 内核 内核调用函数设置定时器</span><br><span class="line">进行调度 保存现场(到 PCB 中) 切换到 进程 2</span><br><span class="line">进程 2 开始执行 若此时定时器时间到了 定时器产生 中断</span><br><span class="line">中断服务例程 让 进程 2 暂停下来 并保护 进程 2 的现场 恢复 进程 1 的现场</span><br><span class="line">让 进程 1 继续执行 进程 1 执行结束 再回到 操作系统</span><br></pre></td></tr></table></figure>
<p><img src="/images/process_switching.png" alt="process_switching"></p>
<h5 id="进程挂起"><a href="#进程挂起" class="headerlink" title="进程挂起"></a>进程挂起</h5><p>处在挂起状态的进程映像在磁盘上 目的是减少进程占用内存</p>
<ul>
<li>等待挂起状态(Blocked-suspend)<ul>
<li>进程在外存并等待某事件的出现</li>
</ul>
</li>
<li>就绪挂起状态(Ready-suspend)<ul>
<li>进程在外存 但只要进入内存 即可运行 (内存空间不够或它的优先级不够高)</li>
</ul>
</li>
<li>挂起(Suspend) 把一个进程从内存转到外存<ul>
<li>等待 到 等待挂起 (没有进程处于就绪状态 或 就绪状态进程要求更多的内存)</li>
<li>就绪 到 就绪挂起 (当有高优先级等待进程和低优先级就绪进程 总之就是 系统会认为高优先级的等待进程会很快就绪 就把低优先级的就绪进程换下 使得 高优先级的等待进程被激活后有足够的内存)</li>
<li>运行 到 就绪挂起 (对抢先式分时系统 当有高优先级等待挂起进程因事件出现而进入就绪状态 而当前没有足够内存空间 它就会把当前正在运行的这个进程变成就绪挂起状态)</li>
</ul>
</li>
<li>激活(Activate) 把一个进程从外存转到内存<ul>
<li>就绪挂起 到 就绪 (没有就绪队列 或 挂起就绪队列进程优先级高于就绪进程)</li>
<li>等待挂起 到 等待 (当一个进释放足够内存 并有 高优先级等待挂起进程)</li>
</ul>
</li>
</ul>
<p><img src="/images/process_suspend.png" alt="process_suspend"></p>
<h5 id="状态队列"><a href="#状态队列" class="headerlink" title="状态队列"></a>状态队列</h5><p>由操作系统来维护一组队列 表示系统中所有进程的当前状态</p>
<ul>
<li>不同队列表示不同状态</li>
<li>根据进程状态不同 进程PCB 加入相应队列</li>
</ul>
<p><img src="/images/process_state_queue.png" alt="process_state_queue"></p>
<h2 id="线程-Thread"><a href="#线程-Thread" class="headerlink" title="线程(Thread)"></a>线程(Thread)</h2><p>线程是进程的一部分 描述指令流执行状态 是进程中的指令执行最小单元 是 CPU 调度的基本单位</p>
<h3 id="线程与进程的关系"><a href="#线程与进程的关系" class="headerlink" title="线程与进程的关系"></a>线程与进程的关系</h3><p>线程 = 进程 - 共享资源 = 执行流</p>
<ul>
<li>线程的优点<ul>
<li>一个进程中可以有多个线程</li>
<li>各个线程之间可并发执行</li>
<li>各个线程之间可以共享地址空间和文件等资源</li>
</ul>
</li>
<li>线程的缺点<ul>
<li>一个线程崩溃 会导致其所属进程的所有线程崩溃</li>
</ul>
</li>
</ul>
<p><img src="/images/process_thread_relationship.png" alt="process_thread_relationship"></p>
<h3 id="线程与进程的区别"><a href="#线程与进程的区别" class="headerlink" title="线程与进程的区别"></a>线程与进程的区别</h3><ul>
<li>进程是资源分配单位 线程是CPU调度单位</li>
<li>进程拥有一个完整的资源平台 而线程只独享指令流执行的必要资源 (寄存器 栈)</li>
<li>线程具有 就绪 等待 运行 三种基本状态和状态间的状态转换</li>
<li>线程能减少 并发执行的时间 和 空间开销<ul>
<li>线程的创建和结束时间 比 进程 短 且 同一进程内的线程切换时间比进程短</li>
<li>同一进程的 各线程间共享内存 和 文件资源 可 不通过 内核 进行 直接通信(省执行时间)</li>
</ul>
</li>
</ul>
<h3 id="线程实现的三种方式"><a href="#线程实现的三种方式" class="headerlink" title="线程实现的三种方式"></a>线程实现的三种方式</h3><ul>
<li>用户线程(在用户空间实现)</li>
<li>内核线程(在内核中实现)</li>
<li>轻权进程(LightWeight Process 在内核中实现 支持用户线程 结合了用户线程和内核线程的优点)</li>
</ul>
<h4 id="用户线程"><a href="#用户线程" class="headerlink" title="用户线程"></a>用户线程</h4><p>由一组用户级的线程库函数来完成线程的管理 包括线程的创建 终止 同步 调度等</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">操作系统内核里 仍然只有 PCB 来描述处理机调度的情况</span><br><span class="line">操作系统并不感知用户态有多线程的支持</span><br><span class="line">多线程的支持是用户的函数库支持的多线程</span><br><span class="line">在应用程序内部构造 线程控制块(TCB) 来控制多个线程的交替和同步</span><br></pre></td></tr></table></figure>

<p><img src="/images/user_thread.png" alt="user_thread"></p>
<h5 id="用户线程的特征"><a href="#用户线程的特征" class="headerlink" title="用户线程的特征"></a>用户线程的特征</h5><p>不依赖于操作系统内核 </p>
<ul>
<li>内核不了解用户线程的存在</li>
<li>可用于不支持线程的多进程操作系统</li>
</ul>
<p>在用户空间实现的线程机制</p>
<ul>
<li>每个进程有私有的线程控制块(TCB) 列表</li>
<li>TCB 由线程库函数维护</li>
</ul>
<p>同一进程内的用户线程切换速度快</p>
<ul>
<li>无需内核态 用户态的切换</li>
</ul>
<p>允许每个进程拥有自己的线程调度算法</p>
<h5 id="用户线程的不足"><a href="#用户线程的不足" class="headerlink" title="用户线程的不足"></a>用户线程的不足</h5><ul>
<li>线程发起系统调用而阻塞时 则整个进程进入等待</li>
<li>不支持基于线程的处理机抢占(除非当前运行线程主动放弃 它所在进程的其他线程无法抢占CPU)</li>
<li>线程只能按进程分配 CPU时间(多个线程的进程中 每个线程的时间片少)</li>
</ul>
<h4 id="内核线程"><a href="#内核线程" class="headerlink" title="内核线程"></a>内核线程</h4><p>由内核通过系统调用实现的线程机制 由内核完成线程的创建 终止 管理</p>
<p><img src="/images/kernel_thread.png" alt="kernel_thread"></p>
<h5 id="内核线程的特征"><a href="#内核线程的特征" class="headerlink" title="内核线程的特征"></a>内核线程的特征</h5><ul>
<li>由内核来维护 PCB 和 TCB</li>
<li>线程执行系统调用而被阻塞 不影响 其他线程</li>
<li>线程的创建 终止 切换 开销大(因为要通过系统调用在内核中走一趟)</li>
<li>以线程为单位进行 CPU时间分配(多线程的进程可获得更多的CPU时间)</li>
</ul>
<h4 id="轻权进程-LightWeight-Process"><a href="#轻权进程-LightWeight-Process" class="headerlink" title="轻权进程(LightWeight Process)"></a>轻权进程(LightWeight Process)</h4><p>内核支持的用户线程 一个进程可有一个或多个轻量级进程 每个轻权<br>进程由一个单独的内核线程来支持</p>
<p><img src="/images/lightWeight_process.png" alt="lightWeight_process"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">永久绑定线程 可看作是 内核支持的线程</span><br><span class="line">未绑定的轻权进程 可由用户态来给出一些策略 提高应用的效率</span><br><span class="line">未绑定的轻权进程 就像用户线程实现的多线程 可以自己决定调度算法之类的</span><br><span class="line">但同时 永久绑定线程 又是 内核原生就支持的线程 所以轻权进程 是融合了 内核线程和用户线程的优点</span><br><span class="line">实际上轻权进程 优点没有体现出来 因此没被广泛使用</span><br></pre></td></tr></table></figure>
<h4 id="内核线程和用户线程和轻权进程的对应关系"><a href="#内核线程和用户线程和轻权进程的对应关系" class="headerlink" title="内核线程和用户线程和轻权进程的对应关系"></a>内核线程和用户线程和轻权进程的对应关系</h4><p><img src="/images/thread_relationship.png" alt="thread_relationship"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">一对一 可看作是 多进程多线程操作系统</span><br><span class="line">多对一 可看作是 多进程单线程操作系统(用户态实现多线程)</span><br><span class="line">多对多 轻权进程</span><br><span class="line">实际上最广泛使用 一对一 多进程多线程操作系统</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>进程</tag>
        <tag>线程</tag>
      </tags>
  </entry>
  <entry>
    <title>操作系统之 进程控制</title>
    <url>/%E6%93%8D%E4%BD%9C%E7%B3%BB%E7%BB%9F%E4%B9%8B-%E8%BF%9B%E7%A8%8B%E6%8E%A7%E5%88%B6/</url>
    <content><![CDATA[<h3 id="进程切换-上下文切换"><a href="#进程切换-上下文切换" class="headerlink" title="进程切换 (上下文切换)"></a>进程切换 (上下文切换)</h3><ol>
<li>暂停当前运行进程 从运行状态变成其他状态</li>
<li>调度另一个进程从就绪状态变成运行状态</li>
</ol>
<h4 id="进程切换的要求"><a href="#进程切换的要求" class="headerlink" title="进程切换的要求"></a>进程切换的要求</h4><ul>
<li>切换前 保存进程上下文(寄存器 CPU状态 内存地址空间(大部分不用保存 因为两个进程的内存地址空间不会被覆盖))</li>
<li>切换后 恢复进程上下文</li>
<li>快速切换</li>
</ul>
<a id="more"></a>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">进程 P0 因为时间片用完 进入时钟中断服务例程</span><br><span class="line">保存 进程状态到 PCB 0 后 从 PCB 1 恢复进程状态继续执行 进程 P1</span><br><span class="line">暂停 进程 P0</span><br></pre></td></tr></table></figure>

<p><img src="/images/context_switch.png" alt="context_switch"></p>
<h4 id="进程状态记录-PCB-过程"><a href="#进程状态记录-PCB-过程" class="headerlink" title="进程状态记录 PCB 过程"></a>进程状态记录 PCB 过程</h4><ul>
<li>内核为每个进程 维护 对应的进程控制块(PCB)</li>
<li>内核将相同状态的 进程的PCB 放置在同一队列</li>
</ul>
<p><img src="/images/PCB_queue.png" alt="PCB_queue"></p>
<h3 id="进程创建"><a href="#进程创建" class="headerlink" title="进程创建"></a>进程创建</h3><ul>
<li>Windows进程创建Api CreateProcess()</li>
<li>Unix进程创建系统调用 fork/exec<ul>
<li>fork() 把一个进程复制成两个进程 父子进程的PID不同</li>
<li>exec() 用新程序来重写当前进程 PID 不变</li>
</ul>
</li>
</ul>
<h4 id="进程复制-fork"><a href="#进程复制-fork" class="headerlink" title="进程复制 fork"></a>进程复制 fork</h4><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> pid = fork()；		<span class="comment">// 创建子进程</span></span><br><span class="line"><span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;			<span class="comment">// 子进程在这里继续</span></span><br><span class="line">    exec(“program”, argc, argv0, argv1, …);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li>fork() 创建一个继承的子进程<ul>
<li>复制父进程的所有变量和内存</li>
<li>复制父进程的所有 CPU 寄存器(一个寄存器例外 是用来识别父进程和子进程的)</li>
</ul>
</li>
<li>fork() 的返回值<ul>
<li>子进程的 fork() 返回值 为 0</li>
<li>父进程的 fork() 返回值为 子进程标识符</li>
<li>子进程可使用 getpid() 获取 PID</li>
</ul>
</li>
</ul>
<h5 id="fork-地址空间复制"><a href="#fork-地址空间复制" class="headerlink" title="fork 地址空间复制"></a>fork 地址空间复制</h5><p>fork() 执行过程对于 子进程来说 是在调用时间 对父进程地址空间的一次复制(父进程 fork() 返回值为 子进程标识符 子进程 fork() 返回值为 0)</p>
<p><img src="/images/fork.png" alt="fork"></p>
<h5 id="fork-循环调用示例"><a href="#fork-循环调用示例" class="headerlink" title="fork 循环调用示例"></a>fork 循环调用示例</h5><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">pid_t</span> pid;</span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    <span class="keyword">for</span> (i=<span class="number">0</span>;  i&lt;LOOP;  i++) &#123;</span><br><span class="line">        pid = fork();</span><br><span class="line">        <span class="keyword">if</span> (pid &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, “Fork Failed”);</span><br><span class="line">            <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (pid == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="built_in">fprintf</span>(<span class="built_in">stdout</span>,  “i=%d,  pid=%d,  parent  pid=%d\n”,I,      </span><br><span class="line">                getpid() ,getppid());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    wait(<span class="literal">NULL</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>每次循环 都会 fork 一个新的子进程 同时原来被 fork 出来的子进程 也会去 fork 一个新的子进程 这里一共循环了 3次 共有 8个 进程</p>
<p><img src="/images/fork_loop.png" alt="fork_loop"></p>
<h5 id="fork-的开销"><a href="#fork-的开销" class="headerlink" title="fork 的开销"></a>fork 的开销</h5><ul>
<li>对子进程分配内存</li>
<li>复制父进程的内存和CPU寄存器到子进程</li>
</ul>
<p>在大多数情况下 调用了 fork() 以后 会调用 exec() 将 fork() 复制出来的子进程的内存给覆盖掉 fork() 复制父进程的内存的开销是可以节约的<br>因此 Windows 下 通过一个系统调用 来完成进程的创建和加载</p>
<p>早期 Unix的 <code>vfork()</code> 也做类似的事情 创建进程时 不再创建一个同样的内存映像 称之为轻量级 fork() 子进程应该立即调用 exec() 现在使用 Copy on Write (COW 写时拷贝) 技术</p>
<h3 id="进程加载与执行-exec"><a href="#进程加载与执行-exec" class="headerlink" title="进程加载与执行 exec"></a>进程加载与执行 exec</h3><p>系统调用 exec() 加载新程序取代当前运行程序</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">fork() 创建一个继承的子进程 并复制 父进程的内存地址空间和CPU寄存器</span><br><span class="line">exec() 加载新程序 并 覆盖原来的内存地址空间 取代当前运行程序 和之前是相同的进程 但运行了不同的程序</span><br></pre></td></tr></table></figure>
<p>代码段 堆栈 堆 完全重写</p>
<p><img src="/images/fork_exec_pcb.png" alt="fork_exec_pcb"></p>
<h3 id="进程等待与退出"><a href="#进程等待与退出" class="headerlink" title="进程等待与退出"></a>进程等待与退出</h3><p>wait() 系统调用用于父进程等待子进程的结束</p>
<ul>
<li>子进程结束时通过 exit() 向父进程返回一个值</li>
<li>父进程通过 wait() 接受并处理返回值</li>
</ul>
<p>当 父进程 先 wait() 子进程 后 exit() 时</p>
<ul>
<li>父进程进入等待状态 等待子进程的返回结果</li>
<li>当某子进程调用 exit() 时 唤醒父进程 将 exit() 返回值作为 父进程 wait() 的返回值</li>
</ul>
<p>当 子进程 先 exit() 父进程 后 wait() 时</p>
<ul>
<li>说明有僵尸子进程等待 wait() 立即返回其中一个值</li>
</ul>
<p>当 无子进程存活 而 父进程 wait() 时</p>
<ul>
<li>wait() 立即返回</li>
</ul>
<h4 id="进程退出-exit"><a href="#进程退出-exit" class="headerlink" title="进程退出 exit"></a>进程退出 exit</h4><p>进程结束执行时 调用 exit() 完成进程资源回收</p>
<ul>
<li>exit() 系统调用的功能<ul>
<li>将调用参数作为进程的 结果(返回值)</li>
<li>关闭所有打开的文件等占用资源</li>
<li>释放内存</li>
<li>释放大部分进程相关的内核数据结构</li>
<li>检查父进程是否还存活<ul>
<li>存活 保留结果的值 直到父进程需要它 进入 僵尸(zombie/defunct)状态</li>
<li>非存活 释放所有的数据结构和结果</li>
</ul>
</li>
<li>清理所有等待的僵尸进程</li>
</ul>
</li>
</ul>
<p>进程终止是最终的垃圾收集(资源回收)</p>
<h3 id="进程控制与进程状态关系"><a href="#进程控制与进程状态关系" class="headerlink" title="进程控制与进程状态关系"></a>进程控制与进程状态关系</h3><p><img src="/images/process_control_with_process_state.png" alt="process_control_with_process_state"></p>
<h3 id="其他进程控制系统调用"><a href="#其他进程控制系统调用" class="headerlink" title="其他进程控制系统调用"></a>其他进程控制系统调用</h3><ul>
<li>优先级控制<ul>
<li>nice() 指定进程的初始优先级</li>
<li>Unix系统中 进程优先级会随着执行时间而衰减</li>
</ul>
</li>
<li>进程调试支持<ul>
<li>ptrace() 允许一个进程控制另一个进程的执行</li>
<li>设置断点和查看寄存器等</li>
</ul>
</li>
<li>定时<ul>
<li>sleep() 可以让进程在定时器的等待队列中等待指定的时间</li>
</ul>
</li>
</ul>
]]></content>
      <categories>
        <category>操作系统</category>
      </categories>
      <tags>
        <tag>操作系统</tag>
        <tag>进程</tag>
        <tag>线程</tag>
      </tags>
  </entry>
  <entry>
    <title>毕业之后, 新篇章</title>
    <url>/%E6%AF%95%E4%B8%9A%E4%B9%8B%E5%90%8E%E6%96%B0%E7%AF%87%E7%AB%A0/</url>
    <content><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>终于的终于, 2016-2020年 这四年大学时光已经结束, 而我也入职了金山西山居, 依然从事游戏服务端的研发.</p>
<p>很久没有更新过博文了, 主要是因为 时间比较紧张, 还有经历了一系列 糟糕的事情(可进行脑补, 你脑补出来的几乎都是对的hhhh), 还有自认学习深度不足, 在探索一种全新的学习模式, 来调整工作与生活的状态.</p>
<h2 id="新篇章"><a href="#新篇章" class="headerlink" title="新篇章"></a>新篇章</h2><a id="more"></a>

<h3 id="珠海入职"><a href="#珠海入职" class="headerlink" title="珠海入职"></a>珠海入职</h3><p>作为一个从小到大在深圳长大的人, 第一次去珠海, 感觉环境非常优美, 海景美丽, 珠海那边的住民都非常友善, 陌生人之间都能很愉快的聊起来, 公司公寓非常 一个字 棒! 可惜啊 只能入住三天. 以下附图几张 供各位看官欣赏.</p>
<p><img src="/images/seasun_house1.jpg" alt="seasun_house1"></p>
<p><img src="/images/seasun_house2.jpg" alt="seasun_house2"></p>
<p><img src="/images/seasun_house3.jpg" alt="seasun_house3"></p>
<p><img src="/images/seasun_house4.jpg" alt="seasun_house4"></p>
<p><img src="/images/seasun_house5.jpg" alt="seasun_house5"></p>
<p>公司饭堂 自我感觉还行 和腾讯的饭菜比的话, 各有春秋吧. 有的时候菜色还是不错的.</p>
<p><img src="/images/seasun_food1.jpg" alt="seasun_food1"></p>
<p><img src="/images/seasun_food2.jpg" alt="seasun_food2"></p>
<h3 id="结语"><a href="#结语" class="headerlink" title="结语"></a>结语</h3><p>接下来的博文 不会出现读书阶段的那种完整的系列, 而是探索了一种全新的模式, 在工作中, 遇到的 各种感兴趣的问题, 我将会将其抽取出来, 然后进行深度的学习, 最后总结成博文.</p>
]]></content>
      <categories>
        <category>胡言乱语</category>
      </categories>
      <tags>
        <tag>毕业</tag>
        <tag>杂谈</tag>
      </tags>
  </entry>
  <entry>
    <title>离线安装 VSCode Remote-SSH</title>
    <url>/%E7%A6%BB%E7%BA%BF%E5%AE%89%E8%A3%85VSCode-Remote-SSH/</url>
    <content><![CDATA[<p>VSCode 近期推出了 Remote-SSH 这个工具 使得 Linux开发者 能够在本地(Windows 机)上直接对开发机源码进行修改 简直拯救了一大批人啊… 但是却没有提到 如何离线安装 因此本文可能是最早 尝试离线安装的教程</p>
<h2 id="离线安装"><a href="#离线安装" class="headerlink" title="离线安装"></a>离线安装</h2><ol>
<li>本地上下载好 <a href="https://code.visualstudio.com/insiders/" target="_blank" rel="noopener">VSCode Insiders</a></li>
<li>安装 <code>Remote-SSH</code> 插件</li>
</ol>
<a id="more"></a>

<ol start="3">
<li>为 <code>Remote-SSH</code> 配置好 服务器 信息 记得要将 <code>Show Login Terminal</code> 置 true</li>
<li>直接连接(中途会多次要求输入密码) 此时 会显示一行 <code>Download XXXXXX</code> 我们不要去理他 直接开一个终端 连上服务器 </li>
<li>输入命令 <code>ps aux | grep vscode</code> 可以看到 有一行 写者 <code>sh /tmp/vscode-remote-install.XXXXXXXXXXXX.sh</code></li>
<li>我们将 XXXXXXX 复制下来 然后 在本地机器 也就是能联网的机器上 打开 以下链接<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">自行将 $COMMIT_ID 替换成 上面得到的数值</span><br><span class="line">https:&#x2F;&#x2F;update.code.visualstudio.com&#x2F;commit:$COMMIT_ID&#x2F;server-linux-x64&#x2F;insider</span><br></pre></td></tr></table></figure></li>
<li>然后打开下载到本地机器 接着进入服务器中的 <code>~/.vscode-remote/bin</code> 这个目录 可以看到 有一个文件夹的名字 和我们上面获得的 数值是一样的 </li>
<li>我们进去 可以看到有个文件的文件名中间有个 lock. 我们将其删除 并 输入命令 <code>touch 0</code> 然后把上面下载到的文件 上传到这个目录下</li>
<li><code>tar -xf filename</code> 解压到当前目录(注意是当前目录)</li>
<li>重启 <code>VSCode Insiders</code> 重新连接服务器 很愉快的就能连接上了!</li>
</ol>
<p><img src="/images/VSCode_Remote_SSH.png" alt="VSCode_Remote_SSH"><br>图片上的 <code>a5536b8f5a16a10d859f3dec1e59701671bf069e</code> 就是我们一开始获得的 XXXXXXX 按照以上步骤完成之后 应该和图片上的一致.</p>
<h2 id="离线安装插件"><a href="#离线安装插件" class="headerlink" title="离线安装插件"></a>离线安装插件</h2><p>装好了 但是没有插件咋整? 打开插件的网页 然后将其下载下来 再传到 <code>~/.vscode-remote/extension</code> 这个目录 最后解压 重启 <code>VSCode Insiders</code> 完事</p>
]]></content>
      <categories>
        <category>搞东搞西</category>
      </categories>
      <tags>
        <tag>VSCode</tag>
      </tags>
  </entry>
  <entry>
    <title>面试腾讯</title>
    <url>/%E9%9D%A2%E8%AF%95%E8%85%BE%E8%AE%AF/</url>
    <content><![CDATA[<h2 id="收到腾讯的-Offer"><a href="#收到腾讯的-Offer" class="headerlink" title="收到腾讯的 Offer"></a>收到腾讯的 Offer</h2><p>前两天 2019年4月3日 我拿到了 腾讯 ieg 的游戏后台研发的暑期实习 Offer<br>这是我人生中第一次面试 也是我人生中第一个 Offer</p>
<h2 id="奇妙的开始"><a href="#奇妙的开始" class="headerlink" title="奇妙的开始"></a>奇妙的开始</h2><p>三月份的时候 看到知乎上有腾讯的大佬发文章 说腾讯的暑期实习已经开始了. 从小在深圳长大 多多少少都会对腾讯有一种向往. 我很想去 但又觉得自己好像什么都不会. 后来和 女朋友 说起了这件事后 她觉得我可以的, “强迫”我去写简历</p>
<a id="more"></a>

<h2 id="妖妖的协助"><a href="#妖妖的协助" class="headerlink" title="妖妖的协助"></a>妖妖的协助</h2><p>在我下定决心开始写简历之后 我却不知道我应该从哪里写起 于是请教了 <a href="https://boxueio.com/" target="_blank" rel="noopener">泊学</a> 的站长 11(帮他打个小广告), 他给我提了几个建议 其中一个就是 你要让看简历的人能够充分了解你 于是就写了好几百字的简历 最后妖妖再帮我做了一些言辞上的修正后 完成了简历</p>
<h2 id="投递简历"><a href="#投递简历" class="headerlink" title="投递简历"></a>投递简历</h2><p>写好简历自然是投简历 投哪家呢? BAT三家 我好像只喜欢腾讯啊 因为我也是 98年生 它也是. 而且它是企鹅 很可爱. 虽然我菜 但是我还是可以有选择投哪家的权利啊 于是只投了腾讯一家</p>
<h2 id="认识更多大佬"><a href="#认识更多大佬" class="headerlink" title="认识更多大佬"></a>认识更多大佬</h2><p>投完简历后 便在<a href="https://www.imooc.com/t/108955" target="_blank" rel="noopener">bobo老师 慕课网精英教师</a>的群里发了一下自己的简历 结果被bobo老师”看上”了 把我拉进了群里 和更多的大佬一起交流</p>
<h2 id="一面前奏"><a href="#一面前奏" class="headerlink" title="一面前奏"></a>一面前奏</h2><p>投完简历的第二天早上八点 就收到了腾讯面试官的短信 说是腾讯天美的 问我有没有兴趣面试. 那肯定有兴趣啊(之前投简历的时候选BG选的无明显意向, 其实我个人还是很想去ieg的, 因为做游戏有意思啊, 但是又担心选了BG以后, 看到我的简历的面试官不够多, 所以最后改成了无明显意向) 沟通完以后 开始准备面试(其实我投简历的时候 啥都没复习… 没想到第二天就被发起面试了 好在自己简历上的 OS项目没有全忘)</p>
<h2 id="一面开始"><a href="#一面开始" class="headerlink" title="一面开始"></a>一面开始</h2><p>面试官声音挺好听的 听起来很年轻(据牛客网所说 ieg的面试官的声音普遍好听 温柔) 就是不知道为啥有回音? 让我脑补了一波 一个人坐在楼梯里苦B的打电话的场景… 一切很顺利 一个多小时后 结束了 提问阶段 就问了下多少岁啊? 因为真的听起来很年轻…</p>
<h2 id="总监面"><a href="#总监面" class="headerlink" title="总监面"></a>总监面</h2><p>二面发短信预约的时候 是有带面试官名字的 所以我就很好奇的去查了一下 是一个技术总监(然后就更紧张了….Emmm) 发短信给我的时候 是中午十一点多 问我有没有时间? 我就在想 嗯? 我有时间啊 因为我已经吃饱了啊….哈哈哈哈 但是你吃饭了吗? 总不能你没吃饭就来面试我吧? 所以我就回了个 你吃饭了吗? 然后就约了时间面试了</p>
<h2 id="HR面"><a href="#HR面" class="headerlink" title="HR面"></a>HR面</h2><p>HR面是通过微信面的 据说她当时在美国出差 然后问了问很常规的问题 就结束了</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>整个春招提前批过程贯穿了 三月的中下旬 在此感谢帮助过我的人 妖妖 bobo老师 Starkwang David 这两位腾讯大佬被我烦了半个月 腾讯里面的人都很好… 虽然我和他们不认识 但是他们还是每一次都帮我查状态 给我建议 没有你们的帮助 我可能没那么顺利 谢谢各位!!!</p>
<p>最后还是那句话</p>
<p><code>结束又是新的开始 —— Yuerer</code></p>
]]></content>
      <categories>
        <category>胡言乱语</category>
      </categories>
      <tags>
        <tag>面试</tag>
        <tag>腾讯</tag>
      </tags>
  </entry>
</search>
