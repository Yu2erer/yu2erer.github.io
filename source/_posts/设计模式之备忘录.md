---
title: 设计模式之 备忘录
categories: 设计模式
date: 2019-06-09 13:06:20
keywords: 设计模式, 备忘录, Memento
tags: [设计模式, 备忘录]
---
## Memento
属于 状态变化 模式

在软件构建过程中 某些对象的状态在转换过程中 可能由于某种需要 要求程序能够回溯到对象之前处于某个点时的状态

在不破坏封装性的前提下 捕获一个对象的内部状态 并在该对象之外保存这个状态 这样以后就可以将该对象恢复到原先保存的状态

直接看代码

<!-- more -->

```c++
class Memento {
    string state;
    /* ... */
public:
    Memento(const string &s) : state(s) {}
    string getState() const { return state; }
    void setState(const string &s) { state = s; }
};
class Originator {
    string state;
    /* ... */
public:
    Originator() {}
    Memento createMomento() {
        Memento m(state);
        return m;
    }
    void setMomento(const Memento &m) {
        state = m.getState();
    }
};
// 我们希望能够保存上面这个类的对象的状态
int main() {
    Originator orginator;
    
    // 捕获对象状态，存储到备忘录
    Memento mem = orginator.createMomento();
    
    // 改变orginator状态
    
    // 从备忘录中恢复
    orginator.setMomento(memento);
}
```

其实就是想保存 一个对象的状态 但不想直接暴露出来 就再设计一个类 作为一个备忘录 存下你想存下的状态 到另一个对象里 就这么简单 现在很少这么做了(过时了) 因为 现代语言的运行时都具有相当的对象序列化的支持 因此往往采用效率较高的序列化方案